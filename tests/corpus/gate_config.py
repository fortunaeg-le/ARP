# -*- coding: utf-8 -*-
"""
gate_config.py — параметры регресс-гейта (tests/corpus/gate.py), этап 0d,
расширен этапом S2 (возврат leak/masking в гейт + сведение с гейтом D).

Этот файл существует ОТДЕЛЬНО от gate.py намеренно: правка допуска должна
быть видимым действием в git-диффе конфига с собственным комментарием в
коммите, а не незаметной строкой внутри логики гейта. Не поднимайте
допуск молча "чтобы прошло красное" — см. ЗАДАНИЕ этапа 0d.

ОБЩЕЕ ОБОСНОВАНИЕ НУЛЕВЫХ ДОПУСКОВ (этап S2, 2026-07-25).
Допуск охраняет от ШУМА измерения, а не от регресса. Значит его величина
обязана следовать из ИЗМЕРЕННОГО разброса между повторными прогонами на
неизменном src/. На этапе S2 корпус прогнан ДВАЖДЫ подряд на одном и том же
HEAD, и результаты сопоставлены поимённо: разброс НУЛЕВОЙ (см. HANDOFF_S2,
раздел «повторяемость»; после этапа E″ детерминизм на корпусе — свойство
кода, а не удача). Шума нет — следовательно любой ненулевой допуск не
компенсирует шум, а ПРОЩАЕТ регресс. Отсюда 0 везде, кроме явно названных
исключений ниже.

Стоимость ошибки в обе стороны названа явно:
  * слишком узкий допуск -> ложное срабатывание на шуме. Здесь невозможно:
    шум измерен и равен нулю;
  * слишком широкий -> регресс проходит молча. Это ровно тот сценарий,
    из-за которого гейт простоял красным пять этапов (ARCHITECTURE_AUDIT §5.1).
"""

# --- условие 3: FP на РАЗМЕЧЕННЫХ негативах (gold.json['negatives']) ---------
# Абсолютное число, суммарно по корпусу, между baseline и текущим прогоном.
# 0 по общему обоснованию выше. Историческая заметка: значение поднималось бы
# явно на этапе, где рост FP ОЖИДАЕМ (этап 2, переезд нормализации в src/) —
# такой рост фиксируется пересборкой baseline, а не расширением допуска.
FP_TOLERANCE = 0

# --- условие 2 (линия «б»): leak_v2 по типам и по агрегату -------------------
# Абсолютное число сущностей, ОТДЕЛЬНО по каждому из 13 типов и отдельно по
# агрегату BIK-excl (чтобы рост одного типа не спрятался за падением другого).
# 0: утечка — та метрика, ради которой существует продукт; прощать «немножко
# больше ПДн наружу» нечем.
LEAK_V2_TOLERANCE = 0

# --- линия «а»: precision NER-хребта (ORG/PER/ADDRESS) -----------------------
# В процентных пунктах. Гейт D (этап D) использовал 0.5пп — это была УСТУПКА
# неизвестному тогда разбросу; на S2 разброс измерен и равен нулю, поэтому
# допуск ужесточён до 0.0. Ужесточение безопасно: прогон на HEAD после
# пересборки baseline даёт значения, РАВНЫЕ baseline побитно.
PRECISION_TOLERANCE_PP = 0.0

# --- линия «в»: masking A (round-trip) --------------------------------------
# В процентных пунктах. A — инвариант проекта (STATE §6: «корректность
# маскировки только растёт», допуск 0). Дополнительно гейт требует
# АБСОЛЮТНОГО A == 100%: baseline с A<100 нельзя было бы отличить от порчи.
MASKING_A_TOLERANCE_PP = 0.0

# --- линия «г»: masking B (границы) -----------------------------------------
# В процентных пунктах, допуск 0 по тому же инварианту STATE §6.
MASKING_B_TOLERANCE_PP = 0.0

# masking C (тип маски) — МЯГКИЙ уровень по решению владельца продукта
# (STATE §6, зафиксировано на этапе 0d и подтверждено на D). Гейт печатает
# падение C как ПРЕДУПРЕЖДЕНИЕ и НЕ роняет прогон. Этап S2 это решение не
# отменяет: C слеп к over-маскированию (FINDINGS D-PRECISION-90), поэтому
# блокирующая роль по типу маски отдана precision (линия «а»), а не C.
MASKING_C_WARN_PP = 0.0

# --- известный долг: ADDRESS found=False & leak (реестр этапа C) ------------
# Штук. Мягкий порог по решению владельца на этапе D сохранён: долг — класс,
# закрывающийся расширением мультиспана, а не текущей правкой. Компенсатор
# мягкости — ДИАГНОСТИКА СОСТАВА: если счёт тот же, но множество утечек
# изменилось, гейт печатает ПРЕДУПРЕЖДЕНИЕ (подмена «N на другой N» видна).
MISSED_LEAK_TOLERANCE = 0
