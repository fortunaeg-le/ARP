# -*- coding: utf-8 -*-
"""
gate_config.py — параметры регресс-гейта (tests/corpus/gate.py), этап 0d.

Этот файл существует ОТДЕЛЬНО от gate.py намеренно: правка допуска должна
быть видимым действием в git-диффе конфига с собственным комментарием в
коммите, а не незаметной строкой внутри логики гейта. Не поднимайте
FP_TOLERANCE молча "чтобы прошло красное" — см. ЗАДАНИЕ этапа 0d.
"""

# Допуск на РОСТ ложных срабатываний по РАЗМЕЧЕННЫМ негативам (gold.json
# ['negatives'], 5226 штук на весь корпус) — абсолютное число, суммарно по
# корпусу, между baseline и текущим прогоном.
#
# Дефолт = 0.  Обоснование: на этапе 0d src/ не меняется, а конвейер
# детерминирован (COVERAGE_AUDIT.md: 0 недетерминированных сущностей из 10315
# на повторных прогонах при неизменном коде) — значит при неизменном src/
# число FP обязано быть БИТ-В-БИТ тем же, что на baseline, и любой рост уже
# является сигналом (либо баг в детекторе, либо в самом гейте/харнессе).
#
# На этапе 2, когда нормализация переедет из измерительного харнесса в саму
# систему (src/), кандидатов у детекторов закономерно станет больше и FP
# вырастут — это ОЖИДАЕМЫЙ, а не аварийный рост. Тогда это число поднимается
# здесь, ЯВНО, с комментарием в коммите, объясняющим новое значение (сколько
# и почему) — не втихую внутри gate.py.
FP_TOLERANCE = 0
