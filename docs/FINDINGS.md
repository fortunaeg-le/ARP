# FINDINGS — открытые долги

**Только то, что ещё не закрыто.** Закрытые и исторические находки —
[`docs/archive/FINDINGS_closed.md`](archive/FINDINGS_closed.md); id сохраняются
для перекрёстных ссылок и не переиспользуются. Краткий статус — [`STATE.md`](STATE.md).

Актуализирован: **2026-08-04**, этап DOC. Из файла вынесено 10 строк: шесть с
пометкой `ЗАКРЫТО`, историческое описание закрытой `PER-B`, а также
`U4-G-TIMEOUT` и две из трёх записей `Eprime-A` — обоснование в разделе «Снято»
в конце файла.

| id | область | находка | тяжесть | чинится |
|---|---|---|---|---|
| TGOLDA-SUM-CASE | `entity_types.yaml`, паттерн `SUM` | **Все промахи и все утечки типа `SUM` — на adversarial-формах, ни одной на canonical.** Первый замер SUM как эталонного типа (этап T-GOLD-A, 648 сущностей): recall 97.5%, не найдено 16, утечка leak_v2>=6 — 28. Разрез по категории: canonical (601 шт.) — recall 100%, утечка 0; adversarial (47 шт.) — вся потеря там. По приёмам промахи: combo 6, case 5, homoglyph 3, linebreak 2. Самый дешёвый и самый частый — **регистр**: паттерн `SUM` не имеет `(?i)`, поэтому «732 589,00 РУБ.» не матчится вовсе (`miss_reason: nothing`), тогда как остальные типы давно смотрят на `detection_text` с нормализованным регистром. Остальные приёмы — омоглиф на месте цифры («7Зч 525,оО»), перенос строки внутри слова валюты («ру» + `\n` + «б.»). Замечание к самой метрике: десятичная запятая рвёт цифровой run, поэтому утечка суммы видна только мягким порогом (>=6), а строгий (>=8) по SUM даёт 0 — это свойство метрики, не заслуга детектора (см. комментарий у `V2_NUMERIC_TYPES` в `run_measurement.py`). | СРЕДНЯЯ (2.5% типа; правильная маскировка сумм — решение владельца, а не необязательная опция) | не запланировано; кандидат — `(?i)` в паттерне `SUM` плюс проверка, что детекция идёт по `detection_text`, с обязательным прогоном гейта: расширение паттерна суммы способно поднять over-mask на прозе |
| Stage3-A | BIRTHDATE | Недобор спана на ОДИН символ в 4 случаях `m2718_homoglyph`: первая цифра даты подменена омоглифом («07.07.1963» → «З7.07.1963»), нормализация её не сводит (в токене «З7» лишь одна настоящая цифра, порог свода — две), и якорный зазор `[^\n\d]{0,10}` съедает «З». Маскируется «7.07.1963». Направление ошибки безопасное (недобор, не перебор); альтернатива — запрет букв в зазоре — обменяла бы 4 недобора на 4 полных пропуска. | НЕЗНАЧИТЕЛЬНО | не запланирован |
| ADDRB-BIRTHDATE-EXPOSED | `src/normalizer.py` × BIRTHDATE (частный случай B4/Stage4-B) | **Дата рождения с омоглифами вместо цифр не детектируется и с этапа ADDR-B больше ничем не прикрыта.** `services_0001__m1337_combo`, строка «Дата рождения: 21.0І.196О» (кириллические `І`/`О` вместо `1`/`0`): детектор BIRTHDATE её не берёт НИКОГДА (`found=False` и до, и после этапа), нормализатор НАМЕРЕННО не сводит одиночные буквы внутри чисел (защита «кв. 2б»). До ADDR-B значение пряталось СЛУЧАЙНО — жадный ADDRESS-спан перелезал через перенос строки, съедал метку «Дата рождения:» и закрывал дату вместе с ней. Удаление этого перебора (цель этапа) дало `BIRTHDATE[leak_v2] 47→48`, +1; агрегат утечки при этом упал 2232→2168. Пришпилено `tests/test_addr_b_boundaries.py::test_birthdate_with_homoglyphs_is_masked` (`xfail(strict)`) плюс контрольный тест на неиспорченной дате. | СРЕДНЯЯ (1 сущность из 228; ПДн открытым текстом) | нормализация омоглифов в датах — вне адресного слоя, не запланировано |
| ADDRB-PRECISION-FRAG | метрика `measure_lib.precision_by_type` | **Precision считается В МАСКАХ, поэтому уменьшение фрагментации выглядит как падение точности.** Этап ADDR-B склеил рваные адреса: там, где эталон закрывали ДВЕ маски, стала одна. `tp` (маски на эталоне своего типа) 1409→1281 при `fp_neg` **1→1**, отчего `precision[ADDRESS]` 99.93→99.92% и линия «а» гейта покраснела, хотя ложных срабатываний не прибавилось ни одного (`on-nothing` 330→330, FP всего 16→16). Метрика наказывает за улучшение. Кандидат: считать precision по ПОКРЫТЫМ ЭТАЛОННЫМ СУЩНОСТЯМ, а не по маскам, либо нормировать `tp` на число эталонов. Трогать её этапом границ не стали: правка метрики двигает baseline всех типов сразу. | СРЕДНЯЯ (метрика вводит в заблуждение при каждой склейке спанов) | не запланировано; область — `tests/corpus/measure_lib.py` |
| ADDRB-NL-HOLE | измеритель границ (`experiments/stage_addr_b/addr_probe.py`) | Адрес, разорванный переносом строки и закрытый ДВУМЯ масками, считается «короче эталона»: открытым остаётся ровно символ `\n` между масками. 44 случая из 53 остатка группы U0. Это артефакт измерения (значение закрыто целиком), но отделять его автоматически прибор не умеет — в отчёте такие случаи видны в колонке «только пунктуация». | НЕЗНАЧИТЕЛЬНО (не дефект системы) | прибор этапа, не продукт |
| 0c-B | PASSPORT | recall_exact 0% (было и после этапа 2: 26.1→30.8% loose, exact контракт спана не чинится нормализацией) — детектор задевает паспорт, но не покрывает спан целиком → частичная утечка. | ВАЖНО | не запланирован |
| 0c-C/0c-D | ADDRESS / NER precision | ~~Жадный `yargy.AddrExtractor` цепляет голые 6-значные прогоны как индекс, прозу и не-адреса~~ **В ОСНОВНОМ ЗАКРЫТО этапом C** (2026-07-22, якорная модель адреса): ложных ADDRESS на негативах **634→1**, ложных ADDRESS всего **3483→14**, exact-границы 41.4→65.1%. Голый индекс/паспортная 6-значная группа больше не якорь (adjacency-guard `_addr_valid_index`). Остаток — 14 ложных ADDRESS, все на адверсариальных мутациях (см. Stage-C-A/B ниже). | В ОСНОВНОМ ЗАКРЫТО (остаток 14) | этап C (`src/ner_detector.py`) |
| Stage-C-A | ADDRESS под адверсариальной мутацией | Recall ADDRESS −4.7пп (93.6→**89.0%**) на мутациях (combo/linebreak-в-слове/all-caps/invisible-NBSP→пробел/digit_spaces): yargy/нормализация не собирают спан из рваного текста. **Чистотекстовый случай `agency_0009` ЗАКРЫТ** (сеяние по house-pattern, `_addr_has_seed`) — на чистом тексте потерь нет. Остаток — 61 адверсариальная мутация, оформлены явным реестром `docs/known_leaks_stage_c.json`. **Сборка РЕАЛИЗОВАНА этапом E′** («расплетающий» вид `_addr_deweave`): долг 116 (61 + 55 пред-существующих) → 57, разрывы `\n`/невидимыми/индексы закрыты; остаток пере-классифицирован в Eprime-B ниже (регистровый класс — не разрывы). | В ОСНОВНОМ ЗАКРЫТО (E′; остаток → Eprime-B) | этап E′ (`_addr_deweave`); остаток — case-нормализация |
| Stage-C-B | ADDRESS: паспорт под гомоглифом → ложный ADDRESS | 7 случаев «Паспорт NN NN NNNNNN, выдан…»: гомоглиф `О9`/`І8` (буква+цифра) рвёт цифровой guard house-pattern (`_addr_house_pattern` num-run-guard видит `О9` как слово, не число), «Паспорт NN» проходит как «улица дом». cross=PASSPORT (накрывает уже-PII, не новая утечка). | НЕЗНАЧИТЕЛЬНО (7 адверсариальных, cross-type) | нормализация буква-в-числе в цифровом guard |
| 1-A | харнесс (`measure_lib.aggregate_results`) | «Отказано» (`refused`, этап 1 fail-closed) смешивается с «упало» (`crashed = outcome != "processed"`). На штатном lossy-прогоне не проявляется (гейт не врёт); в strict-режиме харнесса дало бы ложные 135 «крешей». | НЕЗНАЧИТЕЛЬНО (латентно) | этап 6 (когда `refused_zones` начнёт пустеть) |
| 1-B | `src/extractor.py` / сканер зон | `w:sdt` (content control, частый способ вставки полей формы в шаблонах) на верхнем уровне тела **не читается extractor'ом и не детектируется сканером непрочитанных зон** — тихая потеря текста вне корпуса (в корпусе не встречается, замер не показывает). | ВАЖНО (тот же класс дефекта, что весь этап 1, но не покрыт корпусом) | этап 6 или отдельный этап 1c |
| Stage4-B | `src/regex_detector.py` PHONE / `src/ner_detector.py` ADDRESS-overcapture | До этапа 4 `ADDRESS`-NER иногда неограниченно поглощал ИНН с невалидной КС (детектор его не видел — не было барьера), заодно (случайно) пряча под тем же `[ADDRESS_n]` соседний телефон, который сам по себе НЕ детектируется: гомоглиф-мутация даёт группе `NN-Оо` менее 2 «настоящих» цифр — нормализатор намеренно не сводит омоглиф при `digit_count<2` (защита «кв. 2б»), `\d` в паттерне PHONE не матчит. Этап 4 (якорная детекция ИНН) поставил барьер верно — адрес перестал заходить за ИНН — и обнажил ЭТУ, отдельную, ранее случайно замаскированную дыру PHONE. 2 документа (`loan_0006__m2718_homoglyph`, `services_0002__m1337_linebreak`), оба — редкие адверсариальные мутации. | ВАЖНО (реальная частичная утечка телефона, но не по вине этапа 4 — вскрыта им) | не запланирован (правка `normalizer.py`/`ner_detector.py` вне границ этапа 4) |
| Stage4-C | `src/regex_detector.py` OGRN checksum | Контрольная сумма ОГРНИП (mod 13, 1/13 шанс) может случайно сойтись на ПРОИЗВОЛЬНОЙ 15-значной подстроке длинного числа-не-ПДн. До этапа 4 это было невидимо: КБК целиком поглощался BANK_ACCOUNT (приоритет `_REGEX_PRIORITY` выше OGRN) и «выигрывал» пересечение, обрезая совпавший OGRN-кусок в 0. Этап 4 (якорь `anchor:` для ACCOUNT) перестал матчить КБК как ACCOUNT — и ранее скрытая коллизия КС ОГРНИП всплыла отдельным FP на 5 документах (все с фичами `digit_spaces`/`combo`, где КБК записан через разделители, случайно попавшие в 15-значное окно). Не связано с новой якорной асимметрией INN/OGRN — путь детекции тот же, что и до этапа 4 (унанкорный + валидная КС). | НЕЗНАЧИТЕЛЬНО (FP растёт точечно, суммарный FP по негативам упал — 2284→1975) | не запланирован (КС ОГРНИП как самостоятельный фильтр коротких/произвольных чисел структурно ненадёжна; кандидат — доп. якорь-подтверждение и для унанкорного случая, отдельный этап) |
| Stage2b-A | `src/extractor.py` (регистр) × `src/normalizer.py` (омоглифы) | **Стык двух нормализаций.** Свод `_ALPHA_FOLD` регистрозависим и неполон: `'B'→'В'` есть, `'b'→'В'` НЕТ. Поэтому регистровая правка слова СМЕШАННОГО алфавита (`иBаНоB`, `ПЧЕЛЫ ЕKAТЕРИНЫ` — латиница внутри кириллицы) ломает свод: опущенная в `'b'` латинская `B` больше не сводится в кириллицу, слово остаётся рваным и ФИО не детектируется. Без щита 2b САМ создавал 6 новых утечек (2 ФИО терялись целиком, 3 фамилии оставались открытыми). Замерены и отвергнуты два более слабых щита: (1) на уровне СЛОВА — теряла 7 ФИО целиком, recall PER 67.2→63.9%; (2) на уровне СЕГМЕНТА — дырявый: соседние сегменты того же документа всё равно чинились, PER-маски смещались, а вслед за ними жадный `yargy.AddrExtractor` перестраивал спаны и ОТКРЫВАЛ адрес в другом сегменте. Принято: щит на уровне **ДОКУМЕНТА** — если хоть в одном сегменте есть слово смешанного алфавита, документ откатывается к историческому поведению эвристики. Итог: на combo 2b не добавляет ничего нового и не отнимает старого. Зеркала — `tests/test_case_detection.py::TestStage2bWordLevelCase::test_combo_segment_is_left_alone_entirely`. | ВАЖНО (непокрытая регистровая утечка на combo-мутациях; НЕ самосозданная — эти ФИО текли и до 2b) | отдельный шаг «регистр × омоглиф» (порядок нормализаций либо регистро-независимый свод) |
| B-MASKSHIFT-ORG | `src/anchor_registry.py` (`OrgAnchorDetector`) / выключение Natasha-PER | **ORG «Петров Консалтинг» потерял СЛУЧАЙНУЮ маскировку Natasha-PER.** Имя фирмы, грамматически = фамилия + слово, введённое «именуемое в дальнейшем» БЕЗ org-формы/кавычек/ИНН юрлица, структурного ORG-якоря не имеет (см. Aprime-1). До этапа B Natasha-PER случайно метила «Петров» как PERSON (маскировало → утечки не было); с выключением Natasha-PER 8 сущностей (`cession_0004`, `loan_0001` + мутации, 2 уникальных орг) вернулись в истинное неякорное состояние и текут. НЕ регрессия детекции — ровно MASK-SHIFT, как на этапе A. ORG recall при этом ВЫРОС (93.9→94.2%). | ВАЖНО (тип неверный/утечка на 2 орг, вне области структуры) | ярус recall / якорь «введение без формы» (отклонён этапом A как широкий) |
| B-ADDR-HOMONYM | `src/anchor_registry.py` (`PerAnchorDetector`) × ADDRESS | **Улица-однофамилец под АДВЕРСАРИАЛЬНОЙ мутацией метится PER.** Guard `_addr_type_left` снимает «ул./ш./г. <Фамилия>» на чистом тексте, но перенос/невидимый/регистр внутри маркера или имени рвут проверку — 9 остаточных ложных PER (адверсариальные, cross=ADDRESS). Якорный адрес конфликт НЕ разрешает: PER считается ДО адреса и служит ему БАРЬЕРОМ, поэтому улица, ошибочно помеченная PER, выигрывает. Разрешение требует правки PER-guard либо арбитража PER↔ADDRESS (риск потери реального PER). На чистом тексте ложных PER на улицах 0. | НЕЗНАЧИТЕЛЬНО (9 адверсариальных, точность) | нормализация маркера в `_addr_type_left` или арбитраж PER↔ADDRESS |
| Aprime-1 | `src/anchor_registry.py` (`OrgAnchorDetector`) | **ORG без ЛЮБОГО структурного якоря не ловится.** Если имя организации введено БЕЗ кавычек, БЕЗ org-формы рядом и БЕЗ соседнего ИНН/ОГРН юрлица (только конструкция «…, именуемое в дальнейшем «Х»»), движок сознательно не строит якорь (самостоятельный якорь на «именуем…» отклонён этапом A как слишком широкий). Если при этом имя организации ГРАММАТИЧЕСКИ похоже на ФИО («Петров Консалтинг» = фамилия + слово), оно уходит в PERSON. `cession_0004`, `loan_0001` и их мутации — 12 из 28 пропусков ORG после этапа A′. | ВАЖНО (тип неверный, утечки нет — весь текст замаскирован как PERSON) | не запланирован (нужен явный якорь «введение без формы» — риск роста FP на реальных ФИО, отклонено этапом A) |
| Aprime-2 | `src/anchor_registry.py` (алфавитный фолдинг org-формы) | **Латинский омоглиф org-формы БЕЗ кириллицы в том же токене не сводится.** `normalizer._ALPHA_FOLD` сводит латиницу→кириллицу только ВНУТРИ слова со смешанным алфавитом; в чисто латинском «AO» (маскируется под «АО») нет ни одной кириллической буквы — свод не срабатывает, `_ABBR_FORM_RE` (закрытый кириллический класс) не матчит. `sale_0009__m1337_combo`: 1 пропуск. | НЕЗНАЧИТЕЛЬНО (1 документ, адверсариальная комбо-мутация) | не запланирован (расширение `_ABBR_FORM_RE` на латинские гомоглифы форм — отдельная точечная правка) |
| Aprime-3 | `src/tokenizer.py` (`_render_table`, `\n`→пробел) | **Асимметрия схлопывания пробелов в таблице между «маскировано» и «эталон».** `_render_table` заменяет `\n` внутри ячейки на пробел ПОСЛЕ подстановки токенов: если `\n` попал ВНУТРЬ маскированного спана, токен «прячет» его от замены, а `build_plain_text` (без масок) заменяет тот же `\n` всегда → whole-document `restored == build_plain_text(doc)` не совпадает буквально (харнесс относит это к `channel_ws_only`, `measure_lib.mc_check_a` явно исключает класс из masking A). ПРЕД-СУЩЕСТВУЮЩИЙ (воспроизводится на `labor_0006`/`labor_0008`/`lease_0008`/`supply_0002`/`supply_0014` без правок этапа A′ и даже без `detect_org` вовсе). Этап A′ его не создал, но делает вероятнее. | ВАЖНО (искажает грубую whole-document sha/diff проверку; НЕ реальная утечка/порча — per-mask A = 100%) | не запланирован (симметризация `\n`→пробел между эталоном и маскированием — правка `tokenizer.py`) |
| EPRIME-A-CONFIRM | `src/extractor.py` (`_CapsResolver`) × реальный документ владельца | **Механизм недетерминизма найден и устранён, но подтверждения на реальном документе владельца ПОСЛЕ фикса ни один отчёт не содержит.** Этап E″ нашёл причину (кеш `_CapsResolver` ключевался `id(style._element)` без удержания ссылки — CPython переиспользовал адрес, чужой вердикт капса возвращался из кеша), исправил её (`src/extractor.py:216,222` — ключ по объекту) и закрыл находку `EPRIME-A-CAPS`. Фикс подтверждён на синтетической фикстуре многостилья (2660 ложных попаданий → 0, 4 разных sha → 1) и структурно-поведенческими стражами `tests/test_determinism.py`. Чего в отчётах НЕТ: повторного прогона `experiments/stage_eprime_determinism/owner_repro.py` на реальном договоре владельца после фикса — то есть end-to-end подтверждения, что счётчики масок (`[237,224,226,224,233,224]`) сошлись. Пока его нет, «недетерминизм на целевом жанре закрыт» остаётся выводом по механизму, а не измерением. | СРЕДНЯЯ (долг проверки, не дефект: механизм устранён и закрыт стражами) | один прогон `owner_repro.py` на машине владельца, число прогонов ≥6, сверка sha256 |
| Eprime-B | остаток утечки-долга ADDRESS | Пере-классифицированный остаток после сборки E′ (все — адверсариальные мутации; поимённо в `docs/known_leaks_stage_c.json` с причиной у каждой записи): **case + combo** — регистровый класс (Titlecase-критерии якоря слепнут на сплошном регистре; закрывается регистровой нормализацией адреса, не сборкой); **linebreak** — улицы-однофамильцы (B-ADDR-HOMONYM, PER-арбитраж) и гибриды с case (`\n`-склейка намеренно требует строчного продолжения); **homoglyph** — edge-«б» нормализатора. ⚠ Число долга в этой записи исторически называлось **57**; фактический реестр и полный прогон гейта дают **43** — расхождение не разрешено, см. [`STATE.md`](STATE.md) §5. | ВАЖНО (recall на адверсариальных) | case-нормализация адреса / PER↔ADDRESS-арбитраж / normalizer-edge — отдельные этапы |
| Stage-Cprime-A | `src/anchor_registry.py` (`OrgAnchorDetector._name_run_before_requisite`) | **Реквизитная ветвь ORG даёт усечённый нарицательный фрагмент.** Name-run слева от ОГРН иногда захватывает только ХВОСТ многословного названия реальной фирмы (одно нарицательное единственного числа, напр. «…Технологий»), регистрирует его ключом ORG, и проход 2 тиражирует нарицательное. На реальном договоре — 1 группа ×1 маска (не роль-термин, не утечка ПДн). Кандидат: требовать у name-run перед ИНН/ОГРН примыкающую org-форму или кавычки (как у кавычной ветви). | НЕЗНАЧИТЕЛЬНО (точность, 1 нарицательное) | отдельный этап (ужесточение реквизитной ветви) |
| Stage-Cprime-B | `src/anchor_registry.py` (`OrgAnchorDetector._name_run_before_requisite`) × PER | **Реквизитная ветвь втягивает соседнее ФИО в ORG-ключ.** «ООО "Name" <ФИО-руководителя в родительном> … ОГРН …»: name-run перед ОГРН перетягивает имя человека в ключ ORG. Человек ЗАМАСКИРОВАН (утечки нет), но тип ORG вместо PER (cross-type, домен masking C). На реальном договоре — 1 группа. Кандидат: обрывать name-run на смене падежа/типа или барьером PER-спанов. | НЕЗНАЧИТЕЛЬНО (тип неверный, утечки нет) | отдельный этап (граница name-run) |
| Stage-E-A | `src/multispan.py` (PHONE-сборщик) × INN/PASSPORT | **Цепочка ≥2 цифрогрупп с 10 эффективными цифрами и ведущей 7/8 может быть не телефоном** (безъякорный ИНН-10 региона 7x/8x, паспортная пара «серия номер»). На корпусе дало НОЛЬ новых `fp_neg` и даже снизило утечку PASSPORT (282→268) и SNILS (39→35) — маскирование чужого значения безопаснее утечки; PHONE cross-type 11→29 (диагностика C, не утечка). Приоритеты пересечений защищают: анкорный/валидный INN и SNILS выигрывают у PHONE. Наблюдать при смене корпуса. | НЕЗНАЧИТЕЛЬНО (cross-type, направление безопасное) | наблюдение на гейте |
| Stage-E-B | `src/file_detokenizer.py` × единый id | **decrypt-file подставляет форму ПЕРВОГО вхождения, не канон.** Файловый путь строит mapping `token→original_text` (v1-поле = поверхностная форма первого вхождения); с единым токеном ORG/PER все вхождения в файле получат эту форму вместо канона/своих форм. Текстовый путь (CLI/UI) подставляет канон. Сессия v2 хранит все формы (`occurrences`) — приведение файлового пути к канону = отдельное решение. | НЕЗНАЧИТЕЛЬНО (форма, не утечка) | отдельный этап (канон в `file_detokenizer`) |
| E2-CORPUS-POVERTY | `tests/corpus/**` (охват, не код) | **Синтетический корпус структурно БЕДНЕЕ реальных документов и потому не воспроизводит целый класс дефектов.** Реальный договор владельца — **1097 стилей**; документы `tests/corpus/` — **2 стиля** каждый. Именно поэтому дефект `_CapsResolver` прожил незамеченным через этапы A/A′/B/C/C′/D/E/E′: под старой (дефектной) реализацией на корпусе 0 ложных попаданий, дефект физически не срабатывает. Следствие: корпус хорошо проверяет ПРАВИЛА детекции, но почти не проверяет устойчивость к структурному разнообразию реального `.docx` (стили и их наследование, разнообразие лексики, зоны). Всё, что зависит от богатства структуры — кеши по стилям, наследование форматирования, `w:sdt` (см. 1-B) — корпусом НЕ покрывается принципиально. Частично закрыто фикстурой `tests/fixtures/synthetic_many_styles.docx` (284 стиля) как постоянным стражем. **«Зелёный корпус» ≠ «работает на реальных документах».** | ВАЖНО (ограничение охвата, не дефект кода) | частично закрыто (фикстура многостилья); полноценно — реальные-подобные документы в наборе |
| V2-WALL-NOGUARD | стена между `src/` и генератором корпуса v2 | **Запрет «детекторы не читают `tests/corpus_v2/values.py`, `generate.py` и эталон» не имеет исполняемого стража.** Проверено прямым чтением импортов: совпадений в `src/` нет, все вхождения подстроки «values» — вызовы `dict.values()`. Но AST-скана/import-graph-теста нет ни одного; стена держится процессной дисциплиной и нарративной гарантией в отчётах. Нарушение будет обнаружено только чтением кода — то есть, возможно, никогда, а цена его — цифры замера, которые перестают что-либо значить. | ВАЖНО (гарантия честности всех замеров новых типов) | тест на импорт-граф `src/` (дешёвый, не требует прогона корпуса) |

---

## Снято с этого файла этапом DOC (2026-08-04)

Перенесено в [`archive/FINDINGS_closed.md`](archive/FINDINGS_closed.md):

| id | почему снято |
|---|---|
| `D-PRECISION-90`, `0c-H`, `Stage4-A`, `Stage2b-B`, `PER-B` | Несли колонку «тяжесть» = `ЗАКРЫТО`, то есть нарушали заявленную политику файла |
| `PER-B(hist)` | Историческое описание закрытой `PER-B`, помечено `~~зачёркиванием~~` в самом заголовке |
| `U4-G-TIMEOUT` | **Закрыт коммитом `e44ac61` (2026-07-26):** этап O2 вернул вложенный прогон 488 c → 295 c, жёсткий лимит поднят 300 → 900 c, докстринг теста прямо говорит «находка U4-G-TIMEOUT закрыта» (`tests/component2/test_g_regression.py:34-55`). Числа записи («1 failed, 976 passed») относятся к HEAD `abf3ee8` от 2026-07-25, то есть к состоянию ДО починки |
| `Eprime-A` (огромные документы) и `Eprime-A` (реальный документ) | **Обе описывают тот же недетерминизм, который третья запись `Eprime-A` объявляет ЗАКРЫТЫМ.** Этап E″ нашёл механизм (`_CapsResolver`, ключ по `id()`), устранил его и прямо перечислил «инференс Natasha/numpy» и «холодный BLAS» среди ОПРОВЕРГНУТЫХ гипотез — а это и есть вывод обеих оставленных записей. Фикс присутствует в коде (`src/extractor.py:216,222`) и закрыт стражами `tests/test_determinism.py`. Остаточный долг подтверждения выделен в новую запись `EPRIME-A-CONFIRM` выше |

**Про id `Eprime-A`.** Он стоял на ТРЁХ разных находках сразу (две открытые, одна
закрытая), из-за чего любая перекрёстная ссылка на него была неразрешима. При
переносе они разведены: `EPRIME-A-CAPS` (закрытая, механизм и фикс),
`EPRIME-A-LARGE` и `EPRIME-A-REAL` (обе — симптом, поглощены `EPRIME-A-CAPS`),
`EPRIME-A-CONFIRM` (открытый долг подтверждения). Прежний id `Eprime-A` больше
не используется ни для чего.

**Новая запись `V2-WALL-NOGUARD`** заведена в этом этапе: отсутствие стража стены
было зафиксировано разведкой S0 как факт, но в списке долгов не числилось.

Прочие открытые ограничения охвата (не «находки», а нереализованные
детекторы/зоны) — `DATE` (детектор выключен конфигом), непрочитанные зоны `.docx`
(этап 1, fail-closed), извлечение PDF, веб-компонент. См. [`STATE.md`](STATE.md).
