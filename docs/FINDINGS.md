# FINDINGS — открытые долги

**Только то, что ещё не закрыто.** Закрытые и исторические находки —
[`docs/archive/FINDINGS_closed.md`](archive/FINDINGS_closed.md); id сохраняются
для перекрёстных ссылок и не переиспользуются. Краткий статус — [`STATE.md`](STATE.md).

Актуализирован: **2026-08-08**, этапы TYPE-FACTORY-2 (фабрика типов) и V1-SCOPE (исключение `CONTRACT_NO` из точности v1 — запись `V1-SCOPE-CONTRACTNO`). `V2-WALL-NOGUARD` **ЗАКРЫТА** (AST-страж импортов src/ + белый список сборки + самопроверка подстрок — tests/test_type_factory.py::TestWall), `V2-TRANCHE-NOGUARD` **ЗАКРЫТА вместе со снятием запрета** (запрет 6 снят владельцем: черновик из живой документации; сторож «TRANCHE с числом» в validate.py остаётся, corpus_only-тип с detect-секцией роняет сборку). Обе перенесены в архив. Добавлена `SUMWORDS-DEFERRED`.

До того — этап A6 (сборка значений, разорванных вёрсткой).
`A5-NL-CROSS-RESIDUE` **ЗАКРЫТА** (сборка через стык граничного окна; остаток
поимённо классифицирован, весь пред-существующий). Добавлены
`A6-DESTRUCTIVE-TRANSFORMS` (Часть 3: 11 преобразований, рушащих значение до
детектора, с файл:строка) и `A6-REQ-NAMERUN-OVERREACH` (латентный ORG-якорь на
чужой строке, вскрыт вводом мультиспан-реквизитов, закрыт инвариантом в
`pipeline.py`).

До того — этап A3 (отрицательные примеры и хронометр).
`A2-NO-NEGATIVES-13-TYPES` **ЗАКРЫТА** — у 12 из 13 названных типов появились
типизированные негативы, по 13-му (`EMAIL`) вынесено письменное «нечем»;
перенесена в [`archive/FINDINGS_closed.md`](archive/FINDINGS_closed.md).
Добавлены 13 записей `A3-NEG-<ТИП>` (по одной на тип, с числом ошибочных масок)
и `A3-GATE-OVERMASK-LINE` (предложение линии гейта с ценой).

**ОГОВОРКА, ОБЩАЯ ДЛЯ ВСЕХ ЗАПИСЕЙ `A3-NEG-*` — читать до цифр.** На каждый тип
заведён ОДИН (у четырёх типов — два) вид отрицательного примера, по одному
вхождению на документ, и левое окружение у него в подавляющем большинстве
случаев ОДНО И ТО ЖЕ (замер `experiments/stage_a3/variety.py`: у 8 из 16 видов
различных левых окон ровно 1). Поэтому «138 из 138» означает **одну дыру,
повторённую в 138 документах**, а НЕ масштаб проблемы: числитель и знаменатель
здесь про воспроизводимость, не про распространённость. Прежде чем чинить
любой из этих типов, набор отрицательных примеров надо расширить — иначе
починим ровно тот случай, который сами и придумали. Отдельная колонка «в
реальном договоре» показывает, встречается ли класс вне корпуса
(`experiments/stage_a3/real_probe.py`, только количества).

До того — этап A2 (три типа с тем же классом жёсткого
гэпа: код подразделения, СНИЛС, дата рождения). `A1-PASSPORT-QUALITY-FP`
**ЗАКРЫТА** (anti_anchor в `entity_types.yaml`, 138/138→0/138 на «паспорт
качества»; настоящие паспорта не потеряны, 29/30 на целевом документе) и
`A1-RIGID-GAP-SURVEY` **ЗАКРЫТА** (все три названных типа получили расширенный
гэп/якорь, измерено на реальном документе и на корпусе), обе перенесены в
[`archive/FINDINGS_closed.md`](archive/FINDINGS_closed.md). Добавлены три
записи: `A2-NEWLINE-CROSS` (найдена экспериментально — расширение `\s?`
скрестило структурную границу сегмента, дало измеренную регрессию, откачено
для трёх типов этапа, два места того же класса остаются непроверены),
`A2-SNILS-BIRTHDATE-GAP-PRECISION` (измеренный компромисс точности от
расширения гэпа) и `A2-NO-NEGATIVES-13-TYPES` (разведка Части 5: 13 из 18
маскируемых типов не имеют вовсе ни одного типизированного негатива).

До того — этап A1 (2026-08-06, детектор паспорта): `DOG-PASSPORT-GAP`
**ЗАКРЫТА** (2→29 паспортов из 30 на целевом документе).

До того — этап DOG (2026-08-05, измерение реального договора): добавлены
`DOG-NODE-SILENT`, `DOG-ANCHOR-SPLIT`, `DOG-KPP-NOANCHOR`. Сняты
`EPRIME-A-CONFIRM` — **ЗАКРЫТА** измерением (десять прогонов, шесть слоёв,
ноль расхождений), `1-B` — **поглощена** `DOG-NODE-SILENT` (была про один вид
узла, видов оказалось пять).

До того — этап TESTOPT (`STORE-HOME-CACHE`, `STORE-ACCUM-CONFIRM`), до того —
C+ (`V2-TRANCHE-NOGUARD`), до того — DOC. Из файла вынесено 10 строк: шесть с
пометкой `ЗАКРЫТО`, историческое описание закрытой `PER-B`, а также
`U4-G-TIMEOUT` и две из трёх записей `Eprime-A` — обоснование в разделе
«Снято» в конце файла.

| id | область | находка | тяжесть | чинится |
|---|---|---|---|---|
| V1-SCOPE-CONTRACTNO | `tests/corpus/scope_exclusions.py` × замороженная разметка `tests/corpus/gold.json` | **`CONTRACT_NO` снят с линии «а»/FP корпуса v1: 315 «ложных» срабатываний — столкновение двух определений, а не дефект.** Разметка v1 заморожена под «номер договора — не ПДн»; владелец принял обратное решение (номер договора маскируется). Детектор верен, прибор негоден для этого типа. Исключение УЗКОЕ: одна линия одного типа одного корпуса; линии «б» (утечка), «д» (порча), «е» (границы) на v1 и весь корпус v2 продолжают мерить тип полностью (на v2: 138/138 найдено, точность 50,00 % — 138 fp это негативы DOCNUM, ждущие реклассификации). Замок: запись без автора/обоснования роняет сборку; цифра печатается отдельной строкой отчёта. **Запись закрывается не правкой кода, а реклассификацией разметки** — либо переразметкой v1 (сегодня запрещена заморозкой), либо решением владельца о судьбе negativ'ов «номер договора». | СРЕДНЯЯ (тип не охраняется линией точности на v1; на v2 охраняется полностью) | реклассификация DOCNUM в v2 (решение принято, сессия разметки не проведена); v1 — только вместе с решением о заморозке |
| SUMWORDS-DEFERRED | `tests/corpus_v2/typedefs/SUM.yaml` (форма 2 блока MONEY_WORDS) | **Чисто словесная сумма («восемьсот тысяч рублей») не детектируется — отложено с числом.** Решение владельца TYPE-FACTORY-2: словесные суммы бьются как численные. Форма 1 (составная, «цифры (пропись) валюта [копейки]») СДЕЛАНА: в v2 было 78 полных утечек, стало 0 открытых хвостов. Форма 2 — без цифр вовсе: в v2 ~53 кандидата (грубая верхняя оценка регэкспом), в v1 — 0. Ветвь через закрытый список корней числительных в сессию честно не влезла. | СРЕДНЯЯ (утечка сумм в редкой форме записи; в v1 формы нет вовсе) | отдельный typedef-ветвь SUM (класс «слово закрытого списка»), кандидат следующей сессии фабрики |
| TGOLDA-SUM-CASE | `entity_types.yaml`, паттерн `SUM` | **Все промахи и все утечки типа `SUM` — на adversarial-формах, ни одной на canonical.** Первый замер SUM как эталонного типа (этап T-GOLD-A, 648 сущностей): recall 97.5%, не найдено 16, утечка leak_v2>=6 — 28. Разрез по категории: canonical (601 шт.) — recall 100%, утечка 0; adversarial (47 шт.) — вся потеря там. По приёмам промахи: combo 6, case 5, homoglyph 3, linebreak 2. Самый дешёвый и самый частый — **регистр**: паттерн `SUM` не имеет `(?i)`, поэтому «732 589,00 РУБ.» не матчится вовсе (`miss_reason: nothing`), тогда как остальные типы давно смотрят на `detection_text` с нормализованным регистром. Остальные приёмы — омоглиф на месте цифры («7Зч 525,оО»), перенос строки внутри слова валюты («ру» + `\n` + «б.»). Замечание к самой метрике: десятичная запятая рвёт цифровой run, поэтому утечка суммы видна только мягким порогом (>=6), а строгий (>=8) по SUM даёт 0 — это свойство метрики, не заслуга детектора (см. комментарий у `V2_NUMERIC_TYPES` в `run_measurement.py`). | СРЕДНЯЯ (2.5% типа; правильная маскировка сумм — решение владельца, а не необязательная опция) | не запланировано; кандидат — `(?i)` в паттерне `SUM` плюс проверка, что детекция идёт по `detection_text`, с обязательным прогоном гейта: расширение паттерна суммы способно поднять over-mask на прозе |
| Stage3-A | BIRTHDATE | Недобор спана на ОДИН символ в 4 случаях `m2718_homoglyph`: первая цифра даты подменена омоглифом («07.07.1963» → «З7.07.1963»), нормализация её не сводит (в токене «З7» лишь одна настоящая цифра, порог свода — две), и якорный зазор `[^\n\d]{0,10}` съедает «З». Маскируется «7.07.1963». Направление ошибки безопасное (недобор, не перебор); альтернатива — запрет букв в зазоре — обменяла бы 4 недобора на 4 полных пропуска. | НЕЗНАЧИТЕЛЬНО | не запланирован |
| ADDRB-BIRTHDATE-EXPOSED | `src/normalizer.py` × BIRTHDATE (частный случай B4/Stage4-B) | **Дата рождения с омоглифами вместо цифр не детектируется и с этапа ADDR-B больше ничем не прикрыта.** `services_0001__m1337_combo`, строка «Дата рождения: 21.0І.196О» (кириллические `І`/`О` вместо `1`/`0`): детектор BIRTHDATE её не берёт НИКОГДА (`found=False` и до, и после этапа), нормализатор НАМЕРЕННО не сводит одиночные буквы внутри чисел (защита «кв. 2б»). До ADDR-B значение пряталось СЛУЧАЙНО — жадный ADDRESS-спан перелезал через перенос строки, съедал метку «Дата рождения:» и закрывал дату вместе с ней. Удаление этого перебора (цель этапа) дало `BIRTHDATE[leak_v2] 47→48`, +1; агрегат утечки при этом упал 2232→2168. Пришпилено `tests/test_addr_b_boundaries.py::test_birthdate_with_homoglyphs_is_masked` (`xfail(strict)`) плюс контрольный тест на неиспорченной дате. | СРЕДНЯЯ (1 сущность из 228; ПДн открытым текстом) | нормализация омоглифов в датах — вне адресного слоя, не запланировано |
| ADDRB-PRECISION-FRAG | метрика `measure_lib.precision_by_type` | **Precision считается В МАСКАХ, поэтому уменьшение фрагментации выглядит как падение точности.** Этап ADDR-B склеил рваные адреса: там, где эталон закрывали ДВЕ маски, стала одна. `tp` (маски на эталоне своего типа) 1409→1281 при `fp_neg` **1→1**, отчего `precision[ADDRESS]` 99.93→99.92% и линия «а» гейта покраснела, хотя ложных срабатываний не прибавилось ни одного (`on-nothing` 330→330, FP всего 16→16). Метрика наказывает за улучшение. Кандидат: считать precision по ПОКРЫТЫМ ЭТАЛОННЫМ СУЩНОСТЯМ, а не по маскам, либо нормировать `tp` на число эталонов. Трогать её этапом границ не стали: правка метрики двигает baseline всех типов сразу. | СРЕДНЯЯ (метрика вводит в заблуждение при каждой склейке спанов) | не запланировано; область — `tests/corpus/measure_lib.py` |
| ADDRB-NL-HOLE | измеритель границ (`experiments/stage_addr_b/addr_probe.py`) | Адрес, разорванный переносом строки и закрытый ДВУМЯ масками, считается «короче эталона»: открытым остаётся ровно символ `\n` между масками. 44 случая из 53 остатка группы U0. Это артефакт измерения (значение закрыто целиком), но отделять его автоматически прибор не умеет — в отчёте такие случаи видны в колонке «только пунктуация». | НЕЗНАЧИТЕЛЬНО (не дефект системы) | прибор этапа, не продукт |
| 0c-B | PASSPORT | recall_exact 0% (было и после этапа 2: 26.1→30.8% loose, exact контракт спана не чинится нормализацией) — детектор задевает паспорт, но не покрывает спан целиком → частичная утечка. | ВАЖНО | не запланирован |
| 0c-C/0c-D | ADDRESS / NER precision | ~~Жадный `yargy.AddrExtractor` цепляет голые 6-значные прогоны как индекс, прозу и не-адреса~~ **В ОСНОВНОМ ЗАКРЫТО этапом C** (2026-07-22, якорная модель адреса): ложных ADDRESS на негативах **634→1**, ложных ADDRESS всего **3483→14**, exact-границы 41.4→65.1%. Голый индекс/паспортная 6-значная группа больше не якорь (adjacency-guard `_addr_valid_index`). Остаток — 14 ложных ADDRESS, все на адверсариальных мутациях (см. Stage-C-A/B ниже). | В ОСНОВНОМ ЗАКРЫТО (остаток 14) | этап C (`src/ner_detector.py`) |
| Stage-C-A | ADDRESS под адверсариальной мутацией | Recall ADDRESS −4.7пп (93.6→**89.0%**) на мутациях (combo/linebreak-в-слове/all-caps/invisible-NBSP→пробел/digit_spaces): yargy/нормализация не собирают спан из рваного текста. **Чистотекстовый случай `agency_0009` ЗАКРЫТ** (сеяние по house-pattern, `_addr_has_seed`) — на чистом тексте потерь нет. Остаток — 61 адверсариальная мутация, оформлены явным реестром `docs/known_leaks_stage_c.json`. **Сборка РЕАЛИЗОВАНА этапом E′** («расплетающий» вид `_addr_deweave`): долг 116 (61 + 55 пред-существующих) → 57, разрывы `\n`/невидимыми/индексы закрыты; остаток пере-классифицирован в Eprime-B ниже (регистровый класс — не разрывы). | В ОСНОВНОМ ЗАКРЫТО (E′; остаток → Eprime-B) | этап E′ (`_addr_deweave`); остаток — case-нормализация |
| Stage-C-B | ADDRESS: паспорт под гомоглифом → ложный ADDRESS | 7 случаев «Паспорт NN NN NNNNNN, выдан…»: гомоглиф `О9`/`І8` (буква+цифра) рвёт цифровой guard house-pattern (`_addr_house_pattern` num-run-guard видит `О9` как слово, не число), «Паспорт NN» проходит как «улица дом». cross=PASSPORT (накрывает уже-PII, не новая утечка). | НЕЗНАЧИТЕЛЬНО (7 адверсариальных, cross-type) | нормализация буква-в-числе в цифровом guard |
| 1-A | харнесс (`measure_lib.aggregate_results`) | «Отказано» (`refused`, этап 1 fail-closed) смешивается с «упало» (`crashed = outcome != "processed"`). На штатном lossy-прогоне не проявляется (гейт не врёт); в strict-режиме харнесса дало бы ложные 135 «крешей». | НЕЗНАЧИТЕЛЬНО (латентно) | этап 6 (когда `refused_zones` начнёт пустеть) |
| Stage4-B | `src/regex_detector.py` PHONE / `src/ner_detector.py` ADDRESS-overcapture | До этапа 4 `ADDRESS`-NER иногда неограниченно поглощал ИНН с невалидной КС (детектор его не видел — не было барьера), заодно (случайно) пряча под тем же `[ADDRESS_n]` соседний телефон, который сам по себе НЕ детектируется: гомоглиф-мутация даёт группе `NN-Оо` менее 2 «настоящих» цифр — нормализатор намеренно не сводит омоглиф при `digit_count<2` (защита «кв. 2б»), `\d` в паттерне PHONE не матчит. Этап 4 (якорная детекция ИНН) поставил барьер верно — адрес перестал заходить за ИНН — и обнажил ЭТУ, отдельную, ранее случайно замаскированную дыру PHONE. 2 документа (`loan_0006__m2718_homoglyph`, `services_0002__m1337_linebreak`), оба — редкие адверсариальные мутации. | ВАЖНО (реальная частичная утечка телефона, но не по вине этапа 4 — вскрыта им) | не запланирован (правка `normalizer.py`/`ner_detector.py` вне границ этапа 4) |
| Stage4-C | `src/regex_detector.py` OGRN checksum | Контрольная сумма ОГРНИП (mod 13, 1/13 шанс) может случайно сойтись на ПРОИЗВОЛЬНОЙ 15-значной подстроке длинного числа-не-ПДн. До этапа 4 это было невидимо: КБК целиком поглощался BANK_ACCOUNT (приоритет `_REGEX_PRIORITY` выше OGRN) и «выигрывал» пересечение, обрезая совпавший OGRN-кусок в 0. Этап 4 (якорь `anchor:` для ACCOUNT) перестал матчить КБК как ACCOUNT — и ранее скрытая коллизия КС ОГРНИП всплыла отдельным FP на 5 документах (все с фичами `digit_spaces`/`combo`, где КБК записан через разделители, случайно попавшие в 15-значное окно). Не связано с новой якорной асимметрией INN/OGRN — путь детекции тот же, что и до этапа 4 (унанкорный + валидная КС). | НЕЗНАЧИТЕЛЬНО (FP растёт точечно, суммарный FP по негативам упал — 2284→1975) | не запланирован (КС ОГРНИП как самостоятельный фильтр коротких/произвольных чисел структурно ненадёжна; кандидат — доп. якорь-подтверждение и для унанкорного случая, отдельный этап) |
| Stage2b-A | `src/extractor.py` (регистр) × `src/normalizer.py` (омоглифы) | **Стык двух нормализаций.** Свод `_ALPHA_FOLD` регистрозависим и неполон: `'B'→'В'` есть, `'b'→'В'` НЕТ. Поэтому регистровая правка слова СМЕШАННОГО алфавита (`иBаНоB`, `ПЧЕЛЫ ЕKAТЕРИНЫ` — латиница внутри кириллицы) ломает свод: опущенная в `'b'` латинская `B` больше не сводится в кириллицу, слово остаётся рваным и ФИО не детектируется. Без щита 2b САМ создавал 6 новых утечек (2 ФИО терялись целиком, 3 фамилии оставались открытыми). Замерены и отвергнуты два более слабых щита: (1) на уровне СЛОВА — теряла 7 ФИО целиком, recall PER 67.2→63.9%; (2) на уровне СЕГМЕНТА — дырявый: соседние сегменты того же документа всё равно чинились, PER-маски смещались, а вслед за ними жадный `yargy.AddrExtractor` перестраивал спаны и ОТКРЫВАЛ адрес в другом сегменте. Принято: щит на уровне **ДОКУМЕНТА** — если хоть в одном сегменте есть слово смешанного алфавита, документ откатывается к историческому поведению эвристики. Итог: на combo 2b не добавляет ничего нового и не отнимает старого. Зеркала — `tests/test_case_detection.py::TestStage2bWordLevelCase::test_combo_segment_is_left_alone_entirely`. | ВАЖНО (непокрытая регистровая утечка на combo-мутациях; НЕ самосозданная — эти ФИО текли и до 2b) | отдельный шаг «регистр × омоглиф» (порядок нормализаций либо регистро-независимый свод) |
| B-MASKSHIFT-ORG | `src/anchor_registry.py` (`OrgAnchorDetector`) / выключение Natasha-PER | **ORG «Петров Консалтинг» потерял СЛУЧАЙНУЮ маскировку Natasha-PER.** Имя фирмы, грамматически = фамилия + слово, введённое «именуемое в дальнейшем» БЕЗ org-формы/кавычек/ИНН юрлица, структурного ORG-якоря не имеет (см. Aprime-1). До этапа B Natasha-PER случайно метила «Петров» как PERSON (маскировало → утечки не было); с выключением Natasha-PER 8 сущностей (`cession_0004`, `loan_0001` + мутации, 2 уникальных орг) вернулись в истинное неякорное состояние и текут. НЕ регрессия детекции — ровно MASK-SHIFT, как на этапе A. ORG recall при этом ВЫРОС (93.9→94.2%). | ВАЖНО (тип неверный/утечка на 2 орг, вне области структуры) | ярус recall / якорь «введение без формы» (отклонён этапом A как широкий) |
| B-ADDR-HOMONYM | `src/anchor_registry.py` (`PerAnchorDetector`) × ADDRESS | **Улица-однофамилец под АДВЕРСАРИАЛЬНОЙ мутацией метится PER.** Guard `_addr_type_left` снимает «ул./ш./г. <Фамилия>» на чистом тексте, но перенос/невидимый/регистр внутри маркера или имени рвут проверку — 9 остаточных ложных PER (адверсариальные, cross=ADDRESS). Якорный адрес конфликт НЕ разрешает: PER считается ДО адреса и служит ему БАРЬЕРОМ, поэтому улица, ошибочно помеченная PER, выигрывает. Разрешение требует правки PER-guard либо арбитража PER↔ADDRESS (риск потери реального PER). На чистом тексте ложных PER на улицах 0. | НЕЗНАЧИТЕЛЬНО (9 адверсариальных, точность) | нормализация маркера в `_addr_type_left` или арбитраж PER↔ADDRESS |
| Aprime-1 | `src/anchor_registry.py` (`OrgAnchorDetector`) | **ORG без ЛЮБОГО структурного якоря не ловится.** Если имя организации введено БЕЗ кавычек, БЕЗ org-формы рядом и БЕЗ соседнего ИНН/ОГРН юрлица (только конструкция «…, именуемое в дальнейшем «Х»»), движок сознательно не строит якорь (самостоятельный якорь на «именуем…» отклонён этапом A как слишком широкий). Если при этом имя организации ГРАММАТИЧЕСКИ похоже на ФИО («Петров Консалтинг» = фамилия + слово), оно уходит в PERSON. `cession_0004`, `loan_0001` и их мутации — 12 из 28 пропусков ORG после этапа A′. | ВАЖНО (тип неверный, утечки нет — весь текст замаскирован как PERSON) | не запланирован (нужен явный якорь «введение без формы» — риск роста FP на реальных ФИО, отклонено этапом A) |
| Aprime-2 | `src/anchor_registry.py` (алфавитный фолдинг org-формы) | **Латинский омоглиф org-формы БЕЗ кириллицы в том же токене не сводится.** `normalizer._ALPHA_FOLD` сводит латиницу→кириллицу только ВНУТРИ слова со смешанным алфавитом; в чисто латинском «AO» (маскируется под «АО») нет ни одной кириллической буквы — свод не срабатывает, `_ABBR_FORM_RE` (закрытый кириллический класс) не матчит. `sale_0009__m1337_combo`: 1 пропуск. | НЕЗНАЧИТЕЛЬНО (1 документ, адверсариальная комбо-мутация) | не запланирован (расширение `_ABBR_FORM_RE` на латинские гомоглифы форм — отдельная точечная правка) |
| Aprime-3 | `src/tokenizer.py` (`_render_table`, `\n`→пробел) | **Асимметрия схлопывания пробелов в таблице между «маскировано» и «эталон».** `_render_table` заменяет `\n` внутри ячейки на пробел ПОСЛЕ подстановки токенов: если `\n` попал ВНУТРЬ маскированного спана, токен «прячет» его от замены, а `build_plain_text` (без масок) заменяет тот же `\n` всегда → whole-document `restored == build_plain_text(doc)` не совпадает буквально (харнесс относит это к `channel_ws_only`, `measure_lib.mc_check_a` явно исключает класс из masking A). ПРЕД-СУЩЕСТВУЮЩИЙ (воспроизводится на `labor_0006`/`labor_0008`/`lease_0008`/`supply_0002`/`supply_0014` без правок этапа A′ и даже без `detect_org` вовсе). Этап A′ его не создал, но делает вероятнее. | ВАЖНО (искажает грубую whole-document sha/diff проверку; НЕ реальная утечка/порча — per-mask A = 100%) | не запланирован (симметризация `\n`→пробел между эталоном и маскированием — правка `tokenizer.py`) |
| Eprime-B | остаток утечки-долга ADDRESS | Пере-классифицированный остаток после сборки E′ (все — адверсариальные мутации; поимённо в `docs/known_leaks_stage_c.json` с причиной у каждой записи): **case + combo** — регистровый класс (Titlecase-критерии якоря слепнут на сплошном регистре; закрывается регистровой нормализацией адреса, не сборкой); **linebreak** — улицы-однофамильцы (B-ADDR-HOMONYM, PER-арбитраж) и гибриды с case (`\n`-склейка намеренно требует строчного продолжения); **homoglyph** — edge-«б» нормализатора. ⚠ Число долга в этой записи исторически называлось **57** — это уровень НА ДАТУ E′ (2026-07-23). Рабочее число сегодня — **43**; переход разобран по коммитам этапом C+ (два шага: ADDR-B 57→44, T-ARB 44→43), см. [`JOURNAL.md`](JOURNAL.md), запись 2026-08-04 C+ часть 2. Расхождения нет, есть устаревшая цифра в тексте этой записи. | ВАЖНО (recall на адверсариальных) | case-нормализация адреса / PER↔ADDRESS-арбитраж / normalizer-edge — отдельные этапы |
| Stage-Cprime-A | `src/anchor_registry.py` (`OrgAnchorDetector._name_run_before_requisite`) | **Реквизитная ветвь ORG даёт усечённый нарицательный фрагмент.** Name-run слева от ОГРН иногда захватывает только ХВОСТ многословного названия реальной фирмы (одно нарицательное единственного числа, напр. «…Технологий»), регистрирует его ключом ORG, и проход 2 тиражирует нарицательное. На реальном договоре — 1 группа ×1 маска (не роль-термин, не утечка ПДн). Кандидат: требовать у name-run перед ИНН/ОГРН примыкающую org-форму или кавычки (как у кавычной ветви). | НЕЗНАЧИТЕЛЬНО (точность, 1 нарицательное) | отдельный этап (ужесточение реквизитной ветви) |
| Stage-Cprime-B | `src/anchor_registry.py` (`OrgAnchorDetector._name_run_before_requisite`) × PER | **Реквизитная ветвь втягивает соседнее ФИО в ORG-ключ.** «ООО "Name" <ФИО-руководителя в родительном> … ОГРН …»: name-run перед ОГРН перетягивает имя человека в ключ ORG. Человек ЗАМАСКИРОВАН (утечки нет), но тип ORG вместо PER (cross-type, домен masking C). На реальном договоре — 1 группа. Кандидат: обрывать name-run на смене падежа/типа или барьером PER-спанов. | НЕЗНАЧИТЕЛЬНО (тип неверный, утечки нет) | отдельный этап (граница name-run) |
| Stage-E-A | `src/multispan.py` (PHONE-сборщик) × INN/PASSPORT | **Цепочка ≥2 цифрогрупп с 10 эффективными цифрами и ведущей 7/8 может быть не телефоном** (безъякорный ИНН-10 региона 7x/8x, паспортная пара «серия номер»). На корпусе дало НОЛЬ новых `fp_neg` и даже снизило утечку PASSPORT (282→268) и SNILS (39→35) — маскирование чужого значения безопаснее утечки; PHONE cross-type 11→29 (диагностика C, не утечка). Приоритеты пересечений защищают: анкорный/валидный INN и SNILS выигрывают у PHONE. Наблюдать при смене корпуса. | НЕЗНАЧИТЕЛЬНО (cross-type, направление безопасное) | наблюдение на гейте |
| Stage-E-B | `src/file_detokenizer.py` × единый id | **decrypt-file подставляет форму ПЕРВОГО вхождения, не канон.** Файловый путь строит mapping `token→original_text` (v1-поле = поверхностная форма первого вхождения); с единым токеном ORG/PER все вхождения в файле получат эту форму вместо канона/своих форм. Текстовый путь (CLI/UI) подставляет канон. Сессия v2 хранит все формы (`occurrences`) — приведение файлового пути к канону = отдельное решение. | НЕЗНАЧИТЕЛЬНО (форма, не утечка) | отдельный этап (канон в `file_detokenizer`) |
| E2-CORPUS-POVERTY | `tests/corpus/**` (охват, не код) | **Синтетический корпус структурно БЕДНЕЕ реальных документов и потому не воспроизводит целый класс дефектов.** Реальный договор владельца — **1097 стилей**; документы `tests/corpus/` — **2 стиля** каждый. Именно поэтому дефект `_CapsResolver` прожил незамеченным через этапы A/A′/B/C/C′/D/E/E′: под старой (дефектной) реализацией на корпусе 0 ложных попаданий, дефект физически не срабатывает. Следствие: корпус хорошо проверяет ПРАВИЛА детекции, но почти не проверяет устойчивость к структурному разнообразию реального `.docx` (стили и их наследование, разнообразие лексики, зоны). Всё, что зависит от богатства структуры — кеши по стилям, наследование форматирования, `w:sdt` (см. 1-B) — корпусом НЕ покрывается принципиально. Частично закрыто фикстурой `tests/fixtures/synthetic_many_styles.docx` (284 стиля) как постоянным стражем. **«Зелёный корпус» ≠ «работает на реальных документах».** | ВАЖНО (ограничение охвата, не дефект кода) | частично закрыто (фикстура многостилья); полноценно — реальные-подобные документы в наборе |
| DOG-REPO-BLOB | история git репозитория (не рабочее дерево) — найдено этапом DOG, **локально ЗАКРЫТО задачей LEAK-PURGE 2026-08-05, на сервере ОТКРЫТО** | **СТАТУС.** Локально вычищено: `git reflog expire --expire=now --expire-unreachable=now --all` + `git gc --prune=now` в `C:\Jesus\ARP`. Проверено — `git cat-file -e ad28b62a…` и `git cat-file -e daaef29e…` дают exit 1, `git show daaef29e:wert.docx` отвечает `fatal: invalid object name`, `git fsck --unreachable --no-reflogs` пуст, повторный сплошной поиск 5671 иглой по всем 3638 объектам даёт по признаку «блоб = документ» **ноль**. Объектов 3728→3638. Бэкап-зеркало (две независимые копии) — `C:\Jesus\ARP_LEAK_PURGE_BACKUP_20260805`, не удалять до закрытия вопроса по серверу. **НА GITHUB ОСТАЁТСЯ:** документ по-прежнему отдаётся по SHA (`git fetch --depth=1 origin daaef29e…` объект возвращает — проверено), потому что он там НЕДОСТИЖИМ, а недостижимые объекты не убираются ни пушем, ни переписыванием истории. Решение владельца (поддержка GitHub / пересоздание репозитория) отложено до проверки форков. Разбор — [`JOURNAL.md`](JOURNAL.md), запись LEAK-PURGE. Ниже — исходная формулировка находки. **САМ `dog.docx` ЦЕЛИКОМ ЛЕЖАЛ В ИСТОРИИ GIT И ЧИТАЛСЯ ОДНОЙ КОМАНДОЙ.** `git show daaef29e:wert.docx` возвращает блоб 510 935 байт; sha256 блоба и sha256 файла `C:\shifrator_real\dog.docx` — **совпадают байт-в-байт** (`22813b2c71ed7245…`). То есть документ, ради защиты которого затеян продукт, сам присутствует в объектах репозитория. Коммит `daaef29e` («Исправлен баг с пограничными данными», 12.07.2026), файл удалён из рабочего дерева следующим коммитом `8ebb187a` того же дня — но удаление из дерева блоб не убирает. Рабочее дерево при этом ЧИСТО: сплошная проверка 1389 отслеживаемых файлов 667 иглами (600 словесных 5-грамм документа + 67 длинных цифровых прогонов) дала только шаблонные совпадения 1-из-600, одно из них дословно присутствует и в синтетическом корпусе v1. **Ранее известно ЧАСТИЧНО и в искажённом виде:** архивный отчёт `docs/archive/reports/REAL_DOCS_CHECK.md:3-12` этот блоб нашёл и вынес вердикт «НЕ ЧИСТО», но прямо написал, что он «не подпадает под известный контекст задачи (`real_docs/`, `dog.docx`, `shifrator_real`)» — то есть считал его ДРУГИМ документом. Сверка хешей этапом DOG показывает, что это тот же самый файл, и оценка серьёзности была занижена. Решение (переписывание истории / пересоздание репозитория) — исключительно за владельцем; эта сессия ничего не удаляла и не переписывала. | **КРИТИЧНО** (реальный договор с ПДн доступен в публичном репозитории одной командой; относится к репозиторию, а не к коду продукта) | решение владельца — эта сессия измерительная и историю не трогает |
| A2-NEWLINE-CROSS | `entity_types.yaml` (внутренние разделители значений — серия+номер PASSPORT `:132`, PHONE `:210`) | **`\s` внутри значения матчит не только перенос строки ВНУТРИ прозы, но и СТРУКТУРНУЮ границу сегмента/ячейки — они неразличимы в тексте сегмента, а вторая опаснее первой.** Найдено этапом A2 экспериментально: первая попытка расширить внутренний разделитель года BIRTHDATE `[ ]?`→`\s?` дала РЕГРЕССИЮ на корпусе v1 — 4 документа мутации `linebreak`, где вместо чистого пропуска (было 0 масок) получались 2 разорванных частичных куска (недобор границ). Откачено для BIRTHDATE/SNILS/кода подразделения (оставлен `[- ]?`/`[ ]?`, см. записи там же). Проект УЖЕ ЗНАЛ об этом риске: комментарий у блока B2-fix (`entity_types.yaml:32-36`, про INN/OGRN/ACCOUNT/BIK/KPP) прямо говорит «тот же класс `[ ]`, что и у SUM (**не** `\s`, чтобы не сшивать значения через перенос строки)» — то есть правило уже было сформулировано, но соблюдалось не везде. Два места остались с `\s`, не измерены на корпусе v1 линией «е»: (1) `entity_types.yaml:132`, разделитель серии паспорта `(\d{2}\s?\d{2})` — унаследован от этапа A1 нетронутым, полный гейт после A1 и A2 показывает дельта 0, но конкретно ЭТА позиция под мутацией `linebreak` не проверялась поимённо; (2) `entity_types.yaml:210`, ВСЕ ЧЕТЫРЕ разделителя PHONE `[\s\-]?` — используют bare `\s`, нарушая собственное правило проекта, и это НЕ новая находка A2, а пред-существующее с более раннего этапа. | ВАЖНО (тот же механизм уже дал измеренную регрессию на одном типе; два места того же класса не проверены) | не запланировано — для PHONE: измерить на мутации `linebreak` и, если регрессия подтвердится, сузить `\s`→`[ ]`/`[ \xa0]`; для PASSPORT-серии: та же проверка, вне границ A2 (тема — три других типа) |
| A2-SNILS-BIRTHDATE-GAP-PRECISION | `entity_types.yaml:171` (SNILS), `:201` (BIRTHDATE) × `tests/corpus_v2` типизированные негативы | **Расширенный в этом же этапе зазор (SNILS `{0,20}→{0,30}`, BIRTHDATE `{0,10}→{0,35}`) ловит и НЕСВЯЗАННОЕ число нужной формы, если оно попадает внутрь допуска.** Измерено НАРОЧНО построенными негативами корпуса v2 (зазор подобран короче нового допуска): «СНИЛС не указан, номер заказа ddd-ddd-ddd dd» (зазор 25 симв.) и «Дата рождения не проставлена, документ выдан dd.mm.yyyy» (зазор 32 симв.) — оба ловятся 138 раз из 138 (по разу на документ). Направление ошибки безопасное (лишняя маска на постороннем числе, не утечка настоящего значения), но это прямая цена расширения: гэп не различает «то самое число» от «случайно похожего числа в том же предложении». До этапа A2 это было НЕВИДИМО — оба типа не имели вовсе ни одного типизированного негатива ни в одном корпусе (см. `A2-NO-NEGATIVES-13-TYPES`). | СРЕДНЯЯ (измеренный, а не гипотетический компромисс точности; направление безопасное) | не запланировано — сузить гэп дальше означало бы вернуть часть исходной дыры (`DOG-PASSPORT-GAP`-класс); решение — за владельцем, есть число для сравнения |
| A3-NEG-BIK | `entity_types.yaml:111` (`BIK`) | **Внутренний инвентарный номер из 9 цифр, начатый «04», маскируется как БИК — 138 из 138 (100 %).** У типа нет НИ ЯКОРЯ, НИ КОНТРОЛЬНОЙ СУММЫ: паттерн `\b0[ ]?4(?:[ ]?\d){7}\b` берёт любой такой прогон, и подтвердить догадку ему нечем. Комментарий в конфиге (`:96`) прямо признаёт компромисс — «BIK(04+7) достаточно специфичен, чтобы случайные совпадения были пренебрежимо редки», — теперь у этого утверждения есть цифра, и она не «пренебрежимо редко»: сконструированный по практике инвентарный номер ловится всегда. **В реальном договоре — 0:** единственный 9-значный прогон на «04» подтверждён словом «БИК» слева. Направление ошибки безопасное (лишняя маска, не утечка). | СРЕДНЯЯ (точность; в целевом документе не срабатывает) | не чинится этой сессией (решение владельца). Кандидат — якорь `(?i)бик` по образцу `INN`/`OGRN`; цена — реквизит без подписи рядом перестанет маскироваться. ДО правки расширить набор негативов: измерен один вид случая |
| A3-NEG-KPP **[ЗАКРЫТА этапом A4, 2026-08-06]** | `entity_types.yaml:102` (`KPP`) — якорь `(?i)кпп` добавлен, `dog.docx` 10→0. Побочные эффекты правки — `A4-KPP-ANCHOR-FALLOUT`. | **Складской номер из 9 цифр маскируется как КПП — 138 из 138 (100 %). Единственный из девяти массовых случаев, который ПОДТВЕРЖДЁН НА РЕАЛЬНОМ ДОГОВОРЕ: 10 из 10.** У `KPP` нет ни якоря, ни контрольной суммы (её у КПП не существует), паттерн берёт любые 9 цифр. Замер `real_probe.py`: в целевом документе 10 девятизначных прогонов, слова «КПП» в тексте нет ВОВСЕ (0 вхождений) — то есть все 10 срабатываний безосновательны. Это независимое подтверждение `DOG-KPP-NOANCHOR` с другой стороны: там дефект нашли по маскам, здесь — по отрицательному примеру, числа сошлись. | ВАЖНАЯ (точность; единственный класс, доказанный вне корпуса) | не чинится этой сессией. Кандидат — якорь `(?i)кпп`; закрывает заодно `DOG-KPP-NOANCHOR`. Правка требует полного гейта |
| A4-KPP-ANCHOR-FALLOUT | `entity_types.yaml:102` (`KPP`, якорь `(?i)кпп` + отдельный `pattern`/`span_group` для связки «ИНН/КПП») — найдено этапом A4 полным гейтом и полным набором тестов | **Снятие безъякорной маски `KPP` обнажило три пред-существующих дефекта, которые эта же безъякорная маска СЛУЧАЙНО маскировала (в обоих смыслах слова).** (1) Гейт, линия «б» (`leak_v2`, корпус v1): у `INN` (58→62 / 55→59), `INN_PER` (90→102 / 83→95), `OGRN` (38→44 / 32→42), `ACCOUNT` (68→84 / 68→84), `PHONE` (111→112 / 16→17), `PASSPORT` (266→270 / 250→254), `SNILS` (35→51 / 25→41) выросло число недомаскированных частичных прогонов (`>=6`/`>=8` цифр) — механизм: анкорless-`KPP` раньше СЛУЧАЙНО перекрывал 9-значный хвост чужого реквизита (по невезению совпадавший с формой КПП) своей же неправомерной маской, и утечка была НЕВИДИМА не потому, что её не было, а потому что маска стояла не под тем именем. Линия «а» (precision) при этом дала `INN` fp_neg 2→3 (+1 новый ложный `INN` — вероятно, арбитраж отдал освободившийся спан чек-суммой совпавшему `INN` там, где раньше побеждал `KPP`). Компенсирующее УЛУЧШЕНИЕ на той же линии: `KPP` precision 97,00%→100,00% (fp_neg 7→0, tp 226→226 — recall не тронут), общий FP-счёт 16→10, `masking_correctness` B/C выросли (91,60%→92,24% / 95,21%→95,90%) — сквозное `masking C` для `ACCOUNT`-под-`[KPP_N]` (агентство `agency_0007`) само исчезло (проверено по ВСЕМУ корпусу: 0 оставшихся случаев `gold_type=ACCOUNT` с `c_ok=False`). (2) `pytest`: `test_regex_detector.py::TestDetectRegexKppBikOverlap::test_nine_digit_starting_with_04_produces_both_kpp_and_bik` кодирует СТАРОЕ поведение (любой `04…`-прогон обязан давать И `KPP`, И `BIK`, даже без слова «КПП» рядом) как контракт — этим тестом старый баг был буквально пришпилен как фича. (3) `test_masking_correctness.py::test_e2e_negative_C_account_masked_as_foreign_type[agency_0007]` — зеркало без носителя: класс `ACCOUNT`-под-чужим-типом, который оно ловит, закрыт (см. выше), мигрировать некуда — по конвенции файла (см. докстринг теста, прецедент со СНИЛС на agency_0003) это ЗАКРЫТИЕ, а не регресс, но тест сам не обновлён. (4) `test_wave1_verification.py::TestP1CrossesComponent2::test_boundary_pair_round_trip_through_real_docx_file` — фикстура (`"Приложение 1"` + `"23456789 руб."` в соседних ячейках) полагалась на то, что склеенный на границе ячеек 9-значный прогон поймает анкорless-`KPP`; теперь ничего не маскируется (`replaced == 0`), тест валится не из-за поломки round-trip-механизма, а из-за того, что фикстуре нечего гонять. Ни один из трёх тестов не тронут этой сессией — зона сессии («один детектор — KPP») не включает `test_regex_detector.py`-контракт для BIK, конвенцию миграции зеркал `masking_correctness` и фикстуры `component2`-round-trip; во всех трёх нужно решение о переносе/переформулировке, а не механическая правка. | **ВАЖНАЯ** (гейт красный не по продукту в узком смысле — по метрике, которая раньше пряталась за чужой ошибкой; 3 упавших теста, `pytest` даёт 1330 passed / 4 failed вместо 1334 passed) | решение владельца: (а) `leak_v2`-рост по 7 типам — это реальная, ранее невидимая утечка или пересчёт того же самого через другую маску, разбор поимённый (см. правило `CLAUDE.md` про MASK-SHIFT) этой сессией не сделан из-за бюджета времени; (б) три теста нуждаются в переносе/переформулировке под новую (верную) модель KPP, не в этой сессии («только якорь») |
| A5-NL-CROSS-RESIDUE **[ЗАКРЫТА этапом A6, 2026-08-06]** — сборка через стык граничного окна B3 (`multispan.collect_seam_entities` + обобщение внутрисегментного мультиспана): `ACCOUNT[leak_v2>=6]` 77→10, `OGRN[>=8]` 33→12, оба НИЖЕ планки (68 / 32); остаток 24 случая — целиком пред-существующие в baseline классы (8 эталонов ОГРН с невалидной КС — их и целый детектор не находил никогда; 5 «б»-край; рваный якорь «р/»+«с …» / значение через два стыка — см. `A6-DESTRUCTIVE-TRANSFORMS` пп. 3, 5, 7). Исходная формулировка ниже. | `src/normalizer.py` (проход 1 сохраняет `
` как структуру) × `entity_types.yaml` (внутренние разделители значений) — найдено этапом A5 полным гейтом | **Остаток дыры `A4-KPP-ANCHOR-FALLOUT`, который правка A5 НЕ закрыла: значение, разорванное переносом строки. Гейт красный по двум линиям «б»: `ACCOUNT` 68→77 (≥6 и ≥8), `OGRN` ≥8 32→33.** Состав поимённо: три счёта, рваных `
` (`labor_0009` ×3 мутации, `agency_0007`), два ОГРН (`labor_0008__m1337_linebreak`, `supply_0002__m1337_linebreak`) плюс 13 «соседей по документу» — маска на своём месте, но те же цифры выживают в рваном двойнике того же значения, и метрика засчитывает утечку ОБОИМ. Раньше рваный хвост случайно закрывал безъякорный `KPP`. Чинится не границами паттернов, а `\s`-классом внутри значения, а он запрещён находкой `A2-NEWLINE-CROSS` (перенос строки неотличим от границы ячейки: вместо чистого пропуска получается рваный частичный матч). | **ВАЖНАЯ** (утечка; гейт красный, планка не двинута) | не чинится наспех. Кандидат — мультиспан-сборка по `
` для ACCOUNT/OGRN по образцу этапа E, а не расширение разделителя |
| A5-INN-KBK-FP | `entity_types.yaml:64` (`INN`) × `src/normalizer.py` (схлопывание типографского пробела) — найдено этапом A5 | **Единственное падение точности этапа: `precision[INN]` 99,29 %→98,97 % (tp 280→289, fp_neg 2→3).** Носитель один: `services_0011__m1337_digit_spaces`, КБК из 20 цифр (размеченный негатив `A3-NEG-ACCOUNT`-класса). Схлопывание NBSP склеило группы КБК, и 10-значное окно внутри 20-значного кода сошлось по контрольной сумме ИНН. Механизм пред-существующий: `` не мешает вырезать 10-значное окно из более длинного прогона, разделённого ОБЫЧНЫМИ пробелами; правка A5 лишь чаще подводит к нему. | СРЕДНЯЯ (точность; 1 маска на 289 tp) | не чинится этой сессией. Кандидат — запрет матча `INN`, у которого слева или справа через одиночный пробел стоит ещё цифра (сузит класс форм — требует полного гейта) |
| A6-DESTRUCTIVE-TRANSFORMS | весь конвейер от чтения до детекции — разведка этапа A6 (Часть 3), БЕЗ правок | **Сквозной список преобразований текста, способных разрушить значение ДО того, как его увидит детектор.** Пройден путь `extract()` → нормализация → виды поиска → окна детекции. Поимённо: **(1)** пять нечитаемых видов узлов OOXML (`src/extractor.py:292`, `iter_inner_content` берёт только раны и гиперссылки) — половина значения внутри `w:ins`/`w:sdt`/`w:smartTag`/`w:fldSimple` просто исчезает из текста сегмента; это уже открытые `DOG-NODE-SILENT`/`DOG-ANCHOR-SPLIT`, здесь только место в списке; **(2)** тихий сброс `detection_text` при смене длины регистровой нормализацией (`src/extractor.py:301-313`) — сегмент остаётся с сырым регистром (ФИО в all_caps может не найтись), предупреждение уходит лишь в stderr; **(3)** «б» на краю токена/цепи остаётся буквой (`src/normalizer.py:323-329`, осознанная защита литеры дома) — живой остаток гейта: `ACCOUNT` `services_0010__m2718_homoglyph`, `works_0001__m1337_combo`; `OGRN` `sale_0009__m2718_homoglyph`, `supply_0010__m1337_combo`; **(4)** одиночный U+0020 между цифрами сохраняется (`src/normalizer.py:333-363`), а схлопывание NBSP рядом делает группировку разнобойной — фиксированные группы паттернов не матчатся: `labor_0002__m1337_combo` (ACCOUNT), `lease_0002__m2718_combo` (OGRN); **(5)** окно якоря не пересекает `\n` и цифру (`src/regex_detector.py:118-131`) — якорь строкой ВЫШЕ значения не защитывается: «р/с» на строке 1, число на строке 2; живой пример — второй счёт в столбике под общим якорем; **(6)** `_BOUNDARY_WINDOW = 60` (`src/tokenizer.py:31`) — значение или якорь дальше 60 символов от стыка в граничное окно не попадают; **(7)** окна ПАРНЫЕ — значение, рваное ДВУМЯ переносами (три сегмента), не собирается: замер ДО начала работ — корпус **2 сущности** на 324 документа (обе `sale_0002__m1337_linebreak`: INN_PER `96\n92\t86\n997422` и PER), реальный документ — **0** (все 36 «троек» ложные: средний сегмент не целиком цифровой); принято ограничением решением владельца; **(8)** `_BATCH_SEP` U+2063 (`src/tokenizer.py:444`) — символ категории Cf: `normalize_for_detection` блоба его ВЫРЕЗАЕТ, и в норм-виде окна смыкаются через `\n\n`; сегодня безопасно (`\n` не разделитель ни цепей, ни паттернов), но связь двух механизмов неочевидна — при смене разделителя или правил Cf проверять первой; **(9)** группа без единой настоящей цифры рвёт цепь сборщика (`src/multispan.py:184`) — одинокий сильный двойник («Ч») на отдельном краю разрыва ломает сборку (пересечение с п. 3); **(10)** `w:tab` внутри значения приходит как `\t` (`src/extractor.py:297`, `run.text`) — паттерны реквизитов `\t` не допускают; цепной сборщик A6 покрывает `\t` только для типов `_SEAM_TYPES`; **(11)** виды поиска структурных детекторов стирают границу строки (`src/anchor_registry.py:117` — ORG вырезает `\n`; `:136` — PER заменяет пробелом): для движка текст выглядит слитным, и name-run дотягивается через бывшую границу — см. `A6-REQ-NAMERUN-OVERREACH`. | СПИСОК-СТРАХОВКА (каждый пункт — либо ссылка на открытый долг, либо измеренное ограничение с числом) | по пунктам: 1 — `DOG-NODE-SILENT`; 3-4 — кандидаты следующего слоя нормализации; 5 — кандидат «якорь строкой выше» (требует замера FP); 6-10 — наблюдать; 7 — принято ограничением |
| A6-REQ-NAMERUN-OVERREACH | `src/anchor_registry.py:941-956` (`_name_run_before_requisite`) × `anchor_search_view` (`:117`) — найдено этапом A6 при вводе мультиспан-реквизитов | **Name-run слева от метки «ИНН/ОГРН» не обязан ПРИМЫКАТЬ к метке: первое (правое) слово прогона принимается без проверки зазора, и цифры/мусор между именем и меткой прогона не рвут.** В сочетании с ORG-видом, вырезающим `\n`, это даёт якорь на слове из ЧУЖОЙ строки: на `labor_0006` (чистом!) якорь-кандидат ORG «Профсоюзная» (улица из адреса строкой выше: «…Профсоюзная 24\nИНН 4377232669») существует всегда — его гасит СЛУЧАЙНАЯ ADDRESS-маска, выигрывающая пересечение. На рваной мутации ADDRESS короче, ORG-маска «Профсоюзная» материализовалась (masking B[ORG] −1,23 пп на срезе) — этим A6 и вскрыл класс. В A6 закрыт ИНВАРИАНТОМ (`src/pipeline.py`: мультиспан-реквизиты структурному движку не подаются — рваный реквизит не доказательство имени), поэтому сегодня класс ЛАТЕНТЕН: держится на удаче арбитража с ADDRESS. Родственна `Stage-Cprime-A`/`Stage-Cprime-B` (та же ветвь, другие симптомы). | СРЕДНЯЯ (латентная точность: якорь строится на чужих словах, пока его прячет другой тип) | кандидат — требование примыкания name-run к метке (зазор только пробелы) в `_collect_name_run`; правка меняет ветвь для всего корпуса — отдельный этап с полным гейтом |
| A3-NEG-ACCOUNT | `entity_types.yaml:106` (`BANK_ACCOUNT`, `anchor:`) | **КБК из 20 цифр под словом «счёту-фактуре» маскируется как банковский счёт — 138 из 138 (100 %).** Якорь ACCOUNT `сч[её]т\w*` не различает банковский счёт и счёт-ФАКТУРУ (документ), а КБК имеет ровно ту же длину, что счёт. Своей контрольной суммы у `BANK_ACCOUNT` нет (`:97-98`), якорь — единственная защита, и здесь он же и подводит. КБК как общий негатив в корпусе был с самого начала (`negatives_clause`) и НЕ ловился — ловиться он начал ровно тогда, когда рядом появилось слово-якорь в другом значении. **В реальном договоре — 0** («счёт-фактура» ни разу; слово «счёт» в других формах — 96 раз). | СРЕДНЯЯ (точность) | не чинится этой сессией. Кандидат — `anti_anchor` на «фактур\w*» по механизму этапа A2; цена мала, но набор негативов сначала расширить |
| A3-NEG-INN | `entity_types.yaml:64` (`INN`) | **Два вида, разный результат. (а) Регистрационный номер иностранной организации из 10 цифр под якорем «ИНН» (зазор 35 символов) — 138 из 138 (100 %).** Работает ровно инвариант этапа 4: под якорем контрольная сумма перестаёт быть фильтром и становится сигналом, поэтому произвольные 10 цифр рядом со словом «ИНН» маскируются всегда. Это не дефект реализации, а измеренная ЦЕНА инварианта. **(б) ОКПО индивидуального предпринимателя из 10 цифр БЕЗ якоря — 19 из 138 (13,8 %)**, что совпадает с ожидаемой вероятностью случайного схождения КС ИНН-10 (≈1/10). **В реальном договоре — 0** (оборот «ИНН … не присвоен/регистрационный номер» не встречается; само слово «ИНН» — 1 раз). | СРЕДНЯЯ (точность; (а) — цена сознательного инварианта, а не ошибка) | не чинится. Сужать якорное окно значит вернуть класс `DOG-PASSPORT-GAP`; решение владельца, число для сравнения теперь есть |
| A3-NEG-INN-PER | `entity_types.yaml:76` (`INN_PERSON`) | **Номер платёжного документа из 12 цифр под якорем «ИНН» (зазор 38 символов) — 138 из 138 (100 %).** Механизм тот же, что у `A3-NEG-INN` (а): якорь снимает КС. Отличие в цене ошибки — маска здесь `INN_PER`, то есть тип, входящий в набор «только персональные данные» (этап T2-INN), и лишняя маска приходит пользователю в наборе по умолчанию, а не в расширенном. **В реальном договоре — 0.** ПРОВЕРЕНО ОТДЕЛЬНО: в первой редакции негатив стоял за 45-48 символов от якоря, якорь до него не доставал и давал 0 — «чисто» было артефактом расстояния, а не свойством детектора. | СРЕДНЯЯ (точность в наборе по умолчанию) | не чинится; см. `A3-NEG-INN` |
| A3-NEG-OGRN | `entity_types.yaml:82` (`OGRN`) | **Штрихкод EAN-13 товара маскируется как ОГРН — 18 из 138 (13,0 %).** Ровно ожидаемая вероятность случайного схождения контрольной суммы ОГРН (1/13) на произвольных 13 цифрах. Это ЧИСЛЕННОЕ ПОДТВЕРЖДЕНИЕ ранее выведенной аналитически `Stage4-C` («КС ОГРНИП структурно ненадёжна как фильтр коротких чисел»): там класс был выведен из кода, здесь измерен на конструкции из практики. **В реальном договоре — 0:** все 33 вхождения ОГРН подтверждены якорем «ОГРН» слева. | НЕЗНАЧИТЕЛЬНАЯ (13 % от одного вида негатива; направление безопасное) | не чинится; тот же кандидат, что у `Stage4-C` — якорь-подтверждение для безъякорного случая |
| A3-NEG-PHONE | `entity_types.yaml:210` (`PHONE`) × окно детекции (склейка ячеек, находка B3) | **Номер строки таблицы «8» и 10-значный номер в СОСЕДНЕЙ ЯЧЕЙКЕ той же строки склеиваются в 11-значный прогон формы телефона и маскируются — 138 из 138 (100 %).** Окно детекции соединяет ячейки строки БЕЗ разделителя, поэтому «8» + «6706288293» читается как «86706288293». В 9 случаях из 138 на ту же склейку дополнительно легла маска `INN`. Класс важен тем, что он НЕ про паттерн телефона, а про представление документа: сужение паттерна его не закроет, а правка склейки затрагивает ВСЕ типы сразу. **В реальном договоре — 0** (совпадений формы телефона там нет вовсе). Отдельно: у `PHONE` НЕТ ЯКОРЯ, поэтому класса «слово-якорь в другом значении» у него не существует в принципе, а честного 11-значного НЕтелефонного реквизита в практике российских договоров подобрать не удалось. | СРЕДНЯЯ (точность; механизм общий для всех типов, поэтому цена правки высокая) | не чинится этой сессией и не должно чиниться точечно: разделитель ячеек в окне детекции — общий механизм, правка требует полного гейта и отдельного этапа |
| A3-NEG-PERCENT | `entity_types.yaml:256` (`PERCENT`) | **Технический параметр товара («массовая доля влаги — не более 9 %») маскируется как ставка — 138 из 138 (100 %).** Решение владельца этапа T2 звучало как «ставки маскируются наравне с суммами»; детектор же ловит ЛЮБОЕ число со знаком процента, а границу «ставка / не ставка» не проводит — её в паттерне нет. Маска на влажности искажает спецификацию товара, ничего при этом не защищая (персональными данными технический параметр не является). **В реальном договоре — 0** при 74 вхождениях знака «%» и 54 вхождениях слова «процент»: ни одно не стоит при слове технического параметра. | СРЕДНЯЯ (порча документа без выигрыша в защите; граница «ставка/не ставка» — решение владельца, а не детектора) | не чинится. Кандидат — `anti_anchor` на слова технических параметров; но сперва владелец должен подтвердить, что граница проходит именно там |
| A3-NEG-TERM | `entity_types.yaml:291` (`TERM`) | **Два вида, оба 138 из 138 (100 %). (а) Условие хранения товара** («хранить в течение 12 часов при температуре…») — не срок обязательства Стороны, а свойство товара. **(б) Срок из НОРМЫ ПРАВА** («срок исковой давности … в течение трёх лет», статья 196 ГК РФ) — маска делает нечитаемой ссылку на закон. Причина у обоих одна и признана в самом конфиге (`:272-277`): якорь `в течение` + основа единицы времени говорит, ЧТО СЧИТАТЬ сроком, и намеренно не зависит от смысла — «это ЯКОРЬ ФОРМЫ, а не список запрещённых значений». Цена этого решения теперь измерена. **В реальном договоре — 0** при 136 вхождениях оборота «в течение»: ни одно не стоит в предложении со ссылкой на норму, оборота «исковой давности» нет вовсе. | СРЕДНЯЯ (порча документа; ссылка на норму — худший вид маски, документ перестаёт читаться юридически) | не чинится. Кандидат — `anti_anchor` на «исковой давности» и «установленн\w+ (статьёй\|законом)»; расширить набор негативов до правки |
| A3-NEG-PER | `src/anchor_registry.py` (`PerAnchorDetector`) | **Ноль ошибочных масок на обоих отрицательных примерах (0 из 138 каждый).** Проверено: (а) якорь «в лице», за которым стоит нарицательное обозначение («в лице уполномоченного представителя»), а не имя человека; (б) фамилия учёного в названии лабораторного метода («по методу Кьельдаля»). Структурная модель PER этапа B отработала оба класса верно — якорь без формы ФИО следом ничего не порождает. Оговорка: два вида случая — не доказательство точности типа вообще, а два пройденных экзамена; известные слабости PER (`B-ADDR-HOMONYM`, `Aprime-1`) этими негативами не покрыты. | НЕТ ДЕФЕКТА (запись ради числа: до A3 у типа не было ни одного негатива) | ничего не требуется; расширять набор негативов |
| A3-NEG-ORG | `src/anchor_registry.py` (`OrgAnchorDetector`) | **Ноль ошибочных масок на обоих отрицательных примерах (0 из 138 каждый).** Проверено: (а) название закона в кавычках («Федеральному закону … № 152-ФЗ «О персональных данных»»); (б) марка товара в кавычках («под маркой «Стандарт»»). Кавычная ветвь ORG требует ПРИМЫКАЮЩЕЙ org-формы (ужесточение этапа C′), и оба случая отсекаются именно ею — то, ради чего C′ и делался, здесь измерено отрицательным примером, а не только положительным. | НЕТ ДЕФЕКТА (запись ради числа) | ничего не требуется; расширять набор негативов |
| A3-NEG-ADDRESS | `src/ner_detector.py` (якорная модель ADDRESS, этап C) | **Ноль ошибочных масок (0 из 138).** Проверено: подсудность — топоним в названии суда («Арбитражный суд города Москвы»), который адресом стороны не является. Строгий якорь адреса этапа C города без маркера улицы/дома не берёт. Оговорка та же: один вид случая; остаток долга ADDRESS (`Eprime-B`, 43 записи) — про полноту, а не про точность, и этим негативом не затронут. | НЕТ ДЕФЕКТА (запись ради числа) | ничего не требуется; расширять набор негативов |
| A3-NEG-EMAIL | `entity_types.yaml:214` (`EMAIL`) — **ОТРИЦАТЕЛЬНОГО ПРИМЕРА НЕТ, И ЭТО РЕШЕНИЕ, А НЕ ПРОПУСК** | **По `EMAIL` отрицательный пример не заведён сознательно.** Разбор по классам задания: (1) «слово-якорь в другом значении» — у `EMAIL` якоря нет вовсе, класс неприменим; (2) «цифры нужной длины и формы» — значение не числовое, класс неприменим; (3) «реквизиты самого документа» и (4) «ссылки на нормы» — ни номер договора, ни статья закона литеральной `@` между словесными прогонами не содержат, паттерн `[\w\.-]+@[\w\.-]+\.\w+` их не достигает; (5) «разорванное значение» — разрыв адреса даёт НЕДОБОР (утечку), то есть находку противоположного знака, и меряется линией «е», а не негативом. Строка вида `слово@слово.слово` в российском договоре в практике оформления и есть адрес электронной почты. Выдуманная пустышка мерила бы фантазию автора корпуса, а не детектор. | НЕТ ДЕФЕКТА (осознанный пробел охвата) | не требуется. Если владелец сочтёт корпоративный ящик (`info@…`) не персональными данными — это решение о ПОЛИТИКЕ типа, и тогда негатив появится вместе с ним |
| A3-GATE-OVERMASK-LINE | `tests/corpus/gate.py` × `tests/corpus_v2` — **ПРЕДЛОЖЕНИЕ, НЕ ВНЕДРЕНО** | **Гейт не меряет переусердствование вовсе; линия возможна, но у неё две цены.** Что меряла бы: «сколько отрицательных примеров каждого типа накрыто маской», числитель/знаменатель по типу, точка отсчёта — сегодняшнее состояние (BIK 138, KPP 138, ACCOUNT 138, INN 157, INN_PER 138, OGRN 18, PHONE 138, PERCENT 138, TERM 276, PER/ORG/ADDRESS 0). Чем отличается от линии «а» (precision): та считает ложные срабатывания на негативах КОРПУСА V1, где типизированных негативов нет вовсе, и нормируется на `tp` в масках (см. `ADDRB-PRECISION-FRAG` — наказывает за склейку спанов); новая линия считала бы ПОКРЫТЫЕ ОТРИЦАТЕЛЬНЫЕ ПРИМЕРЫ корпуса V2 и от фрагментации не зависит. **ЦЕНА 1 — корпус.** Гейт сегодня гоняется по корпусу v1 (`gate.py`, 324 док.); линия живёт на корпусе v2, значит гейту нужен второй прогон (`measure_v2.py`, ~1 замер сверх нынешних ~2,5-3 мин) либо перенос негативов в v1 — а v1 ЗАМОРОЖЕН (запрет 2 `CLAUDE.md`), то есть остаётся только второй прогон. **ЦЕНА 2 — точка отсчёта узаконивает дефект.** Планка от сегодняшнего состояния означает «138 ложных масок BIK — норма»; красным она станет только при РОСТЕ. Это честно как регресс-страж, но читается как одобрение. **ЦЕНА 3 — представительность.** Пока на тип заведён один вид случая, линия сторожит одну конструкцию, и «зелено» будет значить «та самая конструкция не ухудшилась». Рекомендация: линию заводить ПОСЛЕ расширения набора негативов, иначе она закрепит охват из одного примера на тип. Решение — владельца. | ПРЕДЛОЖЕНИЕ (внедрения в этой сессии нет по условию задания) | решение владельца |
| DOG-KPP-NOANCHOR **[ЗАКРЫТА этапом A4, 2026-08-06]** | `entity_types.yaml` (паттерн `KPP`) — якорь `(?i)кпп` добавлен, precision 97,00%→100,00%. Побочные эффекты — `A4-KPP-ANCHOR-FALLOUT`. | **10 срабатываний `KPP` на документе, где слова «КПП» нет ни разу.** Замер `dog.docx`: вхождений «кпп» — 0, при этом детекция даёт `KPP` 10 (в маску набора `maximum` проходят 6). Девять из десяти — спаны длиной 11-12 символов с 9 цифрами и буквами слева, десятый — начало ячейки таблицы (`t5_r3_c1@0`). Для сравнения, все 33 найденных `OGRN` подтверждены якорем «ОГРН» слева в 40 символах (33 из 33), то есть у ОГРН картина здоровая, а у КПП — нет. На набор по умолчанию не влияет (`KPP` в `personal` не входит), поэтому это точность, а не утечка: цена — лишние маски в наборах «реквизиты» и шире. Соответствует самой низкой precision из линии «а» гейта (`KPP` 97,00 % — худший показатель среди всех типов). | СРЕДНЯЯ (точность; на наборе по умолчанию не проявляется) | не запланировано — кандидат: якорь `(?i)кпп` для `KPP` по образцу `INN`/`OGRN`; правка требует полного гейта |
| DOG-NODE-SILENT **[ЗАКРЫТА этапом NODES, 2026-08-08]** | было `src/extractor.py:667-691` × `src/unread_zones.py:152-192` (найдено этапом DOG, Часть 2); закрыто переписью узлов — `src/unread_zones.py:73-115` (закрытый список) + `_scan_readable_part:380` (сверка по `w:t`) + `src/extractor.py:289` (`_paragraph_runs` сквозь обёртки) | **ПЯТЬ видов узлов OOXML не читаются И НЕ объявляются зоной — гарантия отказа на них не срабатывает.** Проверено на порождённой фикстуре `tests/fixtures/node_kinds.docx` (по узлу на абзац, в каждом свой опознавательный телефон): прочитаны только `w:hyperlink` и обычный ран; `w:ins`, `w:fldSimple`, `w:smartTag`, `w:sdt` (inline) и `w:sdt` (блочный, верхний уровень тела) — НЕ прочитаны, при этом `scan_unread_zones` вернул **0 зон**, а `shifrator.py encrypt` в strict-режиме по умолчанию завершился **exit 0 без единого предупреждения**. Механизм двойной: (1) `src/extractor.py:667-668,691` обходит тело `body.iterchildren()` и берёт только `w:p`/`w:tbl` — блочный `w:sdt` не является ни тем, ни другим; (2) текст абзаца берётся как `paragraph.text` (`src/extractor.py:681,687`), а в python-docx 1.2.0 это `CT_P.text` = `"".join(e.text for e in self.xpath("w:r \| w:hyperlink"))` — только ПРЯМЫЕ дети `w:p`, поэтому любой промежуточный узел-обёртка прячет свои раны. Сканер зон при этом ищет ровно два подузла — `w:txbxContent` и вложенный `w:tbl` (`src/unread_zones.py:174,179`), остальных не знает. Тем самым нарушен явно заявленный принцип полноты самого модуля (`src/unread_zones.py:19-22`: «ложноотрицательный ответ недопустим»). Поглощает и подтверждает измерением прежнюю запись `1-B` (она называла только `w:sdt` верхнего уровня и помечена «в корпусе не встречается»). **На реальном договоре владельца ни один из пяти видов не встречается** (замер этапа DOG: `w:ins` 0, `w:del` 0, `w:fldSimple` 0, `w:smartTag` 0, `w:sdt` 0; `w:hyperlink` 81 — читается), поэтому дефект классово реален, но на целевом документе не срабатывает. `w:del` (`w:delText`) не читается ПРАВИЛЬНО: удалённый текст рецензирования не является видимым содержимым, `src/unread_zones.py:90-98` исключает его намеренно. | **ВАЖНО** (нарушение основной гарантии fail-closed; тяжесть поднимала `DOG-ANCHOR-SPLIT` ниже) | **ЗАКРЫТА:** все четыре вида узлов ЧИТАЮТСЯ (проверено поимённо на `tests/fixtures/node_kinds.docx`, `tests/test_nodes.py::TestFourNodeKinds` — по опознавательному телефону на узел); пятый, `w:sdt` верхнего уровня, читается тоже. Молчаливого пропуска больше нет как класса: узел вне переписи даёт зону `unknown_node` и отказ, а полноту обхода доказывает не список, а сверка «каждый `w:t` части достигнут читателем» |
| DOG-ANCHOR-SPLIT **[ЗАКРЫТА этапом NODES, 2026-08-08]** | `src/extractor.py` × якорные детекторы (найдено этапом DOG, Часть 2) | **Непрочитанный узел превращает потерю текста в УТЕЧКУ ПДн, когда в нём лежит ЯКОРЬ, а значение — в обычном ране.** Само по себе непрочитанное содержимое в LLM не уходит (его нет в `{sid}.txt`), поэтому `DOG-NODE-SILENT` выглядит «только» тихой потерей. Но абзац читается ЧАСТИЧНО, и если маркер-якорь («ИНН», «СНИЛС», «счёт», «дата рождения») попал в узел-обёртку, а цифры остались прямым раном, якорный детектор не находит маркера — и значение уходит в LLM ОТКРЫТЫМ ТЕКСТОМ. Воспроизведено прогоном на порождённом документе: один и тот же ИНН в контрольном абзаце маскируется в `[INN_PER_1]`, а в абзаце, где «ИНН » лежит в `w:ins`, остаётся в анонимном тексте как есть (1 вхождение открытым). Это ровно главный риск продукта из `CLAUDE.md` — «пропущенная сущность уходит в LLM без предупреждения», — и он проходит мимо обеих защит: детекция маркера не видит, fail-closed зону не объявляет. Направление ошибки хуже обычного пропуска: обычный непрочитанный узел теряет содержимое целиком (заметно человеку), а расколотый якорь оставляет текст на вид полным. Обратный расклад (якорь прочитан, значение в узле) утечки не даёт — значения нет в выходе. | **ВАЖНО** (подтверждённая утечка ПДн в LLM без предупреждения; на `dog.docx` не срабатывала — узлов-носителей в нём нет) | **ЗАКРЫТА вместе с `DOG-NODE-SILENT`, но НЕ отказом, а чтением** — что строго сильнее: якорь и значение снова оказываются в ОДНОМ сегменте вплотную, и маска ставится, а не теряется вместе с абзацем. Живая проба — `tests/test_nodes.py::TestFourNodeKinds::test_anchor_split_is_closed`: «ИНН » в `w:ins` + цифры обычным раном обязаны собраться в один сегмент |
| NODES-ATTR-TEXT | `src/unread_zones.py:380` (`_scan_readable_part`, сверка идёт по `w:t`) — найдено этапом NODES, Часть 1 | **Перепись узлов закрывает текст в ЭЛЕМЕНТАХ, но не в АТРИБУТАХ, и это граница гарантии, а не её нарушение.** Закрытый список плюс сверка «каждый `w:t` достигнут» дают полноту по элементному тексту. Текст, лежащий в атрибуте, под неё не попадает и зоной не объявляется: альтернативное описание рисунка (`wp:docPr/@descr`, `@title`), подсказка элемента управления (`w:sdtPr/w:placeholder`), свойства пакета (`docProps/core.xml`, `docProps/app.xml` — автор, компания, заголовок). Замера, сколько таких мест несёт ПДн в договорах, НЕТ — на этапе NODES он не делался. Направление ошибки то же самое, что у закрытой `DOG-NODE-SILENT`: тихий пропуск, не утечка через LLM (значения нет в `{sid}.txt`), но и предупреждения нет. | СРЕДНЕ (класс реален, частота не измерена; описание рисунка в договоре обычно техническое, свойства пакета — почти всегда автозаполнение Word) | не запланировано: сначала замер частоты на документах владельца, потом решение «читать или объявлять зоной». Расширять сверку на атрибуты вслепую нельзя — `docProps` есть в КАЖДОМ .docx, и отказ стал бы отказом на всём |
| NODES-FOOTER-PAGENUM | `entity_types.yaml` (`ADDRESS`, якорь «стр.» = строение) × колонтитулы, ставшие читаемыми — найдено этапом NODES, Часть 2 | **«Стр. 1 из 3» маскируется как `[ADDRESS_1]`.** Дефект детекции ПРЕД-СУЩЕСТВУЮЩИЙ, а не привнесённый: воспроизведён на обычном `.txt` той же строкой, без всякого .docx (`Стр. 1 из 3 \| Телефон: …` → `[ADDRESS_1] из 3 \| Телефон: [PHONE_1]`). Этап NODES его не создал, но резко поднял ЧАСТОТУ: нумерация страниц живёт именно в нижнем колонтитуле, который до этого этапа не читался вовсе. Направление ошибки — перебор (порча документа), не утечка. | СРЕДНЕ (порча читаемости, не утечка; частота после NODES высокая — колонтитул с номером страницы есть почти в каждом договоре) | зона детекции, не NODES: анти-якорь «стр.» перед арабской цифрой без последующего адресного маркера. Мерить линией «д» гейта (порча документа) |
| STORE-HOME-CACHE | `src/session_store.py` (найдено этапом TESTOPT, изоляция под pytest-xdist) | **`_DEFAULT_STORAGE_DIR` кешируется на уровне МОДУЛЯ при первом импорте `session_store.py` в процессе** (`src/session_store.py:35`: `_DEFAULT_STORAGE_DIR = Path.home() / ".shifrator" / "sessions"`, вычисляется один раз, `default_storage_dir()` лишь возвращает сохранённое значение). От неё зависит запись БЕЗ явного `storage_dir=`: `save_session`/`load_session`/`purge_expired` в самом `session_store.py`, а через них — `app/core.py:511` (`run_encrypt`, UI-путь) и все функции-фасады `src/storage.py`, которые сами не форвардят `storage_dir` (см. докстринг `iso_store` в `tests/test_storage_s1.py:36-44` — тот же дефект, уже обойдённый там monkeypatch). Следствие, подтверждённое живьём: тесты, подменяющие `HOME`/`USERPROFILE` ПОСЛЕ того, как `session_store` уже был импортирован где-то раньше в процессе (обычный случай для pytest — модуль импортируется одним из первых тестовых файлов), пишут сессии мимо `tmp_path`, в РЕАЛЬНЫЙ `~/.shifrator/sessions` того, кто гоняет тесты (`tests/test_stage_b_per.py::test_cli_ui_parity_byte_identical`, `tests/test_type_policy_ui.py` — оба закрыты на уровне теста этапом TESTOPT, `src/` не тронут). ОТКРЫТЫЙ ВОПРОС (не отвечен, предмет отдельной сессии `src/`): как это ведёт себя в собранном PyInstaller-exe — путь резолвится при первом импорте модуля, то есть раньше, чем известно фактическое место записи (пользователь мог сменить `HOME`/профиль/переехать данные до первого запуска новой версии); нужно установить, актуально это для реального процесса продукта или дефект чисто тестовый. | СРЕДНЯЯ (реальная запись мимо намеченного каталога подтверждена; открыт вопрос применимости к продукту, не только к тестам) | не запланировано — правка `src/`, отдельная сессия по зоне `src/` |
| STORE-ACCUM-CONFIRM | хранилище сессий (`~/.shifrator/sessions`) — теоретический долг документирован в [`docs/PRODUCT_LEGAL.md`](PRODUCT_LEGAL.md#хранение-данных-на-диске-пользователя) | **Накопление сессий без очистки — ПОДТВЕРЖДЕНО ЖИВЫМ ФАКТОМ, не новый долг.** `PRODUCT_LEGAL.md` уже описывал теоретически: интерфейс никогда не зовёт `purge_expired` (только CLI `decrypt`), `{sid}.unread.json` не удаляется ни одним путём, `purge_expired_markup()` существует и не вызывается ни одним путём продукта. Этап TESTOPT нашёл живое подтверждение при разведке `STORE-HOME-CACHE`: реальный `~/.shifrator/sessions` владельца на момент находки — **775 файлов**. Состав каталога (без вскрытия содержимого `.enc`/`.doc.json`/`.txt` — только имена файлов и поле `source_name` из `.meta.json`, само не являющееся ПДн): 195 уникальных session_id; `source_name` в подавляющем большинстве — тестовые имена (`dogovor.txt` ×139, `doc.txt` ×45, `agency_0001.txt` ×5 — файл корпуса `tests/corpus/docs/`); даты создания кластеризуются на 2026-07-29…08-04, что совпадает с датами рабочих этапов в `JOURNAL.md`, а не с изолированной единичной датой. Один остаток: 4 сессии несут правдоподобное имя реального договора («Договор_ИП_Пшеничник…», даты 29-30.07, внутри того же кластера дат) — по имени и датировке отличить тестовую генерацию от реальной работы владельца нельзя, содержимое не открывалось. Долг переходит из теоретического в измеренный. | ВАЖНО (накопление подтверждено измерением, не выводом из кода; 4 файла с правдоподобным именем реального договора требуют внимания владельца) | не запланировано — тот же долг, что в `PRODUCT_LEGAL.md`; решение (звать `purge_expired` из интерфейса / TTL для `.unread.json` / включить `purge_expired_markup`) — предмет продуктовой, не тестовой сессии |
| A7-ORG-NOANCHOR-UNSOLVABLE | `src/anchor_registry.py` (`OrgAnchorDetector`) — пятый долг этапа A7, закрыт ВЕРДИКТОМ, а не правкой; поглощает по существу `Aprime-1` и `B-MASKSHIFT-ORG` (те остаются как описание класса) | **«Организация без якоря» правилами НЕ РЕШАЕТСЯ — это решение о цене, а не задача детектора.** Замер вместо догадки (этап A7, конвейер на дату 2026-08-07): на «Исполнитель — Петров Консалтинг, действующий на основании устава» конвейер не даёт НИЧЕГО — ни `ORG`, ни `PERSON` (безъякорная фамилия пропускается сознательно, класс `Aprime-1`); при этом «ООО «Петров Консалтинг»» даёт `ORG`, а «Петров Владимир Сергеевич» даёт `PERSON`, то есть обе якорные ветви живы и дело именно в отсутствии якоря. Единственный механически доступный признак — «второе слово с заглавной, не являющееся личным именем по словарю», то есть СЛОВАРЬ РОДОВ ДЕЯТЕЛЬНОСТИ. Он по построению не покрывает незнакомое название фирмы (список родов деятельности конечен, названия — нет), растёт бесконечно и меняет НАПРАВЛЕНИЕ ошибки на опасное: с недобора (сегодня — пропуск) на перебор по обычной прозе с заглавной буквы. Правка этой сессией не делалась намеренно: цена (маски на прозе) больше выигрыша (2 организации корпуса), и выбор здесь за владельцем. | ВАЖНО (утечка названия организации на безъякорной конструкции; в корпусе 2 уникальных орг / 8 сущностей с мутациями, на реальном договоре не встречается) | **правилами не решается.** Нужно решение владельца о цене: (а) оставить как есть — недобор; (б) словарь родов деятельности — перебор на прозе; (в) ярус recall на модели (кандидат из бейк-оффа A0: GLiNER брал «Восход» единым `ORG` 100 %) |
| INSTR-PKGSPEC-SUBSTRING | `tests/test_u1_packaging.py:205-240` (`test_pyinstaller_spec_lists_every_app_module`, `test_pyinstaller_spec_lists_every_src_module`) — найдено этапом INSTR (Часть 3, разведка мерительных инструментов, по образцу `corpus_anon_sha`/гейта) | **Тест заявляет «модуль отсутствует в `hiddenimports`», а фактически проверяет только присутствие подстроки `"имя_модуля"` ГДЕ УГОДНО в тексте файла `packaging/shifrator.spec`** (`missing = sorted(m for m in app_modules if f'"{m}"' not in spec)` — `spec` здесь строка всего файла, не разобранный список `hiddenimports`). У самой спеки есть верный, строгий страж на ту же тему — `_assert_every_local_module_listed()` (`packaging/shifrator.spec:133-159`), который парсит `hiddenimports` как настоящий список (`set(hiddenimports)`) и останавливает сборку. Тест — заявленное «зеркало» этого стража (докстринг `:194-202`: «проверяем спеку как данные, а не собираем exe»), но зеркалит его СЛАБЕЕ: если имя нового модуля попадёт в спеку любой другой строкой в кавычках (комментарий, `excludes=[...]`, значение `datas`/`pathex`, случайное совпадение с уже существующими литералами вроде `"src"`/`"app"`/`"launcher"` на строках 25-26/144) БЕЗ добавления в сам `hiddenimports`, тест зелёный, а сборка тихо теряет модуль — ровно тот класс отказа («тихая деградация вместо явной ошибки»), ради которого весь этот тест и `_assert_every_local_module_listed` заведены (находка этапа U4). На сегодняшнем составе спеки коллизии нет (проверено: единственные `"quoted"`-литералы вне `hiddenimports` — служебные пути и версии пакетов, ни один не совпадает с именем модуля `src/`/`app/`), поэтому дефект ЛАТЕНТНЫЙ — не воспроизведён на реальных данных, только показан по коду. | НЕЗНАЧИТЕЛЬНАЯ (латентно; настоящий гейт при сборке — `_assert_every_local_module_listed` в самой спеке, он строгий и не имеет этого дефекта; тест — вторичное зеркало) | не запланировано — кандидат: разобрать `hiddenimports` из текста спеки как список (`ast.literal_eval` среза между `hiddenimports = [` и `]`, либо импортировать спеку как модуль) вместо substring-проверки всего файла |
| AUDIT-ARCH-TRANCHE-FALSE | `docs/ARCHITECTURE.md:169-171`, `CLAUDE.md` запрет 6 — найдено этапом AUDIT (сплошная сверка) | **Два документа по-прежнему утверждают, что запрет детекции `TRANCHE` охраняется автоматически, хотя автособираемого стража нет.** `ARCHITECTURE.md:169-171`: «на это стоят четыре сторожа, включая автособираемый `tests/test_corpus_v2_axis_coverage.py`»; корневой `CLAUDE.md`, запрет 6: «на него стоят сторожа». Проверено прогоном и чтением: `grep TRANCHE tests/*.py` даёт РОВНО одно попадание — `tests/test_corpus_v2_axis_coverage.py:213`, и это строка, которая `TRANCHE` из проверки ИСКЛЮЧАЕТ («виды данных с единственной осью проверке не подлежат»). Единственная исполняемая проверка запрета — `tests/corpus_v2/validate.py:161-180`, она запускается руками. То есть названный «автособираемым сторожем» тест — тот самый прибор, который охраняемый тип не проверяет; это ЧЕТВЁРТЫЙ случай класса за блок и второе место, где та же формулировка не была исправлена этапом C+ (он поправил `tests/corpus_v2/CLAUDE.md` и `.claude/rules/`, а `ARCHITECTURE.md` и корневой `CLAUDE.md` — нет). Сам долг — `V2-TRANCHE-NOGUARD`; эта запись про НЕВЕРНОЕ УТВЕРЖДЕНИЕ в документах, которое дороже отсутствия стража: читатель уверен, что защита есть. | СРЕДНЯЯ (документ вводит в заблуждение о наличии защиты) | формулировки исправлены этим этапом (`ARCHITECTURE.md`, `CLAUDE.md`); сам страж — долг `V2-TRANCHE-NOGUARD`, детекцию этап AUDIT не трогает |
| AUDIT-GUARD-PYWRITE **[ЗАКРЫТА этапом AUDIT, 2026-08-08]** | `.claude/hooks/guard.py` × запреты 2 и 3 | **Замки корпуса и точки отсчёта обходились одной командой интерпретатора — замерено живьём, а не выведено из кода.** Хук считал команду изменяющей только по ГЛАГОЛУ оболочки (`rm`/`cp`/`sed -i`/…) или по перенаправлению `>`. Прогон хука с настоящим payload: `python -c "open('tests/corpus/gold.json','w')"` → **exit 0**, `python -c "import shutil;shutil.copy('d.json','tests/corpus/results_baseline.json')"` → **exit 0**. То есть заявление `tests/corpus/CLAUDE.md:6` «заперто хуком» было верно только для одного способа записи из двух. Закрыто: команда, в которой есть запуск интерпретатора И признак записи (`open(...,'w')`, `.write(`, `json.dump`, `shutil.copy/move`, `os.remove/rename`, `Path.write_*`), считается изменяющей; чтение (`json.load(open(gold))`) намеренно оставлено проходящим — иначе сторож мешал бы разбору, ради которого корпус и читают. | ВАЖНАЯ (замок, объявленный физическим, обходился штатным инструментом сессии) | закрыто; зеркала — `.claude/hooks/selftest.py` (2 случая) и `tests/test_audit_instrument_guards.py` |
| AUDIT-ITER-NOLEDGER **[ЗАКРЫТА этапом AUDIT, 2026-08-08]** | `tests/corpus/results_iter_baseline.json` — точка отсчёта быстрого набора | **Точку отсчёта среза можно было переснять БЕЗ СЛЕДА: журнал вёлся только для полного корпуса.** Замерено: `Write` по пути файла → exit 0, `cp d.json …` → exit 0, `run_iter_baseline.py` → exit 0, `update_manifest.py` → exit 0. Файл в `MANIFEST.sha256`, но манифест пересобирается тем же `update_manifest.py`, который хук пропускал, — то есть единственное следствие пересъёмки (расхождение хеша) снималось второй разрешённой командой. Автора, причины и списка расхождений не оставалось нигде. Прибор отладки, который можно бесследно подогнать под текущее поведение, показывает не поведение, а последнюю подгонку — ровно то, из-за чего однажды перестал охраняться полный гейт (`tests/corpus/BASELINES.md`, раздел S2). Закрыто тремя рубежами: журнал `tests/corpus/iter_baseline_ledger.json` (записи только дописываются, пересъёмка C+ внесена задним числом со всеми восемью расхождениями, включая пять «в плюс»), инструмент `tests/corpus/promote_iter_baseline.py` (требует `--author`/`--reason`/`--delta`, отказывает при обосновании «покраснеет гейт»), замок в хуке + страж `tests/test_audit_instrument_guards.py::test_iter_baseline_matches_its_ledger` (хеш файла обязан быть назван последней записью — краснеет при любом способе обхода, включая ручной). | ВАЖНАЯ (закрыта) | закрыто |
| AUDIT-MANIFEST-WHITELIST | `tests/corpus/MANIFEST.sha256` × `update_manifest.py` × заявление `tests/corpus/CLAUDE.md:9` | **`sha256sum -c MANIFEST.sha256` не может обнаружить ДОПОЛНЕНИЕ корпуса — а запрет 2 запрещает и его.** Манифест — белый список, и состав его берётся ИЗ САМОГО МАНИФЕСТА (`update_manifest.py:72`, «состав не угадывается» — осознанное решение против затягивания расходных дампов). Следствие, в документах не названное: новый файл в `tests/corpus/docs/` проверкой не виден вообще, `check_manifest()` (`gate.py:111-134`) обходит только перечисленные пути. Заявление «на них держится `MANIFEST.sha256`… `sha256sum -c` обязан быть OK до и после любой правки» покрывает ПРАВКУ и УДАЛЕНИЕ, но не ДОПОЛНЕНИЕ. Единственная защита от дополнения — хук (`Write` по пути внутри `tests/corpus/docs/`), а он видит только литеральный путь в вызове инструмента: файл, порождённый скриптом по вычисленному пути, пройдёт мимо обоих. | СРЕДНЯЯ (одна из трёх запрещённых операций не покрыта измерительной проверкой; хук покрывает её частично) | не чинится этим этапом (правка состава манифеста — зона корпуса). Кандидат: отдельная проверка «в `tests/corpus/docs/` ровно 324 файла и все они в манифесте» — дешёвая, без прогона корпуса |
| AUDIT-REALDOC-NOGUARD | запрет 7 («реальные документы не коммитить») × `.gitignore` | **Запрет, ценой нарушения которого стала реальная утечка ПДн в публичный репозиторий (`DOG-REPO-BLOB`), не имеет исполняемой проверки — только `.gitignore` ПО ИМЕНАМ.** В игноре перечислены `real_docs/`, `/dogovor.*`, `*_restored.*`, отчёты интерфейса, дампы замеров. Всё это работает, пока файл называется ожидаемым именем и лежит в ожидаемом месте; дамп реального текста, положенный в `experiments/stage_x/probe_out.txt` или в `docs/`, не остановит ничто — при том что сам `.gitignore` в комментарии прямо просит «не переносить дампы в `experiments/`/`docs/`», то есть автор запрета знал этот путь обхода. Ни хука, ни теста, ни pre-commit на содержимое нет. Однажды класс уже сработал: `docs/archive/reports/REAL_DOCS_CHECK.md` нашёл блоб и НЕ опознал его (см. `DOG-REPO-BLOB`). | ВАЖНАЯ (главный риск продукта; защита держится дисциплиной и именами файлов) | не чинится этим этапом (зона — процесс коммита, решение владельца). Кандидат: PreToolUse-замок на `git add`/`git commit` при попадании в индекс `.docx`/`.xlsx` вне `tests/`+`packaging/`, либо проверка на «игл» из реального документа — второе требует хранить иглы, что само по себе риск |

---

## Этап AUDIT (2026-08-08) — сплошная сверка утверждений о защите

Разобрано каждое утверждение вида «здесь стоит сторож / это охраняется / нельзя
никогда» из корневого `CLAUDE.md`, `src/CLAUDE.md`, `tests/corpus/CLAUDE.md`,
`tests/corpus_v2/CLAUDE.md`, `docs/ARCHITECTURE.md`, README обоих корпусов и
докстрингов гейта и хука. Три колонки: чем обеспечено (файл:строка), покрывает
ли заявленное целиком, идёт ли в обычном прогоне.

| # | Заявлено (где) | Чем обеспечено | Целиком? | В обычном прогоне? | Вердикт |
|---|---|---|---|---|---|
| 1 | `git push` не делать (`CLAUDE.md` запрет 1) | `guard.py:PUSH` + `check_shell` | да, включая `bash -c "git push"` и вырезание текста коммита | хук — на каждом вызове; зеркало — `selftest.py`, с этого этапа и `test_audit_instrument_guards.py` | **РАБОТАЕТ** |
| 2 | Корпус v1 не редактировать/дополнять/удалять (`CLAUDE.md` запрет 2, `tests/corpus/CLAUDE.md:4-9`) | `guard.py:FROZEN_CORPUS` (Write/Edit + глаголы оболочки) + `gate.check_manifest()` | нет: правка через интерпретатор проходила (`AUDIT-GUARD-PYWRITE`, закрыто), ДОПОЛНЕНИЕ манифестом не ловится вовсе (`AUDIT-MANIFEST-WHITELIST`, открыто) | манифест — только в полном гейте (~10 мин); хук — всегда | **ДЫРЯВЫЙ** (одна дыра закрыта, вторая открыта) |
| 3 | Планку гейта молча не двигать (`CLAUDE.md` запрет 3) | `guard.py:BASELINE` + `promote_baseline.py` + `overmask_ledger.py` + сверка журнала в `gate.evaluate(check_ledger=True)` | для ПОЛНОГО корпуса — да (после закрытия `AUDIT-GUARD-PYWRITE`); для СРЕЗА не было ничего (`AUDIT-ITER-NOLEDGER`, закрыто этим этапом) | сверка журнала — в полном гейте; замки — всегда | **РАБОТАЕТ** (после правок этапа) |
| 4 | Допуски `gate_config.py` не ослаблять | `guard.py:GATE_CONFIG` | да для правки файла; значения допусков как таковые ничем не пришпилены (тест «все нули» отсутствует) | хук — всегда | **ДЫРЯВЫЙ** (мелко: ослабление возможно только с санкции, но «нулевость» не сторожится) |
| 5 | Стена `src/` ↛ генератор корпуса v2 (`src/CLAUDE.md:3-28`, `ARCHITECTURE.md:165-167`) | ничего исполняемого; проверка чтением импортов | — | нет | **НЕТ ВООБЩЕ** — честно объявлено самим документом, долг `V2-WALL-NOGUARD` |
| 6 | `TRANCHE` не детектировать; «стоят сторожа» / «четыре сторожа, включая автособираемый» | `validate.py:161-180` (руками); `test_corpus_v2_axis_coverage.py:213` `TRANCHE` ИСКЛЮЧАЕТ | нет | нет | **НЕТ ВООБЩЕ**, и документ утверждал обратное — `AUDIT-ARCH-TRANCHE-FALSE` |
| 7 | Красный тест не чинится ослаблением теста (запрет 5) | ничего; конвенция | — | нет | **НЕТ ВООБЩЕ** (процессное правило, механизировать нечем) |
| 8 | Реальные документы не коммитить (запрет 7) | `.gitignore` по именам | нет | нет | **ДЫРЯВЫЙ** — `AUDIT-REALDOC-NOGUARD` |
| 9 | Путь и формат хранилища не менять (запрет 8) | `tests/test_session_store.py`, `test_storage_s1.py`, `test_breaking_session.py`, `test_adversarial_session_store.py` (17 файлов трогают `~/.shifrator`/`key.bin`) | формат и путь пришпилены тестами; смена схемы уронит их | да | **РАБОТАЕТ** |
| 10 | Новый тип обязан быть в `_REGEX_PRIORITY`/`_NER_PRIORITY` (`src/CLAUDE.md:36-38`) | `assert_priority_contract` (`src/tokenizer.py:95`), зовётся из `tokenize` (`:817`) | да | да (любой тест маскирования) | **РАБОТАЕТ** (оговорка: докстринг говорит «уронит сборку», роняет он ПРОГОН — это разные события) |
| 11 | Порядок слоёв — в одном месте, копий нет (`src/CLAUDE.md:30-34`) | `pipeline.run_detection`; исполняемой проверки «копий нет» нет | нет | нет | **НЕТ ВООБЩЕ** (держится тем, что CLI/UI/замер физически зовут одну функцию — это структурная, а не проверяемая гарантия) |
| 12 | Гейт: семь линий, все допуски нулевые (`ARCHITECTURE.md:116-133`) | `gate.py` — по каждой линии есть `regressions.append` (крешы `:176`, leak `:189`/`:201`, FP `:214`, masking A/B `:250`/`:273`/`:277`, precision `:324`, порча `:371`/`:379`, границы `:438`, долг `:456`, зеркало подавления `:504`) | да; отдельно проверено, что «нечем мерить» (нет поля `bnd`, нет baseline) — тоже регресс, а не тишина (`:228`, `:416`, `:422`) | линии — только в полном гейте; юнит-зеркала — `test_gate_regression_detection.py` (33) | **РАБОТАЕТ** |
| 13 | Отчёт наружу безопасен: три рубежа (`ARCHITECTURE.md:191-195`) | белый список `_SCHEMA_KEYS` + нормализация + `assert_structural` (`app/report.py:739`), зовётся на пути сборки отчёта (`:687`) | да | да (`test_u4_report.py`, `test_type_policy.py`) | **РАБОТАЕТ** |
| 14 | Ключ атомарен, не пересоздаётся при чтении (`ARCHITECTURE.md:187-189`) | `session_store`/`vault`, зеркала в `test_session_store.py`/`test_adversarial_session_store.py` | да | да | **РАБОТАЕТ** |
| 15 | Спека PyInstaller знает про каждый модуль (`test_u1_packaging.py`, `shifrator.spec:138`) | строгий `_assert_every_local_module_listed()` в спеке; зеркало-тест **искало подстроку** | зеркало — нет (`INSTR-PKGSPEC-SUBSTRING`) | да | **ДЫРЯВЫЙ → закрыт этим этапом** (разбор `hiddenimports` как списка + само-проверка, что это не подстрока) |
| 16 | Само-тест замков (`.claude/hooks/selftest.py`) | сам себе прибор | 17 случаев из 17 проходили | **нет — запускался только руками** | **МЁРТВЫЙ → закрыт этим этапом** (`test_audit_instrument_guards.py::test_hook_selftest_passes`, 27 случаев) |
| 17 | Корпус v2 правится генератором, не руками (`tests/corpus_v2/CLAUDE.md:3-4`) | `test_corpus_v2_reproducible.py` (побайтная пересборка) | да | да | **РАБОТАЕТ** |
| 18 | Быстрый набор — находка, не приёмка; своя точка отсчёта (`tests/corpus/CLAUDE.md:28-33`) | текст + `run_iter_baseline.py`; до этапа AUDIT точка отсчёта не охранялась | нет | — | **ДЫРЯВЫЙ → закрыт этим этапом** (`AUDIT-ITER-NOLEDGER`) |

**Утечки ПДн открытым текстом эта сверка не вскрыла** — ни одного нового пути,
по которому значение уходило бы в LLM без маски. Известные такие долги
(`DOG-ANCHOR-SPLIT`, `DOG-NODE-SILENT`, `DOG-REPO-BLOB`) остаются на своих
местах и этим этапом не трогались: зона AUDIT — приборы, а не детекция.

---

## Снято с этого файла этапом DOC (2026-08-04)

Перенесено в [`archive/FINDINGS_closed.md`](archive/FINDINGS_closed.md):

| id | почему снято |
|---|---|
| `D-PRECISION-90`, `0c-H`, `Stage4-A`, `Stage2b-B`, `PER-B` | Несли колонку «тяжесть» = `ЗАКРЫТО`, то есть нарушали заявленную политику файла |
| `PER-B(hist)` | Историческое описание закрытой `PER-B`, помечено `~~зачёркиванием~~` в самом заголовке |
| `U4-G-TIMEOUT` | **Закрыт коммитом `e44ac61` (2026-07-26):** этап O2 вернул вложенный прогон 488 c → 295 c, жёсткий лимит поднят 300 → 900 c, докстринг теста прямо говорит «находка U4-G-TIMEOUT закрыта» (`tests/component2/test_g_regression.py:34-55`). Числа записи («1 failed, 976 passed») относятся к HEAD `abf3ee8` от 2026-07-25, то есть к состоянию ДО починки |
| `Eprime-A` (огромные документы) и `Eprime-A` (реальный документ) | **Обе описывают тот же недетерминизм, который третья запись `Eprime-A` объявляет ЗАКРЫТЫМ.** Этап E″ нашёл механизм (`_CapsResolver`, ключ по `id()`), устранил его и прямо перечислил «инференс Natasha/numpy» и «холодный BLAS» среди ОПРОВЕРГНУТЫХ гипотез — а это и есть вывод обеих оставленных записей. Фикс присутствует в коде (`src/extractor.py:216,222`) и закрыт стражами `tests/test_determinism.py`. Остаточный долг подтверждения выделен в новую запись `EPRIME-A-CONFIRM` выше |

**Про id `Eprime-A`.** Он стоял на ТРЁХ разных находках сразу (две открытые, одна
закрытая), из-за чего любая перекрёстная ссылка на него была неразрешима. При
переносе они разведены: `EPRIME-A-CAPS` (закрытая, механизм и фикс),
`EPRIME-A-LARGE` и `EPRIME-A-REAL` (обе — симптом, поглощены `EPRIME-A-CAPS`),
`EPRIME-A-CONFIRM` (открытый долг подтверждения). Прежний id `Eprime-A` больше
не используется ни для чего.

**Новая запись `V2-WALL-NOGUARD`** заведена в этом этапе: отсутствие стража стены
было зафиксировано разведкой S0 как факт, но в списке долгов не числилось.

Прочие открытые ограничения охвата (не «находки», а нереализованные
детекторы/зоны) — `DATE` (детектор выключен конфигом), непрочитанные зоны `.docx`
(этап 1, fail-closed), извлечение PDF, веб-компонент. См. [`STATE.md`](STATE.md).
