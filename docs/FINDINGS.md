# FINDINGS — открытые находки

Только то, что ещё не закрыто. Закрытые/исторические находки (включая все находки
раунда 2 «грязных сценариев», #1–12, и большинство наблюдений замера 0c) —
[`docs/archive/FINDINGS_closed.md`](archive/FINDINGS_closed.md). ID сохранены для
перекрёстных ссылок. Краткий статус детекторов/дефектов — [`STATE.md`](STATE.md) §3;
здесь — расширенное описание находок с id.

| id | область | находка | тяжесть | чинится |
|---|---|---|---|---|
| D-PRECISION-90 | метрика precision (историческая «90%») | **«precision ≈90%» была ФИКЦИЕЙ — помечена неверной.** В проекте до этапа D НЕ было честной метрики precision; за неё принимался `masking_correctness C = 90.81%` (STATE §2). Но masking_correctness B/C считаются ТОЛЬКО по маскам, ЛЁГШИМ НА GOLD (`measure_lib.mc_check_bc`: маска не на gold → `scored=False` → в знаменатель НЕ идёт), то есть СТРУКТУРНО СЛЕПЫ к over-маскированию: маска на объявленном негативе или на неаннотированной прозе в masking C не существует вовсе. На HEAD этапа B это скрывало **634 ADDRESS-маски на негативах + 2610 ADDRESS-масок на прозе** — masking C их «не видел» и показывал ~90% «качества». (Второе историческое число — сырой precision **46.3%** в COVERAGE_MEASUREMENT §Precision — наоборот ЗАНИЖЕН: считал SUM-на-негативах, банки-cross-type и прозу-on-nothing одним мешком FP.) **Честная дефиниция (этап D, `measure_lib.precision_by_type`)**: FP = kept-маска на ОБЪЯВЛЕННОМ негативе, TP = kept-маска на gold СВОЕГО типа, precision_T = TP/(TP+FP_neg). По ней: ORG 99.77%, PER 99.96%, **ADDRESS 66.86%(HEAD b-per)→99.92%(C)**; cross-type и on-nothing — ОТДЕЛЬНАЯ диагностика, не в precision. Зеркала — `tests/test_precision_metric.py`. | ЗАКРЫТО (метрика построена) | этап D (`measure_lib.precision_by_type` + `experiments/stage_d/gate_d.py`) |
| Stage3-A | BIRTHDATE | Недобор спана на ОДИН символ в 4 случаях `m2718_homoglyph`: первая цифра даты подменена омоглифом («07.07.1963» → «З7.07.1963»), нормализация её не сводит (в токене «З7» лишь одна настоящая цифра, порог свода — две), и якорный зазор `[^
\d]{0,10}` съедает «З». Маскируется «7.07.1963». Направление ошибки безопасное (недобор, не перебор); альтернатива — запрет букв в зазоре — обменяла бы 4 недобора на 4 полных пропуска. | НЕЗНАЧИТЕЛЬНО | не запланирован |
| 0c-B | PASSPORT | recall_exact 0% (было и после этапа 2: 26.1→30.8% loose, exact контракт спана не чинится нормализацией) — детектор задевает паспорт, но не покрывает спан целиком → частичная утечка. | ВАЖНО | не запланирован |
| 0c-C/0c-D | ADDRESS / NER precision | ~~Жадный `yargy.AddrExtractor` цепляет голые 6-значные прогоны как индекс, прозу и не-адреса~~ **В ОСНОВНОМ ЗАКРЫТО этапом C** (2026-07-22, якорная модель адреса): ложных ADDRESS на негативах **634→1**, ложных ADDRESS всего **3483→14**, exact-границы 41.4→65.1%. Голый индекс/паспортная 6-значная группа больше не якорь (adjacency-guard `_addr_valid_index`). Остаток — 14 ложных ADDRESS, все на адверсариальных мутациях (см. Stage-C-A/B ниже). См. [`archive/reports/HANDOFF_STAGE_C_ADDRESS.md`](archive/reports/HANDOFF_STAGE_C_ADDRESS.md). | В ОСНОВНОМ ЗАКРЫТО | этап C (`src/ner_detector.py`) |
| Stage-C-A | ADDRESS под адверсариальной мутацией | Recall ADDRESS −4.7пп (93.6→**89.0%** после фикса сеяния 2026-07-23) на мутациях (combo/linebreak-в-слове/all-caps/invisible-NBSP→пробел/digit_spaces): yargy/нормализация не собирают спан из рваного текста. **Чистотекстовый случай `agency_0009` ЗАКРЫТ** (сеяние по house-pattern, `_addr_has_seed`) — на чистом тексте потерь нет. Остаток — **ровно 61 адверсариальная мутация**, оформлены явным реестром `docs/known_leaks_stage_c.json` (`closes_on: entity-spans`) для гейта D. HEAD «ловил» эти мутации лишь через over-capture (3483 ложных ADDRESS). Кандидат: `\n`-стойкий адресный вид + сборка спана ОТ якорей, не от yargy. | ВАЖНО (recall на адверсариальных; 61 в known_leaks) | отдельный этап (адресный вид + якорная сборка); закроются на Entity.spans |
| Stage-C-B | ADDRESS: паспорт под гомоглифом → ложный ADDRESS | 7 случаев «Паспорт NN NN NNNNNN, выдан…»: гомоглиф `О9`/`І8` (буква+цифра) рвёт цифровой guard house-pattern (`_addr_house_pattern` num-run-guard видит `О9` как слово, не число), «Паспорт NN» проходит как «улица дом». cross=PASSPORT (накрывает уже-PII, не новая утечка). | НЕЗНАЧИТЕЛЬНО (7 адверсариальных, cross-type) | нормализация буква-в-числе в цифровом guard |
| 0c-H | метрика leak_v2 (ADDRESS) | ~~v2-метрика сравнивает буквенный токен ЦЕЛИКОМ и пропускает утечку **усечённого** топонима~~ **ЗАКРЫТО** починкой метрики ADDRESS (2026-07-19): сравнение переведено на границы слов, добавлена префиксная ветка (усечённый топоним >=6 букв И >=75% длины токена считается выжившим). Зеркала — `tests/test_leak_v2.py::test_addr_metric_*`. См. [`archive/reports/HANDOFF_METRIC_ADDRESS.md`](archive/reports/HANDOFF_METRIC_ADDRESS.md). | ЗАКРЫТО | починено (только measure_lib.py, src/ не тронут) |
| 1-A | харнесс (`measure_lib.aggregate_results`) | «Отказано» (`refused`, этап 1 fail-closed) смешивается с «упало» (`crashed = outcome != "processed"`). На штатном lossy-прогоне не проявляется (гейт не врёт); в strict-режиме харнесса дало бы ложные 135 «крешей». | НЕЗНАЧИТЕЛЬНО (латентно) | этап 6 (когда `refused_zones` начнёт пустеть) |
| 1-B | `src/extractor.py` / сканер зон | `w:sdt` (content control, частый способ вставки полей формы в шаблонах) на верхнем уровне тела **не читается extractor'ом и не детектируется сканером непрочитанных зон** — тихая потеря текста вне корпуса (в корпусе не встречается, замер не показывает). | ВАЖНО (тот же класс дефекта, что весь этап 1, но не покрыт корпусом) | этап 6 или отдельный этап 1c |
| Stage4-A | метрика leak_v2 (KPP/ACCOUNT/BIK) | ~~`leak_v2_numeric` ищет окно ядра по ВСЕМУ цифровому полю документа, поэтому подстрока открытого КБК засчитывается утечкой замаскированной ЦЕЛИКОМ сущности~~ **ЗАКРЫТО** починкой числовой ветки (2026-07-19): окно засчитывается, только если run, в котором оно найдено, сам является остатком значения (ядро целиком ИЛИ покрытие run-а >= `RUN_COVERAGE_MIN` = 0.8; порог — середина измеренного зазора 73…90%). Ушли 18 ложных утечек: КБК, префикс московских БИК `044525`, общие префиксы счетов `40802810`/`40702810`/`30101810`. Зеркала парные — `tests/test_leak_v2.py::test_numeric_*`. См. [`archive/reports/HANDOFF_METRIC_NUMERIC.md`](archive/reports/HANDOFF_METRIC_NUMERIC.md). | ЗАКРЫТО | починено (только measure_lib.py, src/ не тронут) |
| Stage4-B | `src/regex_detector.py` PHONE / `src/ner_detector.py` ADDRESS-overcapture | До этапа 4 `ADDRESS`-NER иногда неограниченно поглощал ИНН с невалидной КС (детектор его не видел — не было барьера), заодно (случайно) пряча под тем же [ADDRESS_n] соседний телефон, который сам по себе НЕ детектируется: гомоглиф-мутация даёt группе `NN-Оо` менее 2 «настоящих» цифр — нормализатор намеренно не сводит омоглиф при `digit_count<2` (защита «кв. 2б»), `\d` в паттерне PHONE не матчит. Этап 4 (якорная детекция ИНН) поставил барьер верно — адрес перестал заходить за ИНН — и обнажил ЭТУ, отдельную, ранее случайно замаскированную дыру PHONE. 2 документа (`loan_0006__m2718_homoglyph`, `services_0002__m1337_linebreak`), оба — редкие адверсариальные мутации. | ВАЖНО (реальная частичная утечка телефона, но не по вине этапа 4 — вскрыта им) | не запланирован (правка normalizer.py/ner_detector.py вне границ этапа 4) |
| Stage4-C | `src/regex_detector.py` OGRN checksum | Контрольная сумма ОГРНИП (mod 13, 1/13 шанс) может случайно сойтись на ПРОИЗВОЛЬНОЙ 15-значной подстроке длинного числа-не-ПДн. До этапа 4 это было невидимо: КБК целиком поглощался BANK_ACCOUNT (приоритет `_REGEX_PRIORITY` выше OGRN) и «выигрывал» пересечение, обрезая совпавший OGRN-кусок в 0. Этап 4 (якорь `anchor:` для ACCOUNT) перестал матчить КБК как ACCOUNT — и ранее скрытая коллизия КС ОГРНИП всплыла отдельным FP на 5 документах (все с фичами `digit_spaces`/`combo`, где КБК записан через разделители, случайно попавшие в 15-значное окно). Не связано с новой якорной асимметрией INN/OGRN — путь детекции тот же, что и до этапа 4 (унанкорный + валидная КС). | НЕЗНАЧИТЕЛЬНО (FP растёт точечно, суммарный FP по негативам упал — 2284→1975) | не запланирован (КС ОГРНИП как самостоятельный фильтр коротких/произвольных чисел структурно ненадёжна; кандидат — доп. якорь-подтверждение и для унанкорного случая, отдельный этап) |

| Stage2b-A | `src/extractor.py` (регистр) x `src/normalizer.py` (омоглифы) | **Стык двух нормализаций.** Свод `_ALPHA_FOLD` регистрозависим и неполон: `'B'->'В'` есть, `'b'->'В'` НЕТ. Поэтому регистровая правка слова СМЕШАННОГО алфавита (`иBаНоB`, `ПЧЕЛЫ ЕKAТЕРИНЫ` — латиница внутри кириллицы) ломает свод: опущенная в `'b'` латинская `B` больше не сводится в кириллицу, слово остаётся рваным и ФИО не детектируется. Без щита 2b САМ создавал 6 новых утечек (2 ФИО терялись целиком, 3 фамилии оставались открытыми). Замерены и отвергнуты два более слабых щита: (1) на уровне СЛОВА — хуже обоих крайних вариантов, полунормализация имени (чистые слова к Титульному, омоглифное рваным) теряла 7 ФИО ЦЕЛИКОМ, recall PER 67.2->63.9%; (2) на уровне СЕГМЕНТА — дырявый: соседние сегменты того же документа всё равно чинились, PER-маски смещались, а вслед за ними жадный `yargy.AddrExtractor` перестраивал спаны и ОТКРЫВАЛ адрес в другом сегменте (см. Stage2b-B). Принято: щит на уровне **ДОКУМЕНТА** — если хоть в одном сегменте есть слово смешанного алфавита, документ откатывается к ИСТОРИЧЕСКОМУ поведению эвристики (сплошь одно-регистровый сегмент + пункт перечисления), а не к бездействию: полное отключение отняло бы то, что работало до 2b, и само роняло детекцию адреса. Итог: на combo 2b не добавляет ничего нового и не отнимает старого. Зеркала — `tests/test_case_detection.py::TestStage2bWordLevelCase::test_combo_segment_is_left_alone_entirely` (+ парный контроль на чистой кириллице). | ВАЖНО (непокрытая регистровая утечка на combo-мутациях; НЕ самосозданная — эти ФИО текли и до 2b) | отдельный шаг «регистр x омоглиф» (порядок нормализаций либо регистро-независимый свод) |
| Stage2b-B | `src/ner_detector.py` (жадный `yargy.AddrExtractor`) x правка PER | **ЗАКРЫТО этапом C** (2026-07-22): `_MIN_CAPS_RUN` возвращён 3→2. Механизм воид — Natasha-PER/ORG выключены (этапы A/B, случайной over-capture-маски нет), адрес якорный (не хватает заглавные прогоны как топоним). Двусловные заглавные ФИО («ПЕТРОВА ОЛЬГА», «ИВАНОВ И.») снова в детекции: PER recall 72.0→74.1%, 0 новых утечек на чистом тексте. Зеркало обновлено: `test_two_word_caps_run_is_normalized_stage_c`. _(Историческое: порог 3 не давал точной маскировке ФИО обнажать адреса `МОСКВА ЛЕНИНА 47/3 КВ 67`, случайно накрытые Natasha-ORG/PER; после выключения Natasha накрывать нечем.)_ | ЗАКРЫТО | этап C (`_MIN_CAPS_RUN`=2) |
| PER-B | ~~`src/ner_detector.py` (Natasha) / метрика B~~ **ЗАКРЫТО этапом B** (structure-first PER, 2026-07-22): косвенный падеж собирается в ОДИН спан структурным `PerAnchorDetector._fio_run` (фамилия+имя+отчество+инициалы связным прогоном, без опоры на границы Natasha). masking B 81.9→89.8%; PER exact(found) 77.8→87.6%. Natasha-PER выключена. См. [`archive/reports/HANDOFF_STAGE_B_PER.md`](archive/reports/HANDOFF_STAGE_B_PER.md). Ниже — историческое описание. | ЗАКРЫТО | structure-first PER (этап B) |
| ~~PER-B(hist)~~ | `src/ner_detector.py` (Natasha) / метрика B | **Косвенный падеж рвётся двумя спанами.** Natasha отдаёт ФИО в косвенном падеже как ДВЕ соседние сущности (`Морозовой` + `Анне Николаевне`, `кузнецовой` + `марией дмитриевной`), а `mc_check_bc._covered_by_union` не терпит зазор — ОДИН ПРОБЕЛ между масками роняет B, хотя значение спрятано ЦЕЛИКОМ и не течёт. Дефект ПРЕД-СУЩЕСТВУЮЩИЙ: воспроизводится на сегментах с `detection_text` отсутствующим, т.е. на нетронутом тексте нормального регистра (`works_0009` `l19`, `sale_0006` `t0_r0_c0`). Этап 2b его не создал, а обнажил: он переводит ФИО, которые раньше целиком попадали под ОДНУ маску неверного типа **ORG**, на нормальный путь PER — тип становится верным (C 90.56->90.94%), а B рвётся на пробеле. Проверено поэнтитийно: среди сущностей, потерявших полное покрытие, НИ ОДНА не теряет реальный текст — открыт только пробел. | ВАЖНО (метрика B занижена; реальной утечки нет) | этап PER-границ (склейка соседних спанов одного типа через пробел) |
| B-MASKSHIFT-ORG | `src/anchor_registry.py` (`OrgAnchorDetector`) / выключение Natasha-PER | **Aprime-1 ORG «Петров Консалтинг» терял СЛУЧАЙНУЮ маскировку Natasha-PER.** Имя фирмы, грамматически = фамилия + слово, введённое «именуемое в дальнейшем» БЕЗ org-формы/кавычек/ИНН юрлица, структурного ORG-якоря не имеет (Aprime-1). До этапа B Natasha-PER случайно метила «Петров» PERSON (маскировало → утечки не было); с выключением Natasha-PER (structure-first PER) 8 сущностей (`cession_0004`, `loan_0001` + мутации, 2 уникальных орг) вернулись в истинное неякорное состояние и текут. НЕ регрессия детекции — ровно MASK-SHIFT, как на этапе A. ORG recall при этом ВЫРОС (93.9→94.2%). | ВАЖНО (тип неверный/утечка на 2 орг, вне области структуры) | ярус recall / якорь «введение без формы» (отклонён этапом A как широкий) |
| B-ADDR-HOMONYM | `src/anchor_registry.py` (`PerAnchorDetector`) x ADDRESS | **Улица-однофамилец под АДВЕРСАРИАЛЬНОЙ мутацией метится PER — НЕ разрешён этапом C.** Guard `_addr_type_left` снимает «ул./ш./г. <Фамилия>» на чистом тексте, но перенос/невидимый/регистр внутри маркера или имени («Ш.​ПУШКИНA», «Полев\nая», «ГАГАРИНА») рвут проверку — 9 остаточных ложных PER (адверсариальные, cross=ADDRESS; +3 к HEAD от `_MIN_CAPS_RUN`=2). Якорный адрес конфликт НЕ разрешает: PER считается ДО адреса и служит ему БАРЬЕРОМ, поэтому улица, ошибочно помеченная PER, выигрывает. Разрешение требует правки PER-guard (вне границ этапа C — «PER не трогать») либо арбитража PER↔ADDRESS (риск потери реального PER). На чистом тексте ложных PER на улицах 0. | НЕЗНАЧИТЕЛЬНО (9 адверсариальных, точность) | нормализация маркера в `_addr_type_left` или арбитраж PER↔ADDRESS |
| Aprime-1 | `src/anchor_registry.py` (`OrgAnchorDetector`) | **ORG без ЛЮБОГО структурного якоря не ловится.** Если имя организации введено БЕЗ кавычек, БЕЗ org-формы рядом и БЕЗ соседнего ИНН/ОГРН юрлица (только конструкция «…, именуемое в дальнейшем «Х»»), движок сознательно не строит якорь (см. комментарий в `OrgAnchorDetector.detect` про самостоятельный якорь на «именуем…» — отклонён этапом A как слишком широкий). Если при этом имя организации ГРАММАТИЧЕСКИ похоже на ФИО («Петров Консалтинг» = фамилия + слово), Natasha ловит его как PERSON. `cession_0004`, `loan_0001` (и их мутации `__m1337_combo`/`__m2718_homoglyph`/`__m2718_invisible`/`__m1337_cell_split`) — 12 из 28 пропусков ORG после этапа A′. | ВАЖНО (тип неверный, утечки нет — весь текст замаскирован как PERSON) | не запланирован (нужен явный якорь «введение без формы» — риск роста FP на реальных ФИО, отклонено этапом A) |
| Aprime-2 | `src/anchor_registry.py` (алфавитный фолдинг org-формы) | **Латинский омоглиф org-формы БЕЗ кириллицы в том же токене не сводится.** Stage 2 (`normalizer.py: _ALPHA_FOLD`) сводит латиницу→кириллицу только ВНУТРИ слова со смешанным алфавитом; чисто латинское «AO» (маскируется под «АО») в токене нет ни одной кириллической буквы — свод не срабатывает, `_ABBR_FORM_RE` (закрытый кириллический класс) не матчит. `sale_0009__m1337_combo`: 1 пропуск. | НЕЗНАЧИТЕЛЬНО (1 документ, адверсариальная комбо-мутация) | не запланирован (расширение `_ABBR_FORM_RE` на латинские гомоглифы форм — отдельная точечная правка) |
| Aprime-3 | `src/tokenizer.py` (`_render_table`, `\n`→`' '`) | **Асимметрия ws-схлопывания таблицы между «маскировано» и «эталон».** `_render_table` заменяет `\n` внутри ячейки на пробел ПОСЛЕ подстановки токенов: если `\n` попадает ВНУТРЬ маскированного спана, токен «прячет» его от замены, а `build_plain_text` (без масок) заменяет тот же `\n` на пробел всегда → whole-document `restored == build_plain_text(doc)` не совпадает буквально (categоризовано харнессом как `channel_ws_only`, НЕ порча значения — `measure_lib.mc_check_a` явно исключает этот класс из `masking_correctness A`). ПРЕД-СУЩЕСТВУЮЩИЙ (воспроизводится на `labor_0006`/`labor_0008`/`lease_0008`/`supply_0002`/`supply_0014` уже на HEAD, БЕЗ каких-либо правок этапа A′, и даже без detect_org вовсе — чистый PER/ADDRESS путь). Этап A′ его не создал, но делает вероятнее (ORG теперь чаще успешно накрывает спаны с внутренним переносом). | ВАЖНО (искажает грубую whole-document sha/diff проверку; НЕ реальная утечка/порча — per-mask A = 100%) | не запланирован (симметризация `\n`→`' '` между эталоном и маскированием — правка `tokenizer.py`, вне границ этапа A′) |

| Stage-Cprime-A | `src/anchor_registry.py` (`OrgAnchorDetector._name_run_before_requisite`) | **Реквизитная ветвь ORG даёт усечённый нарицательный фрагмент.** Name-run слева от ОГРН иногда захватывает только ХВОСТ многословного названия реальной фирмы (одно нарицательное единственного числа, напр. «…Технологий»), регистрирует его ключом ORG, и проход 2 тиражирует нарицательное. На реальном договоре — 1 группа ×1 маска (не роль-термин, не утечка ПДн — маскируется нарицательное; в пределах допуска ≤2 приёмки C′). Механизм — реквизитная ветвь, вне темы C′ («якорь кавычек»). Кандидат: требовать у name-run перед ИНН/ОГРН примыкающую org-форму или кавычки (как у кавычной ветви). | НЕЗНАЧИТЕЛЬНО (точность, 1 нарицательное) | отдельный этап (ужесточение реквизитной ветви) |
| Stage-E-A | `src/multispan.py` (PHONE-сборщик) x INN/PASSPORT | **Цепочка ≥2 цифрогрупп с 10 эффективными цифрами и ведущей 7/8 может быть не телефоном** (безъякорный ИНН-10 региона 7x/8x, паспортная пара «серия номер»). На корпусе дало НОЛЬ новых fp_neg (650=650, гейт D) и даже снизило утечку PASSPORT (282→268)/SNILS (39→35) — маскирование чужого значения безопаснее утечки; PHONE cross-type 11→29 (диагностика C, не утечка). Приоритеты пересечений защищают: анкорный/валидный INN и SNILS выигрывают у PHONE. Наблюдать при смене корпуса. | НЕЗНАЧИТЕЛЬНО (cross-type, направление безопасное) | наблюдение на гейте D |
| Stage-E-B | `src/file_detokenizer.py` x единый id | **decrypt-file подставляет форму ПЕРВОГО вхождения, не канон.** Файловый путь строит mapping token→original_text (v1-поле = поверхностная форма первого вхождения); с единым токеном ORG/PER все вхождения в файле получат эту форму вместо канона/своих форм. Текстовый путь (CLI/UI) подставляет канон. Сессия v2 хранит все формы (occurrences) — приведение файлового пути к канону/точным формам = отдельное решение. | НЕЗНАЧИТЕЛЬНО (форма, не утечка) | отдельный этап (канон в file_detokenizer) |
| Stage-Cprime-B | `src/anchor_registry.py` (`OrgAnchorDetector._name_run_before_requisite`) x PER | **Реквизитная ветвь втягивает соседнее ФИО в ORG-ключ.** «ООО "Name" <ФИО-руководителя в родительном> … ОГРН …»: name-run перед ОГРН перетягивает имя человека в ключ ORG. Человек ЗАМАСКИРОВАН (утечки нет), но тип ORG вместо PER (cross-type, домен masking C). На реальном договоре — 1 группа. Кандидат: обрывать name-run на смене падежа/типа или барьером PER-спанов (PER считается тем же движком). | НЕЗНАЧИТЕЛЬНО (тип неверный, утечки нет) | отдельный этап (граница name-run) |
| Eprime-A | `src/ner_detector.py` / `src/syntax_compound.py` (NER-слой Natasha, numpy/OpenBLAS) | **Недетерминизм детекции на большом реальном документе (~2500 сегментов) — НЕ подтверждён на синтетике.** Наблюдение этапа E′: 3 прогона В ОДНОМ процессе на реальном договоре дали разные наборы сущностей (239/226/226 масок), подпись «первый прогон отличается, следующие два совпадают» (не случайный разброс). Этап E″ (2026-07-23) построил синтетический двойник (`tests/fixtures/synthetic_corporate_large.docx`, тот же класс конструкций: раздел определений, роли-коллективы, реквизитные таблицы, ФИО в косвенном падеже) на ДВУХ масштабах — 2641 и 5051 сегментов, 10 прогонов в одном процессе + 3 отдельных процесса каждый раз — и изолированный стресс-тест самого Natasha NER-тэггера (до 500K символов, форсированный `OPENBLAS_NUM_THREADS=8`): **ни один прогон не воспроизвёл недетерминизм** (побайтно идентичный вывод во всех случаях). Статический аудит `anchor_registry.py`/`ner_detector.py`/`multispan.py`/`pipeline.py` не нашёл ни глобальных мутируемых контейнеров на уровне модуля, ни `@lru_cache`, ни живущего между вызовами `Registry()`/`PersonBook()` (создаются заново на каждый `AnchorEngine.run`); единственный найденный `id(obj)`-ключ (`suppress_conflicts`, `anchor_registry.py:1356`) строится и потребляется в пределах одного вызова функции — объекты не успевают собраться GC, коллизия id() невозможна. Отвергнутые гипотезы: объём документа, плотность таблиц/адресов/определений, протекающий реестр, недетерминизм самой NER-модели на CPU. Рабочая гипотеза (не проверена на реальном документе намеренно — `real_docs/` не хранится в проекте по решению владельца): непрогретый пул потоков BLAS (numpy собран с OpenBLAS, `DYNAMIC_ARCH`, до 24 потоков) — холодный первый вызов может использовать другое число потоков/порядок редукции, чем прогретые последующие, и на порогах уверенности NER-модели это способно перекинуть решение по сущностям около границы. Защитная мера внесена (не «на всякий случай», а по классу дефекта): `src/ner_detector.py`/`src/syntax_compound.py` фиксируют `OMP_NUM_THREADS`/`OPENBLAS_NUM_THREADS`/`MKL_NUM_THREADS`/`NUMEXPR_NUM_THREADS=1` ДО импорта `natasha` (через `os.environ.setdefault`, не перебивает явную настройку владельца) и прогревают модели фиктивным текстом при импорте модуля — «холодный первый вызов» происходит один раз при старте процесса, до первого реального документа. Автономный скрипт для владельца — `experiments/stage_eprime_determinism/owner_repro.py` (гоняет реальный файл N раз, печатает только числа масок по типам и sha256, без текста документа; флаг `--pin-threads` для сравнения). Тест-страж — `tests/test_determinism.py` (не может поймать сам дефект без репро-фикстуры, но ловит РЕГРЕССИЮ: если в детекцию просочится глобальное состояние, тест покраснеет). **ОБНОВЛЕНИЕ 2026-07-24 (реальные данные владельца, гипотеза «холодный BLAS» ОПРОВЕРГНУТА):** владелец прогнал `owner_repro.py` на реальном документе — counts `[237, 224, 226, 224, 233, 224]`, 6 разных sha256 = **случайный разброс, НЕ «первый ≠ остальные»** (значит холодный старт/прогрев ни при чём — прогрев проблему не решает). КЛЮЧЕВАЯ УЛИКА — КАКИЕ типы плавают: СТАБИЛЬНЫ BIK/EMAIL/OGRN/PASSPORT (чистый regex + КС, Natasha не касается); ПЛАВАЮТ ADDRESS 27-37, ORG 69-71, PERSON 67-69, слегка KPP/SUM. Это ровно Natasha-зависимые слои: ADDRESS (natasha LOC/AddrExtractor) — большой размах, ORG/PER ±2 (немного appos-склеек `merge_compound_entities` через NewsSyntaxParser то срабатывают, то нет). Анализ кода: (а) ADDRESS-спаны УЖЕ порядко-канонизированы (`_build_address_spans` делает `sorted({(s,e)…})` до всего) — значит размах ADDRESS = РАЗНЫЙ НАБОР LOC-спанов Natasha, НЕ порядок; (б) `_resolve_overlaps` — max-fold по тотальному порядку `_winner` (порядко-независим); (в) структурный слой (pymorphy+regex) детерминирован во всех тестах. Вывод: причина — недетерминизм ИНФЕРЕНСА Natasha (numpy) на сборке/машине владельца, дающий разные НАБОРЫ спанов, а НЕ порядок обхода. thread-pinning в `src/` был активен в прогоне владельца (owner_repro импортирует `src/`), но разброс сохранился — значит либо пиннинг не «долетает» до numpy в их процессе (numpy импортирован раньше `src/ner_detector`), либо однопоточный numpy у них всё равно нестабилен (особенность сборки). Диагностика уточнена: `owner_repro.py` переписан — фиксирует потоки ПЕРВОЙ строкой (до ЛЮБОГО импорта) и печатает ПОСЛОЙНЫЙ детерминизм (L1 regex / L2 structural / L3 ner-ADDRESS / L4 compound). Один прогон на машине владельца локализует слой: L3/L4 плавают → Natasha (лечится гарантированно однопоточным numpy, `--threads`); L1/L2 плавают → чистый Python (тогда план меняется на детерминированный порядок в разрешении спанов — но текущий анализ этого НЕ предсказывает). НЕ внесено спекулятивной переупорядочки в замороженный пайплайн: доказательств, что причина в порядке, нет, а риск для sha256-корпуса реален. См. [`archive/reports/HANDOFF_STAGE_EPRIME_DETERMINISM.md`](archive/reports/HANDOFF_STAGE_EPRIME_DETERMINISM.md). | ВАЖНО (воспроизводится у владельца на реальном документе; причина сужена до инференса Natasha, не порядка; окончательная локализация — за послойным `owner_repro.py`) | thread-pinning (потоки=1) внесён; послойная диагностика готова; окончательный фикс зависит от того, какой слой плавает у владельца |

Прочие открытые ограничения охвата (не «находки», а нереализованные детекторы/зоны) —
см. [`STATE.md`](STATE.md) §3: регистровая дыра (2b), непрочитанные зоны .docx (этап 1,
fail-closed), PDF-извлечение, компонент 1 (веб).
