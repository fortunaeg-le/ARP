# Этап BUILD — пересборка поставки (2026-07-29)

Ничего в программе не менялось, кроме метки версии сборки. Собрано из чистого
дерева, проверено запуском собранного приложения.

## Шаг 0. Факты

**а) Чем собирается.** PyInstaller по спеке
[`packaging/shifrator.spec`](../packaging/shifrator.spec) (команда — в её шапке,
строка 10):

```
venv/Scripts/pyinstaller.exe packaging/shifrator.spec --distpath dist --workpath build
```

Режим `--onedir`. Результат — `dist/SHIFRATOR/` (`SHIFRATOR.exe` + `_internal/`),
рабочие файлы сборщика — `build/shifrator/`.

**б) Метка версии.** [`app/core.py:68`](../app/core.py) — константа `BUILD_MARK`.
Оттуда она расходится в: футер интерфейса (`server.py:231`, подстановка
`{{BUILD_MARK}}` в `index.html`), `/api/ping` (`server.py:201`), окно лаунчера
(`launcher.py:129`), файл `launcher.lock` (`launcher.py:90`), поле `build_mark`
КАЖДОЙ записи разметки (`core.py:680/701/735`) и в отчёт — как `tool_build` и
как ключи разбивки `by_build` (`report.py:574`). Формат ограничен сторожом
отчёта `report._BUILD_RE` = `^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$`
([`report.py:220`](../app/report.py)); слаг сборки разрешён только в
`report.tool_build`, `report.findings[].build` и ключах `report.by_build`
(`report.py:628–632`, починка U4-FIX). Неверный формат не роняет отчёт, а
схлопывает значение в `unknown` — сборки перестали бы различаться.

**в) Что попадает внутрь.** Точка входа `app/launcher.py`; код `src/` и `app/`
(через `pathex` + явный `hiddenimports`); данные: `entity_types.yaml`,
`app/index.html`, `collect_data_files("natasha")`,
`collect_data_files("pymorphy2_dicts_ru")`, `copy_metadata("pymorphy2_dicts_ru")`.
Корпус, `gold.json`, `tests/`, `docs/`, `bench/`, `experiments/`, история git в
спеке не упомянуты — фактическая проверка поставки на шаге 5.

Спека сама роняет сборку в трёх случаях: окружение разошлось с
`packaging/requirements-build.lock.txt` (17 критичных пакетов); файл данных из
спеки исчез с диска; модуль `src/`/`app/` не перечислен в `hiddenimports`. В
этой сборке все три проверки прошли (вывод: «окружение совпадает с lock (17
пакетов), python 3.11.9», «файлы данных на месте (21 шт.)», «hiddenimports
покрывает все модули src/ и app/ (27 имён)»).

**г) Веса моделей.** Включены явно и лежат в поставке:

```
dist/SHIFRATOR/_internal/natasha/data/emb/navec_news_v1_1B_250K_300d_100q.tar
dist/SHIFRATOR/_internal/natasha/data/model/slovnet_morph_news_v1.tar
dist/SHIFRATOR/_internal/natasha/data/model/slovnet_ner_news_v1.tar
dist/SHIFRATOR/_internal/natasha/data/model/slovnet_syntax_news_v1.tar
```

Плюс словарь `pymorphy2_dicts_ru` и его dist-info метаданные (без них pymorphy2
не находит словарь языка). Скачивать нечего: Natasha берёт веса из своего
пакета внутри поставки — собранное приложение отработало все проверки шага 4 на
этих файлах. Отсутствие сетевых обращений подтверждается составом поставки, а
не перехватом трафика.

## Шаг 1. Перед сборкой

* Рабочее дерево приведено в чистое: два незакоммиченных файла документации
  (`ANCHOR_REGISTRY.md`, `TECH_AUDIT.md`) закоммичены отдельно (`f190dfd`),
  кода не касаются.
* `venv/Scripts/python.exe -m pytest -q` — **1217 passed, 3 skipped, 3
  deselected, 10 xfailed** за 734 с. Тесты не ослаблялись.
* `venv/Scripts/python.exe tests/corpus/gate.py` — **ЗЕЛЁНЫЙ**, все линии
  `X -> X` (precision / leak_v2 / masking A / masking B), креши 0, FP не выросли,
  MANIFEST OK, известный долг ADDRESS 44 -> 44.
* **Хеш коммита сборки: `e92e8a632b6e7fd675f0451a4c5625bf60892dd7`** (`e92e8a6`,
  ветка `main`, дерево чистое).

## Шаг 2. Метка версии

`u1b-rebuild-20260725` -> **`build-20260729`** (коммит `e92e8a6`).

Проверено против сторожа отчёта до сборки: `_BUILD_RE` матчит, `_norm_build`
возвращает саму метку (а не `unknown`), `_build_slug_allowed` разрешает её в
`report.tool_build` и запрещает в чужом поле. Целевые тесты после правки:
`test_u4_report.py`, `test_u1_packaging.py`, `test_type_policy_ui.py` — 80
passed.

## Шаг 3. Сборка

| | |
|---|---|
| время | **33 с** (`real 0m33.293s`) |
| размер | **128 МБ** (131 019 944 байта), 1108 файлов |
| предупреждения | отдельных предупреждений сборщика нет; `build/shifrator/warn-shifrator.txt` (282 строки) — обычный список «модуль не найден» для необязательных зависимостей чужих пакетов (`jinja2`/`pyparsing`/`railroad` из `pkg_resources`, `html5lib` из `lxml`, POSIX-only `resource`/`termios`, атрибуты `multiprocessing`). Ни одного пропущенного модуля `src/`/`app/`. |

Старая сборка не удалена — сохранена как `dist/SHIFRATOR_prev_u1b/`.

## Шаг 4. Проверка запуском собранного приложения

Запускался именно `dist/SHIFRATOR/SHIFRATOR.exe`, общение — живым HTTP.
Скрипт: [`experiments/stage_build/exe_live_check.py`](../experiments/stage_build/exe_live_check.py),
вывод: `experiments/stage_build/exe_live_check.log`. Домашняя директория
подменена на временную — реальный `~/.shifrator` не тронут; принадлежность
процесса проверена по `settings_path` внутри временной директории.

| # | Проверка | Результат |
|---|---|---|
| 1 | Приложение запускается, интерфейс открывается | OK. Порт 8934; `/api/ping` -> `build-20260729`, та же метка в `launcher.lock` и в HTML страницы (65 682 байта) |
| 2 | Обезличивание документа корпуса | OK. `agency_0003.txt`, набор по умолчанию: 18 масок (EMAIL 2, ADDRESS 4, BIRTHDATE 1, INN_PERSON 1, PASSPORT 3, SNILS 1, PHONE 4, PERSON 2), сессия выдана |
| 3 | Экран выбора набора | OK. 4 набора (`personal`, `personal_requisites`, `with_money`, `maximum`), 15 галочек типов, среди них **оба** ИНН: `INN` «ИНН организации (10 цифр)», `INN_PERSON` «ИНН человека (12 цифр)» |
| 4 | Переключение набора меняет результат | OK. «Только персональные данные»: ИНН организации `9177276266` ОСТАЛСЯ, ИНН человека `864067395705` замаскирован. Галочка «ИНН организации» -> 19 масок, оба замаскированы. Снятая галочка «ИНН человека» -> 17 масок, оба остались. «Максимум» -> 28 масок |
| 5 | Отчёт | OK. `status: ok`, `tool_build: build-20260729`, `by_build: ['build-20260729']`, состав включённых типов: набор `personal`, `['PERSON','ADDRESS','INN_PERSON','PASSPORT','SNILS','BIRTHDATE','PHONE','EMAIL']`. В тексте отчёта: «Сборка инструмента: `build-20260729`». Сторож не сработал |
| 6 | Восстановление текста из токенов | OK. 3608 символов, нерешённых токенов 0, токенов-масок в тексте не осталось, 16 из 18 значений вернулись дословно |
| 7 | Документ с непрочитанными зонами | OK. `agency_0002.docx` -> `status: refused` + перечень зон: вложенная таблица (`word/document.xml`, 86 символов), верхний колонтитул (`word/header1.xml`, 51 символ) |

Про пункт 6. Побайтного равенства с исходным документом нет и не ожидается:
восстановление возвращает ФИО в КАНОНИЧЕСКОЙ форме (именительный падеж,
раскрытые инициалы) — «Виноградова Павла Юрьевича» -> «Виноградов Павел
Юрьевич». Это канон восстановления этапа E, а не свойство сборки: проверено
отдельно — тот же результат даёт прогон на ИСХОДНИКАХ. Содержательный критерий
(нет нерешённых токенов, ни одной маски не осталось в тексте) выполнен.

## Шаг 5. Что попало внутрь лишнего

**Ничего лишнего не найдено, исключать нечего.** Поиск по `dist/SHIFRATOR`:

* файлы корпуса, `gold.json`, `results_*.json`, `baseline*.json`,
  `known_leaks*` — **нет**;
* каталог `tests/`, `docs/`, `bench/`, `experiments/` — **нет**;
* отчёты (`.md`) наши — **нет**; найденные `.md` — только тексты лицензий внутри
  `numpy-2.4.6.dist-info/licenses/`;
* `.docx` — ровно один, `_internal/docx/templates/default.docx`, шаблон
  библиотеки python-docx (разрешён постановкой); `.xlsx`/`.pptx` — нет;
* история git (`.git*`, `*.pack`, `ORIG_HEAD`) — **нет**;
* каталог `_internal/docx/` — это библиотека python-docx, не наши документы.

## Шаг 6. Лишние процессы

На машине владельца на момент этапа заняты порты **8765, 8791, 8828** — чужие
давно запущенные копии (`netstat`: PID 6664, 2276, 12084). Они не трогались.
Проверка шага 4 велась **на порту 8934**; порт брался не наугад: он прочитан из
`launcher.lock`, который написал НАШ процесс, и дополнительно сверено, что
`/api/settings` отдаёт путь настроек внутри нашей временной домашней директории,
а `/api/ping` — метку `build-20260729`. То есть разговор с чужой сборкой
исключён структурно, а не надеждой на свободный порт.
