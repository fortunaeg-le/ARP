# SNAPSHOT — разведка S0, 2026-08-04

Разовый снимок состояния репозитория SHIFRATOR после переноса чата тех.директора.
Ничего не чинилось и не улучшалось. Каждое утверждение — со ссылкой `файл:строка`
или путём к артефакту прогона. Модель: Sonnet 5.

---

## 1. Репозиторий

**Ветка:** `main`, синхронизирована с `origin/main` (`git status`).
**git status на старте сессии:** чисто, нечего коммитить.
**git status после обоих прогонов корпуса (шаги 8–9):** чисто, нечего коммитить —
дамп `tests/corpus/results_gate_current.json`, который перезаписал прогон гейта,
оказался байт-в-байт идентичен уже закоммиченной версии (полный прогон дал 0
регрессов, см. §9), поэтому `git status --short` пуст.

**Последние 25 коммитов** (hash, дата, заголовок):

```
ee46735 2026-08-04 T2: в отчёт — результат полного набора тестов (1334 passed, падений нет)
fcc0b9d 2026-08-04 T2: движение линии «д» через promote_baseline + отчёт этапа
d12dc3f 2026-07-31 Почти готов эта т
d1bdbf0 2026-07-31 T2: тесты PERCENT/TERM + починка сравнения приоритета арбитража
666d6ca 2026-07-31 T2: детекторы PERCENT и TERM — шаблонный слой, арбитраж, наборы, измерительные списки
c66eeef 2026-07-31 T2: замерная дорожка корпуса v2 (шаг 1) + точка отсчёта по SUM
f8fc944 2026-07-30 T4: отчёт этапа
30989b2 2026-07-30 T4: зеркало подавления в замере + седьмая линия гейта "ж"
d8180c5 2026-07-30 T4: тесты отрицательных классов — детекция, барьер, разворот, зеркало
1290afe 2026-07-30 T4: отрицательные классы CLAUSE_REF/ROLE_TERM/COLLECTIVE — детекция и барьер
4d89b83 2026-07-30 docs: история нейросетевых компонентов (Natasha/GLiNER) — только чтение
db13cc0 2026-07-30 T-ARB: отчёт этапа
4efb531 2026-07-30 T-ARB: инструмент поимённого сравнения масок корпуса + закрытие записи долга
11fcc0c 2026-07-30 T-ARB: тесты контракта арбитража — полнота, классы пересечений, красное состояние
b713d91 2026-07-30 T-ARB: арбитраж по силе якоря PER↔ADDRESS + страж контракта приоритетов
2f61b89 2026-07-30 U5b догонка: починка теста отставного гейта D — не окружение, а пропущенный env
4b2039e 2026-07-30 U5b: контракт четырёх случаев на экране проверки + починка границ
c6e3eda 2026-07-30 U5a: диагностика правки границ в интерфейсе — контракт данных
30b2871 2026-07-30 GATE-2 догонка: планку линии «д» молча не поднять
37e34e8 2026-07-30 GATE-2 догонка: отставной гейт этапа D больше не может выстрелить
f2d26ad 2026-07-30 GATE-2: отчёт этапа
47d9783 2026-07-30 GATE-2: пересборка точки отсчёта — новое поле границ, поведение системы не менялось
2c048f0 2026-07-29 GATE-2: само-тесты новых линий и защита эталона от возврата сумм в негативы
d251724 2026-07-29 GATE-2: три линии в гейт — порча документа, границы по направлению, точность всех типов
09a0375 2026-07-29 GATE-2: измеритель границ по направлению ошибки — общий код для всех типов
```

(`git log -25 --format="%h %ad %s" --date=short`.)

**Незакоммиченное/неотслеживаемое:** нет (см. выше).

**Python:** 3.11.9 (`python --version`; также `pyproject.toml:6` `requires-python = ">=3.11"`).
Два venv в дереве — `venv/` и `.venv/` (оба untracked, `.gitignore:1-2`); фактически
используется `venv/Scripts/python.exe` (та же версия 3.11.9), это же предписывает
`bench/README.md:5-7` и docstring `gate.py:67`.

**Ключевые зависимости** (перечислены из файлов проекта, не устанавливались):
- `requirements.txt` (верхнеуровневые, без части пинов): `python-docx`, `PyYAML`,
  `natasha==1.6.0`, `pymorphy2==0.9.1`, `yargy==0.16.0`, `cryptography`, `lxml`, `wheel`.
- `pyproject.toml:9-16` — тот же список без `wheel`, плюс src-layout конфигурация
  сборки (`pyproject.toml:19-35`, 13 плоских py-modules).
- `packaging/requirements-build.lock.txt` (56 строк) — точный слепок окружения
  сборки exe, включая транзитивные пакеты; сверяется на сборке
  (`packaging/requirements-build.lock.txt:1-16`). Значимые пины: `numpy==2.4.6`,
  `navec==0.10.0`, `slovnet==0.6.0`, `python-docx==1.2.0`, `python-pptx==1.0.2`,
  `cryptography==49.0.0`, `pyinstaller==6.21.0`, `pytest==9.1.1`.

---

## 2. Карта репозитория

Счётчики — трекнутые git-файлы (`git ls-files | ... | uniq -c`).

| Каталог | Трекнуто файлов | Назначение (по фактическому содержимому) |
|---|---|---|
| `src/` | 20 | Библиотека детекции/маскировки/восстановления — плоские модули (contract зафиксирован в `pyproject.toml:22-35`): `models`, `extractor`, `regex_detector`, `ner_detector`, `syntax_compound`, `tokenizer`, `session_store`, `detokenizer`, `ooxml_core`, `docx_rewriter`, `pptx_rewriter`, `xlsx_rewriter`, `file_detokenizer` + `anchor_registry.py`, `multispan.py`, `normalizer.py`, `pipeline.py`, `storage.py`, `type_policy.py`. |
| `tests/` | 1053 | pytest-набор + `tests/corpus/` (корпус v1, гейт), `tests/corpus_v2/` (корпус v2), `tests/component2/`, `tests/fixtures/`, `tests/golden_addresses.py`. |
| `experiments/` | 160 | По-этапные разведочные скрипты/логи, один подкаталог на этап (`stage_a_prime`, `stage_addr_b`, `stage_b`, `stage_build`, `stage_c`, `stage_c_prime`, `stage_d`, `stage_e`, `stage_e_prime`, `stage_eprime_determinism`, `stage_gate2`, `stage_o2`, `stage_s3`, `stage_t1`, `stage_t2_inn`, `stage_tarb`, `a0_gliner`) — трекнуто, кроме `a0_gliner/venv_gliner/`, `a0_gliner/hf_cache/`, части `stage_o2/*` (`.gitignore:40-41,68-70`). |
| `docs/` | 101 | 32 markdown-отчёта верхнего уровня + `archive/` (историчные отчёты/находки) + `known_leaks_stage_c.json` (реестр долга ADDRESS). |
| `bench/` | 10 | Легаси-харнесс проверки регресса детекции АДРЕСА («волна 2»); **не связан** с регресс-гейтом (`bench/README.md:1-31`; подтверждено разведкой §4 — нет ссылок на `gate_config`/`overmask_ledger`/`results_baseline` внутри `bench/*.py`). |
| `app/` | 10 | Десктопный пилот-интерфейс: stdlib `http.server`-бэкенд (`core.py`, `server.py`, `report.py`, `selfcheck.py`, `procutil.py`, `paths.py`) + один `index.html`, `launcher.py`/`start.bat`, только `127.0.0.1`. |
| `packaging/` | 2 | `.spec` для PyInstaller + `requirements-build.lock.txt`. |
| корень | 8 отдельных файлов | `shifrator.py` (CLI-точка входа, добавляет `src/` в `sys.path`, `shifrator.py:9-10`), `entity_types.yaml` (конфиг типов/детекторов/приоритетов, 29005 байт), `conftest.py` (тот же трюк с `sys.path` для тестов), `pytest.ini` (`addopts = -m "not slow"`), `pyproject.toml`, `requirements.txt`, `README.md`/`README_USER.md`/`docs/archive/TECH_AUDIT.md`/`ANCHOR_REGISTRY.md`. |

**Untracked, но присутствуют на диске** (все — в `.gitignore`): `build/`
(промежуточная сборка PyInstaller, `build/shifrator/`), `dist/` (готовые артефакты:
`SHIFRATOR/`, `SHIFRATOR.zip`, `SHIFRATOR_prev_u1b/`), `scratch/` (разрозненные
дебаг-дампы прошлых сессий), `test_run/` (ручные smoke-прогоны конвейера),
`venv/`, `.venv/`, `__pycache__/`.

---

## 3. Типы: приоритет/арбитраж, эталон, детектор

Реестр типов — `entity_types.yaml` (`entity_types:` с `entity_types.yaml:9`).
Порядок арбитража при пересечении спанов — **отдельно**, в `src/tokenizer.py`
(`assert_priority_contract`, `src/tokenizer.py:96-130`), YAML порядка не задаёт:

- `_REGEX_PRIORITY` (`src/tokenizer.py:56-60`): `BANK_ACCOUNT, OGRN, INN, INN_PERSON, BIK, SNILS, KPP, PASSPORT, PHONE, SUM, EMAIL, BIRTHDATE, DATE, PERCENT, TERM`
- `_NER_PRIORITY` (`src/tokenizer.py:69`): `ADDRESS, ORG, PERSON`

**21 объявленный тип**, 18 из них маскируемые (несут `token_prefix`,
`src/type_policy.py:123-136`) и входят в списки арбитража выше (15+3), 3 —
отрицательные классы без `token_prefix` (работают только барьером).

| Тип | `entity_types.yaml` | Эталон v1 (`tests/corpus/gold.json`) | Эталон v2 (`tests/corpus_v2/gold_v2.json`) | Детектор |
|---|---|---|---|---|
| PERSON | 17-20 | Y, как `PER` | Y, как `PER` | Y — `src/anchor_registry.py:1147` `PerAnchorDetector` |
| ORG | 27-30 | Y | Y | Y — `src/anchor_registry.py:728` `OrgAnchorDetector` |
| ADDRESS | 31-34 | Y | Y | Y — `src/ner_detector.py:1274` (`natasha.AddrExtractor`) |
| INN | 64-69 | Y | Y | Y — `src/regex_detector.py:219-265`, КС `inn10_checksum` |
| INN_PERSON | 76-81 | Y, как `INN_PER` | **N** — 0 записей | Y — КС `inn12_checksum` (`regex_detector.py:66-69`) |
| OGRN | 82-87 | Y | Y | Y — КС `ogrn_checksum` (`regex_detector.py:42-51`) |
| KPP | 102-105 | Y | Y | Y — без КС (по конструкции) |
| BANK_ACCOUNT | 106-110 | Y, как `ACCOUNT` | Y, как `ACCOUNT` | Y — якорный, без КС |
| BIK | 111-114 | Y | Y | Y |
| PASSPORT | 115-133 | Y | Y | Y — 2 паттерна |
| SNILS | 142-146 | Y | Y | Y |
| BIRTHDATE | 153-163 | Y | Y | Y — 2 паттерна |
| PHONE | 164-167 | Y | Y | Y |
| EMAIL | 168-171 | Y | Y | Y |
| SUM | 172-181 | Y | Y, как `MONEY` (алиас `measure_v2.py:69`) | Y |
| PERCENT | 210-216 | **N** — эталона в v1 нет вовсе (`measure_lib.py:1012-1020`) | Y | Y |
| TERM | 245-248 | **N** — та же причина | Y | Y |
| DATE | 250-254, `enabled: false` на 254 | N | N | Код есть, детекция выключена конфигом (`regex_detector.py:160-161`) |
| CLAUSE_REF (негатив) | 277-282 | N (0 записей типа) | N | Y — `regex_detector.py`, `suppress_masking: true` — детектится, но не маскируется |
| ROLE_TERM (негатив) | 300-302 | N | N | Y — `src/anchor_registry.py:1049` `detect_negative_classes` |
| COLLECTIVE (негатив) | 308-310 | N | N | Y — тот же `detect_negative_classes` |

**Отрицательные классы CLAUSE_REF / ROLE_TERM / COLLECTIVE — провизорный статус.**
Формализованы этапом T4 (2026-07-30, `docs/archive/reports/T4_REPORT.md:1`). Более раннее
планирование (`docs/archive/specs/ENTITY_SPEC.md`, дата составления 2026-07-26,
статус документа «МАТЕРИАЛ ДЛЯ РЕШЕНИЙ», `ENTITY_SPEC.md:3,7`) называло их
«де-факто 3, не оформлены» (`ENTITY_SPEC.md:189`) — это описание **более раннего**,
уже сменившегося состояния. Актуальный (пост-T4) провизорный остаток:
ROLE_TERM/COLLECTIVE «невидимы для метрик по конструкции» — 0 эталонных записей
не баг, а дизайн (`docs/archive/reports/T4_REPORT.md:85-89`); счётчик их подавлений не доведён до
пользовательского отчёта U4 (`docs/archive/reports/T4_REPORT.md:155-163`, явно «Не сделано»).
Также в `ENTITY_SPEC.md` обсуждается 4-й негативный класс `POSITION` —
он **нигде не реализован**, только предложение (`ENTITY_SPEC.md:80,173,189`).

**Наборы экрана выбора типов** — определены в `src/type_policy.py` (НЕ в `app/`;
`app/core.py:187-189` явно говорит, что экран не хранит своего состава наборов):

| id | подпись | состав | строки |
|---|---|---|---|
| `personal` | «Только персональные данные» | `PERSON, ADDRESS, PHONE, EMAIL, PASSPORT, SNILS, BIRTHDATE, INN_PERSON` | `type_policy.py:47-50` |
| `personal_requisites` | «Персональные данные и реквизиты» | PERSONAL + `INN, OGRN, KPP, BIK, BANK_ACCOUNT, ORG` | `type_policy.py:55-57` |
| `with_money` | «Всё, включая деньги» | PERSONAL_REQUISITES + `SUM, PERCENT, TERM` | `type_policy.py:67` |
| `maximum` | «Максимум (все типы)» | сентинел `None` = все 18 типов с `token_prefix`, включая `DATE` (детектор которого выключен) | `type_policy.py:74` |

**Дефолт:** `DEFAULT_PROFILE = PROFILE_PERSONAL` (`type_policy.py:118`). Логика
выбора в `ensure_settings()` (`type_policy.py:270-296`): существующий
`~/.shifrator/settings.json` не трогается (:287-289); при первом запуске без
следов прежней работы — пишется `personal` (:279-280,291); при апгрейде БЕЗ
файла настроек, но со следами прежней работы (`has_previous_work`,
`type_policy.py:252-267`) — форсируется `maximum`, чтобы сохранить прежнее
(неотфильтрованное) поведение (:275-278,291).

Отдельно, не путать: `app/core.py:169-172` `MARKUP_TYPES` — независимый
14-элементный список для выпадающего списка ручной разметки «отметить
пропущенное» на экране проверки, к профилям масштабирования не относится.

---

## 4. Гейт

Живёт в `tests/corpus/gate.py` (конфиг допусков — `tests/corpus/gate_config.py`,
поддержка — `measure_lib.py`, `run_measurement.py`, `overmask_ledger.py`).
`bench/*` — НЕ гейт (см. §2). Есть отставной предшественник
`experiments/stage_d/gate_d.py`, самоотказывается при запуске (`RETIRED_REFUSAL`,
exit 2), задокументирован как отставной в `docs/archive/reports/GATE2_REPORT.md:187-202,305-330`.

**7 линий** (буква/что мерит/где определена/планка/допуск):

| Линия | Что мерит | Определена | Планка (константа) | Допуск |
|---|---|---|---|---|
| **а** precision | Точность по эталону КАЖДОГО типа (FP = маска на объявленном негативе, TP = маска на gold своего типа); GATE-2 расширила с NER-хребта на все типы | `compare_precision()` — `gate.py:293-315` | — | `PRECISION_TOLERANCE_PP = 0.0` — `gate_config.py:47` |
| **б** leak_v2 | Рост частичной утечки, по каждому из 17 типов (14 маскируемых-с-эталоном + доп.) и по агрегату BIK-excl, оба порога (>=6, >=8) | `compare()` — `gate.py:147-194` | — | `LEAK_V2_TOLERANCE = 0` — `gate_config.py:40` |
| **в** masking A (round-trip) | Обязана быть РОВНО 100%, восстановление байт-в-байт | `compare()` — `gate.py:207-266`, инвариант `gate.py:235-239` | — | `MASKING_A_TOLERANCE_PP = 0.0` — `gate_config.py:53` |
| **г** masking B (границы) | Эталон закрыт масками ЦЕЛИКОМ, по типу и агрегату | `compare()` — `gate.py:207-266` | — | `MASKING_B_TOLERANCE_PP = 0.0` — `gate_config.py:57` |
| (masking C, мягкая) | Тип маски == тип эталона; НЕ роняет гейт, только предупреждение | `evaluate()` — `gate.py:553-563` | — | `MASKING_C_WARN_PP = 0.0` — `gate_config.py:64` |
| **д** over-mask прозы | Маски, не легшие ни на эталон, ни на негатив — порча документа | `compare_overmask()` — `gate.py:321-370` | Уровень — журнал `overmask_ledger.json` (см. ниже) | `OVERMASK_NOTHING_TOLERANCE = 0` — `gate_config.py:99` |
| **е** границы по направлению | Недобор (утечка) и перебор (порча) раздельно, по типу и по символам/сущностям | `compare_boundaries()` — `gate.py:385-433` | — | `BOUNDARY_UNDER_TOLERANCE = 0`, `BOUNDARY_OVER_TOLERANCE = 0` — `gate_config.py:113-114` |
| **ж** зеркало подавления | Число эталонных сущностей, погашенных отрицательным классом — БЕЗУСЛОВНО 0, не дельта к baseline | `compare_suppression()` — `gate.py:465-496` | Жёстко `0` в коде, `gate.py:485` | Нет допуска, абсолютное условие |

Независимые условия (не буквенные): крешей 0 (`gate.py:160-166`); FP на негативах —
`FP_TOLERANCE = 0` (`gate_config.py:33`); `sha256sum -c MANIFEST.sha256` до/после
(`check_manifest()`, `gate.py:105-128`); известный долг ADDRESS —
`MISSED_LEAK_TOLERANCE = 0` (`gate_config.py:71`, мягкий по составу).

Все константы допусков перепроверены прямым чтением `gate_config.py` — совпадают
с указанными строками.

**promote_baseline.py** (`tests/corpus/promote_baseline.py`) — единственный
санкционированный способ двигать `results_baseline.json`/`MANIFEST.sha256`; пишет
журнал `tests/corpus/overmask_ledger.json`. Журнал целиком, **2 записи**:

1. `overmask_ledger.json:4-16` — 2026-07-29, «заведение линии», `total: 330`
   (`ADDRESS: 7, ORG: 322, PER: 1`), обоснование — первый полный прогон GATE-2,
   уровень зафиксирован «как есть».
2. `overmask_ledger.json:17-31` — 2026-08-04, «поднятие уровня», `total: 1721`
   (`ADDRESS: 7, ORG: 322, PER: 1, PERCENT: 1031, TERM: 360`), обоснование —
   этап T2 завёл маскирование PERCENT/TERM; в СТАРОМ (v1) корпусе эталона для
   них нет, поэтому каждая их маска попадает в `nothing` по построению метрики,
   а не из-за ошибки детектора; эталон по этим типам — только в корпусе v2
   (PERCENT recall 96.5%/precision 100%, TERM 95.1%/100%).

**Расхождение внутри самого gate.py** (не доки vs код, а код сам с собой):
docstring `gate.py:13` говорит «ШЕСТЬ ЛИНИЙ», но баннер `main()` (`gate.py:789`)
и финальное сообщение (`gate.py:855-858`) корректно перечисляют семь (линия «ж»
добавлена позже, этапом T4, и не попала в верхний docstring).

`docs/archive/reports/GATE2_REPORT.md` (2026-07-29) описывает «шесть линий»/«15 типов» — это
хронологически верно на момент написания (до линии «ж» этапа T4 и до типов
PERCENT/TERM этапа T2), не живое противоречие, но требует знания хронологии
от читателя.

---

## 5. Корпуса

### v1 — `tests/corpus/`

**Полный набор:** 324 документа = 162 `.txt` + 162 `.docx`
(`tests/corpus/README.md:16`, подтверждено прямым подсчётом файлов в
`tests/corpus/docs/`: 162+162). `gold.json` содержит 324 записи (подсчитано
`json.load` + `len()`). `MANIFEST.sha256` — 659 строк (324 `_model/*.json` +
324 `docs/*` + 11 прочих трекнутых файлов корпуса) — **659 это число строк
манифеста хешей, НЕ число документов**.

**Быстрый/итерационный набор:** `tests/corpus/subsample.py`, читает
ЗАФИКСИРОВАННЫЙ список `tests/corpus/subset_iter.json`. **Расхождение**: docstring
`subsample.py:26` утверждает «25 документов» — устарело; фактически
`subset_iter.json:3` поле `"n_docs": 33`, и массив `doc_ids` содержит ровно
33 записи (подсчитано). История роста задокументирована в
`tests/corpus/subset_lib.py:41-64`: потолок 25→28 (сессия CORPUS-V2, исправление
признака разрыва форматированием) →31 (этап T-GOLD-A, новый тип SUM) →33 (этап
T2-INN, разделение ИНН на INN/INN_PER). Ожидание задания «~31» — тоже устаревшее,
отражает состояние ДО этапа T2-INN. **Факт — 33**, подтверждено и запуском (см. §8).

**`corpus_anon_sha`** — `experiments/stage_eprime_determinism/corpus_anon_sha.py:69-84`.
Хеширует НЕ файлы корпуса как есть: обрабатывает только `.docx` (строка 70,
`.txt` вне хеша — 162 из 324 документов не участвуют, подтверждено также
`docs/archive/reports/T0V_REPORT.md:653`), для каждого гоняет полный конвейер
`extract→run_detection→tokenize` и хеширует РЕЗУЛЬТАТ анонимизации (замаскированный
текст, строки 72-75), затем агрегирует `sha256(json.dumps({имя_файла: хеш}, sort_keys=True))`
(строки 82-84) — то есть хеш зависит от вывода пайплайна, а не от сырого
содержимого корпуса, и не видит `gold.json`/структуру масок/типы.

### v2 — `tests/corpus_v2/`

**Фактическое число документов:** 138 = 54 `.txt` + 84 `.docx` (подсчитано;
совпадает с `tests/corpus_v2/README.md:72`). Манифест — `tests/corpus_v2/MANIFEST.sha256`
(285 строк = 138+138+9). Эталон — `tests/corpus_v2/gold_v2.json`, генератор —
`generate.py`, манифест строит `make_manifest.py`.

**Сторожа**, включая TRANCHE:
1. Баннер README: `tests/corpus_v2/README.md:11-29` — TRANCHE объявлен
   неизмеримым («37 вхождений, 5 форм, ни одного числа»), «детектор траншей
   не писать».
2. `validate.py:155-175` — считает `tranche_digits` (эталонные записи TRANCHE
   с цифрой в тексте); >0 — жёсткая ошибка валидации. **`validate.py` не
   `test_*.py`, pytest его НЕ подбирает автоматически** — только ручной запуск.
3. `values.py:115-127,357,852-871` — реестр TRANCHE как 1-осевого типа с явной
   пометкой «оборот речи без числового значения».
4. `tests/test_corpus_v2_axis_coverage.py:106-107,185-196` — **это уже
   автособираемый pytest**, параметризован по всем типам `values.AXES`,
   включая TRANCHE; падает, если единственная ось TRANCHE обнулится.
5. `tests/test_corpus_v2_axis_coverage.py:199-221` — TRANCHE (как
   1-осевой тип) явно skip-ается в проверке «формы = комбинации осей».
6. Прямой grep `TRANCHE` по `src/` — 0 совпадений: детектор действительно не
   написан, инструкция сторожей соблюдена.

**Стена между детекторами и `values.py`/генератором v2:**
- Прямая проверка импортов `src/regex_detector.py`, `src/ner_detector.py`,
  `src/anchor_registry.py`, `src/syntax_compound.py`, `src/type_policy.py` —
  ни один не импортирует `values`/`values.py`/что-либо из `tests/corpus_v2/`.
  Все вхождения подстроки «values» в этих файлах — вызовы `dict.values()`.
- Импортёры `values.py` по всему репо — только внутри `tests/corpus_v2/` и
  `tests/test_corpus_v2_*.py` (`generate.py:40`, `validate.py:23`,
  `test_corpus_v2_value_tricks.py:45`, `test_corpus_v2_structure_groups.py:33`,
  `test_corpus_v2_axis_coverage.py:64`). Ни одного совпадения в `src/`.
- **Автоматической проверки-стража этой стены (AST-скан импортов/import-graph
  тест) НЕ найдено.** Есть только нарративная гарантия в отчётах и docstring'ах:
  `docs/archive/reports/T2_REPORT.md:168-170` («Стена соблюдена: … не открывались»),
  `tests/test_t2_percent_term.py:7-8`. Стена держится сегодня фактом (проверено
  чтением: импортов нет) и процессной дисциплиной, не исполняемым тестом.

---

## 6. FINDINGS

Путь: `docs/FINDINGS.md` (54 строки, одна markdown-таблица). Шапка утверждает
«только то, что ещё не закрыто» (`docs/FINDINGS.md:3-7`), но фактически 6 из 35
строк таблицы имеют колонку «тяжесть» = буквально `ЗАКРЫТО`
(`D-PRECISION-90`:14, `0c-H`:24, `Stage4-A`:27, `Stage2b-B`:32, `PER-B`:33, и
одна из трёх строк с id `Eprime-A`:49) — расхождение с заявленной политикой
файла, см. §«Расхождения». Эти 6 исключены из списка открытых ниже.

**Открытые находки (29), заголовок + суть одной строкой:**

| id | суть |
|---|---|
| TGOLDA-SUM-CASE | Все промахи/утечки SUM — на adversarial-формах; паттерн SUM без `(?i)` не матчит верхний регистр валюты |
| U4-G-TIMEOUT | Вложенный pytest-прогон внутри `test_g_regression.py` теперь 488с против жёсткого timeout=300 — воспроизведено на простаивающей машине |
| Stage3-A | BIRTHDATE недобор на 1 символ в 4 случаях гомоглифа первой цифры даты |
| ADDRB-BIRTHDATE-EXPOSED | Дата рождения с омоглифами вместо цифр не детектируется и ничем не прикрыта с этапа ADDR-B |
| ADDRB-PRECISION-FRAG | Метрика precision наказывает за склейку рваных ADDRESS-спанов (меньше масок → ниже precision без новых FP) |
| ADDRB-NL-HOLE | Адрес, разорванный `\n` и закрытый двумя масками, считается «короче эталона» — артефакт измерения |
| 0c-B | PASSPORT recall_exact 0% — детектор задевает паспорт, но не покрывает спан целиком |
| 0c-C/0c-D | ADDRESS/NER precision — в основном закрыто этапом C, остаток 14 ложных на адверсариальных мутациях |
| Stage-C-A | Recall ADDRESS на адверсариальных мутациях — в основном закрыто E′, остаток переклассифицирован в Eprime-B |
| Stage-C-B | 7 случаев: гомоглиф в номере паспорта рвёт guard, «Паспорт NN» читается как улица+дом |
| 1-A | Харнесс смешивает «отказано» (fail-closed) и «упало» (crashed) |
| 1-B | `w:sdt` (content control) на верхнем уровне тела не читается экстрактором и не детектируется сканером зон |
| Stage4-B | Телефон рядом с гомоглиф-мутированным ИНН с невалидной КС может утечь после того, как барьер ADDRESS перестал его случайно накрывать |
| Stage4-C | КС ОГРНИП (mod 13, 1/13) может случайно совпасть на произвольной 15-значной подстроке (напр. КБК) |
| Stage2b-A | Асимметричный `_ALPHA_FOLD` (`'B'→'В'` есть, `'b'→'В'` нет) ломает ФИО смешанного алфавита под регистровой мутацией |
| ~~PER-B(hist)~~ | Историческая заметка: Natasha рвала косвенный падеж ФИО на 2 сущности, ложно роняя masking B без реальной утечки |
| B-MASKSHIFT-ORG | ORG без структурного якоря («Петров Консалтинг») потерял случайную маскировку Natasha-PER после её выключения |
| B-ADDR-HOMONYM | Улица-однофамилец под адверсариальной мутацией метится PER (9 остаточных случаев), PER — барьер для ADDRESS |
| Aprime-1 | ORG без кавычек/формы/ИНН, похожий на ФИО грамматически, ловится Natasha как PERSON |
| Aprime-2 | Чисто латинский гомоглиф org-формы (без кириллицы в токене) не сводится `_ALPHA_FOLD` |
| Aprime-3 | Асимметрия `\n`→` ` между маскированным выводом и эталонным plain-текстом внутри ячейки таблицы (артефакт измерения) |
| Eprime-A (огромные документы) | Недетерминизм детекции на ~2500-сегментных документах (Natasha NER на большом блобе); корпус сам детерминирован |
| Eprime-B | Остаток долга ADDRESS (реестр `docs/known_leaks_stage_c.json`, фактически 43 записи на сейчас, см. §9) — регистровый класс, в основном |
| Stage-Cprime-A | Реквизитная ветвь ORG иногда захватывает только усечённый хвост многословного названия |
| Stage-E-A | Цепочка ≥2 цифрогрупп с ведущей 7/8 может читаться как PHONE вместо ИНН/паспорта; направление безопасное |
| Stage-E-B | decrypt-file подставляет форму ПЕРВОГО вхождения, не канон, во все вхождения файла |
| Stage-Cprime-B | Реквизитная ветвь ORG втягивает соседнее ФИО (в родительном) в ключ ORG — тип неверный, утечки нет |
| Eprime-A (реальный документ) | Недетерминизм на реальном документе владельца (6 разных sha256), не воспроизведён на синтетике; причина сужена до инференса Natasha/numpy, не порядка |
| E2-CORPUS-POVERTY | Синтетический корпус структурно беднее реальных документов (2 стиля/док vs 1097 у реального) — целый класс дефектов (кеши по стилям, `w:sdt`) корпусом не покрывается принципиально |

**Закрытые находки:** 24, посчитано по строкам таблиц в
`docs/archive/FINDINGS_closed.md` (раунд 2 «грязных сценариев» #1-12 — 15 строк,
замер этапа 0c — 4 строки, этап 1 непрочитанные зоны — 1 строка, поправка
Stage2-N/M — 3 строки, отдельная запись T1-A — 1). Это число **не включает**
6 строк, помеченных `ЗАКРЫТО`, но оставшихся в живом `FINDINGS.md` (гигиена
данных, см. §«Расхождения»).

---

## 7. docs/

32 markdown-файла верхнего уровня + `archive/` (историчные) + `known_leaks_stage_c.json`.
Полный список с датами изменения (новые сверху) — см. вывод `Get-ChildItem`,
воспроизведён в сессии; три самых свежих:

1. **`docs/archive/reports/T2_REPORT.md`** (2026-08-04 13:00:48) — отчёт этапа T2
   (money/percent/term), коммиты `c66eeef`/`666d6ca`/`d1bdbf0`
   (`T2_REPORT.md:3-5`). Таблица разбивки `gold_v2.json` по типам, включая
   TRANCHE = 37 вхождений «не трогаем» (`T2_REPORT.md:17-38`); описывает два
   новых regex-детектора PERCENT/TERM и подтверждает соблюдение «стены»
   (`T2_REPORT.md:168-170`).
2. **`docs/STATE.md`** (mtime 2026-08-04 12:47:10; последний коммит,
   касающийся файла — `fcc0b9d`, 2026-08-04, `git log -- docs/STATE.md`) —
   «живой» слепок состояния кода (`STATE.md:1`). Фиксирует, что `pytest -q`
   на HEAD `abf3ee8` НЕ зелёный: «1 failed, 976 passed, 3 deselected,
   10 xfailed» (`STATE.md:21-23`), падение — предсуществующее
   `U4-G-TIMEOUT`.
3. **`docs/archive/reports/T4_REPORT.md`** (2026-07-30 22:34:16) — отчёт этапа T4
   (отрицательные классы), `T4_REPORT.md:1`. Три механизма подавления
   (M1/M1b/M2) внутри `OrgAnchorDetector.detect()` формализованы в явные
   Entity-эмиссии (`T4_REPORT.md:11-35`).

---

## 8. Прогон быстрого набора

**Команда:** `venv/Scripts/python.exe tests/corpus/subsample.py`.
**Время:** 54.68 с (`time`, real 0m54.683s).
**Состав:** 33/324 документа (см. §5 — расхождение с ожиданием «~31»
подтверждено фактом запуска, вывод скрипта: «Подвыборка: 33/324 документа»).
Крешей: 0/33.

Важная оговорка: `subsample.py` — это НЕ `gate.py`. Он сравнивает срез с СОБСТВЕННЫМ
замороженным `tests/corpus/results_iter_baseline.json` через тот же код
`gate.evaluate()`, но docstring явно предупреждает: «НЕ заменяет
tests/corpus/gate.py» и печатает regressions на срезе как «находку, а не
приёмку» (`subsample.py:187-198`). Полный вывод сохранён в артефакте прогона
(путь ниже), сюда — числа по всем линиям:

- recall/leak_v2/FP по типам — по срезу все дельты нулевые, кроме улучшений
  ADDRESS (leak_v2>=6 15→14, >=8 15→14) и агрегата (287→286, 262→261) — доли
  улучшения на срезе, обычная случайность выборки.
- precision — все типы совпали срез-к-срезу (100% там где было 100%, KPP
  89.47%→89.47%, OGRN 97.30%→97.30%).
- masking A — 100.00%→100.00%. masking B — 90.94%→91.03% (улучшение).
  masking C — 94.66%→94.75% (мягкая).
- **«Регрессы на срезе» (3), НЕ гейт-регресс**: over-mask прозы
  `PERCENT` 0→110, `TERM` 0→34, ВСЕГО 34→178. Причина — `results_iter_baseline.json`
  (собственная точка отсчёта подвыборки) снята ДО этапа T2 (PERCENT/TERM ещё не
  существовали), не переснята после; это разрыв в обслуживании
  вспомогательного baseline'а, не регресс продукта (полный гейт §9 — зелёный,
  та же точка сравнения `results_baseline.json` УЖЕ учитывает PERCENT/TERM
  через журнал `overmask_ledger.json`).

Прогон не упал (exit 0) — вопрос владельцу не требуется.
Артефакт: `.../scratchpad/quick_run.txt` (временный, не коммитится).

---

## 9. Прогон полного корпуса v1 (ЕДИНСТВЕННЫЙ, ПОСЛЕДНИЙ)

**Команда:** `venv/Scripts/python.exe tests/corpus/gate.py`.
**Время:** 9м16.7с (`time`, real 9m16.747s).
**Состав:** 324/324 документа (подтверждено выводом: «Прогон измерения: 324
документов», индикатор дошёл до `[320/324]`). Крешей: 0/324.
**MANIFEST.sha256:** OK до прогона (659 файлов) и OK после.

**Результат — гейт ЗЕЛЁНЫЙ (exit 0), все 7 линий, числа против точки
отсчёта `results_baseline.json`:**

| Линия | Итог |
|---|---|
| а precision | Все 15 измеримых типов побитно совпали с baseline (PER 99.96%, ORG 100%, ADDRESS 99.92%, INN 99.29%, OGRN 98.69%, KPP 97.00%, остальные 100% или n/a для PERCENT/TERM — precision не определён, масок-решений нет в v1) |
| б leak_v2 | По всем 17 типам и по агрегату BIK-excl (17272%→17.2% на >=8) — дельта 0 везде |
| в masking A | 100.00% → 100.00% (инвариант держится) |
| г masking B | 91.60% → 91.60% (агрегат), по типам — 0 дельт |
| д over-mask прозы | 1721 → 1721 (ВСЕГО), по типам (PER 1, ORG 322, ADDRESS 7, PERCENT 1031, TERM 360) — 0 дельт; уровень сверен с журналом `overmask_ledger.json`, совпадает |
| е границы по направлению | Недобор/перебор по сущностям и символам — 0 дельт по каждому из 15 типов и по агрегату (660 недобор шт / 4557 недобор симв / 330 перебор шт / 2709 перебор симв — все «→» те же числа) |
| ж зеркало подавления | 0 (эталонных сущностей, погашенных отрицательным классом) |
| FP на негативах | 16 → 16 |
| Известный долг ADDRESS | **43 → 43** (реестр `docs/known_leaks_stage_c.json` документирует 43; независимо подсчитано `len(leaks)` в этом файле = 43) |

Итоговая строка вывода: «Гейт ЗЕЛЁНЫЙ: регрессов не найдено по всем семи линиям…».

Артефакт: `.../scratchpad/full_run.txt` (временный, не коммитится; также
`tests/corpus/results_gate_current.json`, отслеживаемый файл, перезаписан
байт-в-байт идентично прежнему committed-состоянию — `git status` после
прогона пуст).

---

## Расхождения

1. **Быстрый набор — 33, не 25 и не 31.** `subsample.py:26` (docstring) говорит
   «25 документов», `subset_lib.py:41-64` документирует рост потолка
   25→28→31→33 (последний шаг — этап T2-INN), актуальный `subset_iter.json`
   и фактический прогон дают 33. Ожидание задания «~31» — снимок состояния
   ДО этапа T2-INN; сам docstring `subsample.py` не обновлён вовсе ни разу
   после 25.
2. **`gate.py:13` docstring** говорит «ШЕСТЬ ЛИНИЙ», хотя код и вывод (и
   баннер `main()`, `gate.py:789`) реализуют семь — верхний docstring не
   обновлён при добавлении линии «ж» этапом T4.
3. **`docs/archive/reports/GATE2_REPORT.md`** (2026-07-29) описывает «шесть линий»/«15 типов» —
   хронологически верно на момент написания (до линии «ж» и типов
   PERCENT/TERM), но вводит в заблуждение читателя без даты в голове.
4. **`docs/FINDINGS.md` нарушает собственную заявленную политику** («только
   не закрытое», `FINDINGS.md:3-7`): 6 строк несут `тяжесть = ЗАКРЫТО`
   (`D-PRECISION-90`, `0c-H`, `Stage4-A`, `Stage2b-B`, `PER-B`, одна из трёх
   `Eprime-A`) — гигиена данных, не перенесены в архив.
5. **id `Eprime-A` использован трижды** в `docs/FINDINGS.md` (строки 41, 47,
   49) для трёх разных находок (2 открытых, 1 закрытая) — ломает
   уникальность id для перекрёстных ссылок.
6. **`docs/STATE.md` — дата актуализации отстаёт от истории коммитов.**
   Заголовок файла (`STATE.md:3-4`) заявляет «Составлен/актуализирован:
   2026-07-19», но `git log -- docs/STATE.md` показывает содержательное
   изменение файла 2026-08-04 (коммит `fcc0b9d`, «T2: движение линии «д»…»)
   и ещё два коммита между этими датами (2026-07-29 ×2) — шапка не
   обновлялась при этих правках, хотя контент менялся (⚠-абзац про
   `U4-G-TIMEOUT` присутствует).
7. **Реестр долга ADDRESS — 43, а не 57.** `docs/FINDINGS.md:42` (Eprime-B)
   называет «остаток 57» после сборки E′; фактический
   `docs/known_leaks_stage_c.json` сейчас содержит 43 записи (подсчитано
   `len(leaks)`), и полный прогон гейта (§9) независимо подтверждает
   текущий долг = 43 (`base_count`/`cur_count` совпали, регресса нет — то
   есть 43 это ЗАФИКСИРОВАННОЕ на текущем baseline число, а не разовая
   аномалия прогона). Ни один найденный отчёт не документирует явно шаг
   57→43 — вероятный кандидат источника изменения — этап T2-INN (переразметка
   ИНН), но это предположение, не факт со ссылкой; **требует уточнения у
   владельца**, в отчёт внесено как найденное несоответствие, не разрешено.
8. **Вспомогательный baseline подвыборки не обслуживается.** `results_iter_baseline.json`
   не переснят после появления типов PERCENT/TERM (этап T2), из-за чего
   `subsample.py` показывает 3 «регресса на срезе», которых на полном
   гейте нет (см. §8). Инструмент для пересборки существует
   (`tests/corpus/run_iter_baseline.py`, упомянут в `subsample.py:159-161`),
   но не был запущен с даты этапа T2 (2026-08-04, судя по датам файлов).

---

## Итог

**Что блокирует T3:** явных блокеров в FINDINGS/STATE, направленных
конкретно на «этап T3», не найдено — ни один открытый пункт §6 не назван T3
по имени. Ближайшие незакрытые технические долги, которые логично лежат на
пути расширения детекции (в порядке серьёзности по колонке «тяжесть»):
`1-B` (`w:sdt` не читается экстрактором, ВАЖНО, `docs/FINDINGS.md:26`),
`Stage2b-A` (регистровая дыра ФИО на combo-мутациях, ВАЖНО, `:31`),
`Aprime-1`/`B-MASKSHIFT-ORG` (ORG без якоря, ВАЖНО, `:35,37`),
`E2-CORPUS-POVERTY` (структурная бедность корпуса — ограничивает, что вообще
может показать T3, ВАЖНО, `:50`).

**Что блокирует REG:** несобранный вспомогательный `results_iter_baseline.json`
(расхождение 8) даёт ложные «регрессы» на любом быстром прогоне после этапа
T2, пока кто-то не перезапустит `run_iter_baseline.py` — это шум для REG,
не блокер по существу, но стоит починить перед тем, как полагаться на
`subsample.py` для быстрой проверки во время REG.

**Что висит на владельце:** расхождение 7 (43 vs 57 в реестре долга ADDRESS —
нужно подтверждение, какой этап и почему изменил число, реестр либо отчёт
устарел) и незавершённый `Eprime-A` (реальный документ, `docs/FINDINGS.md:47`) —
недетерминизм воспроизведён только на машине владельца, локализация
остановлена на «нужен послойный прогон `owner_repro.py` у владельца».
