# HANDOFF — этап 0c (снятие baseline)

**Коммит:** `8330dd2` · **Дата:** 2026-07-14 · **pytest:** `533 passed, 2 deselected, 1 xfailed`
**Манифест корпуса:** `c50798cb…3beaae` (OK) · **Тип сессии:** ЗАМЕР (src/ и метрики не тронуты)

Полный отчёт: [`BASELINE.md`](BASELINE.md). Наблюдения: [`FINDINGS.md`](FINDINGS.md) §0c.

## Что сделано
- 324/324 документа обработаны, **0 крешей** (критерий приёмки выполнен).
- Изолированное хранилище (`tempfile.mkdtemp`), `~/.shifrator` не тронут.
- Снимок `tests/corpus/results_baseline.json` (= `results_8330dd2.json`) — точка отсчёта регресс-гейта 0d.
- В драйвер добавлено поле исхода `outcome: processed|refused|crashed` (refused=0, задел под 1b).
- `results_04452df.json` НЕ перезатёрт (COMMIT в харнессе переставлен на `8330dd2`).

## Главные цифры (BIK исключён из leak, все обработанные)
| метрика | значение |
|---|---:|
| recall_loose | 56.3% |
| recall_exact | 39.2% |
| leak_v1 | 49.7% |
| leak_v2 ≥6 (мягкий) | 45.9% |
| leak_v2 ≥8 (строгий) | 40.9% |
| base·canonical recall | **87.3%** (≈ прежние 87.2% — база здорова) |
| adversarial leak_v1 | **68.8%** (весь провал — из состязательного слоя) |

## Разведение эффектов (BIK-incl, сопоставимо со старым отчётом)
- **Починка креша (0a):** leak_v1 `55.3% (324, креши=утечка) → 49.3%` = **−6.0 пп**. 31 ранее-крешивший док теперь защищается. Сравнивать надо с 55.3%, НЕ с 50.0% (по 293).
- **Новая метрика (0b):** `leak_v1 49.3% → leak_v2 45.8%`. Нетто **−419** сущностей, и это уточнение, не противоречие: v2 **ловит +333** частичные утечки (PER, ACCOUNT, EMAIL…), но **снимает −752** (447 адресов-одиночек + 228 BIRTHDATE, к которым v2 неприменима). «+354» из 0b — это был первый эффект (дополнения), воспроизведён как **+333**; это НЕ нетто.

## Что удивило
1. **PHONE: строгий порог почти обнуляет утечку** (≥8 → 4.2%, ≥6 → 42.4%). Не защита — формат `+7 (XXX) XXX-XX-XX` скобками режет номер на run-ы ≤7 цифр. Порог 8 выше естественной длины run-а телефона. (FINDINGS 0c-E.)
2. **ADDRESS v2 срезал 447 «утечек» v1** — одиночный номер дома/квартиры. Оценка v2 честнее; примеры в BASELINE §9.
3. **PASSPORT recall_exact = 0%** при loose 26% — детектор всегда мимо границ спана.
4. **FP на неразмеченной прозе — 5299** (жадные ADDRESS/ORG/PER ловят «3.2.», «4.2.» как адрес).
5. **doc_leaked v1 = 323/324** — почти каждый документ утекает хоть чем-то; чистых документов практически нет.

## Куда смотреть на следующем этапе (0d / фикс)
- **Блокер покрытия:** нет детекторов SNILS и BIRTHDATE (recall 0%, leak 94–100%). Первый кандидат. (FINDINGS 0c-A.)
- **Спан-контракт PASSPORT** (0c-B) и **over-capture ADDRESS** (0c-C) — источники крупной утечки при «наличии» recall.
- **Precision NER** (0c-D): 5299 ложных срабатываний ест доверие к системе.
- Состязательный слой `combo` (омоглиф+невидимый+регистр): recall ~12%, leak ~86% — самый пробиваемый приём.
- **Регресс-гейт 0d:** сверять новые прогоны против `results_baseline.json`. Метрику вести по v2 для числовых/буквенных, помня что ADDRESS v2-строже, а BIRTHDATE v2 не покрывает.

## Границы соблюдены
`sha256sum -c MANIFEST.sha256` → OK · `git diff src/ measure_lib.py` → пусто · метрика не менялась.
