# FINDINGS — закрытые / исторические находки

Перенесено из живого `docs/FINDINGS.md` при реорганизации доков (2026-07-18). ID сохранены
для перекрёстных ссылок. Полный текст с репро, тестами, разбором — исходный документ
[`reports/FINDINGS.md`](reports/FINDINGS.md) (не редактировался при переносе, историчен).
Открытые находки — [`../FINDINGS.md`](../FINDINGS.md).

## Раунд 2: находки в «грязных» сценариях эксплуатации (#1–12)

Все воспроизводятся тестами `tests/test_adversarial_*.py`. Статусы: `FIXED` — было
дефектом, исправлено; `PASS (pins)` — фактическое поведение зафиксировано тестом как есть
(не дефект, но не идеально); `PASS (спека OK)` / `PASS (docs)` — подтверждение, что защита
работает или ограничение задокументировано.

| id | блок | суть | статус |
|---|---|---|---|
| 1 | 5 session_store | Гонка `_load_or_create_key` (TOCTOU) при двух конкурентных `encrypt` могла испортить `key.bin` | FIXED — атомарное создание ключа (`os.link`) |
| 2 | 5 session_store | Повреждённый `key.bin` → неперехваченный `ValueError` из `Fernet()` вместо `SessionNotFoundError` | FIXED |
| 3 | 5 session_store | Повреждённый `key.bin` → `purge_expired` падал, `decrypt` рушился целиком | FIXED |
| 4 | 1 extractor | `.txt`, переименованный в `.docx` → неперехваченный `PackageNotFoundError` | FIXED |
| 5 | 1 extractor | Обрезанный `.docx` → та же ошибка | FIXED (тот же фикс, что #4) |
| 6 | 1 extractor | Пустой `.docx` (0 байт) → та же ошибка | FIXED (тот же фикс, что #4) |
| 7 | 1 extractor | `.txt` в UTF-16 → тихий откат на `cp1251`, моджибейк, утечка ПДн | FIXED — детекция BOM UTF-16 до отката |
| 8 | 5 session_store | `key.bin` удалён между save/load → `load_session` молча пересоздаёт ключ, другие сессии становятся нечитаемы | PASS (pins) — поведение сохранено намеренно |
| 9 | 7 CLI | Битый `.docx` печатал сырой traceback вместо `Ошибка: …` | FIXED (тем же фиксом, что #4) |
| 10 | 7 CLI | Истёкшая сессия → «не найдена» вместо «истекла» (`purge_expired` удалял `.enc` до `load_session`) | FIXED — `purge_expired(exclude_session_id=...)` |
| 11 | 5 session_store | `ttl_hours=0` → сессия истекает мгновенно (`>=` включительно) | PASS (pins) |
| 12 | 4 tokenizer | `Entity.end > len(segment.text)` → тихая обрезка текста, без исключения | PASS (docs) — предусловие не проверяется по спеке |
| — | 6 detokenizer | Токен, разорванный переносом строки, тихо теряется (известное ограничение блока 6) | PASS (спека OK) |
| — | 4 tokenizer | Цепочки пересечений 4–5 звеньев разрешаются детерминированно | PASS (спека OK) |
| — | 5 session_store | `session_id` защищён от path traversal / инъекций | PASS (спека OK) |

## Замер этапа 0c (baseline `8330dd2`) — закрытые/методологические пункты

| id | наблюдение | статус |
|---|---|---|
| 0c-E | leak_v2 порог ≥8 для PHONE занижен форматом `+7 (XXX)…` (скобки режут run на ≤7 цифр) | методологическая заметка, метрику не трогали |
| 0c-F | leak_v2 не применима к BIRTHDATE (метрика рапортовала «none» на всех 228) | ЗАКРЫТО этапом 0b-fix (см. 0c-G) |
| 0c-G | BIRTHDATE молча выпадал из знаменателя leak_v2 (два дефекта: тип вне `V2_NUMERIC_TYPES`, и даже внутри — точки в дате рвут числовое ядро) | **FIXED** — ветка `leak_v2_birthdate` + `v2_date_field`, 228/228 → `full` |
| 0c-I | leak_v1 даёт ложные утечки на фрагментах адреса (гомоглиф-замены рвут топоним на куски, случайно совпадающие с посторонними словами) | ЗАФИКСИРОВАНО — v1 историческая метрика, не правится |

Аудит 447 адресов-одиночек (только номер дома выжил в 438/447 случаях — v2 права, номер
сам по себе не идентифицирует) — полный разбор в `reports/FINDINGS.md`.

## Этап 1: непрочитанные зоны .docx — закрытые пункты

| id | наблюдение | статус |
|---|---|---|
| 1-C | `test_g_regression` таймаут (300с) один раз сработал ложно из-за параллельной нагрузки CPU, не из-за размера набора | ЗАКРЫТО — не дефект, запас ×2.6 на свободной машине |

## Поправка после этапа 2: ложно закрытые xfail-контракты (Stage2-N/M)

| id | наблюдение | статус |
|---|---|---|
| Stage2-N | Перевод `TestStage3SnilsHasNoDetector`/`TestStage3PassportSubdivisionHasNoDetector` в `strict=False` после этапа 2 был **ложным закрытием**: нормализация инцидентно маскирует утечку (SNILS через KPP-overmatch, код подразделения через ADDRESS/ORG-overmatch), детектора по-прежнему нет (см. открытый `0c-A`) | ИСПРАВЛЕНО — оба контракта возвращены в `xfail(strict=True)` на нехрупких фикстурах |
| Stage2-M | Подтверждение (не дефект): recall_exact на срезе base·canonical не изменился этапом 2 (76.2%→76.2%) — карта индексов не сдвигает спаны на неискажённых сущностях, рост recall пришёл из `mut:*`-документов | ЗАФИКСИРОВАНО (не дефект) |
| 0c-A | **SNILS и BIRTHDATE не детектируются вовсе** (recall 0%, утечка 100%); код подразделения паспорта — 305 пропусков внутри PASSPORT | ЗАКРЫТО этапом 3 (коммит ниже): якорные детекторы SNILS/BIRTHDATE + второй паттерн PASSPORT. recall 0→73.7% / 0→74.1% / 30.8→69.6%. Три xfail-контракта погашены. См. `reports/HANDOFF_STAGE_3.md` |


## T1-A — сторож отчёта пропускал латиницу (ЗАКРЫТА этапом U4-FIX, 2026-07-29)

Исходная формулировка (из живого `FINDINGS.md`):

| T1-A | `app/report.py`, `_string_ok`/`_BUILD_RE` | **Структурный часовой отчёта пропускает любую ЛАТИНСКУЮ строку.** Ветка `_BUILD_RE` (`^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$`) заведена под слаг сборки, но применяется к КАЖДОЙ строке отчёта — и к ключам, и к значениям. Следствие: поле, не внесённое ни в один белый список, проходит часового, если названо латиницей (проверено этапом T1: `assert_structural({"totally_new_field": 1})` не поднимает `ReportLeakError`); и, что важнее, латинское ЗНАЧЕНИЕ вида `dogovor.docx` — имя файла — тоже прошло бы. Кириллический текст документа часовой ловит, латинский нет. Пред-существующая (этап U4), этапом T1 НЕ внесена — вскрыта требованием «показать красное состояние сторожа». | СРЕДНЯЯ (последний рубеж против выноса текста наружу работает только на кириллице; первые два рубежа — белый список полей и нормализация в коды — на месте и не затронуты) | не запланировано; кандидат — применять `_BUILD_RE` ТОЛЬКО в позициях `tool_build`/`build`, а не ко всем строкам. Зеркало-маркер: `tests/test_type_policy.py::test_report_guard_should_also_catch_a_latin_field_outside_the_whitelist` (`xfail(strict)`) |

**Как закрыта.** `app/report._string_ok` получил параметр `path` (путь обхода в
`_walk`), а ветка слага сборки привязана к МЕСТУ в схеме — `_build_slug_allowed`:
значение `report.tool_build`, значение `report.findings[N].build`, ключи словаря
`report.by_build`. Везде остальном свободная латиница отвергается наравне с
кириллицей. Белые списки полей НЕ расширялись; реальный отчёт по документу корпуса
собирается байт-в-байт тем же.

**Уточнение к исходной формулировке:** адрес электронной почты как значение
отсекался и ДО починки — символа `@` нет в классе `_BUILD_RE`. Протекали
имя файла, домен, название организации латиницей, фамилия транслитом и любое
новое поле с латинским именем.

**Тесты:** `tests/test_u4_report.py::test_guard_rejects_marker_everywhere`
(7 видов маркера × 4 места), `::test_guard_still_lets_the_build_slug_through_where_it_belongs`,
`tests/test_type_policy.py::test_report_guard_should_also_catch_a_latin_field_outside_the_whitelist`
(метка `xfail(strict)` СНЯТА).

---

# Перенесено из живого FINDINGS.md этапом DOC (2026-08-04)

Десять записей. Текст находок перенесён дословно; изменён только формат
(подраздел вместо строки таблицы — строки содержали переносы внутри ячеек и
ломали таблицу). Обоснование каждого переноса — в разделе «Снято» файла
[`../FINDINGS.md`](../FINDINGS.md).

## D-PRECISION-90 — метрика precision (историческая «90%») — ЗАКРЫТО

**«precision ≈90%» была ФИКЦИЕЙ.** В проекте до этапа D НЕ было честной метрики
precision; за неё принимался `masking_correctness C = 90.81%`. Но
masking_correctness B/C считаются ТОЛЬКО по маскам, ЛЁГШИМ НА GOLD
(`measure_lib.mc_check_bc`: маска не на gold → `scored=False` → в знаменатель НЕ
идёт), то есть СТРУКТУРНО СЛЕПЫ к over-маскированию: маска на объявленном
негативе или на неаннотированной прозе в masking C не существует вовсе. На HEAD
этапа B это скрывало **634 ADDRESS-маски на негативах + 2610 ADDRESS-масок на
прозе**. (Второе историческое число — сырой precision **46.3%** — наоборот
ЗАНИЖЕН: считал SUM-на-негативах, банки-cross-type и прозу-on-nothing одним
мешком FP.) **Честная дефиниция (этап D, `measure_lib.precision_by_type`)**:
FP = kept-маска на ОБЪЯВЛЕННОМ негативе, TP = kept-маска на gold СВОЕГО типа,
precision_T = TP/(TP+FP_neg). По ней: ORG 99.77%, PER 99.96%, ADDRESS
66.86% (HEAD b-per) → 99.92% (C). Зеркала — `tests/test_precision_metric.py`.
**Закрыто:** этап D.

## 0c-H — метрика leak_v2 (ADDRESS) — ЗАКРЫТО

v2-метрика сравнивала буквенный токен ЦЕЛИКОМ и пропускала утечку **усечённого**
топонима. Починено 2026-07-19: сравнение переведено на границы слов, добавлена
префиксная ветка (усечённый топоним ≥6 букв И ≥75% длины токена считается
выжившим). Зеркала — `tests/test_leak_v2.py::test_addr_metric_*`.
**Закрыто:** только `measure_lib.py`, `src/` не тронут.

## Stage4-A — метрика leak_v2 (KPP/ACCOUNT/BIK) — ЗАКРЫТО

`leak_v2_numeric` искал окно ядра по ВСЕМУ цифровому полю документа, поэтому
подстрока открытого КБК засчитывалась утечкой замаскированной ЦЕЛИКОМ сущности.
Починено 2026-07-19: окно засчитывается, только если run, в котором оно найдено,
сам является остатком значения (ядро целиком ИЛИ покрытие run-а ≥
`RUN_COVERAGE_MIN` = 0.8; порог — середина измеренного зазора 73…90%). Ушли 18
ложных утечек: КБК, префикс московских БИК `044525`, общие префиксы счетов
`40802810`/`40702810`/`30101810`. Зеркала — `tests/test_leak_v2.py::test_numeric_*`.
**Закрыто:** только `measure_lib.py`, `src/` не тронут.

## Stage2b-B — `src/ner_detector.py` (жадный yargy) × правка PER — ЗАКРЫТО

Закрыто этапом C (2026-07-22): `_MIN_CAPS_RUN` возвращён 3→2. Механизм воид —
Natasha-PER/ORG выключены (этапы A/B, случайной over-capture-маски нет), адрес
якорный (не хватает заглавные прогоны как топоним). Двусловные заглавные ФИО
(«ПЕТРОВА ОЛЬГА», «ИВАНОВ И.») снова в детекции: PER recall 72.0→74.1%, 0 новых
утечек на чистом тексте. Зеркало — `test_two_word_caps_run_is_normalized_stage_c`.
_(Историческое: порог 3 не давал точной маскировке ФИО обнажать адреса, случайно
накрытые Natasha-ORG/PER; после выключения Natasha накрывать нечем.)_

## PER-B — косвенный падеж ФИО — ЗАКРЫТО

Закрыто этапом B (structure-first PER, 2026-07-22): косвенный падеж собирается в
ОДИН спан структурным `PerAnchorDetector._fio_run` (фамилия+имя+отчество+инициалы
связным прогоном, без опоры на границы Natasha). masking B 81.9→89.8%;
PER exact(found) 77.8→87.6%. Natasha-PER выключена.

### PER-B(hist) — историческое описание того же дефекта

**Косвенный падеж рвётся двумя спанами.** Natasha отдавала ФИО в косвенном падеже
как ДВЕ соседние сущности (`Морозовой` + `Анне Николаевне`), а
`mc_check_bc._covered_by_union` не терпит зазор — ОДИН ПРОБЕЛ между масками ронял
метрику B, хотя значение спрятано ЦЕЛИКОМ и не течёт. Дефект ПРЕД-СУЩЕСТВУЮЩИЙ:
воспроизводился на нетронутом тексте нормального регистра (`works_0009` `l19`,
`sale_0006` `t0_r0_c0`). Этап 2b его не создал, а обнажил. Проверено поэнтитийно:
среди сущностей, потерявших полное покрытие, НИ ОДНА не теряла реальный текст —
открыт был только пробел.

## U4-G-TIMEOUT — тестовая обвязка — ЗАКРЫТО коммитом `e44ac61` (2026-07-26)

**Симптом:** `test_existing_block1_7_suite_still_fully_green` был КРАСНЫМ на HEAD
`abf3ee8`. Тест запускает вложенный `pytest tests --ignore=tests/component2
-m "not slow" -q` с ЖЁСТКИМ `timeout=300`. Вложенный прогон дорос до **488 c**
(936 passed) — бюджет превышен в 1.6 раза, вложенный pytest убивался по таймауту.
Доминировали два ПРЕД-существующих теста:
`test_determinism.py::test_two_runs_byte_identical_fast` **184 c** и
`test_corpus_no_crash.py::test_fast_set_encrypt_never_crashes` **104 c** (вместе
288 c = 59% бюджета). Воспроизводилось на простаивающей машине и в отдельном
worktree на чистом HEAD — то есть падение было пред-существующим, не внесённым
этапом U4.

**Закрытие:** этап O2 оптимизировал конвейер и вернул вложенный прогон к 295 c;
жёсткий лимит поднят 300 → 900 c (тройной запас — переживает вдвое более медленную
машину и рост набора, но остаётся КОНЕЧНЫМ и ловит зависший дочерний pytest).
Докстринг теста говорит об этом прямо: «БЮДЖЕТ ВРЕМЕНИ (этап O2, находка
U4-G-TIMEOUT закрыта)» — `tests/component2/test_g_regression.py:34-55`.

Числа записи («1 failed, 976 passed, 3 deselected, 10 xfailed») относятся к
2026-07-25 и в 2026-08-04 уже не воспроизводятся: полный набор даёт
`1334 passed, 3 skipped, 3 deselected, 10 xfailed`, падений нет.

## EPRIME-A-CAPS — `src/extractor.py` (`_CapsResolver`) — ЗАКРЫТО этапом E″ (2026-07-24)

**Симптом:** один документ в ОДНОМ процессе давал разные наборы сущностей.
Реальный договор — 239/226/226, затем `[237,224,226,224,233,224]` масок,
6 разных sha256. Плавали ADDRESS 27-37, ORG 69-71, PERSON 67-69; стабильны
BIK/EMAIL/OGRN/PASSPORT (regex + КС).

**Механизм:** `_CapsResolver._style_chain` ключевал memo-кеш И защиту от циклов
ЧИСЛОМ `id(style._element)`, НЕ удерживая ссылку на объект. `paragraph.style` /
`run.style` в python-docx — не кешированные свойства (`p.style is p.style` →
**False**, новый прокси на каждое обращение), прокси lxml живёт лишь пока есть
ссылка, а CPython ПЕРЕИСПОЛЬЗУЕТ освободившийся адрес ⇒ ДРУГОЙ стиль получал тот
же `id()` и **ложное попадание** возвращало ЧУЖОЙ вердикт капса; симметрично в
`seen` — **ложный цикл** обрывал обход. Оба зависят от момента сборки мусора.
Качало ОБА слоя, потому что вердикт капса формирует `detection_text` (лежит в
`metadata`, поэтому `segment.text` оставался побайтно стабильным и диагностика
долго считала вход идентичным), а его читают `per_search_view`/`anchor_search_view`
(ORG/PER) и `detection_view` (ADDRESS).

**Числа:** реальный договор — 1097 стилей, **1290 ложных попаданий на 2396
вызовов**, 4 разных `detection_text` на 4 попытки; фикстура
`synthetic_many_styles.docx` (284 стиля) — старая реализация **2660** ложных
попаданий и 4 разных sha, новая — **0 и 1**.

**Фикс:** ключ по САМОМУ объекту (`key = (style._element, attr)`, `seen` —
множество объектов); dict/set удерживают ссылку ⇒ адрес не переиспользуется; lxml
хеширует по идентичности, семантика прежняя. Детекционные правила не тронуты.
Корпус НЕ задет (в корпусных документах по 2 стиля, 0 ложных попаданий даже под
старой реализацией), поэтому замеры прошлых этапов шума не содержат.
Стражи — `tests/test_determinism.py` (структурный: ключ кеша не `int`;
поведенческий: `detection_text` стабилен).

**Опровергнутые по дороге гипотезы (не возвращаться):** холодный BLAS и прогрев,
недетерминизм инференса Natasha/numpy, порядок обхода в разрешении пересечений,
кеш pymorphy.

## EPRIME-A-LARGE — недетерминизм на огромных документах — ПОГЛОЩЕНО `EPRIME-A-CAPS`

Симптом: детекция на документах ~2500 сегментов недетерминирована; реальный
договор давал 239/226/226 сущностей в трёх прогонах ОДНОГО процесса; плавали
PER/ORG/ADDRESS и даже regex-SUM через сдвиг барьеров. Корпус при этом
детерминирован (документы маленькие), структурный round-trip держался в КАЖДОМ
прогоне. Подозрение было на пороговые предсказания нейро-NER на большом блобе
B3-окон × тредовость BLAS — гипотеза опровергнута, настоящая причина в
`EPRIME-A-CAPS`.

## EPRIME-A-REAL — недетерминизм на реальном документе владельца — ПОГЛОЩЕНО `EPRIME-A-CAPS`

Запись фиксировала расследование до нахождения механизма: синтетический двойник
на двух масштабах (2641 и 5051 сегментов, 10 прогонов в процессе + 3 отдельных
процесса) и изолированный стресс-тест NER-тэггера недетерминизм НЕ воспроизвели;
данные владельца (`[237,224,226,224,233,224]`, 6 разных sha256) показали
СЛУЧАЙНЫЙ разброс, а не «первый ≠ остальные», чем опровергли гипотезу холодного
BLAS. Вывод записи — «причина в инференсе Natasha (numpy), дающем разные НАБОРЫ
спанов, а не в порядке обхода» — этапом E″ **опровергнут**: причина оказалась в
кеше `_CapsResolver`. Защитная мера, внесённая по ходу расследования
(`OMP/OPENBLAS/MKL/NUMEXPR_NUM_THREADS=1` через `os.environ.setdefault` до импорта
`natasha` + прогрев моделей), в коде осталась.

Остаточный долг подтверждения на машине владельца вынесен в открытую запись
`EPRIME-A-CONFIRM` — [`../FINDINGS.md`](../FINDINGS.md).

## EPRIME-A-CONFIRM — подтверждение недетерминизма на реальном документе — ЗАКРЫТО этапом DOG (2026-08-05)

Долг был не дефектом, а **непроведённым измерением**: механизм недетерминизма
(`EPRIME-A-CAPS`, кеш `_CapsResolver` по `id()`) этап E″ нашёл, устранил
(`src/extractor.py:216,222` — ключ по объекту) и закрыл стражами
`tests/test_determinism.py`, но end-to-end прогона на реальном договоре владельца
после фикса ни один отчёт не содержал. Прежняя цифра долга — шесть прогонов, шесть
разных контрольных сумм (`[237,224,226,224,233,224]`).

**Измерение этапа DOG: расхождений НЕТ.** Десять прогонов реального договора —
пять подряд в ОДНОМ процессе и пять в РАЗНЫХ процессах — дали побитно совпадающий
результат на всех шести снятых слоях:

| слой | что это | результат |
|---|---|---|
| L1 | текст сегментов после `extract` | `33b6ce7353d8930b` ×10 |
| L2 | вход нейросети (строки, поданные в `Doc()`/`tag_ner`) | `dcfeebb9468ae7e3` ×10 |
| L3 | выход нейросети (все спаны `NewsNERTagger`: тип+границы) | `0f3638863b6d62b6` ×10 |
| L4 | выход адресного слоя (`detect_ner`) | `ec380ecc294048de` ×10 |
| L5 | после арбитража (весь `run_detection`) | `a5406daffde31e0a` ×10 |
| L6 | анонимный текст (та самая «контрольная сумма») | `354ffb28a28ee164` ×10 |

Счётчики тоже неподвижны: 2467 вызовов тэггера, 4081 спан, 1404 сущности,
386 масок, 448859 символов анонимного текста — одинаково во всех десяти прогонах.
Совпал и одиннадцатый, независимый прогон другим скриптом (`ent`/`anon` те же).

**Чем закрылось:** ровно фиксом этапа E″. Слой L2 — тот самый `detection_text`,
который и «плыл» (он лежит в `metadata` и читается `per_search_view` /
`anchor_search_view` / `detection_view`, поэтому `segment.text` оставался
стабильным, а детекция — нет). Сегодня L2 неподвижен, а раз неподвижен вход —
неподвижны и все слои ниже. Ранее выдвигавшиеся гипотезы (инференс Natasha/numpy,
холодный BLAS) подтверждения не получили и здесь: L3 стабилен и внутри процесса,
и между процессами.

Прибор — `experiments/stage_dog/determinism.py` (послойный, снимает L1-L6 обёрткой
над `ner_detector.Doc`). Разбор — [`../JOURNAL.md`](../JOURNAL.md), запись DOG.

## 1-B — `w:sdt` верхнего уровня не читается и не объявляется зоной — ПОГЛОЩЕНО `DOG-NODE-SILENT` (2026-08-05)

Запись была верна, но УЖЕ по существу и без измерения: она называла один вид
узла (`w:sdt` на верхнем уровне тела) и несла оговорку «в корпусе не
встречается, замер не показывает».

Этап DOG проверил класс целиком на порождённой фикстуре
`tests/fixtures/node_kinds.docx` и установил, что видов **пять**, а не один:
`w:ins`, `w:fldSimple`, `w:smartTag`, `w:sdt` (inline) и `w:sdt` (блочный) —
все не читаются И не объявляются зоной, то есть гарантия отказа на них не
срабатывает. Сверх того найден механизм, превращающий тихую потерю в утечку
ПДн (`DOG-ANCHOR-SPLIT`). Долг не закрыт, он расширен и измерен — живёт в
открытых записях `DOG-NODE-SILENT` и `DOG-ANCHOR-SPLIT`
([`../FINDINGS.md`](../FINDINGS.md)). Прежний id `1-B` больше не используется.

## DOG-PASSPORT-GAP — ЗАКРЫТА этапом A1 (2026-08-06)

Найдена этапом DOG (2026-08-05): паспортные данные реального договора
(`C:\shifrator_real\dog.docx`, вне репозитория) уходили открытыми — слово
«паспорт» встречается 34 раза, серия+номер стоит рядом в 30 случаях, детектор
находил **2**. Причины ДВЕ, каждая ломала матч независимо: (1) зазор
якорь→серия — паттерн допускал `[^\n\d]{0,20}`, реальная формулировка давала
21-40 символов; (2) разделитель серия→номер — паттерн допускал только `\s?`
(один пробел), документ ставил между ними `', №'`/`' №'`.

Этап A1 (`entity_types.yaml:132`, тема сессии — только этот паттерн): зазор
`{0,20}` → `{0,40}` (запас сверх измеренного максимума, не безграничный),
разделитель `\s?` → `[ \xa0,]{0,2}№?\.?[ \xa0]{0,2}` (запятая/NBSP/«№» как
опциональные добавки — старое поведение не ломается, каждый элемент
опционален). Старое поведение сохранено байт-в-байт на корпусе v1 (полный
гейт: дельта 0 по всем семи линиям). Результат на целевом
документе: **29 из 30** (было 2), три прогона в одном процессе — sha256
анонимного текста побитно совпал (детерминизм не задет). Оставшийся 1 случай
(зазор >34 символов) сознательно не покрыт — потолок не сделан безграничным.
Эталон корпуса v2 расширен формами разделителя/зазора (`tests/corpus_v2/data.py`,
`generate.py`, флаг `passport` из `UGLY_POOL`, ранее заведённый и не читавшийся
никем). Разбор — [`../JOURNAL.md`](../JOURNAL.md), запись «этап A1» от
2026-08-06.

## A1-PASSPORT-QUALITY-FP — ЗАКРЫТА этапом A2 (2026-08-06)

Найдена этапом A1: фраза «паспорт качества № SS SS NNNNNN» (10 цифр в форме
серии+номера рядом со словом «паспорт», но документ на товар, а не на
человека) детектировалась как `PASSPORT` — 138 из 138 на корпусе v2.
Проверено, что дефект пред-существующий (тот же образец ловился и СТАРЫМ, до
A1, паттерном байт-в-байт) — расширение гэпа/разделителя этапа A1 тут ни при
чём, причина — отсутствие у паспортных данных контрольной суммы и якорь по
слову без семантики.

Этап A2 (`entity_types.yaml:132`) добавил механизм `anti_anchor` (симметричный
`anchor:`, но требующий ОТСУТСТВИЯ дисквалифицирующего слова рядом — новая
функция `regex_detector._has_anti_anchor`, зона `src/regex_detector.py`)
со списком уточнителей практики оформления: «качества», «изделия», «объекта»,
«сделки», «транспортного средства», «технический» (перед якорем — единственный
случай зеркального направления) и другие. Результат: «паспорт качества» и
четыре родственные формы (изделие/ПТС/технический/дept-артикул) — 0 из 138
масок типа `PASSPORT` (было 138/138), при этом настоящие паспорта не задеты —
29 из 30 на целевом документе (без изменений), «паспорт гражданина РФ»
проверен отдельно и детектируется. Часть образцов, лишённых PASSPORT-маски,
всё равно попадает под ДРУГОЙ, пред-существующий и не относящийся к A1/A2
механизм (`src/multispan.py`, «Stage-E-A» — сборка цепочки цифрогрупп с
ведущей 7/8 в PHONE-кандидат) — маскируется как `PHONE` вместо `PASSPORT`;
направление ошибки по-прежнему безопасное (маскируется чем-то, не течёт).
Разбор — [`../JOURNAL.md`](../JOURNAL.md), запись «этап A2» от 2026-08-06.

## A1-RIGID-GAP-SURVEY — ЗАКРЫТА этапом A2 (2026-08-06)

Найдена этапом A1, Часть 4 (беглая разведка, не измерение): три места в
`entity_types.yaml` используют жёсткий `[^\n\d]{0,N}`-гэп между якорем и
значением без обоснования измерением — тот же класс, что был у паспорта до
A1 (`DOG-PASSPORT-GAP`). Код подразделения паспорта (гэп `{0,10}`), СНИЛС
(гэп `{0,20}`), дата рождения (гэп `{0,10}`).

Этап A2 расширил все три по образцу A1: код подразделения `{0,10}→{0,25}`
(плюс попутно починен якорь — усечение «код подр.» было заявлено
комментарием, но не поддерживалось кодом, теперь поддерживается);
СНИЛС `{0,20}→{0,30}` (плюс официальная формулировка «...государственного/
обязательного пенсионного страхования» добавлена В ЯКОРЬ, не в гэп); дата
рождения `{0,10}→{0,35}`. Внутренние разделители значений (дефис/пробел между
триплетами СНИЛС, между частями кода, между числами даты) НЕ расширены на
`\s?` — попытка дала измеренную регрессию (см. `A2-NEWLINE-CROSS` в открытых
находках). Эталон трёх типов расширен в корпусе v2 (стили разделителя/зазора/
регистра якоря, плюс типизированные негативы на каждый тип). Полный гейт:
дельта 0 по всем семи линиям, небольшое УЛУЧШЕНИЕ (не регрессия) от починки
якоря кода подразделения. Реальный документ: код подразделения и СНИЛС в
целевом документе отсутствуют вовсе (0 и до, и после — не дефект, данных нет),
дата рождения — только в неразмеченной прозе (2 упоминания якоря без числа
рядом, тоже 0 и до, и после). Разбор — [`../JOURNAL.md`](../JOURNAL.md),
запись «этап A2» от 2026-08-06.

## A2-NO-NEGATIVES-13-TYPES — ЗАКРЫТА этапом A3 (2026-08-06)

**Исходная находка (этап A2, Часть 5).** 13 из 18 маскируемых типов не имели
НИ ОДНОГО типизированного негатива ни в одном из двух корпусов: корпус v1 —
типизированных негативов нет вовсе ни у одного типа (только общие
нетипизированные «why»-негативы, введённые до появления самого понятия);
корпус v2 — только у `DOCNUM`, `DATE`, `MONEY` и, с этапа A2, `PASSPORT`,
`SNILS`, `BIRTHDATE`. Без единого негатива оставались `PER`, `ORG`, `ADDRESS`,
`INN`, `INN_PERSON`, `OGRN`, `KPP`, `ACCOUNT`, `BIK`, `PHONE`, `EMAIL`,
`PERCENT`, `TERM`. Следствие: переусердствование этих детекторов гейт не
показал бы — линия точности ловит ложное срабатывание только на ОБЪЯВЛЕННОМ
негативе.

**Чем закрыта.** Этап A3 завёл 16 типизированных негативов по 12 из 13 типов
(`tests/corpus_v2/generate.py`, `negatives_a3`), по 13-му (`EMAIL`) вынесено
письменное обоснование, почему честного отрицательного примера не существует —
см. `A3-NEG-EMAIL` в живом [`../FINDINGS.md`](../FINDINGS.md). Заведён второй
вид типизированного негатива — ДВОЙНИК (`lookalike`): в отличие от негатива
CORPUS-V2-B, он не принадлежит виду данных, а лишь похож на него, формы в
реестре `values.py` у него нет по построению, и приборы осей его исключают.

**Что измерение показало сразу (числа — в записях `A3-NEG-*`).** Девять
отрицательных примеров из шестнадцати маскируются 138 из 138, то есть в каждом
документе корпуса. Один из девяти классов подтверждён и на реальном договоре
(`KPP`, 10 из 10).

**Чего эта запись НЕ закрывает.** Гейт по-прежнему переусердствование не мерит:
Часть 4 этапа A3 была предложением с ценой, а не внедрением — открытая запись
`A3-GATE-OVERMASK-LINE`. И охват набора негативов остаётся одним видом случая
на тип — оговорка вынесена в шапку живого `FINDINGS.md`.

## Закрыты этапом TYPE-FACTORY-2 (2026-08-08)

Обе записи перенесены из FINDINGS.md без изменений; закрытие — фабрика типов (сборщик tests/corpus_v2/assemble_types.py + стражи tests/test_type_factory.py).

| id | область | находка | тяжесть | чинится |
|---|---|---|---|---|
| V2-TRANCHE-NOGUARD | запрет детекции `TRANCHE` | **Запрет «детектор траншей не писать» не имеет автособираемого стража, хотя два документа утверждали обратное.** Проверено чтением: единственная исполняемая проверка — `tests/corpus_v2/validate.py:153-175` (появление у `TRANCHE` вхождения с числом = ошибка валидатора), и она запускается ТОЛЬКО руками; в pytest её не зовёт никто (`grep validate` по `tests/*.py` даёт лишь упоминания в докстрингах). `tests/test_corpus_v2_axis_coverage.py:206` — не страж запрета, а страж осей, и `TRANCHE` он из проверки прямо ИСКЛЮЧАЕТ («ось одна, комбинировать нечего»). Прежние редакции `.claude/rules/corpus-frozen.md` и `tests/corpus_v2/CLAUDE.md` называли этот тест сторожем TRANCHE — формулировка исправлена этапом C+ (2026-08-04). Остаётся баннер `README.md:11` и реестр `values.py:115` — оба нарративные. | СРЕДНЯЯ (запрет держится дисциплиной; нарушение обнаружится только чтением кода) | автособираемый тест: `TRANCHE` не должен появляться ни в `_REGEX_PRIORITY`/`_NER_PRIORITY`, ни в `entity_types.yaml` с `token_prefix` |
| V2-WALL-NOGUARD | стена между `src/` и генератором корпуса v2 | **Запрет «детекторы не читают `tests/corpus_v2/values.py`, `generate.py` и эталон» не имеет исполняемого стража.** Проверено прямым чтением импортов: совпадений в `src/` нет, все вхождения подстроки «values» — вызовы `dict.values()`. Но AST-скана/import-graph-теста нет ни одного; стена держится процессной дисциплиной и нарративной гарантией в отчётах. Нарушение будет обнаружено только чтением кода — то есть, возможно, никогда, а цена его — цифры замера, которые перестают что-либо значить. | ВАЖНО (гарантия честности всех замеров новых типов) | тест на импорт-граф `src/` (дешёвый, не требует прогона корпуса) |

## Закрыты этапом FIX-3 (2026-08-08)

Три записи AUDIT: две (`AUDIT-MANIFEST-WHITELIST`, `AUDIT-REALDOC-NOGUARD`) фактически держались замками этапа FIX-2 — проверено ЖИВЫМИ ПРОБАМИ этого этапа (лишний файл в корпусе -> `ФАЙЛ ПОЯВИЛСЯ`; ZIP с `word/document.xml` под именем `notes_2024.bin` -> `ЗАБЛОКИРОВАНО … признак 3`), записи протухли и закрываются. `AUDIT-ARCH-TRANCHE-FALSE` — формулировки `ARCHITECTURE.md` исправлены, сам запрет снят владельцем. `SUMWORDS-DEFERRED` — закрыт второй ветвью SUM.

| id | область | находка | тяжесть | чинится |
|---|---|---|---|---|
| AUDIT-ARCH-TRANCHE-FALSE | `docs/ARCHITECTURE.md:169-171`, `CLAUDE.md` запрет 6 — найдено этапом AUDIT (сплошная сверка) | **Два документа по-прежнему утверждают, что запрет детекции `TRANCHE` охраняется автоматически, хотя автособираемого стража нет.** `ARCHITECTURE.md:169-171`: «на это стоят четыре сторожа, включая автособираемый `tests/test_corpus_v2_axis_coverage.py`»; корневой `CLAUDE.md`, запрет 6: «на него стоят сторожа». Проверено прогоном и чтением: `grep TRANCHE tests/*.py` даёт РОВНО одно попадание — `tests/test_corpus_v2_axis_coverage.py:213`, и это строка, которая `TRANCHE` из проверки ИСКЛЮЧАЕТ («виды данных с единственной осью проверке не подлежат»). Единственная исполняемая проверка запрета — `tests/corpus_v2/validate.py:161-180`, она запускается руками. То есть названный «автособираемым сторожем» тест — тот самый прибор, который охраняемый тип не проверяет; это ЧЕТВЁРТЫЙ случай класса за блок и второе место, где та же формулировка не была исправлена этапом C+ (он поправил `tests/corpus_v2/CLAUDE.md` и `.claude/rules/`, а `ARCHITECTURE.md` и корневой `CLAUDE.md` — нет). Сам долг — `V2-TRANCHE-NOGUARD`; эта запись про НЕВЕРНОЕ УТВЕРЖДЕНИЕ в документах, которое дороже отсутствия стража: читатель уверен, что защита есть. | СРЕДНЯЯ (документ вводит в заблуждение о наличии защиты) | формулировки исправлены этим этапом (`ARCHITECTURE.md`, `CLAUDE.md`); сам страж — долг `V2-TRANCHE-NOGUARD`, детекцию этап AUDIT не трогает |
| AUDIT-MANIFEST-WHITELIST | `tests/corpus/MANIFEST.sha256` × `update_manifest.py` × заявление `tests/corpus/CLAUDE.md:9` | **`sha256sum -c MANIFEST.sha256` не может обнаружить ДОПОЛНЕНИЕ корпуса — а запрет 2 запрещает и его.** Манифест — белый список, и состав его берётся ИЗ САМОГО МАНИФЕСТА (`update_manifest.py:72`, «состав не угадывается» — осознанное решение против затягивания расходных дампов). Следствие, в документах не названное: новый файл в `tests/corpus/docs/` проверкой не виден вообще, `check_manifest()` (`gate.py:111-134`) обходит только перечисленные пути. Заявление «на них держится `MANIFEST.sha256`… `sha256sum -c` обязан быть OK до и после любой правки» покрывает ПРАВКУ и УДАЛЕНИЕ, но не ДОПОЛНЕНИЕ. Единственная защита от дополнения — хук (`Write` по пути внутри `tests/corpus/docs/`), а он видит только литеральный путь в вызове инструмента: файл, порождённый скриптом по вычисленному пути, пройдёт мимо обоих. | СРЕДНЯЯ (одна из трёх запрещённых операций не покрыта измерительной проверкой; хук покрывает её частично) | не чинится этим этапом (правка состава манифеста — зона корпуса). Кандидат: отдельная проверка «в `tests/corpus/docs/` ровно 324 файла и все они в манифесте» — дешёвая, без прогона корпуса |
| AUDIT-REALDOC-NOGUARD | запрет 7 («реальные документы не коммитить») × `.gitignore` | **Запрет, ценой нарушения которого стала реальная утечка ПДн в публичный репозиторий (`DOG-REPO-BLOB`), не имеет исполняемой проверки — только `.gitignore` ПО ИМЕНАМ.** В игноре перечислены `real_docs/`, `/dogovor.*`, `*_restored.*`, отчёты интерфейса, дампы замеров. Всё это работает, пока файл называется ожидаемым именем и лежит в ожидаемом месте; дамп реального текста, положенный в `experiments/stage_x/probe_out.txt` или в `docs/`, не остановит ничто — при том что сам `.gitignore` в комментарии прямо просит «не переносить дампы в `experiments/`/`docs/`», то есть автор запрета знал этот путь обхода. Ни хука, ни теста, ни pre-commit на содержимое нет. Однажды класс уже сработал: `docs/archive/reports/REAL_DOCS_CHECK.md` нашёл блоб и НЕ опознал его (см. `DOG-REPO-BLOB`). | ВАЖНАЯ (главный риск продукта; защита держится дисциплиной и именами файлов) | не чинится этим этапом (зона — процесс коммита, решение владельца). Кандидат: PreToolUse-замок на `git add`/`git commit` при попадании в индекс `.docx`/`.xlsx` вне `tests/`+`packaging/`, либо проверка на «игл» из реального документа — второе требует хранить иглы, что само по себе риск |
| SUMWORDS-DEFERRED | `tests/corpus_v2/typedefs/SUM.yaml` (форма 2 блока MONEY_WORDS) | **Чисто словесная сумма («восемьсот тысяч рублей») не детектируется — отложено с числом.** Решение владельца TYPE-FACTORY-2: словесные суммы бьются как численные. Форма 1 (составная, «цифры (пропись) валюта [копейки]») СДЕЛАНА: в v2 было 78 полных утечек, стало 0 открытых хвостов. Форма 2 — без цифр вовсе: в v2 ~53 кандидата (грубая верхняя оценка регэкспом), в v1 — 0. Ветвь через закрытый список корней числительных в сессию честно не влезла. | СРЕДНЯЯ (утечка сумм в редкой форме записи; в v1 формы нет вовсе) | отдельный typedef-ветвь SUM (класс «слово закрытого списка»), кандидат следующей сессии фабрики |

## Закрыта этапом LAYOUT (2026-08-09)

`FIX3-NODES-MUTATION-CLASS` закрыта ремонтным проходом (`src/layout_repair.py` + ремонт стыка B3 в `tokenizer._detect_boundary_entities`): значение, разорванное вёрсткой внутри токена, детектируется по залеченному виду с картой индексов; маска — в исходных координатах. Все 11 красных строк NODES закрыты (в т.ч. `makarov@gma
il.com`, `Кузнецо
в А. О.», телефон колонтитула); 4 значения «омоглифы в инициалах» — не вёрстка и остаются в классе регистра/омоглифов. Класс: 1864 утекающих → 1488; leak_v2 TOTAL(BIK-excl)>=6 1826→1470. Остаток класса — новая запись `LAYOUT-RESIDUE` в FINDINGS.md. Запись ниже — без изменений.

| id | область | находка | тяжесть | чинится |
|---|---|---|---|---|
| FIX3-NODES-MUTATION-CLASS | `src/normalizer.py` (NBSP/NNBSP), `entity_types.yaml` (EMAIL/PHONE), `src/anchor_registry.py` (PER) — разобрано этапом FIX-3 | **Одиннадцать красных строк гейта после NODES — это 13 значений в колонтитулах, и все 13 принадлежат ПРЕД-СУЩЕСТВУЮЩЕМУ классу, а не новому дефекту.** Разбор поимённо (дампы baseline vs current): 8 EMAIL, 4 PER, 1 PHONE. Причины, названные числом: (1) ПЕРЕНОС СТРОКИ ВНУТРИ ЗНАЧЕНИЯ — 5 случаев (`makarov@gma
il.com`, `+7 (49
9) 167-19-83`, `Кузнецо
в А. О.`); (2) NBSP/NNBSP ВНУТРИ СЛОВА — нормализатор сводит их к ПРОБЕЛУ, а не удаляет, и пробел рвёт e-mail (`m elnik@sibt⁠r ans.ru` -> `m elnik@sibt rans.ru`); (3) омоглифы в инициалах ФИО. Нулевой ширины (ZWSP/ZWJ/WJ/SHY) нормализатор уже удаляет — проверено пробой. **Размер класса в точке отсчёта: 1854 утекающих сущности, из них 634 с переносом внутри значения, 457 с нулевой шириной, 343 с NBSP** — то есть 13 новых это 0,7 % класса, ставшие видимыми оттого, что колонтитул начали читать. **Чинится НОВЫМ механизмом, а не приёмами A1-A7/FIX-2, поэтому этап остановился и доложил:** «расплетающий вид» (`_addr_deweave`) есть только у ADDRESS, для EMAIL/PHONE его нет; сборка через стык (`multispan._SEAM_TYPES`) PHONE исключает НАМЕРЕННО (документировано: телефоны в столбик), и у PHONE нет якоря, которого сборка требует от всех; удаление NBSP внутри слова в нормализаторе склеило бы «Иванов И.И.» -> «ИвановИ.И.» и сломало бы PER — то есть обменяло бы 8 EMAIL на неизвестное число ФИО. | **ВАЖНАЯ** (13 частичных утечек ПДн видимы на гейте; класс шире — 1854) | отдельный этап «расплетающий вид для EMAIL/PHONE/PER» либо решение владельца о цене NBSP-склейки; оценка: сопоставима с этапом E′ (сборка адреса), то есть полная сессия с полным гейтом |


## Перенесено из живого FINDINGS.md этапом REG (2026-08-09)

Строки таблицы, у которых закрытие УЖЕ состоялось, но которые оставались в живом файле и мешали планировать. Текст строк перенесён дословно; колонка «закрыта этапом» добавлена этапом REG.

**`ADDRB-NL-HOLE` — ЗАКРЫТА этапом REG (2026-08-09) — починка прибора «немаскируемый шов».**

| ADDRB-NL-HOLE | измеритель границ (`experiments/stage_addr_b/addr_probe.py`) | Адрес, разорванный переносом строки и закрытый ДВУМЯ масками, считается «короче эталона»: открытым остаётся ровно символ `\n` между масками. 44 случая из 53 остатка группы U0. Это артефакт измерения (значение закрыто целиком), но отделять его автоматически прибор не умеет — в отчёте такие случаи видны в колонке «только пунктуация». | НЕЗНАЧИТЕЛЬНО (не дефект системы) | прибор этапа, не продукт |

**`A3-NEG-KPP` — ЗАКРЫТА этапом A4 (2026-08-06).**

| A3-NEG-KPP **[ЗАКРЫТА этапом A4, 2026-08-06]** | `entity_types.yaml:102` (`KPP`) — якорь `(?i)кпп` добавлен, `dog.docx` 10→0. Побочные эффекты правки — `A4-KPP-ANCHOR-FALLOUT`. | **Складской номер из 9 цифр маскируется как КПП — 138 из 138 (100 %). Единственный из девяти массовых случаев, который ПОДТВЕРЖДЁН НА РЕАЛЬНОМ ДОГОВОРЕ: 10 из 10.** У `KPP` нет ни якоря, ни контрольной суммы (её у КПП не существует), паттерн берёт любые 9 цифр. Замер `real_probe.py`: в целевом документе 10 девятизначных прогонов, слова «КПП» в тексте нет ВОВСЕ (0 вхождений) — то есть все 10 срабатываний безосновательны. Это независимое подтверждение `DOG-KPP-NOANCHOR` с другой стороны: там дефект нашли по маскам, здесь — по отрицательному примеру, числа сошлись. | ВАЖНАЯ (точность; единственный класс, доказанный вне корпуса) | не чинится этой сессией. Кандидат — якорь `(?i)кпп`; закрывает заодно `DOG-KPP-NOANCHOR`. Правка требует полного гейта |

**`A5-NL-CROSS-RESIDUE` — ЗАКРЫТА этапом A6 (2026-08-06).**

| A5-NL-CROSS-RESIDUE **[ЗАКРЫТА этапом A6, 2026-08-06]** — сборка через стык граничного окна B3 (`multispan.collect_seam_entities` + обобщение внутрисегментного мультиспана): `ACCOUNT[leak_v2>=6]` 77→10, `OGRN[>=8]` 33→12, оба НИЖЕ планки (68 / 32); остаток 24 случая — целиком пред-существующие в baseline классы (8 эталонов ОГРН с невалидной КС — их и целый детектор не находил никогда; 5 «б»-край; рваный якорь «р/»+«с …» / значение через два стыка — см. `A6-DESTRUCTIVE-TRANSFORMS` пп. 3, 5, 7). Исходная формулировка ниже. | `src/normalizer.py` (проход 1 сохраняет `
` как структуру) × `entity_types.yaml` (внутренние разделители значений) — найдено этапом A5 полным гейтом | **Остаток дыры `A4-KPP-ANCHOR-FALLOUT`, который правка A5 НЕ закрыла: значение, разорванное переносом строки. Гейт красный по двум линиям «б»: `ACCOUNT` 68→77 (≥6 и ≥8), `OGRN` ≥8 32→33.** Состав поимённо: три счёта, рваных `
` (`labor_0009` ×3 мутации, `agency_0007`), два ОГРН (`labor_0008__m1337_linebreak`, `supply_0002__m1337_linebreak`) плюс 13 «соседей по документу» — маска на своём месте, но те же цифры выживают в рваном двойнике того же значения, и метрика засчитывает утечку ОБОИМ. Раньше рваный хвост случайно закрывал безъякорный `KPP`. Чинится не границами паттернов, а `\s`-классом внутри значения, а он запрещён находкой `A2-NEWLINE-CROSS` (перенос строки неотличим от границы ячейки: вместо чистого пропуска получается рваный частичный матч). | **ВАЖНАЯ** (утечка; гейт красный, планка не двинута) | не чинится наспех. Кандидат — мультиспан-сборка по `
` для ACCOUNT/OGRN по образцу этапа E, а не расширение разделителя |

**`DOG-KPP-NOANCHOR` — ЗАКРЫТА этапом A4 (2026-08-06).**

| DOG-KPP-NOANCHOR **[ЗАКРЫТА этапом A4, 2026-08-06]** | `entity_types.yaml` (паттерн `KPP`) — якорь `(?i)кпп` добавлен, precision 97,00%→100,00%. Побочные эффекты — `A4-KPP-ANCHOR-FALLOUT`. | **10 срабатываний `KPP` на документе, где слова «КПП» нет ни разу.** Замер `dog.docx`: вхождений «кпп» — 0, при этом детекция даёт `KPP` 10 (в маску набора `maximum` проходят 6). Девять из десяти — спаны длиной 11-12 символов с 9 цифрами и буквами слева, десятый — начало ячейки таблицы (`t5_r3_c1@0`). Для сравнения, все 33 найденных `OGRN` подтверждены якорем «ОГРН» слева в 40 символах (33 из 33), то есть у ОГРН картина здоровая, а у КПП — нет. На набор по умолчанию не влияет (`KPP` в `personal` не входит), поэтому это точность, а не утечка: цена — лишние маски в наборах «реквизиты» и шире. Соответствует самой низкой precision из линии «а» гейта (`KPP` 97,00 % — худший показатель среди всех типов). | СРЕДНЯЯ (точность; на наборе по умолчанию не проявляется) | не запланировано — кандидат: якорь `(?i)кпп` для `KPP` по образцу `INN`/`OGRN`; правка требует полного гейта |

**`DOG-NODE-SILENT` — ЗАКРЫТА этапом NODES (2026-08-08).**

| DOG-NODE-SILENT **[ЗАКРЫТА этапом NODES, 2026-08-08]** | было `src/extractor.py:667-691` × `src/unread_zones.py:152-192` (найдено этапом DOG, Часть 2); закрыто переписью узлов — `src/unread_zones.py:73-115` (закрытый список) + `_scan_readable_part:380` (сверка по `w:t`) + `src/extractor.py:289` (`_paragraph_runs` сквозь обёртки) | **ПЯТЬ видов узлов OOXML не читаются И НЕ объявляются зоной — гарантия отказа на них не срабатывает.** Проверено на порождённой фикстуре `tests/fixtures/node_kinds.docx` (по узлу на абзац, в каждом свой опознавательный телефон): прочитаны только `w:hyperlink` и обычный ран; `w:ins`, `w:fldSimple`, `w:smartTag`, `w:sdt` (inline) и `w:sdt` (блочный, верхний уровень тела) — НЕ прочитаны, при этом `scan_unread_zones` вернул **0 зон**, а `shifrator.py encrypt` в strict-режиме по умолчанию завершился **exit 0 без единого предупреждения**. Механизм двойной: (1) `src/extractor.py:667-668,691` обходит тело `body.iterchildren()` и берёт только `w:p`/`w:tbl` — блочный `w:sdt` не является ни тем, ни другим; (2) текст абзаца берётся как `paragraph.text` (`src/extractor.py:681,687`), а в python-docx 1.2.0 это `CT_P.text` = `"".join(e.text for e in self.xpath("w:r \| w:hyperlink"))` — только ПРЯМЫЕ дети `w:p`, поэтому любой промежуточный узел-обёртка прячет свои раны. Сканер зон при этом ищет ровно два подузла — `w:txbxContent` и вложенный `w:tbl` (`src/unread_zones.py:174,179`), остальных не знает. Тем самым нарушен явно заявленный принцип полноты самого модуля (`src/unread_zones.py:19-22`: «ложноотрицательный ответ недопустим»). Поглощает и подтверждает измерением прежнюю запись `1-B` (она называла только `w:sdt` верхнего уровня и помечена «в корпусе не встречается»). **На реальном договоре владельца ни один из пяти видов не встречается** (замер этапа DOG: `w:ins` 0, `w:del` 0, `w:fldSimple` 0, `w:smartTag` 0, `w:sdt` 0; `w:hyperlink` 81 — читается), поэтому дефект классово реален, но на целевом документе не срабатывает. `w:del` (`w:delText`) не читается ПРАВИЛЬНО: удалённый текст рецензирования не является видимым содержимым, `src/unread_zones.py:90-98` исключает его намеренно. | **ВАЖНО** (нарушение основной гарантии fail-closed; тяжесть поднимала `DOG-ANCHOR-SPLIT` ниже) | **ЗАКРЫТА:** все четыре вида узлов ЧИТАЮТСЯ (проверено поимённо на `tests/fixtures/node_kinds.docx`, `tests/test_nodes.py::TestFourNodeKinds` — по опознавательному телефону на узел); пятый, `w:sdt` верхнего уровня, читается тоже. Молчаливого пропуска больше нет как класса: узел вне переписи даёт зону `unknown_node` и отказ, а полноту обхода доказывает не список, а сверка «каждый `w:t` части достигнут читателем» |

**`DOG-ANCHOR-SPLIT` — ЗАКРЫТА этапом NODES (2026-08-08).**

| DOG-ANCHOR-SPLIT **[ЗАКРЫТА этапом NODES, 2026-08-08]** | `src/extractor.py` × якорные детекторы (найдено этапом DOG, Часть 2) | **Непрочитанный узел превращает потерю текста в УТЕЧКУ ПДн, когда в нём лежит ЯКОРЬ, а значение — в обычном ране.** Само по себе непрочитанное содержимое в LLM не уходит (его нет в `{sid}.txt`), поэтому `DOG-NODE-SILENT` выглядит «только» тихой потерей. Но абзац читается ЧАСТИЧНО, и если маркер-якорь («ИНН», «СНИЛС», «счёт», «дата рождения») попал в узел-обёртку, а цифры остались прямым раном, якорный детектор не находит маркера — и значение уходит в LLM ОТКРЫТЫМ ТЕКСТОМ. Воспроизведено прогоном на порождённом документе: один и тот же ИНН в контрольном абзаце маскируется в `[INN_PER_1]`, а в абзаце, где «ИНН » лежит в `w:ins`, остаётся в анонимном тексте как есть (1 вхождение открытым). Это ровно главный риск продукта из `CLAUDE.md` — «пропущенная сущность уходит в LLM без предупреждения», — и он проходит мимо обеих защит: детекция маркера не видит, fail-closed зону не объявляет. Направление ошибки хуже обычного пропуска: обычный непрочитанный узел теряет содержимое целиком (заметно человеку), а расколотый якорь оставляет текст на вид полным. Обратный расклад (якорь прочитан, значение в узле) утечки не даёт — значения нет в выходе. | **ВАЖНО** (подтверждённая утечка ПДн в LLM без предупреждения; на `dog.docx` не срабатывала — узлов-носителей в нём нет) | **ЗАКРЫТА вместе с `DOG-NODE-SILENT`, но НЕ отказом, а чтением** — что строго сильнее: якорь и значение снова оказываются в ОДНОМ сегменте вплотную, и маска ставится, а не теряется вместе с абзацем. Живая проба — `tests/test_nodes.py::TestFourNodeKinds::test_anchor_split_is_closed`: «ИНН » в `w:ins` + цифры обычным раном обязаны собраться в один сегмент |

**`NODES-FOOTER-PAGENUM` — ЗАКРЫТА этапом FIX-3 (2026-08-08); живая проба этапа REG подтвердила.**

| NODES-FOOTER-PAGENUM | `entity_types.yaml` (`ADDRESS`, якорь «стр.» = строение) × колонтитулы, ставшие читаемыми — найдено этапом NODES, Часть 2 | **«Стр. 1 из 3» маскируется как `[ADDRESS_1]`.** Дефект детекции ПРЕД-СУЩЕСТВУЮЩИЙ, а не привнесённый: воспроизведён на обычном `.txt` той же строкой, без всякого .docx (`Стр. 1 из 3 \| Телефон: …` → `[ADDRESS_1] из 3 \| Телефон: [PHONE_1]`). Этап NODES его не создал, но резко поднял ЧАСТОТУ: нумерация страниц живёт именно в нижнем колонтитуле, который до этого этапа не читался вовсе. Направление ошибки — перебор (порча документа), не утечка. | СРЕДНЕ (порча читаемости, не утечка; частота после NODES высокая — колонтитул с номером страницы есть почти в каждом договоре) | зона детекции, не NODES: анти-якорь «стр.» перед арабской цифрой без последующего адресного маркера. Мерить линией «д» гейта (порча документа) |

**`AUDIT-GUARD-PYWRITE` — ЗАКРЫТА этапом AUDIT (2026-08-08).**

| AUDIT-GUARD-PYWRITE **[ЗАКРЫТА этапом AUDIT, 2026-08-08]** | `.claude/hooks/guard.py` × запреты 2 и 3 | **Замки корпуса и точки отсчёта обходились одной командой интерпретатора — замерено живьём, а не выведено из кода.** Хук считал команду изменяющей только по ГЛАГОЛУ оболочки (`rm`/`cp`/`sed -i`/…) или по перенаправлению `>`. Прогон хука с настоящим payload: `python -c "open('tests/corpus/gold.json','w')"` → **exit 0**, `python -c "import shutil;shutil.copy('d.json','tests/corpus/results_baseline.json')"` → **exit 0**. То есть заявление `tests/corpus/CLAUDE.md:6` «заперто хуком» было верно только для одного способа записи из двух. Закрыто: команда, в которой есть запуск интерпретатора И признак записи (`open(...,'w')`, `.write(`, `json.dump`, `shutil.copy/move`, `os.remove/rename`, `Path.write_*`), считается изменяющей; чтение (`json.load(open(gold))`) намеренно оставлено проходящим — иначе сторож мешал бы разбору, ради которого корпус и читают. | ВАЖНАЯ (замок, объявленный физическим, обходился штатным инструментом сессии) | закрыто; зеркала — `.claude/hooks/selftest.py` (2 случая) и `tests/test_audit_instrument_guards.py` |

**`AUDIT-ITER-NOLEDGER` — ЗАКРЫТА этапом AUDIT (2026-08-08).**

| AUDIT-ITER-NOLEDGER **[ЗАКРЫТА этапом AUDIT, 2026-08-08]** | `tests/corpus/results_iter_baseline.json` — точка отсчёта быстрого набора | **Точку отсчёта среза можно было переснять БЕЗ СЛЕДА: журнал вёлся только для полного корпуса.** Замерено: `Write` по пути файла → exit 0, `cp d.json …` → exit 0, `run_iter_baseline.py` → exit 0, `update_manifest.py` → exit 0. Файл в `MANIFEST.sha256`, но манифест пересобирается тем же `update_manifest.py`, который хук пропускал, — то есть единственное следствие пересъёмки (расхождение хеша) снималось второй разрешённой командой. Автора, причины и списка расхождений не оставалось нигде. Прибор отладки, который можно бесследно подогнать под текущее поведение, показывает не поведение, а последнюю подгонку — ровно то, из-за чего однажды перестал охраняться полный гейт (`tests/corpus/BASELINES.md`, раздел S2). Закрыто тремя рубежами: журнал `tests/corpus/iter_baseline_ledger.json` (записи только дописываются, пересъёмка C+ внесена задним числом со всеми восемью расхождениями, включая пять «в плюс»), инструмент `tests/corpus/promote_iter_baseline.py` (требует `--author`/`--reason`/`--delta`, отказывает при обосновании «покраснеет гейт»), замок в хуке + страж `tests/test_audit_instrument_guards.py::test_iter_baseline_matches_its_ledger` (хеш файла обязан быть назван последней записью — краснеет при любом способе обхода, включая ручной). | ВАЖНАЯ (закрыта) | закрыто |



## Перенесено из живого FINDINGS.md этапом METRIC-FIX (2026-08-09)

Четыре долга АУДИТА-1 про сам ПРИБОР. Все закрыты этапом METRIC-FIX; строка
каждого приводится в том виде, в каком она жила, с добавленной колонкой «чем
закрыт» — чтобы было видно не только «закрыто», но и чем именно.

| id | суть одной строкой (как жила) | чем закрыт |
|---|---|---|
| `AUD1-LEAK-SUBTHRESHOLD` | leak_v2 слепа к значению, раздробленному на цифровые куски <6: `704
-068` открыт → «none» | Правилом ВОССТАНОВИМОСТИ (`measure_lib.assembled_open_value`): куски засчитываются вместе, если дожили ПОДРЯД соседними цифровыми группами, в порядке ядра, с зазором ≤3. Голая сумма кусков отвергнута замером (+753 шума). Стало видно **41 вхождение** (PASSPORT 36, KPP 3, BIK 2); узость доказана сличением дампов (11992 сущности побайтно, расхождения только в `leak_v2`, все `none → full`) |
| `AUD1-HEADER-FALSEMISS` | значения в колонтитулах маскируются, но прибор пишет «не найдено»; в записи стояло «35 вхождений» | Локализацией ПО ЧАСТЯМ пакета (`measure_lib.part_regions`): у каждой части свой участок PT-1 и свой курсор. Механизм назван: сегменты колонтитула лежат в PT-1 ДО тела, а поиск шёл от `body_start`. Верное число — **60** (PHONE 30 + EMAIL 30 в `word/header1.xml`), оценка «35» не воспроизвелась. Тот же класс починен на корпусе v2 (17 из 84 docx имеют колонтитул) |
| `AUD1-RESULTS-TRUST` | `--results`-режим доверяет предвычисленным полям: «сняты все маски в 10 док.» двигает только точность | `measure_lib.check_results_consistency` + отмена оценки (код 2) в `gate.py` и `measure_v2.py`. Пробито шестью видами подложенной порчи (`tests/test_results_consistency.py`), включая дословную катастрофу АУДИТА-1; на трёх настоящих дампах репозитория претензий 0 |
| `AUD1-CIRCLE-THIN` | круг exe: один .txt, `.docx` через сборку не ходит, «маски расставлены» = n>0 | Круг усилен до 16 шагов: точное число масок (28/.txt, 22/.docx), конкретные ожидаемые значения (исчезают и возвращаются байт-в-байт), документ Word целиком через собранную программу, значение из ВЕРХНЕГО КОЛОНТИТУЛА в списке ожидаемых. Пробит подложенной деградацией 22 → 1 (падение на шаге 13). Усиленный круг сразу вскрыл две настоящие находки — `MFIX-CANON-FEMSURN` и `MFIX-PROFILE-IP-PER` |

Строки в исходном виде, как они лежали в живом файле:

| `AUD1-LEAK-SUBTHRESHOLD` (АУДИТ-1) | leak_v2 слепа к значению, раздробленному на цифровые куски <6: `704\n-068` открыт → «none» | метрика дробления ниже порога / счёт по совокупности кусков | **ЖИВ: доказано пробоем; «тихих» в теле дампа до 90 вхождений (PASSPORT 36)** |
| `AUD1-HEADER-FALSEMISS` (АУДИТ-1) | значения в колонтитулах маскируются, но прибор пишет «не найдено» (локализация PT-1) — recall занижен ложно | сопоставление колонтитульных сегментов в `build_segment_offsets` | **ЖИВ: 35 вхождений; живая проба agency_0002 (PHONE колонтитула закрыт, в дампе found=False)** |
| `AUD1-RESULTS-TRUST` (АУДИТ-1) | `--results`-режим гейта/measure_v2 доверяет предвычисленным полям: «сняты все маски в 10 док.» двигает только точность | перекрёстная сверка masks↔entities при чтении дампа | **ЖИВ: доказано пробоем** |
| `AUD1-CIRCLE-THIN` (АУДИТ-1) | круг exe: один .txt, `.docx` через сборку не ходит, «маски расставлены» = n>0 (28→1 прошло бы) | .docx-шаг в круге + сверка счёта масок с эталоном круга | **ЖИВ: accept_circle.py:32,128** |


## Перенесено из живого FINDINGS.md этапом DEFAULT-GATE (2026-08-09)

Обе находки вскрыты усиленным кругом на собранной программе (этап METRIC-FIX) и
закрыты решениями владельца в этапе DEFAULT-GATE.

| id | суть одной строкой (как жила) | чем закрыт |
|---|---|---|
| `MFIX-PROFILE-IP-PER` | в наборе по умолчанию ФИО внутри имени ИП уходит в LLM открытым: на максимуме спан закрывает маска `ORG`, а `ORG` в наборе выключен | Решение владельца: эмитить `PERSON` внутри ORG-спана; `ORG` в набор по умолчанию НЕ включать. Склейка «ИП + ФИО» запоминает поглощённое имя (`Entity.shadowed`), `tokenizer._recover_shadowed` возвращает его под маску, когда победивший тип выключен. Класс на v1: **380 → 6** вхождений (остаток — `DEFGATE-CROSSTYPE-RESIDUE`, другой механизм), на v2 — 3. На МАКСИМУМЕ не изменилось НИЧЕГО: 12033 сущности дампа гейта сошлись побайтно |
| `MFIX-CANON-FEMSURN` | канон восстановления портит женскую фамилию: «Кузнецова» → «Кузнецов» | Решение владельца: женская именительная форма не должна проигрывать мужской родительной. `_nominative_word` получил три правила: род спана (из имени и отчества) сильнее score; уже именительный ЕДИНСТВЕННОГО числа не склоняется; иначе прежнее поведение. Цена названа тестом, а не спрятана: без подсказки рода «Иванова» останется «Иванова». Стережёт шаг круга на собранной программе |

Строки в исходном виде, как они лежали в живом файле:

| `MFIX-PROFILE-IP-PER` (METRIC-FIX) | в наборе ПО УМОЛЧАНИЮ ФИО внутри имени ИП уходит открытым: на максимуме спан закрывает маска `ORG` («ИП Беляев Олег Олегович»), а в наборе «Только персональные данные» `ORG` выключен и закрывать нечем | **решение владельца**: либо `PERSON` внутри ORG-спана эмитится отдельно, либо `ORG` входит в набор по умолчанию | **ЖИВ: доказано прогоном обоих наборов на одном документе (2 вхождения открытым текстом).** Гейт этого класса НЕ ВИДИТ по построению — он всегда меряет на МАКСИМУМЕ |
| `MFIX-CANON-FEMSURN` (METRIC-FIX) | восстановление подставляет КАНОН, и канон портит женскую фамилию: «Кузнецова» → «Кузнецов». Механизм: женская фамилия в именительном омонимична мужской в родительном, разбор берёт `masc,gent` (score 0.5 против 0.333 у верного `femn,nomn`), `inflect({'Nom'})` даёт мужскую форму — `src/anchor_registry.py:472` `_nominative_word`. Докстринг функции обещает сохранение рода; обещание держится только на однозначных формах («Новиковой» → «Новикова» верно) | выбор разбора по роду, а не по score, либо канон = форма первого вхождения для ФИО | **ЖИВ: вскрыт усиленным кругом exe, воспроизведён прогоном `_nominative_word` на пяти фамилиях.** Пользователь получает восстановленный документ, где женщина названа мужской фамилией |
