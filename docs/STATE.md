# STATE — живой снимок SHIFRATOR

**Этот файл перезаписывается целиком в конце каждого этапа.** Здесь только «как
сейчас». История — [`JOURNAL.md`](JOURNAL.md), он только дописывается.
Устройство — [`ARCHITECTURE.md`](ARCHITECTURE.md). Открытые долги —
[`FINDINGS.md`](FINDINGS.md).

**Снимок на: 2026-08-08**, этап **FIX-3** (закрыть красное и хвосты), в `main`,
**не пушено**. До него в тот же день — TYPE-FACTORY-1/2 и V1-SCOPE.

**ГЕЙТ КРАСНЫЙ — 11 строк, все прежние (NODES, колонтитулы), НИ ОДНОЙ НОВОЙ.**
Цель этапа «11 → 0» НЕ достигнута, и это доложено числом, а не обойдено: все
13 значений разобраны поимённо (§4), но они — 0,7 % пред-существующего класса
в 1854 утечки, и закрываются НОВЫМ механизмом, а не приёмами A1-A7/FIX-2.
Задание требовало в таком случае остановиться и доложить — сделано.
**Планка НЕ двигалась**, `promote_baseline.py` не запускался.

Набор тестов: **1637+ passed, 0 failed** (число уточняется финальным прогоном;
+35 стражей FIX-3). Собранная программа: круг **ПРОЙДЕН** (§7).

Источник истины — код и прогоны. Разошёлся с ними — прав код, а файл чинится.

---

## 0. Решения владельца, которых ждёт работа

1. **Класс «значение, разорванное вёрсткой» (`FIX3-NODES-MUTATION-CLASS`).**
   11 красных строк гейта — его видимая часть. Цена закрытия оценена: сессия
   масштаба E′ («расплетающий вид» для EMAIL/PHONE/PER). Альтернатива —
   принять и двинуть планку; это решение владельца, не сессии.
2. **Точка отсчёта гейта** — пересборка только `promote_baseline.py`.
3. **Реклассификация `DOCNUM` v2 → `CONTRACT_NO`** — решение принято, сессия
   разметки не проведена (см. манифест исключений §6).
4. **`DOG-REPO-BLOB`** (сервер) и **часть 5 STORE** — без изменений.

## 1. Проверить состояние двумя командами

```
venv/Scripts/python.exe -m pytest -q            # набор тестов, ~10-13 мин
venv/Scripts/python.exe tests/corpus/gate.py    # полный гейт, 324 док., ~10 мин
```

## 2. Что принёс FIX-3

| Часть | Итог |
|---|---|
| 1. 13 значений NODES | разобраны поимённо, причины названы, **не починены** — нужен новый механизм (§4) |
| 2. `NODES-FOOTER-PAGENUM` | **закрыт**: «Стр. 1 из 3» не адрес; адреса не потеряны (§5) |
| 3. `FIX2-ANTIANCHOR-RIGHT` | **закрыт**: правый край анти-якоря, `term/norm-ref` 138 → **0** |
| 4. `SUMWORDS-DEFERRED` | **закрыт**: словесные суммы, 52 эталона v2, до правки 0 |
| 5. Шаблоны К4/К5 | **сделаны**, перевод SUM/PERCENT/TERM **посимвольный** |
| 6. Приборы AUDIT | 3 записи закрыты **живыми пробами** (§8) |
| — | **попутно:** exe не работал вовсе — найдено и починено (§7) |

## 3. Фабрика типов: классы форм

Четыре класса: `raw`, `digit_run`, **`number_with_unit` (К4)** — слоты
number/decimals/parenthetical/**scale**/gap/units/suffix/right_boundary,
**`compound` (К5)** — `branches[]` + необязательный `tail[]` + границы.
SUM переведён на К4, PERCENT и TERM на К5 — собранный `entity_types.yaml`
после перевода **не изменился ни на байт** (требование задания). Тип, который
шаблон не воспроизводит посимвольно, остаётся `raw`; подгонять паттерн запрещено.

## 4. Одиннадцать строк NODES — разбор поимённо (приёмка «а»)

13 значений, все в колонтитулах, все под состязательной мутацией:
**8 EMAIL, 4 PER, 1 PHONE.** Причины РАЗНЫЕ (ответ на вопрос задания):

| Причина | Сколько | Пример |
|---|---:|---|
| перенос строки ВНУТРИ значения | 5 | `makarov@gma\nil.com`, `+7 (49\n9) 167-19-83`, `Кузнецо\nв А. О.` |
| NBSP/NNBSP внутри слова → нормализатор даёт ПРОБЕЛ, а не удаляет | 4 | `m⁠elnik@sibt r ans.ru` |
| омоглифы в инициалах ФИО | 4 | `нoвикoв c. ю.` |

Символы нулевой ширины (ZWSP/ZWJ/WJ/SHY) нормализатор **уже удаляет** —
проверено пробой, они не причина. **Размер класса в точке отсчёта: 1854
утекающих сущности** (634 с переносом внутри значения, 457 с нулевой шириной,
343 с NBSP). 13 «новых» — 0,7 % класса, ставшие видимыми оттого, что
колонтитул начали читать.

**Почему не починено проверенными приёмами** (каждый проверен и отклонён по
причине, а не по трудоёмкости): «расплетающий вид» есть только у ADDRESS;
сборка через стык (`multispan._SEAM_TYPES`) исключает PHONE НАМЕРЕННО
(телефоны в столбик) и требует якоря, которого у PHONE нет по построению;
удаление NBSP внутри слова в нормализаторе склеило бы «Иванов И.И.» →
«ИвановИ.И.» и обменяло бы 8 EMAIL на неизвестное число ФИО.

## 5. Номер страницы больше не адрес — и адреса целы

«стр.» — это и «страница», и «строение» (законный адресный элемент). Маркер
объявлен ЗАВИСИМЫМ: анкорит только при спутнике (улица, локальность или
топоним) и только если он не «Стр.» с заглавной в начале спана.

⚠ **Первая редакция стоила двух настоящих адресов** — полный гейт показал долг
ADDRESS 43→45 и падение precision. Ограничитель сужен ПО ЗАМЕРУ (хвост адреса
после переноса: «…автодороги М-3 «Дон»\n, стр. 3»), оба вернулись. Урок в силе:
ограничитель проверяется полным гейтом, а не пробой на выдуманных строках.

Реальный документ: **ADDRESS 28** (как до этапа), масок на нумерации — **0**.

## 6. Правый край анти-якоря (новый механизм, санкция дана)

`anti_anchor_right` + `anti_anchor_right_window` — **свои ключи, своя функция,
своя константа** (левое окно FIX-2 не тронуто). Не пересекает `\n`, контекст
сводится тем же фолдингом. Заведён ровно одному типу — `TERM`;
**`term/norm-ref` 138/138 → 0**, настоящие сроки живы. **PASSPORT не тронут** —
ключа у него нет, левое окно на умолчании 20 (проверяется стражем).

## 7. Собранная программа — дефект найден и починен

**exe отказывался шифровать ЛЮБОЙ документ.** `src/` искал `entity_types.yaml`
`__file__`-арифметикой, PyInstaller кладёт его в `sys._MEIPASS`; файл не
находился, фолбэк срабатывал молча, фабричные типы выпадали из порядка
арбитража, и `assert_priority_contract` валил обезличивание. Дефект внесён
сессией TYPE-FACTORY-2 и **прожил через полный гейт и 1637 тестов** — все они
ходят по рабочему дереву. Путь сведён в `config_cache.default_config_path()`,
страж на оба режима — `tests/test_fix3.py::TestFrozenConfigPath`.

**Круг после пересборки ПРОЙДЕН:** 46 сущностей 18 типов; нерешённых токенов 0;
токенов в восстановленном тексте 0; значений не вернулось 0; сессия видна в
списке и открывается по id повторно. Два ФИО возвращаются канонической формой —
норма этапа E, а не потеря.

## 8. Приборы AUDIT — закрыто живыми пробами

| Запись | Проба | Итог |
|---|---|---|
| `AUDIT-MANIFEST-WHITELIST` | лишний файл в `tests/corpus/docs/` | `ФАЙЛ ПОЯВИЛСЯ в охраняемом каталоге`, после уборки `ok=True` — держится замком FIX-2, запись протухла |
| `AUDIT-REALDOC-NOGUARD` | ZIP с `word/document.xml` под именем `notes_2024.bin` | `ЗАБЛОКИРОВАНО … признак 3`, `exit 2` — держится замком FIX-2 |
| `AUDIT-ARCH-TRANCHE-FALSE` | чтение + правка | формулировки `ARCHITECTURE.md` о стене и TRANCHE приведены к действительности |

## 9. Гейт — 11 строк, все прежние

Линии «а» (precision), «в»/«г» (masking), «д» (порча прозы 1717→1717), «ж»
(подавление) — без движения. Улучшения: границы ADDRESS (недобор −125 символов
после снятия ложных перехватов), masking B 90,74 → 90,78 %. Красное — те же
PER/PHONE/EMAIL/TOTAL по линии «б» и PER по линии «е». Крешей 0, отказов 0,
`MANIFEST.sha256` 659 OK. Детерминизм: 3 прогона на реальном документе,
отпечаток сущностей совпал 3/3.

## 10. Реальный документ

ADDRESS 28, BIK 1, EMAIL 10, OGRN 33, ORG 70, PASSPORT 29, PERCENT 81,
PERSON 67, REG_ID 1, REG_NUMBER 6, SHARE_PCT 3, **SUM 20** (было 17: три
словесные суммы «10 (десять) миллионов рублей» теперь одним спаном), TERM 77.
Масок на нумерации страниц — 0. Ложных на просмотр — нет.

## 11. Что красное или висит

1. **Гейт красный по 11 строкам** — класс `FIX3-NODES-MUTATION-CLASS` (§4).
   Решение владельца: чинить отдельной сессией или двигать планку.
2. **Реклассификация DOCNUM v2** — §0.3.
3. **`DOG-REPO-BLOB`** (сервер), **`NODES-ATTR-TEXT`**, сноски/надписи/
   вложенные таблицы (зона+отказ), **`A7-ORG-NOANCHOR-UNSOLVABLE`**, остатки
   переусердствования ACCOUNT/INN/INN_PER, **`STORE-ACCUM-CONFIRM`**,
   yargy 73 % времени гейта — без изменений.
4. **Правый анти-якорь заведён одному типу.** Другие виды случая с
   дисквалификатором справа не искались — механизм есть, применение точечное.

## 12. Хронометр

`.chrono/6a90420d-….tsv`: этот день — окно 17:21 → 23:40, ~200 вызовов,
собственное время ~3000 с (три полных гейта, три полных набора тестов, два
замера v2 и две сборки exe шли фоном и в это число не входят).

## 13. Ветки и рабочие деревья

Всё сделано в `main` серией коммитов по частям, **не пушено**. Единственное
рабочее дерево — `C:\Jesus\ARP`.
