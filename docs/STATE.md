# STATE — живой снимок SHIFRATOR

**Этот файл перезаписывается целиком в конце каждого этапа.** Здесь только «как
сейчас». История — [`JOURNAL.md`](JOURNAL.md), он только дописывается.
Устройство — [`ARCHITECTURE.md`](ARCHITECTURE.md). Открытые долги —
[`FINDINGS.md`](FINDINGS.md).

**Снимок на: 2026-08-08**, этап **MERGE-1** (слияние блока из четырёх веток —
`exp`, `instr`, `a7`, `store`), ветка `main`, не пушено.

**ГЕЙТ ЗЕЛЁНЫЙ к новой точке отсчёта, семь линий.** Точка переснята штатной
процедурой `promote_baseline.py --accept-red`: 21 улучшение против 6 красных
строк сдвига масок (MASK-SHIFT класса A2/A6 — значение перешло из «не найдено,
открыто целиком» в «найдено, открыто частично»; ни одной снятой маски, ни одной
сущности `found=True→False`). Обоснование, шесть принятых строк дословно и
`carried_debts` — в `overmask_ledger.json`, запись 2026-08-08.
**Два долга помечены `carried_debts` — долг, не норма:** перебор PASSPORT
281→367 / 2251→2945 (`0c-B`), перебор ADDRESS +2/+22. Третий долг прошлой
записи, `precision[INN]` 99,02 %, **ЗАКРЫТ этапом A7** (100,00 %).

Источник истины — код и прогоны. Разошёлся с ними — прав код, а файл чинится.

---

## 0. Одно срочное решение владельца (осталось)

1. **`DOG-REPO-BLOB` — реальный договор в истории git. ЛОКАЛЬНО ВЫЧИЩЕН
   2026-08-05 (задача LEAK-PURGE), НА СЕРВЕРЕ ОСТАЁТСЯ.** Бэкап (две копии) —
   `C:\Jesus\ARP_LEAK_PURGE_BACKUP_20260805`, **не удалять до закрытия вопроса
   по серверу**. На GitHub документ отдаётся по SHA (недостижимые объекты не
   убираются пушем) — нужна сборка мусора силами хостинга либо пересоздание
   репозитория. Решение отложено до проверки форков.

## 1. Проверить состояние двумя командами

```
venv/Scripts/python.exe -m pytest -q            # набор тестов, ~8 мин (xdist -n auto)
venv/Scripts/python.exe tests/corpus/gate.py    # полный гейт, 324 док.
```

**Набор тестов (приёмка MERGE-1): 1390 passed, 3 skipped, 8 xfailed, 0 failed.**
Разложение роста с 1348 (BASE-A6) поимённым сравнением собранных узлов:
+37 зеркал A7, +1 исполнившийся xfail-контракт BIRTHDATE, +3 теста INSTR
(разведение `crashed`/`refused`), +1 тест STORE (домашний каталог при вызове).
Пропавших узлов — ноль.

⚠ Два готчи прогонов, стоившие времени в этом блоке:
- **в свежем `git worktree` нет `venv/`**, а часть тестов зовёт
  `<root>/venv/Scripts/python.exe` подпроцессом — лечится junction'ом (правило
  в `CLAUDE.md`), кода не касается;
- **`gate.py` падает `UnicodeEncodeError`** на последней строке, если stdout
  перенаправлен в файл под cp1251 — гонять с `PYTHONIOENCODING=utf-8`.
  Вердикт по готовому дампу берётся без повторного прогона корпуса:
  `gate.py --results tests/corpus/results_gate_current.json`.

## 2. Что принесли четыре ветки блока

- **`a7` — детекция, пять долгов качества.** Регистр в суммах (`(?i)` — «РУБ.»
  не матчилось вовсе), свод алфавитов, ЗАМКНУТЫЙ ПО РЕГИСТРУ, двойники цифр
  внутри `BIRTHDATE` (только под якорем рождения, нормализатор не тронут), два
  ограничителя КС на безъякорное значение (анти-якорь чужих номенклатур +
  `_digit_run_neighbour`). Пятый долг закрыт вердиктом, не правкой.
- **`store` — хранилище.** Путь `~/.shifrator` переспрашивается ПРИ ВЫЗОВЕ, а не
  застывает на импорте; шифруются все шесть файлов сессии; ключ завёрнут в
  DPAPI (`src/vault.py`); срок хранения 7/30 дней настраиваемый; структурная
  запись разметки (`src/markup_record.py`) без фрагментов реального текста.
- **`instr` — измерительный слой.** Хеш корпуса считался только по `.docx`
  (162 из 324 документов мимо проверки) — расширен; `crashed` и `refused`
  разведены на разные счётчики (отказ больше не роняет гейт как креш).
- **`exp` — мусорка.** 168 файлов `experiments/` разобраны поштучно: живые
  инструменты на месте, 74 лога и дампа в `experiments/archive/`, опись —
  `experiments/INDEX.md`. Физически не удалено ничего.

## 3. Цифры последнего полного прогона гейта (корпус v1, MERGE-1)

Дамп прогона **байт-в-байт равен дампу A7** на всех 324 документах — вклад
хранилища, инструментов и мусорки в линии измеренный ноль, а не ожидание.

| Линия | Значение |
|---|---|
| **а** precision | PER 99,96 %, ORG 100 %, ADDRESS 99,92 %, **INN 99,02→100,00 %**, **OGRN 99,29→100,00 %**, KPP/ACCOUNT/BIK/PHONE/EMAIL/PASSPORT/SNILS/BIRTHDATE/SUM/INN_PER 100 %. FP по негативам 8→**2** |
| **б** leak_v2 | Ни одного регресса. Агрегат BIK-excl 1860→**1826** (≥6) / 1715→**1686** (≥8). BIRTHDATE 34→**14**, PER 997→989, SUM(≥6) 28→23, SNILS 8→7 |
| **в** masking A | 100,00 % — инвариант держится |
| **г** masking B | 90,68→**90,74 %** (агрегат вырос). Красные по типам: ORG 92,35→92,23 %, SUM 97,01→96,88 % — MASK-SHIFT, см. ниже |
| **д** порча | 1717 → **1717**, роста нет (ADDRESS 3, ORG 322, PER 1, PERCENT 1031, TERM 360) |
| **е** границы | Недобор ВСЕГО 765→**761** сущ. / 4658→**4633** симв. Красные: ORG 28→29 / 113→119, SUM 16→17 / 125→135 |
| **ж** зеркало подавления | 0 |
| Масок всего | 12574 → 12600 (на эталоне 10843 → 10875) |
| Исходы | processed 324, крешей 0, отказов 0; `MANIFEST.sha256` 659 OK до и после |

**Шесть красных строк — две сущности и один класс.** `sale_0009__m1337_combo`
(«ooo «tе хнопром»»): свод алфавитов, замкнутый по регистру, сделал латинскую
«t» кириллической — кавычная ветвь ORG название увидела, недобор 17→6.
`agency_0006__m1337_combo` («ч77 143,00 РуБ.»): регистронезависимое слово
валюты, `found=False→True`, недобор 17→10. По метрике недобор, по существу —
закрытая утечка.

**Реальный документ** (`C:\shifrator_real\dog.docx`): OGRN 33, PASSPORT 29,
BIK 1, **ORG 69**, PERSON 67, ADDRESS 28, EMAIL 10, PERCENT 84, TERM 77, SUM 8;
INN/INN_PER/KPP/ACCOUNT/SNILS/PHONE — 0. **Ложных нет** (числа совпадают с
замером A7 до единицы; детекция в слиянии тождественна `a7` по коду).
**Детерминизм**: 3 прогона в одном процессе — отпечаток набора спанов побитно;
на корпусе — тот же дамп, что у A7.

## 4. Собранная программа

`pyinstaller packaging/shifrator.spec` проходит; строгий страж спеки:
«hiddenimports покрывает все модули `src/` и `app/` (30 имён)» — слияние новых
непокрытых модулей не принесло. Живая проверка `dist/SHIFRATOR/SHIFRATOR.exe`
(порт из `launcher.lock`, НЕ угадывать): запуск 2,9 с, метка `build-20260729`,
защита ключа `dpapi`; шифрование `agency_0003.txt` — 18 масок; восстановление —
нерешённых токенов 0, масок 0; **сохранённая сессия открывается повторно**
(путь DPAPI → `vault` → Fernet в сборке живой). Два значения из 18 возвращаются
в канонической форме — этап E, к сборке отношения не имеет.

## 5. Хронометр

`.claude/hooks/chrono.py` пишет `.chrono/<session>.tsv` сам.
Сессия MERGE-1: 12:32–13:23, ~51 минута, 81 вызов, собственное время 1068 с.

## 6. Типы

**21 объявленный тип**, 18 маскируемых, 3 отрицательных класса-барьера.
Детектор есть у всех маскируемых, кроме `DATE` (выключен конфигом). Эталон v1 —
15 типов; `PERCENT`/`TERM` — только v2; `INN_PERSON` — только v1. Негативы —
у 17 из 18 (без `EMAIL`, осознанно); в v2 добавлен набор `negatives_a7`.

## 7. Рабочий контур документации

| Файл | Правило жизни | Потолок |
|---|---|---|
| `CLAUDE.md` (корень) | правится по месту | **150** строк (сейчас 133) |
| `docs/JOURNAL.md` | только дописывается снизу | нет |
| `docs/STATE.md` | перезаписывается целиком каждый этап | нет |
| `docs/ARCHITECTURE.md` | правится по месту | **400** строк |
| `docs/FINDINGS.md` | только открытые долги | нет |

Рядом неприкасаемые: `PRODUCT_LEGAL.md`, `NN_HISTORY.md`;
`known_leaks_stage_c.json` — реестр долга ADDRESS, читается гейтом. Остальное —
`docs/archive/`, опись переездов — `docs/archive/DOC_REGISTRY.md`; опись живых
инструментов разведки — `experiments/INDEX.md`. Замки в `.claude/hooks/`.

## 8. Что красное или висит

1. **`DOG-REPO-BLOB`** — см. §0. Решение владельца.
2. **`DOG-NODE-SILENT` + `DOG-ANCHOR-SPLIT`** — пять видов узлов OOXML не
   читаются и не объявляются зоной; рваный вёрсткой ЯКОРЬ — родственный класс.
   Фикстура готова.
3. **`A7-ORG-NOANCHOR-UNSOLVABLE`** (поглощает `Aprime-1`, `B-MASKSHIFT-ORG`) —
   организация без структурного якоря правилами НЕ РЕШАЕТСЯ; нужно решение
   владельца о цене (недобор сегодня / перебор на прозе от словаря родов
   деятельности / ярус recall на модели).
4. **Три инструмента, проверявших не то, что заявлено** — `V2-TRANCHE-NOGUARD`,
   хеш половины корпуса (закрыт INSTR), `INSTR-PKGSPEC-SUBSTRING` (открыт,
   латентный). Класс подтверждён; кандидат — системная проверка измерительного
   слоя, а не точечные починки.
5. **`A6-REQ-NAMERUN-OVERREACH`** — латентный ORG-якорь на чужой строке; закрыт
   инвариантом, корень не чинился.
6. **`A3-NEG-*`** (восемь массовых классов негативов) и
   **`A3-GATE-OVERMASK-LINE`** — без изменений, решение владельца.
7. **`STORE-ACCUM-CONFIRM`**, **`V2-WALL-NOGUARD`**, **yargy 73 % времени
   гейта** — без изменений. `STORE-HOME-CACHE` закрыт этапом STORE,
   **`experiments/` разобран** этапом EXP.
8. **Часть 5 STORE**: тексты экрана «Что хранится» приходят с сервера и ждут
   утверждения владельцем.

## 9. Где мы по этапам

Пройдено: **0a–0d**, **1/1b**, **2/2b**, **3**, **4**, **A/A′/B/C/C′**, **D**,
**E/E′/E″**, **O2**, **U1–U5b**, **S1–S3**, **T0/T0V/GENV**, **CORPUS-V2/V2-B**,
**T-GOLD-A**, **T1**, **T1-UI**, **U4-FIX**, **T2-INN**, **ADDR-B**, **BUILD**,
**GATE-2**, **T-ARB**, **T4**, **T2**, **S0**, **DOC**, **C+**, **SPEED**,
**TESTOPT**, **DOG**, **A1**–**A6**, **BASE-A6**, **EXP**, **INSTR**, **A7**,
**STORE**, **MERGE-1** (этот). По каждому — запись в [`JOURNAL.md`](JOURNAL.md).

Впереди: расширение негативов, `DOG-NODE-SILENT` (зона extract), системная
проверка измерительного слоя, решение владельца по безъякорному ORG.

## 10. Закрыто, но встречается в старых текстах

- **`A5-INN-KBK-FP` ЗАКРЫТА** этапом A7 (`precision[INN]` 100,00 %); из
  `carried_debts` она снята и в новую точку отсчёта не перенесена.
- **`Stage3-A`, `TGOLDA-SUM-CASE`, `Stage2b-A`, `ADDRB-BIRTHDATE-EXPOSED`,
  `Stage4-C`, `A3-NEG-OGRN`** — закрыты этапом A7.
- **`STORE-HOME-CACHE` ЗАКРЫТА** этапом STORE (путь переспрашивается при
  вызове + autouse-фикстура `isolated_home` на весь прогон).
- **xfail-контракт «дата рождения с двойниками не маскируется» ИСПОЛНИЛСЯ** —
  маркер снят, тест остался стражем (`test_addr_b_boundaries.py`).
- Ветки блока (`exp`, `instr`, `a7`, `store`) слиты в `main`; их рабочие
  деревья `C:\Jesus\ARP_*` ещё существуют — удалять их решение владельца.
