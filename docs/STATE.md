# STATE — живой снимок SHIFRATOR

**Этот файл перезаписывается целиком в конце каждого этапа.** Здесь только «как
сейчас». История — [`JOURNAL.md`](JOURNAL.md), он только дописывается.
Устройство — [`ARCHITECTURE.md`](ARCHITECTURE.md). Открытые долги —
[`FINDINGS.md`](FINDINGS.md), раздел «РЕЕСТР ОТКРЫТЫХ ДОЛГОВ» — единый список.

**Снимок на: 2026-08-18**, этап **PAREN-ZERO** (свод хвостовых пустых скобок
«500 ()()()» в шумовом классе `paren_zero`, ветка `pdf-read`), в `main`,
**запушено**. До него — PER-SPREAD, SINGLE-GUARD, MERGE-W1, BUILD-NOW,
DEFAULT-GATE, METRIC-FIX, REG.

**Гейт КРАСНЫЙ по 16 строкам — состав НЕ ИЗМЕНИЛСЯ этим этапом**, совпадает
слово в слово с точкой отсчёта PER-SPREAD (`experiments/per_spread/gate_per_spread.log`):
шесть строк линии «б» (`KPP`/`BIK`/`PASSPORT`), четыре строки линии «г»
(`masking B`), две строки линии «д» (`over-mask` ORG), четыре строки линии «е»
(недобор `PHONE`/`EMAIL`). Правка этого этапа лежит в `src/normalizer.py`
(свод OCR-артефакта пустой скобки) и НЕ затрагивает корпус v1 — класс
`paren_zero` в нём не встречается, только в шумовом корпусе v2/noise.
Набор тестов этим этапом: **1849 passed, 3 skipped, 6 xfailed, 0 failed**.
Круг на собранной программе: **ПРОЙДЕН** (20 шагов, пересборка exe) — этап
тронул `src/`, круг обязателен по запрету 9.

**Замер шумового корпуса, класс `paren_zero` (11 классов, `tests/corpus_v2/noise`,
32 док. / 948 парных вхождений), ДО/ПОСЛЕ ОДНИМ прогоном штатного прибора
`tests/corpus_v2/measure_noise.py` на одном и том же коде постройки (коммит
слияния `2c8d4d1`, с/без правки `PAREN-ZERO`):** утечка грязн. **69.5 % → 59.3 %**,
полнота грязн. **25.4 % → 35.6 %**, потеряно **36 → 30** (из 59). Остальные
10 классов и ложные срабатывания (347/336) числом не сдвинулись — эффект
локален правке. **Найдена и НЕ разобрана находка `NOISE-METRIC-MISMATCH`:**
число ДО в этом прогоне (69.5 %) не совпадает с числом из точки отсчёта
`JOURNAL.md`/этап NOISE-CORPUS (89.8 %) на том же классе и `n` — подробности
и гипотеза в `FINDINGS.md`.

Источник истины — код и прогоны. Разошёлся с ними — прав код, а файл чинится.

---

## 0. Решения владельца, которых ждёт работа

1. **Пересборка точки отсчёта гейта — ОДНА НА ВСЁ РАЗОМ.** Обоснование готово
   (§3, §3в), красное объяснено, планка этим этапом НЕ двигалась. До пересборки
   гейт будет краснеть на каждом прогоне.
2. **`DOG-REPO-BLOB`** — реальный договор с ПДн в истории ПУБЛИЧНОГО
   репозитория. Локально вычищено, удалённое — нет. Самый дорогой открытый долг.
3. **Остаток класса вёрстки** (`LAYOUT-RESIDUE`): 2/3 упирается в регистровый
   класс (`Aprime-1`) — решение о сессии регистрового класса не принято.
4. **`A7-ORG-NOANCHOR-UNSOLVABLE`** — организация без якоря правилами не
   решается; нужно решение о цене.
5. **`STORE-ACCUM-CONFIRM`** — звать ли `purge_expired` из интерфейса.
6. **Окно yargy вокруг сида** — оставшиеся ~3/4 времени разбора адреса.
7. **`A3-GATE-OVERMASK-LINE`** — заводить ли линию гейта на переусердствование.
8. **`ENV-LOCK-INCOMPLETE`** — где держать три недостающих шага поднятия
   окружения (§8): отдельный dev-лок или строка в `packaging/CLAUDE.md`.

## 1. Проверить состояние двумя командами

```
venv/Scripts/python.exe -m pytest -q            # набор тестов, ~11.5 мин
venv/Scripts/python.exe tests/corpus/gate.py    # полный гейт, 324 док.
```

Гейт умеет оценить ГОТОВЫЙ прогон без пересчёта корпуса:
`gate.py --results tests/corpus/results_gate_current.json`.

## 2. ЧТО ПРОГРАММА ЗАЩИЩАЕТ И НАСКОЛЬКО — сводная картина

**Четыре оговорки читать ДО чисел, они в силе для всей таблицы.**

1. **Цифры на корпусах — верхняя граница, а не измерение.** Оба корпуса
   ПОРОЖДЕНЫ генератором: конструкция, которую генератор не предусмотрел, не
   появится ни в тексте, ни в разметке, и её отсутствие будет невидимым
   (`E2-CORPUS-POVERTY`). Размеченных реальных договоров нет ни одного.
2. **Реальный документ разметки не имеет вовсе.** По нему считаются только
   КОЛИЧЕСТВА масок и проверка значений глазами. **Этим этапом реальный документ
   НЕ гонялся** — его на этой машине нет (запрет 7), и колонка ниже перенесена
   из снимка DEFAULT-GATE как есть.
3. **`CONTRACT_NO` исключён из точности корпуса v1** — v1 заморожен с ОБРАТНЫМ
   определением. Цена исключения: 316 расхождений разметки (`V1-SCOPE-CONTRACTNO`).
4. **Числа этого снимка НЕ сравнимы с числами до 2026-08-09 напрямую.** Прибор
   починен дважды (§3): утечка выросла на 41 вхождение, полнота — на 60. Обе
   дельты — конец слепоты, а не изменение системы.

### 2.1. По типам

`полн.` — полнота (найдена маска своего типа), `утеч.` — доля вхождений, от
которых в анонимном тексте дожил фрагмент (v1: `leak_v2` ≥ 6; v2: позиционная
метрика), `точн.` — tp/(tp+fp на ОБЪЯВЛЕННОМ негативе), `fp` — число масок на
объявленном негативе. Прочерк — эталона этого типа в корпусе нет.
**Колонки v1 сняты прогоном этого этапа; колонки v2 и реального документа
перенесены из снимка DEFAULT-GATE (этим этапом не перемерялись).** Строка `PER`
сдвинулась этапом PER-SPREAD: полнота 76.9 → 80.3 %, утечка 26.1 → 18.0 %
(894 → 615 вхождений). Прочие строки v1 не изменились НИ НА ЕДИНИЦУ.

| тип | v1: полн. | v1: утеч. | v1: точн. | v1: fp | v2: полн. | v2: утеч. | v2: точн. | v2: fp | реальный документ |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| PER | **80.3 %** | **18.0 %** | 99.97 % | 1 | — | — | — | 0 | 67 масок |
| ORG | 97.4 % | 19.0 % | 100.00 % | 0 | — | — | — | 0 | 70 |
| ADDRESS | 94.5 % | 16.1 % | 99.92 % | 1 | — | — | — | 0 | 30 |
| PASSPORT | **84.4 %** | **15.4 %** | 100.00 % | 0 | 100.0 % | 0.0 % | 74.44 % | 138 | 29 |
| PASSPORT_ISSUER | — | — | — | 0 | 100.0 % | 0.0 % | 100.00 % | 0 | 2 |
| BIRTHDATE | 95.6 % | 4.4 % | 100.00 % | 0 | 100.0 % | 0.0 % | 26.60 % | 276 | 0 |
| SNILS | 97.8 % | 1.8 % | 100.00 % | 0 | 100.0 % | 0.0 % | 42.02 % | 138 | 0 |
| PHONE | 93.7 % | 3.8 % | 100.00 % | 0 | 97.1 % | 4.3 % | 60.10 % | 328 | 0 |
| EMAIL | 100.0 % | 0.0 % | 100.00 % | 0 | 100.0 % | 0.0 % | 100.00 % | 0 | 10 |
| INN_PER | 95.6 % | 4.9 % | 100.00 % | 0 | — | — | — | 189 | 0 |
| INN | 78.3 % | 7.6 % | 100.00 % | 0 | 29.3 % | 3.1 % | 28.73 % | 315 | 0 |
| OGRN | 96.7 % | 3.3 % | 100.00 % | 0 | 100.0 % | 0.0 % | 100.00 % | 0 | 33 |
| KPP | 92.3 % | 7.7 % | 100.00 % | 0 | 100.0 % | 0.0 % | 100.00 % | 0 | 0 |
| ACCOUNT | 94.7 % | 1.4 % | 100.00 % | 0 | 96.7 % | 3.3 % | 37.88 % | 492 | 0 |
| BIK | 95.6 % | 4.4 % | 100.00 % | 0 | 100.0 % | 0.0 % | 83.11 % | 38 | 1 |
| SUM | 98.9 % | 2.6 % | 100.00 % | 0 | 88.8 % | 33.7 % | 70.04 % | 222 | 20 |
| PERCENT | — | — | — | 0 | 100.0 % | 0.1 % | 77.41 % | 230 | 81 |
| TERM | — | — | — | 0 | 98.6 % | 1.4 % | 100.00 % | 0 | 77 |
| CONTRACT_NO | — | — | **исключён** | 316 | 52.6 % | 47.4 % | 100.00 % | 0 | 0 |
| CONTRACT_KIND | — | — | — | 0 | 100.0 % | 0.0 % | 100.00 % | 0 | 39 |
| REGISTRY_NAME | — | — | — | 0 | 100.0 % | 0.0 % | 100.00 % | 0 | 0 |
| REG_ID / REG_NUMBER | — | — | — | 0 | 100.0 % | 0.0 % | 100.00 % | 0 | 1 / 6 |
| SHARE_PCT | — | — | — | 0 | 100.0 % | 0.0 % | 100.00 % | 0 | 3 |
| TRANCHE | — | — | — | 0 | 100.0 % | 0.0 % | 100.00 % | 0 | 0 |

**Жирным — клетки, которые сдвинул этап DEBTS** (спан паспорта теперь начинается
со значения, долг `0c-B` закрыт): полнота `PASSPORT` 87.8 → 84.4 %, утечка
14.9 → 15.4 %. Цена названа своей веткой заранее и подтверждена этим прогоном —
это те же 4 мутанта; плата за то, что перебор паспорта упал с 428 сущностей до
нуля (§3в).

**Три типа блока 4 (`PASSPORT_ISSUER`, `REGISTRY_NAME`, `CONTRACT_KIND`) в
разметке v1 отсутствуют вовсе** — корпус заморожен до их появления.

**Про `fp` на корпусе v2 — обязательная оговорка.** На каждый тип заведён один
(у части типов два) вид отрицательного примера, повторённый в 138 документах.
«138 из 138» означает **одну дыру, повторённую 138 раз**, а не масштаб
проблемы.

### 2.2. По продукту целиком

Колонка v1 — прогон этого этапа; v2 и реальный документ перенесены.

| | корпус v1 (324 док.) | корпус v2 (138 док.) | реальный документ |
|---|---|---|---|
| эталонных вхождений | 12 033 | 8 249 | разметки нет |
| полнота, агрегат BIK-excl | **88.8 %** | по типам, см. выше | — |
| утечка ≥ 6, агрегат BIK-excl | **13.1 %** (1513 сущн.) | по типам | — |
| ложных на объявленном негативе | **318** (из них 316 — `CONTRACT_NO`, исключение) | 2366 | 0 |
| масок, не легших ни на что | **2544** (+25 к точке отсчёта — маски ORG в колонтитулах, эталон v1 эту зону не размечает) | 484 | — |
| обратимость (masking A) | **100.00 %** | 100.00 % | 3/3 побитно |
| границы масок (masking B) | **88.18 %** | 95.31 % | — |
| тип маски (masking C) | **96.34 %** (мягкий уровень, гейт не роняет) | 94.36 % | — |
| крешей | **0** | 0 | 0 |
| перебор границ, ВСЕГО | **51 сущн. / 480 симв.** (было 479 / 3921) | — | — |
| недобор границ, ВСЕГО | **472 сущн. / 3359 симв.** | — | — |

### 2.3. ПО СУЩНОСТЯМ, а не по вхождениям — главная цифра для человека

**Числа перенесены из снимка DEFAULT-GATE: `tests/corpus/by_entity.py` этим
этапом НЕ гонялся.** После правки границ паспорта (DEBTS) они верны на свою
дату, а не на сегодня — пересчитывать их отдельным замером.

| | по вхождениям | по сущностям | разница |
|---|---:|---:|---:|
| ПРЯМЫЕ идентификаторы — найдено | 86.17 % | 85.82 % | −0.35 пп |
| ПРЯМЫЕ — защищено целиком | 79.41 % | 81.35 % | +1.94 пп |
| КОСВЕННЫЕ — найдено | 94.33 % | 94.04 % | −0.29 пп |
| КОСВЕННЫЕ — защищено целиком | 88.64 % | 88.72 % | +0.08 пп |
| ВСЕГО — найдено | 89.31 % | 89.36 % | +0.05 пп |
| ВСЕГО — защищено целиком | 82.96 % | 84.52 % | +1.56 пп |

**Самое слабое место по-прежнему люди, но место сдвинулось.** Сущностей типа
PER 2272 по поверхности значения; сведённых по фамилии — **1824 человека**, из
них защищено целиком (строгий критерий: все вхождения замаскированы И ничего не
утекло) **66.50 %** — было 62.28 % до этапа PER-SPREAD. То есть **у 611 человек
из 1824 в документе остаётся открытым хотя бы одно упоминание** (было 688).

По продуктовому критерию (НЕ УТЁКЛО ничего — то, что реально уходит в LLM)
защищено целиком **82.35 %** против 76.64 % до этапа. Два числа расходятся
законно: строгий требует ещё и маску СВОЕГО типа, а у 361 значения `PER` маска
стоит под именем `ORG` (долг `DEFGATE-CROSSTYPE-LATENT`, §3аа).

## 3. Прибор: что починено (этап METRIC-FIX) и почему гейт красный

**3.1. Раздроблённое значение.** Утечка мерилась самым длинным НЕПРЕРЫВНЫМ
окном ядра, поэтому значение, разорванное вёрсткой на куски короче порога 6,
получало «утечки нет», будучи открытым целиком («704\n-068»). Принято правило
ВОССТАНОВИМОСТИ: куски засчитываются вместе, если дожили ПОДРЯД — соседними
цифровыми группами, в порядке ядра, без пропуска, с зазором ≤ 3 символов.
Стало видно **41 вхождение**: PASSPORT 36, KPP 3, BIK 2.

**3.2. Ложное «не найдено» в колонтитулах.** Сегменты колонтитулов лежат в PT-1
ВНЕ тела, а локализация искала их от начала тела — и не находила никогда.
Теперь у каждой части пакета свой участок PT-1 и свой курсор. Перестали ложно
считаться ненайденными **60 вхождений** (PHONE 30 + EMAIL 30).

**3.3. Узость обеих правок доказана сличением дампов** на неизменном `src/`:
часть 1 — 11992 сущности побайтно, расхождений 41 и все в поле `leak_v2` в
направлении `none → full`; часть 2 — 11973 побайтно, расхождений 60 и все ВНЕ
тела. Число масок не изменилось: 14435 → 14435.

**3.4. Шестнадцать строк от METRIC-FIX.** Восемь строк линии «б» — те самые
41 раздроблённых значения. Две строки линии «д» — over-mask прозы ORG 322 → 347:
25 масок организаций в колонтитулах, которые эталон v1 в этой зоне не размечает
вовсе; масок не прибавилось ни одной, прибавилось ВИДИМЫХ. Шесть строк линии
«е» — недобор PHONE +1/+7 символов и EMAIL +1/+6.

## 3а. Набор ПО УМОЛЧАНИЮ: восьмая линия гейта (этап DEFAULT-GATE)

**Гейт меряет только на ПОЛНОМ наборе типов, а пользователь работает на наборе
ПО УМОЛЧАНИЮ.** Значит целый класс дыр был невидим ПО ПОСТРОЕНИЮ: значение,
которое на максимуме закрывает маска ДРУГОГО типа, при настройках по умолчанию
уходит открытым.

Линия «з» считает эталонную сущность типа ИЗ НАБОРА ПО УМОЛЧАНИЮ, закрытую на
максимуме и открытую по умолчанию. Требование двойное: число не растёт (допуск
0) И каждый случай документирован реестром
`tests/corpus/known_default_leaks.json`.

**Сейчас: 6 случаев на v1** — `INN_PER` 4, `ADDRESS` 1, `PHONE` 1; состав
совпал с реестром цифра в цифру на прогоне этого этапа (было 380 до
DEFAULT-GATE). На v2 — 3 случая (все `ADDRESS`). У остатка ДРУГОЙ механизм:
это ОШИБКА ТИПА, а не вытеснение — `DEFGATE-CROSSTYPE-RESIDUE`, разложен на три
долга (§7.3).

## 3аа. Значение под ЧУЖОЙ маской: девятая линия гейта (этап SINGLE-GUARD)

**Линия «з» этот класс не видит — и не по ошибке, а по построению.** Она меряет
РАЗНИЦУ двух раскладок масок; у значения, которое на максимуме спрятано маской
чужого типа, а по умолчанию — маской своего, разница нулевая.

Линия «и» считает то же ружьё ДВУМЯ единицами, и обе печатаются рядом:

* **431 маска неверного типа** (эталон типа из набора по умолчанию закрыт
  маской типа ВНЕ набора) — ровно тот счёт, которым мерила разведка
  `experiments/cross_pairs.py`, теперь он живёт в `measure_lib.crosstype_mask_pairs`;
* **394 значения под ОДИНОЧНОЙ защитой** — эталонные сущности, у которых на
  максимуме нет ни одной маски СВОЕГО типа: `PER` 361, `PASSPORT` 32, `SNILS` 1.
  Поимённо — `tests/corpus/known_single_guard.json`.

Числа выросли с 409/375 этапом PER-SPREAD, и рост объяснён: найденный человек,
записанный как «ИП + ФИО», склеивается `syntax_compound` в одну сущность с
именем `ORG` — то есть новонайденный попадает в этот класс сразу, минуя
состояние «защищён своим типом». Реестр пересобран; из класса не выпало НИ
ОДНО значение (только в дампе 19, только в реестре 0).

**Уровень линии двойной.** Число — МЯГКИЙ (предупреждение в каждом отчёте):
сегодня значение закрыто, ронять гейт нечем. Поимённая сверка реестра —
ЖЁСТКИЙ, допуск 0: реестровый случай, ставший открытым, красит гейт. Второе
важнее первого, потому что переход тихий: потеряв маску, случай ВЫПАДАЕТ из
класса и уменьшает оба счётчика сразу — то есть выглядит улучшением, а линия
«з» продолжает молчать (обе раскладки одинаково дырявы). Проверено нажатием:
подставной случай на живом дампе дал 18-ю красную строку при счёте, упавшем
375 → 374, и при неподвижной линии «з».

Арбитраж этим этапом НЕ трогался: смена победителей двигает маски по всему
корпусу и остаётся решением владельца. Сделан прибор, не лечение.

## 3б. Две находки продукта — ЗАКРЫТЫ (этап DEFAULT-GATE)

* **`MFIX-PROFILE-IP-PER`.** ФИО внутри имени ИП уходило открытым в наборе по
  умолчанию. Механизм оказался не в арбитраже, а в СКЛЕЙКЕ: `syntax_compound`
  поглощает ФИО в составную `ORG`. Теперь склейка запоминает поглощённое имя
  (`Entity.shadowed`), а `tokenizer._recover_shadowed` возвращает его под маску,
  когда победивший тип выключен.
* **`MFIX-CANON-FEMSURN`.** Канон восстановления делал женскую фамилию мужской
  («Кузнецова» → «Кузнецов»). Теперь род спана сильнее score.
* Обе под стражей круга: шаги 6 и 10 проверяют их на собранной программе.

## 3в. Что добавил этап BUILD-NOW к красноте — одна строка

Семнадцатая строка — **линия «г», `masking B / PASSPORT` 90.97 → 90.75 %**. Её
принесла ветка `debts`, назвав цену заранее: закрытие долга `0c-B` (спан
паспорта начинается со значения, а не со слова «паспорт») убрало перебор
`PASSPORT` **428 → 0 сущностей и 3441 → 0 символов** (ВСЕГО по корпусу
479 → 51 и 3921 → 480), а платой стали те же 4 мутанта в границах. Агрегат
`masking B` при этом ВЫРОС: 88.14 → 88.18 %.

**Четыре предупреждения гейта — не краснота, все по устройству:** у линии «з»
нет поля в точке отсчёта (снята до появления линии, судим по реестру); две
строки линии «и» — число «под чужой маской» 375 значений / 409 масок и его
дельта к точке отсчёта 380 → 409 (§3аа), уровень МЯГКИЙ; `masking C`
96.58 → 96.34 % — МЯГКИЙ уровень решением владельца, печатается, гейт не
роняет.

## 4. Корпуса и наборы типов

Корпус v1: 324 док. (162 `.txt` + 162 `.docx`), 12 033 эталонных вхождения,
**заморожен навсегда**, `MANIFEST.sha256` — 659 файлов, OK до и после прогона.
Корпус v2: 138 док., 8 249 эталонных сущностей, 11 537 негативов,
`MANIFEST.sha256` — 314 файлов.
Набор по умолчанию «Только персональные данные» — 9 типов.
`entity_types.yaml` — СОБРАННЫЙ артефакт; после слияния пересобран из источников
(дельта 0, `assemble_types.py --check` зелёный).

## 5. Собранная программа и обратимость

Поставка: `dist/SHIFRATOR/SHIFRATOR.exe`, **126 МБ, 1070 файлов**, метка
`build-20260729` (метка в спеке не обновлялась — это ярлык, а не версия кода).
Все три стража спеки зелёные: окружение совпадает с локом (17 пакетов,
python 3.11.9), файлы данных на месте (21), `hiddenimports` покрывает все модули
`src/` и `app/` (31 имя).

Круг `packaging/accept_circle.py` — **ПРОЙДЕН, 17/17**: `.txt` 28 масок,
`.docx` **24** маски, ожидаемые значения исчезают и возвращаются байт-в-байт,
ФИО внутри имени ИП исчезает, канон возвращает ТОГО ЖЕ человека, сессия
открывается повторно по id. Одно замечание (печатается, круг не роняет):
восстановленный текст побайтно НЕ равен исходному — восстановление подставляет
КАНОН (именительный падеж) вместо исходной падежной формы.

## 6. Контрольная сумма: две дороги, два разных правила

**Записано решением владельца (этап CROSSTYPE, 2026-08-10).** Противоречия между
инвариантом этапа 4 и стражем сборки A6 НЕТ — они про РАЗНЫЕ ДОРОГИ:

* **Посегментная детекция** (`regex_detector.detect_regex`) — значение лежит в
  тексте ЦЕЛИКОМ. КС — **СИГНАЛ**: под якорем значение маскируется даже с
  несошедшейся суммой. Цена ошибки — лишняя маска, порча документа, не утечка.
* **Сборка через стык** (`multispan._match_seam_type`) — значения в тексте НЕТ,
  оно КОНСТРУИРУЕТСЯ из кусков. КС — **ШЛАГБАУМ**: последняя проверка, что
  собран реквизит, а не совпадение длин. Цена ошибки хуже: неверная маска и
  неверное ВОССТАНОВЛЕНИЕ.

Практическое следствие, замеренное и отказанное: снятие КС со стража сборки
закрывает 4 из 6 случаев линии «з» и роняет утечку на 15 сущностей, но **на
реальном документе не даёт ничего**. Долг оставлен открытым —
`DEFGATE-SEAM-CHECKSUM-TRADEOFF`.

## 7. Что красное или висит

1. **Гейт красный по 17 строкам** — 16 от METRIC-FIX (§3.4) и 1 от DEBTS (§3в).
   Новых нет, вся краснота ждёт ОДНОЙ пересборки точки отсчёта. **Планка этим
   этапом НЕ двигалась** (`promote_baseline.py` не запускался).
2. **Обе находки продукта ЗАКРЫТЫ** (§3б) и обе под стражей круга.
3. **Остаток класса набора по умолчанию** — `DEFGATE-CROSSTYPE-RESIDUE`:
   6 случаев на v1 и 3 на v2, механизм — ошибка ТИПА. Разложен на три долга:
   `DEFGATE-SEAM-CHECKSUM-TRADEOFF` (4 случая, починка замерена и ОТКАЗАНА, §6),
   `DEFGATE-PHONE-AS-INN` (1), `DEFGATE-ADDR-TAIL-ORG` (1). Шире линии «з» тот
   же класс — `DEFGATE-CROSSTYPE-LATENT`, **409 масок / 375 значений** верного
   значения ЧУЖОГО типа (замер 2026-08-18, прежняя цифра 379 — от 2026-08-10).
   Сегодня прикрыты и потому не утечка; с этапа SINGLE-GUARD число печатается
   линией «и» каждым прогоном, а состав охраняется реестром (§3аа). Лечение
   (типизация) не делалось.
4. **`ENV-LOCK-INCOMPLETE`** (заведён этим этапом, §8).
5. Открытые долги — единым списком в [`FINDINGS.md`](FINDINGS.md).
6. Самое дорогое из списка: `DOG-REPO-BLOB`, `LAYOUT-RESIDUE`, защита людей
   62.28 % (§2.3).

## 8. Окружение этой машины — поднято с нуля этапом BUILD-NOW

`venv` собран ровно на локовом питоне **3.11.9 AMD64** (`py install 3.11-64`) по
`packaging/requirements-build.lock.txt`; сверка 17 критичных пакетов с локом —
**расхождений 0** (и до, и после доставленных ниже пакетов).

**Лока НЕ ХВАТАЕТ, чтобы прогнать набор тестов** — найдено прогоном, записано
долгом `ENV-LOCK-INCOMPLETE`. Доставлено вручную три вещи, ни одна не входит в
`_PINNED_CRITICAL`, в exe не попадает и на детекцию не влияет:

```
venv/Scripts/python.exe -m pip install pytest-xdist   # pytest.ini требует -n auto
venv/Scripts/python.exe -m pip install wheel          # без него не встаёт editable
venv/Scripts/python.exe -m pip install -e . --no-build-isolation
```

Третья строка обязательна: подпроцессные тесты `test_g_regression.py` импортируют
`file_detokenizer` из произвольного `cwd`, и без editable-установки они красные
(`pyproject.toml:22-24`, `conftest.py`).

## 9. Ветки

**В `main` влиты этим этапом:** `pdf-read` (свод хвостовых пустых скобок,
`PAREN-ZERO`). **Запушено.** Рабочее дерево `ARP-pdf` и ветка `pdf-read`
удалены после слияния.

**Прежде — этапом MERGE-W1:** `tools-fix` (`cb09b38`), `paths-fix`
(`3777694`). Рабочие деревья `ARP-tools`/`ARP-paths` и обе ветки удалены после
слияния.

**Прежде — этапом BUILD-NOW:** `client` (`06dbb25`), `crosstype` (`97e9080`),
`debts` (`0468348`).

**НЕ влиты намеренно — исследовательские, ни одна не трогает `src/`:**
`per-recon`, `arch-probe`, `r-rdr`, `r-guarantee`. Это отчёты и харнессы
разведки в `experiments/research/`, в продукт они не идут.

**PDF-ARCH** сдан замыслом без кода — код по нему не влит и не начат, ветки
в `main` нет.

Старые слитые ветки на сервере (`a7`, `audit`, `debt1`, `exp`, `instr`,
`markup2`, `neg`, `store`, `types2`, семь `stage-*`, `u1-desktop-packaging`)
можно снимать — решение владельца, этот этап их не трогал.
