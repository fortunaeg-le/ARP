# STATE — что в коде SHIFRATOR прямо сейчас

Составлен/актуализирован: **2026-07-19**, на коммите этапа 2b (ветка `main`,
регистровая детекция ФИО). Источник истины — **код** (`src/`, `shifrator.py`,
`entity_types.yaml`) и **тесты**. Этот файл — их короткий слепок, ≤200 строк. Если он
расходится с кодом — прав код, чини этот файл.

## Как пользоваться

**Для старта любого этапа читать ТОЛЬКО этот файл** (+ `ARCHITECTURE.md`, если правишь
логику детекции; `CODEMAP.md`, если ориентируешься в коде). Летопись — в `archive/`
(`docs/archive/INDEX.md`), читать только при аудите провенанса/цифр, не для работы.

---

## 0. Как проверить состояние за одну команду

```
venv/Scripts/python.exe -m pytest -q
```
⚠ **Эталон ниже УСТАРЕЛ, и на HEAD `abf3ee8` прогон НЕ зелёный.** Фактически:
**1 failed, 976 passed, 3 deselected, 10 xfailed (~880 c)**. Единственное
падение — `tests/component2/test_g_regression.py::test_existing_block1_7_suite_still_fully_green`,
находка **U4-G-TIMEOUT** в [`FINDINGS.md`](FINDINGS.md): вложенный прогон внутри
этого теста стоит теперь 488 c против ЖЁСТКОГО `timeout=300`. **Это НЕ ложный
таймаут от нагрузки** (см. абзац ниже — он про другое и к этому случаю больше не
применим): воспроизведено на простаивающей машине И на чистом `HEAD` в отдельном
worktree, то есть падение ПРЕД-существующее, не внесено ни этапом U4, ни
сессией S3. Не привыкать игнорировать: 976 остальных тестов зелёные, и любая
НОВАЯ краснота на этом фоне обязана быть замечена.

Исторический эталон (до влития E′/E″): **933 passed, 2 deselected, 10 xfailed**
(~485 c; этап S1, 2026-07-25: +13 тестов `tests/test_storage_s1.py`, см. §6).
Другой интерпретатор (`natasha` не установлена) — ошибки сбора; рабочий только
`venv/Scripts/python.exe`.

10 `xfailed` — намеренно отложенные дефекты/дыры покрытия, помечены `@xfail(strict=True)`:
если станут проходить незамеченно — тест «покраснеет» (XPASS→FAIL), это сигнал снять метку.
Крупнейшая группа — `tests/test_future_contracts.py` (карта «дыра → этап», см. §4).

Если в сессии всё равно будет запущен `tests/corpus/gate.py` (полный корпус,
условие 1 = креши + leak_v2/FP/masking) — отдельный `pytest -m slow`
(`test_full_corpus_encrypt_never_crashes`, тоже полный корпус, только креши)
не нужен: гейт строго надмножество, гонять оба — два прогона Natasha ради
одной проверки (docs/archive/reports/EFFICIENCY_AUDIT.md §2.1). Между
правками внутри сессии — `tests/corpus/subsample.py` (без `--флаги` или с
`--trick`/`--type`/`--feature`), быстрая ОТЛАДОЧНАЯ подвыборка 20-50
документов; НЕ замена `gate.py` перед коммитом. Ложный таймаут
`test_g_regression` = конкуренция за CPU с параллельным полным прогоном
(`gate.py`/`pytest -m slow` в другом терминале той же сессии), НЕ дефект —
при таймауте сначала проверь нагрузку, не диф. **С 2026-07-25 одного этого
объяснения НЕДОСТАТОЧНО**: тот же тест падает и на ПРОСТАИВАЮЩЕЙ машине, потому
что вложенный прогон перерос бюджет `timeout=300` сам по себе (488 c) —
находка **U4-G-TIMEOUT**. Порядок разбора теперь: (1) проверить нагрузку;
(2) если машина свободна — это U4-G-TIMEOUT, а не твой диф.

## 1. Статус релиза

Активной живой утечки ПДн, не покрытой xfail или §3, в коде нет. Полноценного релиза
нет: не начаты компонент 1 (веб) и PDF-извлечение; охват покрытия неполон (§3).

## 2. Актуальные цифры (полный корпус, 324 док., 0 крешей; BIK-excl; этап 2b)

| метрика | значение |
|---|---:|
| recall (found) | **77.1%** |
| leak_v2 ≥6 (мягкий порог) | **23.1%** |
| leak_v2 ≥8 (строгий порог) | **19.4%** |
| в т.ч. ADDRESS leak_v2 ≥6 | **10.8%** (было 32.7% — метрика, не система) |
| FP на негативах | **1975** (этап 2b: побайтно не изменились) |
| **masking_correctness A** (round-trip, знам. — 18571 масок) | **100.0%** |
| **masking_correctness B** (границы, знам. — 10207 масок на эталоне) | **79.59%** |
| masking_correctness C (тип, мягкий, тот же знам.) | 90.81% |

⚠ **masking_correctness C = 90.81% — это НЕ precision** (частая путаница, помечена в
FINDINGS D-PRECISION-90): знаменатель B/C — только маски, ЛЁГШИЕ НА GOLD, поэтому C
СЛЕП к over-маскированию (маска на негативе/прозе в него не входит). Честная precision
(этап D, единая FP-дефиниция) — ORG 99.77% / PER 99.96% / ADDRESS 99.92% на C
(ADDRESS 66.86% на HEAD b-per). См. §«Этап D» ниже.

**Этап 2b (регистр ФИО), 2026-07-19** — правка только в `src/extractor.py`:
PER recall 68.9→**72.0%**, PER leak_v2 44.0→**37.6%** (−217 сущностей); на сплошь
строчных ФИО recall был РОВНО 0.0%, на ЗАГЛАВНЫХ 16.0%. Инвариант этапа —
**ноль новых утечек реального текста** — выдержан поштучно (0 новых при 231
устранённой), FP не изменились ни по одному типу. B 79.76→79.59% — единственный
остаточный регресс, и он НЕ про потерю текста: во всех 6 случаях открыт только
ПРОБЕЛ (Natasha рвёт косвенный падеж двумя спанами — пред-существующий дефект
**PER-B**, обнажённый переводом ФИО с неверного типа ORG на верный PER, отчего C
и вырос). Baseline сдвинут по тому же критерию, что при починках метрик: значение
не теряется. Осталось PER-границам: PER-B (склейка соседних спанов), **Stage2b-A**
(combo: регистр × омоглиф), **Stage2b-B** (PER × ADDRESS через жадный
`AddrExtractor`; после починки адреса порог `_MIN_CAPS_RUN` можно вернуть к 2).
См. [`archive/reports/HANDOFF_STAGE_2B.md`](archive/reports/HANDOFF_STAGE_2B.md).

Этап 4 (КС — сигнал уверенности, а не шлагбаум): INN recall 71.6→**82.6%** (leak_v2≥6
18.0→10.8%), OGRN 77.4→**89.5%** (leak 13.3→6.4%). Асимметрия «якорь/без якоря»
(`src/regex_detector.py` `_has_anchor`, `entity_types.yaml` `anchor:` у INN/OGRN/
BANK_ACCOUNT): под якорём («ИНН»/«ОГРН»/«р/с» и т.п. слева, окно 40 симв., не
пересекает `\n`/цифру, устойчиво к латиница↔кириллица омоглифам якоря — см.
`_fold_anchor_context`) КС больше не отбраковывает; без якоря КС по-прежнему фильтр.
BANK_ACCOUNT своей КС не имеет — без якоря голое 20-значное число теперь вообще не
берётся, это и убрало КБК-FP (было ~321 срабатывание ACCOUNT на КБК/бюджетных кодах).
Два xfail-контракта этапа 4 в `test_future_contracts.py` ПОГАШЕНЫ, метки сняты
(`--runxfail` подтверждён). Три остаточных «регресса» leak_v2 (KPP/ACCOUNT, 6
документов) — метрика leak_v2 путала случайное совпадение подстроки открытого КБК с
утечкой НИКАК не связанной сущности (Stage4-A, **закрыт починкой числовой ветки
2026-07-19** — см. ниже). Два реальных, но пре-существующих
(вскрытых, не внесённых этапом 4) — PHONE в 2 адверсариальных документах (Stage4-B).

`masking_correctness` — корректность ТОГО, ЧТО СИСТЕМА ЗАМАСКИРОВАЛА; знаменатель — **маски
системы, не gold** (recall/leak: «сколько поймали»; эта метрика: «из пойманного, сколько
спрятано корректно»). Введена контрактом MC, см.
[`archive/reports/HANDOFF_CONTRACT_MC.md`](archive/reports/HANDOFF_CONTRACT_MC.md).

**Метрика ADDRESS починена** (2026-07-19, `measure_lib.leak_v2_address`; `src/` не
тронут): сравнение компонентов переведено с ПОДСТРОКИ на границы слов (убрало вечно
красный адрес: «пер» больше не находится внутри «передать», дом «12» — внутри «2024»),
добавлена ветка усечённого топонима (префикс ≥6 букв И ≥75% длины — «Екатеринбур»
считается утёкшим), топоним признан значимым сам по себе, голый номер дома — нет.
ADDRESS leak_v2 32.7%→**11.1%** (−284 подстрочных ложных, +1 реальная утечка
топонима); не-ADDRESS типы и masking_correctness A/B/C — байт-в-байт прежние.
Дыра **0c-H закрыта** (занижение), вместе с ней — завышение подстроками, описанное в
`archive/reports/FUTURE_CONTRACTS.md` §«leak_v2_address завышает утечку». См.
[`archive/reports/HANDOFF_METRIC_ADDRESS.md`](archive/reports/HANDOFF_METRIC_ADDRESS.md).

**Числовая ветка leak_v2 починена от того же завышения** (2026-07-19,
`measure_lib.longest_surviving_window`; `src/` не тронут, **Stage4-A закрыт**): окно
засчитывается, только если run, в котором оно найдено, сам является остатком значения
(ядро целиком ИЛИ покрытие run-а ≥0.8 — середина измеренного зазора 73…90%). Ушли 18
ложных утечек на общих префиксах (КБК, `044525`, `40802810`): ACCOUNT 16.5→**14.9%**,
BIK 6.4→**5.5%**, KPP 4.9→**4.1%**; INN/OGRN/PHONE/PASSPORT/SNILS и все не-числовые
типы — байт-в-байт прежние. См.
[`archive/reports/HANDOFF_METRIC_NUMERIC.md`](archive/reports/HANDOFF_METRIC_NUMERIC.md).

Источник — `docs/archive/reports/HANDOFF_STAGE_2B.md`. Точка отсчёта регресс-гейта —
`tests/corpus/results_baseline.json` (пересобран этапом 2b 2026-07-19, в
`MANIFEST.sha256` не входит).

## 3. 14 типов сущностей корпуса — есть ли детектор

| тип | детектор | метод |
|---|---|---|
| PERSON (PER) | есть | **структурный** (anchor_registry, этап B; Natasha-PER выключена) |
| ORG | есть | структурный (anchor_registry, этап A; Natasha-ORG выключена) |
| ADDRESS | есть, неточен (см. §4 0c-C/D) | NER (`yargy.AddrExtractor`) |
| INN | есть | regex + чек-сумма КАК СИГНАЛ (этап 4: под якорем маскируется и при невалидной КС) |
| OGRN | есть | regex + чек-сумма КАК СИГНАЛ (этап 4, симметрично INN) |
| KPP | есть (без чек-суммы — её не существует) | regex |
| BANK_ACCOUNT (ACCOUNT) | есть (без чек-суммы; этап 4: без якоря не берётся вовсе) | regex + якорь |
| BIK | есть (без чек-суммы) | regex |
| PASSPORT | есть, спан-контракт сломан (recall_exact 0%, см. §4 0c-B) | regex + контекстный якорь |
| PHONE | есть | regex |
| EMAIL | есть | regex |
| SNILS | **есть** (этап 3) | regex + якорь «СНИЛС», `span_group` (без КС — см. ниже) |
| BIRTHDATE | **есть** (этап 3) | regex + якорь рождения, `span_group` |

Код подразделения паспорта (ддд-ддд) — **есть** (этап 3), второй паттерн типа
PASSPORT под якорем «код подразделения». Все 14 типов корпуса имеют детектор.
Новые детекторы принципиально ЯКОРНЫЕ: значение без якоря не берётся (дата без якоря
рождения — не BIRTHDATE, голый ддд-ддд — не код). Маскируется `span_group` — только
значение, якорь в спан не входит. На SNILS НАМЕРЕННО не повешена контрольная сумма:
отбраковка по КС = утечка (инвариант этапа 4), см. HANDOFF_STAGE_3 §2.5.

`SUM` — 14-й тип корпуса с **этапа T-GOLD-A** (2026-07-28): 648 денежных сумм
переклассифицированы из негативов в эталонные сущности (границы не менялись), потому что
суммы маскируются по решению владельца, а прежняя разметка засчитывала правильное
срабатывание как ложное. Детектор — regex в `entity_types.yaml`, включён.

`DATE` есть в `entity_types.yaml`, но `enabled: false` и в типы корпуса не входит.
Классы негативов «номер счёта-фактуры» (324), «дата документа» (324) и серая зона
«дата выдачи паспорта» (228) роль НЕ меняли: детектора у них нет, а эталонная сущность
без детектора — мгновенно 100% утечки при нулевом допуске гейта.

## 4. Что открыто (по приоритету)

Полное описание с id — [`FINDINGS.md`](FINDINGS.md). Кратко:

- **PASSPORT спан-контракт сломан**, recall_exact 0% (0c-B) — не запланирован.
- **ADDRESS: жадный yargy** цепляет голые 6-значные числа не-ПДн как индекс, FP выросли
  этапом 2 (1968→2284) (0c-C/D) — не запланирован.
- ~~**Регистровая дыра**~~ — закрыта этапом 2b (2026-07-19) без второго прохода NER:
  смена регистра сохраняет длину, поэтому хватило расширения `detection_text`. Остатки —
  `Stage2b-A` (combo) и `Stage2b-B` (PER × ADDRESS) в [`FINDINGS.md`](FINDINGS.md).
- **Непрочитанные зоны `.docx`** (колонтитулы/сноски/надписи/вложенные таблицы) —
  **обнаруживаются**, но не читаются. `encrypt` по умолчанию **отказывается** (`exit 2`,
  fail-closed), обход — `--allow-lossy`/`strict_zones: false` (`entity_types.yaml`), текст
  зон уходит в `{session_id}.unread.json` (**содержит ПДн открытым текстом**, наружу в
  LLM не идёт). На корпусе отказывается 135/324. Научиться читать — этап 6. `w:sdt`
  (content control) — та же дыра, вне корпуса, не покрыт сканером (1-B).
- **B4** — кириллические омоглифы в цифрах реквизита ломают `\d` в НЕ покрытых
  нормализацией случаях, `xfail(strict)` в `tests/test_breaking_leaks.py`.
- **Stage4-B: PHONE не детектируется**, если омоглиф-группа даёт <2 «настоящих» цифр
  (нормализатор намеренно не сводит — защита «кв. 2б»); вскрыто этапом 4 (правильно
  сузившим ADDRESS-overcapture, который раньше случайно прятал эти телефоны под собой),
  2 документа — не запланирован.
- **Stage4-C: КС ОГРНИП (mod 13) может случайно сойтись** на произвольной 15-значной
  подстроке длинного не-ПДн числа (КБК); раньше пряталось за ACCOUNT-overcapture,
  этап 4 вскрыл — 5 документов, точечный рост FP (агрегат FP при этом упал) — не
  запланирован.
- ~~Крах-баги эксплуатации (session_store/extractor/CLI)~~ — закрыто:
  гонка ключа, битый/удалённый `key.bin`, битый/переименованный `.docx`,
  UTF-16 `.txt`, порядок purge/decrypt — все 8 сценариев см.
  `archive/reports/HANDOFF_CRASHFIX.md`.
- **SplitEntities** — разрыв сущности через границу сегмента/ячейки не закрыт (не область
  этапа 2); контракт красный по замыслу.
- `encrypt` принимает только `.docx`/`.txt`; `decrypt-file` — `.docx/.xlsx/.pptx`.
- PDF-извлечение не реализовано. Компонент 1 (веб-интерфейс) не начат.

## 5. Дорожная карта

- **Сделано:** блоки 1–7 (encrypt/decrypt/delete `.docx/.txt`); компонент 2 (`decrypt-file`
  `.docx/.xlsx/.pptx`, сохранение оформления); волны 1–2 детекции адреса + фикс
  over-capture (W2-D1/D2/D3); этап 0a (фикс креша encrypt на 31/324 док.); этап 0b/0b-fix
  (метрика leak_v2, честная частичная утечка, BIRTHDATE-фикс); этап 0c (baseline-замер);
  этап 0d (регресс-гейт `tests/corpus/gate.py`); этап 1 (сканер непрочитанных зон .docx,
  fail-closed по умолчанию); этап 2 (нормализация текста перед детекцией, карта индексов); этап 3 (детекторы
  SNILS, BIRTHDATE, кода подразделения паспорта — якорные, см.
  `archive/reports/HANDOFF_STAGE_3.md`); этап 4 (КС INN/OGRN — сигнал, а не шлагбаум;
  якорь `anchor:` для BANK_ACCOUNT против КБК-FP, см.
  `archive/reports/HANDOFF_STAGE_4.md`); этап 2b (регистровая детекция ФИО —
  словная/сегментная нормализация `detection_text`, см.
  `archive/reports/HANDOFF_STAGE_2B.md`).
- **Этап A (structure-first ORG), 2026-07-20** — `src/anchor_registry.py`: ORG теперь
  даёт ЯКОРЬ+РЕЕСТР+ПРОХОД 2 по лемме ядра (org-форма/кавычки/введение/ИНН-ОГРН
  юрлица), Natasha-ORG выключена (`ner_label: ORG` убран). На dogovor.docx мусор
  Natasha-ORG (Большинство/Доля/Общество-алиас/заголовки, ~152 спана) → **0**;
  «Восход» из 3 типов Natasha → **единый ORG, 122/122**. Корпус: ORG маски 4002→1011,
  ORG FP на негативах **682→1**, FP всего −682, masking B 79.6→81.8%, A=100%, ORG
  recall 88.3→89.8%, 0 крешей. **Гейт КРАСНЫЙ по условию 2 (leak_v2): +126 PER/+30
  ADDRESS/+15 ORG — это MASK-SHIFT** (Natasha-ORG случайно прятала фамилии-омонимы
  нарицательных, ИП-ФИО и фрагменты адреса, которые PER/ADDRESS-детекторы не берут;
  разрыв имени переносом строки движок не сшивает). Механизм ORG утечки НЕ вносит
  (числовые типы байт-в-байт); закрытие — за PER/ADDRESS-якорями и cross-segment ORG.
  4 находки помечены `@xfail(strict)`. См.
  [`archive/reports/HANDOFF_STAGE_A_ORG.md`](archive/reports/HANDOFF_STAGE_A_ORG.md).
- **Открыто/следующее:** ~~PER-границ (PER-B)~~ закрыт этапом B; этап 5 (адрес без маркеров), этап 6
  (чтение непрочитанных зон + харнесс 1-A), PDF-извлечение, компонент 1 (веб).
  Контракты-заглушки будущих этапов — `tests/test_future_contracts.py`
  (`docs/archive/reports/FUTURE_CONTRACTS.md` — карта «дыра → тест → этап»).
- **Замер A0 (GLiNER vs Natasha, 2026-07-20, `experiments/a0_gliner/`, в пайплайн НЕ
  влит):** может ли GLiNER заменить Natasha как генератор кандидатов — **НЕТ**. На
  чистой синтетике теряет ≥19% того, что Natasha берёт (реальные строчные ФИО, города),
  и удваивает over-masking родовых слов (Участник→PERSON, Общество→ORG: 150→308 спанов).
  НО решает REAL-GENRE: «Восход» (склоняемое имя фирмы) — единый тип ORG 100% против
  Natasha PER/ADDR/ORG-мешанины. Итог: GLiNER → ярус recall позже, этап A structure-first.
  Латентность CPU ~2–3× медленнее Natasha. См. `archive/reports/HANDOFF_A0_GLINER.md`.

**UI отставал от этапа A** (2026-07-20, фикс): `app/core.py` не вызывал
`anchor_registry.detect_org`/`suppress_conflicts` (написан до этапа A) — ORG-меток
не давал, при том что `src/`/CLI не тронуты. Починено (3 строки в `app/core.py`),
byte-diff CLI↔UI подтверждён. См. [`archive/reports/HANDOFF_UI.md`](archive/reports/HANDOFF_UI.md).

- **Этап A′ (нормализация якоря + порядок пайплайна), 2026-07-20** — два точечных
  фикса поверх этапа A. (1) `anchor_search_view` в `src/anchor_registry.py`: свой,
  независимый от Stage 2, поисковый вид, вырезающий `\n\r\xad​⁠‌
  ‍ \xa0` НАПРОЧЬ до фолдинга омоглифов (NBSP/NNBSP к моменту обычного
  `detection_view` уже необратимо стали пробелом) — чинит org-форму/кавычки,
  разорванные этими символами ВНУТРИ одного сегмента. (2) `detect_org` теперь
  считается ДО `detect_ner` (`shifrator.py`/`app/core.py`/`run_measurement.py`) и
  передаётся туда барьером `org_entities` в `_address_barriers` — закрывает A-4
  (`test_wave2_overcapture::test_following_org_keeps_its_own_token`, xfail снят).
  Изолированный эффект (Stage A+A′ vs Stage A): ORG leak_v2 120→**88** (−32), ORG
  recall 88.3→**93.9%** (было 89.8% на одном этапе A); ВСЕ остальные 12 типов —
  побайтно без изменений. Round-trip на 48 вариантах именованных мутаций — 0 порчи
  значения (единственный класс whole-doc несовпадений — предсуществующая
  `\n`→`' '` асимметрия `_render_table`, найдена и задокументирована как Aprime-3,
  НЕ внесена этой сессией). Разбор оставшихся 28 пропусков ORG показал 3 отдельные,
  вне-границ находки (`Aprime-1/2/3` в `FINDINGS.md`) — не одна причина, как
  предполагала исходная постановка. См.
  [`archive/reports/HANDOFF_STAGE_APRIME.md`](archive/reports/HANDOFF_STAGE_APRIME.md).

- **Этап B (structure-first PER + консолидация точек входа), 2026-07-22** — двухпроходный
  механизм распространён на ЛЮДЕЙ. `PerAnchorDetector` в `src/anchor_registry.py` в том
  же `AnchorEngine`, что ORG; **Natasha-PER выключена** (`entity_types.yaml`), как
  Natasha-ORG на этапе A. Якорь ФИО — по пометам pymorphy `Surn/Name/Patr` (порог счёта
  0.01 против мусорных разборов), само-якорь «фамилия+имя/отчество» или «фамилия+инициалы»,
  голая фамилия — только при маркере (гражданин/должность/«в лице»/ИП/ИНН-12/СНИЛС).
  Косвенный падеж — ОДИН спан (`_fio_run`, закрывает **PER-B**). Реестр по лемме фамилии,
  арбитраж типов ORG↔PER (двухфазный pass-1). Раздельные виды `per_search_view`(\n→пробел)
  vs `anchor_search_view`(\n вырезан). **A-2** (ИП Иванов → 2-й ORG) и **A-3** (Глава КФХ
  Петров → PER, не блоб) погашены, xfail сняты. Барьер PER↔ORG↔ADDRESS в `detect_ner`
  (`per_entities`). **Консолидация**: `src/pipeline.py::run_detection` — единственная копия
  порядка шагов, зовут CLI/UI/замер (устраняет UI-рассинхрон). Изолированный эффект
  (A′→B): PER exact(found) 77.8→**87.6%**, PER leak_v2≥6 41.3→**28.7%**, masking B
  81.9→**89.8%**, masking A=**100%**, ложных PER на негативах 11→**1**; PER recall
  72.2→71.7% (−0.5pp — Aprime-1 безъякорные); ORG recall ↑ 93.9→94.2%; 10 не-PER/ORG/ADDRESS
  типов байт-в-байт; 0 крешей. ORG leak +1.6pp = MASK-SHIFT (8 «Петров Консалтинг»
  Aprime-1, теряли случайную маскировку Natasha-PER — не регрессия). Паритет CLI↔UI —
  побайтный тест в обязательном прогоне. Результаты — `experiments/stage_b/`, НЕ в корпусе;
  sha256 корпуса OK до/после. См.
  [`archive/reports/HANDOFF_STAGE_B_PER.md`](archive/reports/HANDOFF_STAGE_B_PER.md).

- **Этап C (якорная модель ADDRESS), 2026-07-22** — ветка `stage-c-address`. Жадный
  `yargy.AddrExtractor`+LOC метил адресом прозу/ролевые слова/паспортный хвост/топоним-
  одиночку. Теперь СТРОГИЙ ЯКОРЬ поверх сырых LOC/yargy-спанов (`src/ner_detector.py`
  `_addr_span_strong`): улица+топоним/номер, помещение+номер (д./кв./а/я/уч.),
  house-pattern «Titlecase-топоним+короткий №, ≥2 топонима» (ловит «Москва, Ленина 5»,
  отсекает «Статья 5»/«ГОСТ Р 52044»/«Паспорт 43 15»), индекс ТОЛЬКО вплотную к городу
  (6-значная группа паспорта/кода — не якорь). Границы: обрезка меток («по адресу:»),
  дробление по предложениям, барьеры ORG/PER/реквизитов подрезаются ДО якоря. Тот же
  гейт в B3-кросс-сегментном пути. `_MIN_CAPS_RUN` 3→2 (уступка Stage2b-B снята —
  Natasha-PER/ORG выключены, адрес якорный). GOLDEN адресов 89/89 детекция+полнота,
  0/16 ложных — контракт волны 2 держится. Изолированный эффект (HEAD B→C, полный
  корпус): **ложных ADDRESS на негативах 634→1, ложных ADDRESS всего 3483→14**,
  exact-границы 41.4→**65.1%**, masking B 79.6→**86.1%**, FP всего 1975→**650**;
  ADDRESS recall 93.6→87.5% (потеря −6.2пп ТОЛЬКО на адверсариальных мутациях, ~1 на
  чистом); PER recall 72.0→74.1% (↑ от `_MIN_CAPS_RUN`), ORG 88.3→94.2%, masking A=100%,
  round-trip 100% (44 ws-only), 0 крешей. **Гейт КРАСНЫЙ по условию 2 (leak_v2)** —
  в ОСНОВНОМ MASK-SHIFT: жадный ADDRESS случайно маскировал фрагменты реквизитов/
  паспорта, их детекторы под мутацией не берут; отброс/сужение адреса обнажило
  пред-существующие дыры (masking A/B не упали, а выросли; для ВСЕХ не-ADDRESS типов
  100% mask-shift — их детекторы байт-в-байт). НО поэлементный разбор (§9.2 HANDOFF)
  нашёл **62 ADDRESS — реальную потерю детекции** (гейт ОТБРАСЫВАЕТ спаны, тезис «код
  только сужает» СКОРРЕКТИРОВАН): 61 адверсариальная мутация + 1 чистый. Тот же класс
  красноты, что A/B. 2 контракта тестов обновлены намеренно. Результаты —
  `experiments/stage_c/`, НЕ в корпусе; sha256 656 OK до/после. См.
  [`archive/reports/HANDOFF_STAGE_C_ADDRESS.md`](archive/reports/HANDOFF_STAGE_C_ADDRESS.md).

- **Этап C — закрытие 2 хвостов дельты, 2026-07-23** (перед этапом D). (1) Чистотекстовый
  `agency_0009` «Москва Ленина 47/3 кв 67» не маскировался: причина — СЕЯНИЕ (yargy не
  запускался — Natasha-LOC на «Москва» под прозой-префиксом молчит, seed-регэксп на
  безмаркерный склеенный адрес не срабатывал). Фикс — 1 функция `_addr_has_seed`: сеять
  ТАКЖЕ по house-pattern (тот же предикат, что гейт → жадность не возвращается). ADDRESS
  recall 87.5→**89.0%** (+19 адресов), ложных ADDRESS на негативах **1→1** (не выросли),
  masking A=100%/B 86.13→86.15%, не-ADDRESS типы байт-в-байт, round-trip 100%. (2) Реестр
  `docs/known_leaks_stage_c.json` — **ровно 61** известного долга ADDRESS (адверсариальные
  мутации, `closes_on: entity-spans`), agency_0009 ИСКЛЮЧЁН (покрыт). Реестр — для гейта D,
  детектор его не читает. См. HANDOFF §10.

- **Этап D (метрика precision + гейт), 2026-07-23** — КАПСТОУН precision-хребта. Детекцию
  НЕ трогали (0 правок в `src/`): построен ИЗМЕРИТЕЛЬ + ГЕЙТ, замораживающий достигнутый
  precision, чтобы будущая recall-работа не уронила его тихо. **Метрика** —
  `measure_lib.precision_by_type` (единая FP-дефиниция разбора C §9.1: FP = kept-маска на
  ОБЪЯВЛЕННОМ негативе; TP = kept-маска на gold своего типа; precision_T = TP/(TP+FP_neg)).
  cross-type и on-nothing — ОТДЕЛЬНАЯ диагностика, НЕ в precision и НЕ в гейт. **Precision
  HEAD b-per → C+фикс**: ORG 99.77→99.77%, PER 99.96→99.96%, **ADDRESS 66.86→99.92%**
  (fp_neg 634→1, over-mask прозы 2610→1); fp_neg TOTAL 1283→650. **Гейт**
  `experiments/stage_d/gate_d.py` краснеет по: (2a) precision NER-типа < baseline−0.5пп;
  (2b) FP>650+5; (2c) утечка-долг(ADDRESS found=False&leak) >116+3. Диагностика состава vs
  known_leaks: подмена «N на другой N» → ПРЕДУПРЕЖДЕНИЕ (не красит). Baseline D зафиксирован
  (`experiments/stage_d/baseline_d.json`): precision выше, FP=650, утечка-долг=116 (=61
  документированных + 55 пред-существующих, HEAD b-per тоже не брал — ЧЕСТНО показано, не
  подогнано под 61). **Зеркала**: `tests/test_precision_metric.py` (метрика двигается на
  маске-на-негативе, не двигается на корректной; over-mask не тонет), `tests/test_gate_d.py`
  (само-тест: искусственный регресс precision→красный 2a, лишняя утечка→2c, подмена
  состава→предупреждение). Старая «precision 90%» (=masking C, слеп к over-mask) помечена
  ФИКЦИЕЙ (FINDINGS D-PRECISION-90). Детекция байт-в-байт, round-trip 100%; sha256 656 OK.
  См. [`archive/reports/HANDOFF_STAGE_D_PRECISION.md`](archive/reports/HANDOFF_STAGE_D_PRECISION.md).

- **Этап C′ (ужесточение якоря кавычек/формы ORG), 2026-07-23** — ветка
  `stage-c-prime-quotes`. Только `src/anchor_registry.py` (`OrgAnchorDetector`);
  PER/ADDRESS и Entity.spans не тронуты. Реальный корпоративный договор (жанр «раздел
  определений») был «решетом»: роль-термины/термины в кавычках и развёрнутая форма-слово
  тиражировались проходом 2 в сотни ORG-масок. Три структурных под-механизма БЕЗ списков
  слов: (1) развёрнутая форма-СЛОВО («Общество…»/«Акционерное общество…») больше НЕ
  якорит голое бескавычное имя — только аббревиатура (ООО/ПАО/АО); (1b) голое имя за
  формой, чья ВЕРШИНА — МНОЖЕСТВЕННОЕ нарицательное (`plur+NOUN` pymorphy, без
  Name/Surn/Geox/Orgn) — коллектив-роль, не название (гасит «…Закона об АО <Роль>…»);
  (2) конструкция определения «"X" означает/— это/под "X" понимается/имеет значение» —
  анти-якорь. **Реальный документ (обезличенно): ORG-маски 2834→70, роль-термины
  575/378/223/114/22→0 каждый, потеряно организаций 0, реальных орг детектировано 30.**
  **Корпус (изолированно main→C′): ORG recall 94.16%→94.16% (0 flips), over-mask прозы
  326→322, FP 1488→1484, не-ORG байт-в-байт, masking A=100%.** Гейт D ЗЕЛЁНЫЙ
  (precision/FP/утечка-долг = baseline), sha256 656 OK, pytest 887 passed (+16 зеркал
  `tests/test_stage_c_prime_quotes.py`, вкл. (+)/(−)). Остаток (вне темы, доложен):
  реквизитная ветвь даёт усечённый нарицательный фрагмент (×1) и втягивает соседнее ФИО
  в ORG-ключ (замаскировано, тип неверный) — Stage-Cprime-A/B в FINDINGS. См.
  [`archive/reports/HANDOFF_STAGE_C_PRIME.md`](archive/reports/HANDOFF_STAGE_C_PRIME.md).

- **Этап E (Entity.spans + единый id + канон восстановления), 2026-07-23** — ветка
  `stage-e-spans`. Смена модели данных, целостность выше функциональности. (1)
  `Entity.spans` — список диапазонов значения (hull [start,end] маскируется одним
  токеном, разделители в original_text байт-в-байт); применён УЗКО через
  `src/multispan.py`: PHONE в произвольной группировке (леч. утечки 32.3→**9.1%**,
  found 702→989), \n-рваные ACCOUNT (leak 15.4→9.3%) и BIRTHDATE (25.9→21.1%);
  анти-склейка: цепочка = длина ровно ОДНОГО значения, телефон не цепляется через \n.
  (2) ЕДИНЫЙ ID: токен по `group_key` реестра — падежи «Меркурия/Меркурием» → один
  [ORG_N]; PER — PersonBook (тёзки различаются по имени/отчеству/инициалам, разные id).
  (3) Восстановление КАНОНОМ (`detokenize` mode="canonical" умолчание; exact — позиционное
  точное для приёмки) + **формат сессии v2**: canonical + occurrences (ВСЕ поверхностные
  формы каждого вхождения — пишем всё, читаем пока канон); v1-поля сохранены, старые
  сессии читаются (тест). Приёмка: **1a структурный 0 FAIL на всех 324 док.** (280 ok +
  44 ws_only=Aprime-3), 1b — все 2450 расхождений доказаны каноном; гейт D ЗЕЛЁНЫЙ
  (fp_neg 650=baseline, НОЛЬ новых FP); masking A=100%, B 86.15→86.79%; не-мультиспан
  типы байт-в-байт; реальный договор: 243/243 масок побайтно OK, ORG 72 вх.→27 id,
  PER 67→32. 4 контракта тестов обновлены намеренно (HANDOFF §6); pytest 903 passed.
  file_detokenizer НЕ тронут (подставляет форму 1-го вхождения — задокументировано).
  См. [`archive/reports/HANDOFF_STAGE_E_SPANS.md`](archive/reports/HANDOFF_STAGE_E_SPANS.md).

- **Этап E′ (мультиспан на отложенных классах), 2026-07-23** — ветка
  `stage-e-prime-apply`. «Расплетающий» вид адреса (`_addr_deweave`: разрыв в слове —
  \n при строчном продолжении/NBSP/невидимые — и в числе вырезается, рваный индекс
  «420 111» склеивается) + тот же конвейер/строгий якорь C по виду: в сегменте и в
  B3-окнах (cross-segment .txt); cross-segment ORG — OrgAnchorDetector на окнах
  (только якоря через стык, half-pair). Якоря НЕ ослаблены; 3 страховки точности
  (эмит только со склейкой deweave, \n-склейка только к строчному, подрезка краёв
  через бывший \n). **Утечка-долг ADDRESS 116→57** (−59, 0 новых; поимённо
  `experiments/stage_e_prime/debt_final.json`); остаток: case 26/combo 24 (регистровый
  класс, вне темы), 6 linebreak-остатков (3 = B-ADDR-HOMONYM, 3 гибрида с case),
  1 homoglyph-edge. **known_leaks_stage_c.json пересобран = ровно 57** (включая 55
  пред-существующих, которых в файле не было). **Cross-segment ORG: 14/14 собраны**
  (ORG found 435→449). Гейт D ЗЕЛЁНЫЙ до и после обновления baseline
  (baseline_d: precision заморожен выше — ORG 99.79/ADDRESS 99.93, долг 57);
  ADDRESS fp_neg 1→1, GOLDEN 195/195. 1a: FAIL=0 на 324 (ws_only 44→61 — рост
  известного класса Aprime-3 от новых \n-hull-масок, per-mask A=100%, доложено);
  1b: 2447 расхождений = все канон. pytest 914; sha256 656 OK. **Находка Eprime-A:
  недетерминизм детекции на ОГРОМНЫХ доках (~2500 сегментов, реальный договор) —
  ПРЕД-существующий (HEAD тоже), корпус детерминирован; инвариант round-trip
  держится в каждом прогоне.** Полный корпус ~1.6× дороже (двойной NER на \n-сегментах).
  См. [`archive/reports/HANDOFF_STAGE_E_PRIME.md`](archive/reports/HANDOFF_STAGE_E_PRIME.md).
- **Этап E″ (детерминизм детекции), 2026-07-23** — ветка `stage-eprime-determinism`,
  поверх этапа E; E′ слита в main отдельно (этап S2, 2026-07-25).
  Находка Eprime-A (недетерминизм на реальном ~2500-сегментном документе, 239/226/226
  масок) **НЕ воспроизведена** на синтетическом двойнике (`tests/fixtures/
  synthetic_corporate_large.docx`, коммитится навсегда — заменяет удалённый владельцем
  `real_docs/`) на двух масштабах (2641/5051 сегментов) и в изолированном NER-стрессе
  (до 508K символов). Внесена защитная мера класса дефекта (не диагностированный фикс):
  `src/ner_detector.py`/`src/syntax_compound.py` фиксируют потоки BLAS
  (`OMP/OPENBLAS/MKL/NUMEXPR_NUM_THREADS=1`, `setdefault`) до импорта `natasha` и
  прогревают модели при импорте модуля. pytest 903+2 (новый `test_determinism.py`),
  гейт D побайтно = baseline, sha256 корпуса 656 OK, round-trip синтетики 100%.
  Автономный `experiments/stage_eprime_determinism/owner_repro.py` — для проверки
  гипотезы владельцем на реальном документе. Находка остаётся ОТКРЫТОЙ. См.
  [`archive/reports/HANDOFF_STAGE_EPRIME_DETERMINISM.md`](archive/reports/HANDOFF_STAGE_EPRIME_DETERMINISM.md).
  **ОБНОВЛЕНИЕ 2026-07-24 (данные владельца):** на реальном документе воспроизвелось —
  counts `[237,224,226,224,233,224]`, СЛУЧАЙНЫЙ разброс (не «первый≠остальные»),
  гипотеза «холодный BLAS/прогрев» ОПРОВЕРГНУТА. Плавают только Natasha-зависимые
  типы (ADDRESS 27-37, ORG/PER ±2), regex+КС стабильны. Анализ: ADDRESS уже
  порядко-канонизирован (`sorted` в `_build_address_spans`), `_resolve_overlaps` —
  тотальный порядок → причина в РАЗНЫХ НАБОРАХ спанов от инференса Natasha, не в
  порядке. `owner_repro.py` переписан на послойную диагностику (L1-L4), потоки
  фиксируются ПЕРВОЙ строкой до импорта numpy; один прогон владельца локализует слой.
  Спекулятивная переупорядочка в пайплайн НЕ внесена (нет доказательств, риск корпусу).

**UI второй фикс — старый процесс отвечал вместо нового** (2026-07-20): `http.server`
даёт Windows молча забиндить новый `server.py` на занятый порт (`allow_reuse_address`)
— старые процессы после перезапусков не умирали, ОС отдавала запросы то старому коду,
то новому. Починено (`allow_reuse_address=False`+явная ошибка занятого порта, `app/server.py`),
плюс постоянный `BUILD_MARK` в логе старта. Приёмка — живым кликом в браузере, не прямым вызовом.

## 6. Границы, действующие всегда

- Корпус `tests/corpus/**` заморожен — `sha256sum -c MANIFEST.sha256` из `tests/corpus/`
  обязан быть OK до и после любой правки. Корпус не редактировать.
- **Корректность маскировки только растёт.** `masking_correctness` A (round-trip) и B
  (границы) не должны ПАДАТЬ ни на одном этапе — гейт роняется с допуском 0 (условие 5).
  C (тип) — мягкий, печатается, но не роняет. Асимметрия ошибок: пропуск человек поймает на
  проверке, кривую маскировку — нет, поэтому этап, который начнёт маскировать криво ради
  recall, обязан покраснеть.
- Регресс-гейт (`tests/corpus/gate.py`) не должен показывать рост крешей/`leak_v2`/FP или
  смену MANIFEST. Не вешать на pre-commit — CI на PR, трогающем `src/`.
- `~/.shifrator/sessions/` — путь хранилища, имена `key.bin`/`{sid}.enc`/`{sid}.txt`. Смена
  делает существующие сессии невосстановимыми. В тестах — только временный
  `USERPROFILE`/`HOME` (`tmp_path`), никогда реальный `~`.
- Контракт CLI (`python shifrator.py encrypt|decrypt|delete|decrypt-file`), коды возврата,
  `encrypt` печатает в stdout **только** `session_id`.
- Плоские импорты `src/` (не превращать `src/` в пакет).
- Разделитель граничного окна B3: соседние ячейки склеиваются `""`, не `" | "`
  (`src/tokenizer.py`) — иначе «ИНН/телефон через ячейку» снова недетектируем. См.
  [ARCHITECTURE.md](ARCHITECTURE.md), инвариант B3.
- «Пришпиливающие» тесты (`tests/test_wave2_overcapture.py`,
  `tests/component2/test_e_malformed_input.py::...zip_without_any_target_part...`) —
  зелёные = дефект закрыт; не «зеленить» вручную, чинить в коде при поломке.
- **Ключ атомарен, не пересоздаётся на чтении**: `key.bin` создаётся только в
  `save_session` (атомарно, `os.link`/`O_EXCL`); `load_session`/`purge_expired`
  при отсутствии ключа кидают ошибку, а не генерируют новый — тихое
  пересоздание хоронит все прочие сессии директории.

**Этап U1 (упаковка для конечного пользователя), 2026-07-24** — ветка
`u1-desktop-packaging`, детекция не тронута. PyInstaller `--onedir`
(`packaging/shifrator.spec`) + лаунчер `app/launcher.py` (self-check моделей
до старта, файл-замок против зомби-процессов, динамический порт, окно
tkinter вместо консоли) + `src/storage.py` (интерфейс хранения сессий, задел
серверной версии). Проверено на собранном exe: CLI↔UI паритет, self-check на
удалённом файле модели даёт человеческий текст, PATH без Python/без админа.
См. [`archive/reports/HANDOFF_U1_PACKAGING.md`](archive/reports/HANDOFF_U1_PACKAGING.md),
[`README_USER.md`](../README_USER.md).

**Этап S1 (гигиена данных и жизненный цикл разметки), 2026-07-25** — ветка
`u1-desktop-packaging`, детекция НЕ тронута (0 правок в `src/` за пределами
`storage.py`/`session_store.py`). Три правки в слое хранения: (1) разметка
(U3) вынесена в отдельное хранилище `~/.shifrator/markup/` со своей политикой
срока (30 дней, не 24ч TTL сессии) — переживает удаление/просрочку сессии;
явное удаление (одна сессия/вся разметка), миграция старых `.markup.json`,
видимость в интерфейсе (`GET /api/markup/summary`); (2) `{sid}.doc.json`
больше не сериализует служебные кеши видов текста детекции
(`detection_text`/`_norm_cache`/`_anchor_search_cache`/`_per_search_cache`) —
файл меньше в 6.7–7.8× на реальных документах, копий ПДн в файле стало 1
(было до 4); (3) провенанс маски: `session_store.save_session` теперь пишет
`detector`/`group_key` (уже существовавшие поля `Entity`, раньше терялись при
сериализации) на уровне записи и каждого вхождения; ручные маски U3 явно
`detector="manual"`, отличимы от автоматических; попутно исправлен дефект
`apply_pending_markup`, стиравший провенанс ВСЕХ масок сессии при любой
пересборке. Обратная совместимость: старые сессии без этих полей читаются
(`.get(...)`). `pytest -q`: **933 passed, 10 xfailed** (+13 новых тестов
`tests/test_storage_s1.py`, остальное байт-в-байт); гейт D зелёный; корпус
`MANIFEST.sha256` OK; агрегат E″ (`corpus_anon_sha.py`) — `6c9b5bd5a1ab2227…`,
совпадает с эталоном. См.
[`archive/reports/HANDOFF_S1.md`](archive/reports/HANDOFF_S1.md).

**Этап U4 (метрики по разметке и отчёт для разработчика без ПДн), 2026-07-25** —
ветка `u1-desktop-packaging`, **0 правок в `src/`** (`storage.py` только на
чтение; параллельная сессия S3 работает в `src/`). Новый модуль
`app/report.py` + ручка `GET /api/report` + экран «4 · Аналитика». Метрики
считаются ПО РУЧНОЙ РАЗМЕТКЕ U3: `precision = (M−FP)/M`,
`recall = (M−FP)/(M−FP+MISS)`, доли правок границы и типа; разбивка по типам,
механизмам (`regex`/`ner`/`manual`/`none`) и по `BUILD_MARK`. **Обе метрики —
ВЕРХНИЕ ОЦЕНКИ** (маска, на которую не посмотрели, считается верной;
незамеченный пропуск не в знаменателе), выборка смещена — всё это напечатано
в шапке самого отчёта, не только в доке; полный замер на корпусе они НЕ
заменяют. **Отчёт наружу безопасен по построению, тремя рубежами**: белый
список полей (`_FINDING_FIELDS` — структурные коды вместо текста: тип,
механизм, абзац/ячейка, длина, слов, цифры/латиница/кириллица/инициалы,
регистр), нормализация всех входных строк в коды словаря, и **структурный
часовой** `assert_structural` — КАЖДАЯ строка отчёта (и ключи, и значения)
обязана быть схемным ключом, кодом словаря, ISO-датой, ASCII-слагом сборки или
дословной константой модуля; иначе `ReportLeakError` и отчёт не отдаётся
вообще. `value`/`session_id`/имя файла в схеме отсутствуют намеренно.
**Знаменатель переживает 24-часовую сессию**: `app/core.py` кладёт в каждую
запись разметки слепок `census` (масок всего/по типам/по детекторам), отчёт
берёт слепок самой ранней записи = состояние до ручных правок; разметка без
слепка (до U4) честно даёт «документ без знаменателя», а не подделанное число.
Предпросмотр = выгрузка **структурно**: `downloadReport` сохраняет строку из
того же `<pre>`, который прочитал пользователь (побайтная сверка в живом
браузере: md 6243 Б, json 8892 Б, `bytes_equal=true`). Приёмка: тест на утечку
ищет маркер целиком, в другом регистре И любой подстрокой ≥6 символов — 0
попаданий в json/md/DOM; 23 теста `tests/test_u4_report.py` (18 unit + 5 live),
включая 4 само-теста часового и санитарную проверку самих маркеров (первый
вариант маркера содержал слово «ОРГАНИ», встречающееся в прозе отчёта, и давал
ЛОЖНУЮ тревогу). sha256 корпуса 656 OK. **Известное ограничение, не дефект:**
документ, в котором пользователь не отметил ничего, в агрегат не попадает
(смещение выборки) — закрывается только правкой модели разметки U3.
`report._markup_session_ids()` — единственное место в `app/`, идущее в ФС мимо
`storage.py` (там нет `list_markup_sessions()`); добавить её в `src/storage.py`,
когда `src/` освободится. **Побочная находка, починена:**
`packaging/shifrator.spec` не знал про ленивый импорт нового модуля — в
собранном exe это дало бы ТИХУЮ деградацию метрик, а не падение; добавлен
`"report"` + страж `test_pyinstaller_spec_lists_every_app_module` (фальсифицирован).
**Вторая находка (НЕ внесена U4, живёт на HEAD):** `test_g_regression` красный —
вложенный прогон 488 c против жёсткого `timeout=300`; доказано worktree на
чистом `HEAD` и простаивающей машиной, см. U4-G-TIMEOUT в
[`FINDINGS.md`](FINDINGS.md) и §0 выше. См.
[`archive/reports/HANDOFF_U4_REPORT.md`](archive/reports/HANDOFF_U4_REPORT.md).

**Этап U1b (пересборка поставки на актуальном коде), 2026-07-25** — ветка
`main`, **0 правок в `src/`**. Прошлая поставка была собрана до влития
E′/E″/S1/U2/U3/U4/S3. Пересобрано, `BUILD_MARK` = **`u1b-rebuild-20260725`**
(виден в футере, `/api/ping` и в поле `tool_build` отчёта U4). Поимённая сверка
показала, что спека УЖЕ знала про все 19 модулей `src/` и 7 модулей `app/`
(включая `report.py`, дописанный самим U4), новых файлов данных после U1 не
появилось, а `threadpoolctl` — НЕ зависимость поставки (используется только
диагностикой владельца; отгружаемый код фиксирует потоки BLAS переменными
окружения в `src/ner_detector.py`). Поэтому добавлены не имена, а **три барьера
на пути сборки**, роняющие `pyinstaller`: полнота `hiddenimports` по `src/`+`app/`,
совпадение окружения с новым `packaging/requirements-build.lock.txt` (**38 точных
пинов**, python 3.11.9; scipy в окружении нет вообще) и существование файлов
`datas`. Зеркала — 3 новых теста в `tests/test_u1_packaging.py` (8→11),
фальсифицированы. **Приёмка на СОБРАННОМ exe, 26/26:** детерминизм — 5 прогонов
одного документа побайтно идентичны (до E″ этого не было); полный путь U2/U3
(перетащил→прогресс→результат→скачал→пометил пропущенное→поправил границу→
применил→пересобрал→восстановил); U4 (аналитика, отчёт, предпросмотр==выгрузка,
тест на утечку маркеров — 0 попаданий); S1 (разметка переживает удаление сессии,
`doc.json` без кешей и с одной копией ПДн, провенанс `old_detector`); CLI↔UI
паритет побайтно; self-check без файла модели — человеческое окно, трейсбек
только в лог; запуск без Python в PATH из папки в профиле без прав администратора.
**Числа:** поставка 124.9 МиБ / 131.0 МБ (было ~127 МБ), сборка 41 c, холодный
старт **1.66–1.80 c** (было ~2.0–2.1 c), первый запуск свежераспакованной копии
3.78 c. `pytest -q`: **1000 passed, 10 xfailed** (+3 зеркала; единственное
падение — всё тот же пред-существующий `test_g_regression`, U4-G-TIMEOUT/S3-6).
Корпус `MANIFEST.sha256` 656 OK до и после. См.
[`archive/reports/HANDOFF_U1b_REBUILD.md`](archive/reports/HANDOFF_U1b_REBUILD.md).

**Этап T1 (переключатель типов данных), 2026-07-28** — ветка `main`. Программа
больше не маскирует все типы всегда: пользователь выбирает НАБОР типов, а
детекция при этом работает ЦЕЛИКОМ, всегда, всеми слоями. **Фильтр стоит на
этапе МАСКИРОВАНИЯ**, не в детекции: `tokenize` расщеплён на
`resolve_for_masking` (границы B3 + разрешение пересечений — общая, дорогая
часть) и `apply_masking` (единственная точка, где выключенный тип не получает
токена). Обоснование ПРОВЕРЕНО по коду, а не принято на веру: без
regex-барьеров `AddrExtractor` разрастается на цифры соседнего ИНН (граница
адреса 47→58 симв. на пробе), то есть выключение типа в детекции сдвинуло бы
границы масок соседних, ВКЛЮЧЁННЫХ типов — это дефект, а не оптимизация.
Зеркало — `tests/test_type_policy.py::test_disabling_in_detection_would_move_neighbour_boundaries`
(положительный тест: покраснеет, если барьеров не станет).
**Четыре набора** (`src/type_policy.py`): `personal` (**умолчание**: PERSON,
ADDRESS, PHONE, EMAIL, PASSPORT, SNILS, BIRTHDATE), `personal_requisites`
(+INN, OGRN, KPP, BIK, BANK_ACCOUNT, ORG), `with_money` (+SUM), `maximum` (все
типы конфига, сентинел `None` = фильтр не применяется). **ИНН в набор 1 не
входит вынужденно**: ИНН физлица (12 цифр) и юрлица (10) — ОДИН тип `INN`,
число цифр наружу не выходит (используется лишь как признак якоря PER в
`anchor_registry`). Файл настроек `~/.shifrator/settings.json`
(`{"profile": ..., "types": {ТИП: true|false}}`) — неизвестный тип и
неизвестный набор ИГНОРИРУЮТСЯ, программа не падает.
**Замер и гейт — ВСЕГДА на «Максимуме»**, явной константой
`type_policy.MAXIMUM` в `run_measurement.py` (не умолчанием): иначе гейт мерил
бы настройку пользователя, и падение полноты стало бы неотличимо от
выключенного типа. **Отчёт программы** (`app/report.py`) записывает состав
включённых типов (раздел «Состав»); слепок берётся МОМЕНТА ШИФРАЦИИ из sidecar
сессии (`storage.save_session_meta(policy=...)`), разные составы в выборке дают
честное `profile: "mixed"` без списков, а не усреднение. Новые поля внесены в
белый список `_SCHEMA_KEYS` и `_ALLOWED_VALUES`.
**Приёмка на ПОЛНОМ корпусе** (`experiments/stage_t1/`, один прогон детекции,
15 вариантов маскирования поверх): (1) «Максимум» побайтно = HEAD~1 —
**324/324 документа, 0 расхождений sha текста, 0 расхождений карты масок**;
(2) выключение каждого из 14 типов — **0 сдвигов границ у остальных типов** во
всех 14 прогонах (таблица в `experiments/stage_t1/ACCEPTANCE.txt`). Корпус
`MANIFEST.sha256` 658 OK до и после; `src/` детекции не тронут.
**Находка T1-A ЗАКРЫТА этапом U4-FIX** (2026-07-29): структурный часовой отчёта
пропускал любую ЛАТИНСКУЮ строку через ветку слага сборки (`_BUILD_RE`
применялась к каждому ключу и каждому значению) — протекали имя файла, домен,
название организации латиницей, фамилия транслитом, любое новое поле с
латинским именем. Теперь ветка привязана к МЕСТУ в схеме
(`report._build_slug_allowed`: `tool_build`, `findings[].build`, ключи
`by_build`), в остальных местах латиница отвергается наравне с кириллицей.
Белые списки полей НЕ расширялись, отчёт по документу корпуса не изменился.
Маркерный тест — `tests/test_u4_report.py::test_guard_rejects_marker_everywhere`
(7 видов маркера × 4 места внесения); метка `xfail(strict)` снята. Разбор —
[`archive/FINDINGS_closed.md`](archive/FINDINGS_closed.md).

**Этап T1-UI (экран выбора набора), 2026-07-29** — ветка `main`. Механизм
фильтрации T1 не тронут, этап даёт к нему доступ из интерфейса. Вкладка
**«5 · Что маскировать»** (`app/index.html` `scr-set`, маршруты
`GET`/`POST /api/settings`, данные — `core.settings_view`/`core.save_settings`):
четыре набора с человеческим описанием + 14 галочек по типам. **Состав набора
считает СЕРВЕР** (`src/type_policy.py`), браузер ничего не вычисляет — иначе
экран разошёлся бы с механизмом при появлении типа. Хранилище настроек ОДНО —
тот же `~/.shifrator/settings.json`. `DATE` на экран не выводится (детектор
выключен, галочка обещала бы невыполнимое).
**Однократное решение при первом запуске** — `type_policy.ensure_settings()`
(зовут `app/core.current_policy` и `shifrator.cmd_encrypt`): файл есть — не
трогаем (в т.ч. битый); файла нет, но есть следы прошлой работы — пишем
`maximum` (**обновление**: до T1 маскировалось всё, сузить молча нельзя); ни
файла, ни следов — `personal` (первая установка). «Следы» —
`type_policy.has_previous_work()`, ЗАКРЫТЫЙ список (`sessions/` непустой,
`sessions/key.bin`, `markup/` непустой); `launcher.lock` в список НЕ входит
намеренно — его создаёт сам запуск интерфейса, и первая установка выглядела бы
обновлением. Запись атомарная, ошибка записи не фатальна.
**Экран проверки** показывает, каким набором получен результат (слепок момента
шифрации, `core.policy_summary`), и при неполном наборе прямо перечисляет, что
осталось в тексте НАМЕРЕННО.
Приёмка: на `agency_0002.docx` набор меняет число масок 12 / 14 (+галочка ИНН) /
28 / 30 / 26 (максимум минус телефон); **урезанный файл настроек не сдвинул ни
одной цифры полного гейта** (ЗЕЛЁНЫЙ, все `X -> X`), `MANIFEST.sha256` 658 OK,
`pytest` 1145 passed. Разбор — [`T1_UI_REPORT.md`](T1_UI_REPORT.md).

## 7. Куда за подробностями (не читать по умолчанию)

- Инварианты кода/архитектурные решения — [`ARCHITECTURE.md`](ARCHITECTURE.md).
- Карта модулей — [`CODEMAP.md`](CODEMAP.md).
- Отвергнутые решения — [`DECISIONS.md`](DECISIONS.md).
- Спека компонента 2 (детокенизация файлов) — [`SHIFRATOR_SPEC_FILE_DECRYPT.md`](SHIFRATOR_SPEC_FILE_DECRYPT.md).
- Летопись этапов (baseline, замеры, хендоффы, находки — цифры и репро, могут врать
  относительно текущего кода) — [`archive/INDEX.md`](archive/INDEX.md).
- Замерный харнесс — `bench/` (`bench/README.md`).
- Отладочная подвыборка корпуса (не для baseline/коммита) — `tests/corpus/subsample.py`.
- Аудит лицензий зависимостей (прод+эксперимент, GPL/AGPL/non-commercial НЕТ; единственная
  жёлтая — CC BY-SA у словаря OpenCorpora) — [`LICENSES_AUDIT.md`](LICENSES_AUDIT.md) +
  черновик [`THIRD_PARTY_LICENSES.md`](THIRD_PARTY_LICENSES.md).
