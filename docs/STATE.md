# STATE — живой снимок SHIFRATOR

**Этот файл перезаписывается целиком в конце каждого этапа.** Здесь только «как
сейчас». История — [`JOURNAL.md`](JOURNAL.md), он только дописывается.
Устройство — [`ARCHITECTURE.md`](ARCHITECTURE.md). Открытые долги —
[`FINDINGS.md`](FINDINGS.md).

**Снимок на: 2026-08-04**, этап **C+** (измерительные инструменты и финал
документации), ветка `main`, не пушено.

Источник истины — код и прогоны. Разошёлся с ними — прав код, а файл чинится.

---

## 1. Проверить состояние двумя командами

```
venv/Scripts/python.exe -m pytest -q            # набор тестов, ~15 мин
venv/Scripts/python.exe tests/corpus/gate.py    # полный гейт, 324 док., ~10 мин
```

**Набор тестов зелёный:** `1334 passed, 3 skipped, 3 deselected, 10 xfailed`
за 895 с (этап T2, 2026-08-04; независимо подтверждено этапом DOC в тот же день).
`10 xfailed` — намеренно отложенные дефекты, `@xfail(strict=True)`: начнут
проходить — тест покраснеет (XPASS→FAIL), и это сигнал снять метку.

**Гейт зелёный:** последний полный прогон — 2026-08-04, этап S0: 324/324
документа, 0 крешей, 9 м 16,7 с, `MANIFEST.sha256` 659 файлов OK до и после,
все семь линий — дельта 0 против `results_baseline.json`.

## 2. Цифры последнего полного прогона

| Линия | Значение |
|---|---|
| **а** precision | PER 99,96 %, ORG 100 %, ADDRESS 99,92 %, INN 99,29 %, OGRN 98,69 %, KPP 97,00 %, остальные 100 %. PERCENT/TERM — n/a: эталона в корпусе v1 нет |
| **б** leak_v2 | Дельта 0 по всем 17 типам и по агрегату (17,2 % на пороге ≥8) |
| **в** masking A (round-trip) | 100,00 % — инвариант держится |
| **г** masking B (границы) | 91,60 % агрегат, 0 дельт по типам |
| **д** порча документа | 1721 маска «мимо всего» (PER 1, ORG 322, ADDRESS 7, PERCENT 1031, TERM 360), сверено с журналом `overmask_ledger.json` |
| **е** границы | Недобор 660 шт / 4557 симв., перебор 330 шт / 2709 симв. — 0 дельт |
| **ж** зеркало подавления | 0 |
| FP на негативах | 16 |
| Известный долг ADDRESS | 43 (реестр `docs/known_leaks_stage_c.json`) |

**Из 1721 «порчи» 1391 — не ошибка детектора, а свойство метрики:** PERCENT и
TERM появились этапом T2, эталона для них в старом корпусе нет вовсе. Их
настоящий замер — корпус v2: PERCENT recall 96,5 % / precision 100 %,
TERM 95,1 % / 100 %.

## 3. Типы

**21 объявленный тип**, 18 маскируемых (несут `token_prefix`), 3 отрицательных
класса-барьера (`CLAUSE_REF`, `ROLE_TERM`, `COLLECTIVE`) без маски.

Детектор есть у всех маскируемых, кроме `DATE` (`enabled: false` в
`entity_types.yaml` — код есть, детекция выключена конфигом).

Эталон в корпусе v1 есть у 15 типов; `PERCENT` и `TERM` размечены только в
корпусе v2, `INN_PERSON` — только в v1.

## 4. Рабочий контур документации

Четыре файла, больше в `docs/` быть не должно (правило `CLAUDE.md` «Рождение
документов»):

| Файл | Правило жизни | Строк | Потолок |
|---|---|---:|---|
| `CLAUDE.md` (корень) | правится по месту | 103 | **150** |
| `docs/JOURNAL.md` | только дописывается снизу | 255 | нет |
| `docs/STATE.md` | перезаписывается целиком каждый этап | этот файл | нет |
| `docs/ARCHITECTURE.md` | правится по месту | 249 | **400** |
| `docs/FINDINGS.md` | только открытые долги | 69 | нет |

Рядом два неприкасаемых: `PRODUCT_LEGAL.md` (материал к юристу) и `NN_HISTORY.md`
(вход в отложенное решение по второй нейросети). `known_leaks_stage_c.json` — не
документ: реестр долга ADDRESS, его читает гейт. Всё остальное — `docs/archive/`,
реестр переездов — `docs/archive/DOC_REGISTRY.md`.

### Стоимость сессии — сколько строк грузится в начало

Замер этапа C+ (2026-08-04):

| Что | Строк | Когда грузится |
|---|---:|---|
| корневой `CLAUDE.md` | 103 | **всегда** |
| `src/CLAUDE.md` | 40 | при работе в `src/` |
| `tests/corpus/CLAUDE.md` | 35 | при работе в `tests/corpus/` |
| `tests/corpus_v2/CLAUDE.md` | 21 | при работе в `tests/corpus_v2/` |
| **безусловный автозагруз** | **103** | |
| потолок при касании всех трёх зон | 199 | |

До этапа C+ безусловный автозагруз был **149 строк**: корневой `CLAUDE.md` (86)
плюс `.claude/rules/corpus-frozen.md` (35) и `.claude/rules/detection-wall.md`
(28) — этот каталог подхватывался целиком, независимо от того, какую зону трогает
сессия. Каталог `.claude/rules/` убран (`git mv` в `docs/archive/rules/`), смысл
перенесён во вложенные `CLAUDE.md`; безусловная часть **149 → 103**.

Это планка. Растёт — сокращать, а не мириться.

## 5. Что красное или висит

Ничего блокирующего: гейт и тесты зелёные. Висит следующее.

1. **Вспомогательная точка отсчёта подвыборки не переснята.**
   `tests/corpus/results_iter_baseline.json` снят до этапа T2, поэтому
   `subsample.py` показывает три «регресса на срезе» (PERCENT 0→110, TERM 0→34),
   которых на полном гейте нет. Инструмент пересборки —
   `tests/corpus/run_iter_baseline.py` (`promote_baseline.py` двигает ТОЛЬКО
   полную точку отсчёта). Это шум перед этапом REG, а не дефект продукта.
2. **Стена «детекторы не читают генератор корпуса v2» не имеет исполняемого
   стража** (`V2-WALL-NOGUARD`). Проверена чтением импортов, держится
   дисциплиной.
3. **Запрет детекции `TRANCHE` тоже без автостража** (`V2-TRANCHE-NOGUARD`,
   заведена этапом C+). Единственная исполняемая проверка — `validate.py`,
   запускается руками; `tests/test_corpus_v2_axis_coverage.py` TRANCHE прямо
   ИСКЛЮЧАЕТ и сторожем запрета никогда не был, хотя два документа так его
   называли. Формулировки исправлены.
4. **Недетерминизм детекции: механизм устранён, подтверждения на реальном
   документе владельца нет** (`EPRIME-A-CONFIRM`). Причина найдена этапом E″
   (кеш `_CapsResolver` ключевался `id(style._element)` без удержания ссылки),
   исправлена (`src/extractor.py:216,222`), закрыта стражами
   `tests/test_determinism.py`, подтверждена на фикстуре многостилья
   (2660 ложных попаданий → 0). Не сделан повторный прогон
   `experiments/stage_eprime_determinism/owner_repro.py` на реальном договоре.
5. **`experiments/` не разобран** — живой код вперемешку с логами прогонов.
   Опись — `docs/archive/DOC_REGISTRY.md` §4, решение отложено в отдельную сессию.

## 6. Где мы по этапам

Пройдено: **0a–0d**, **1/1b**, **2/2b**, **3**, **4**, **A/A′/B/C/C′**, **D**,
**E/E′/E″**, **O2**, **U1–U5b**, **S1–S3**, **T0/T0V/GENV**, **CORPUS-V2/V2-B**,
**T-GOLD-A**, **T1**, **T1-UI**, **U4-FIX**, **T2-INN**, **ADDR-B**, **BUILD**,
**GATE-2**, **T-ARB**, **T4**, **T2**, **S0**, **DOC**, **C+** (этот).
По каждому — запись в [`JOURNAL.md`](JOURNAL.md); отчёты этапов до DOC
включительно лежат в `docs/archive/reports/`.

Впереди: **A, DOG, C, B, T3, REG**.

## 7. Что закрыто, но ещё встречается в старых текстах

- **U4-G-TIMEOUT закрыт** коммитом `e44ac61` (2026-07-26): вложенный прогон
  оптимизирован этапом O2 (488 c → 295 c), лимит поднят 300 → 900 c.
- **Гейт — семь линий, не шесть.** `GATE2_REPORT.md` описывает шесть: линия «ж»
  добавлена позже, этапом T4.
- **Быстрый набор — 33 документа, не 25 и не 31.** Потолок рос 25 → 28 → 31 → 33
  вместе с новыми типами.
- **`.claude/rules/` больше нет.** Правила зон живут во вложенных `CLAUDE.md`;
  старые копии — в `docs/archive/rules/` как исторический слепок.
