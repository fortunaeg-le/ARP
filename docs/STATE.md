# STATE — живой снимок SHIFRATOR

**Этот файл перезаписывается целиком в конце каждого этапа.** Здесь только «как
сейчас». История — [`JOURNAL.md`](JOURNAL.md), он только дописывается.
Устройство — [`ARCHITECTURE.md`](ARCHITECTURE.md). Открытые долги —
[`FINDINGS.md`](FINDINGS.md).

**Снимок на: 2026-08-08**, этап **NODES** — чтение документа целиком, в `main`,
**не пушено**.

**ГЕЙТ КРАСНЫЙ — 11 регрессов, все объяснены поимённо и все из документов с
колонтитулом.** Это не регресс детекции: колонтитул раньше не читался вовсе, и
метрика видела «утечки нет» потому, что кусок документа выбрасывался. Разбор —
§6. **Планка НЕ двигалась**, `promote_baseline.py` не запускался,
`MANIFEST.sha256` 659 OK до и после. **Пересборку точки отсчёта решает владелец
— см. §0 пункт 2.**

Набор тестов: **1518 passed, 3 skipped, 6 xfailed, 0 failed.**

Источник истины — код и прогоны. Разошёлся с ними — прав код, а файл чинится.

---

## 0. Решения владельца, которых ждёт работа

1. **`DOG-REPO-BLOB` — реальный договор в истории git. ЛОКАЛЬНО ВЫЧИЩЕН
   2026-08-05 (задача LEAK-PURGE), НА СЕРВЕРЕ ОСТАЁТСЯ.** Бэкап (две копии) —
   `C:\Jesus\ARP_LEAK_PURGE_BACKUP_20260805`, **не удалять до закрытия вопроса
   по серверу**. На GitHub документ отдаётся по SHA — нужна сборка мусора
   силами хостинга либо пересоздание репозитория. Решение отложено до проверки
   форков. **Повторение этого пути теперь заперто** (см. §2, замок 2.2), но
   УЖЕ СЛУЧИВШЕЕСЯ замок не отменяет.

2. **Точка отсчёта гейта после NODES.** Гейт красный по 11 строкам, все
   разобраны поимённо (§6) и все — следствие того, что колонтитулы стали
   читаться. Пересборка `results_baseline.json` возможна ТОЛЬКО через
   `promote_baseline.py` с автором и обоснованием; я её не делал сознательно —
   размен «читать колонтитул» против «+13 частичных утечек на мутированных
   документах» это решение владельца, а не сессии.

## 1. Проверить состояние двумя командами

```
venv/Scripts/python.exe -m pytest -q            # набор тестов, ~7 мин (xdist -n auto)
venv/Scripts/python.exe tests/corpus/gate.py    # полный гейт, 324 док., ~10 мин
```

**Набор тестов (приёмка NODES): 1518 passed, 3 skipped, 6 xfailed, 0 failed.**
Рост с 1426 (FIX-2) — новый `tests/test_nodes.py` и разросшаяся параметризация
зонных тестов. `xfailed` 8 → 6: два будущих контракта
`TestStage6UnreadZones[header/footer]` ИСПОЛНИЛИСЬ (XPASS strict), маркер снят
поштучно; сноска, надпись и вложенная таблица остались xfail — они не сделаны.

⚠ Готчи прогонов, стоившие времени:
- **в свежем `git worktree` нет `venv/`** — сразу junction (правило в `CLAUDE.md`);
- **`gate.py` падает `UnicodeEncodeError`**, если stdout перенаправлен в файл под
  cp1251 — гонять с `PYTHONIOENCODING=utf-8`; вердикт по готовому дампу берётся
  без повторного прогона: `gate.py --results tests/corpus/results_gate_current.json`;
- **проба, трогающая общий корпус, ломает `xdist`**: соседний воркер видит
  подложенный файл как настоящее нарушение. Пробы — на своём дереве через
  подмену `HERE`/`MANIFEST` (`tests/test_fix2_guards.py`).

## 2. Что принёс NODES

- **Перепись узлов `.docx` — закрытый список.** Таблица из 24 строк с вердиктом
  и файлом:строкой у каждой — `ARCHITECTURE.md` §10. Всё, чего в ней нет, даёт
  зону `unknown_node` и ОТКАЗ.
- **`DOG-NODE-SILENT` закрыт ЧТЕНИЕМ**, а не отказом: `w:ins`, `w:fldSimple`,
  `w:smartTag`, `w:sdt` (inline и блочный) читаются. `w:del` не читается — и это
  правильно, удалённая правка не видимое содержимое.
- **`DOG-ANCHOR-SPLIT` закрыт вместе с ним и строго сильнее**: якорь и значение
  снова в одном сегменте, маска ставится, а не теряется вместе с абзацем.
- **Колонтитулы читаются** тем же конвейером, что тело; зоной больше не
  являются. Реальный документ: непрочитанных зон **6 → 0**.
- **Страж закрытого списка** — живая проба на пяти случаях (`m:oMath` в теле и в
  колонтитуле, `w:sdt` вокруг строки таблицы).

## 3. Механизм: два слоя, а не один список

Наборы `BLOCK_/CELL_/TABLE_/ROW_/INLINE_` (`src/unread_zones.py:73-115`) говорят,
КУДА идти. Сверка «каждый `w:t` части достигнут читателем»
(`_scan_readable_part:380`) проверяет, что текста в стороне не осталось.
**Полноту доказывает вторая, а не первая** — список сам себя проверить не может,
и ровно на этом жил `DOG-NODE-SILENT`.

`src/extractor.py` ИМПОРТИРУЕТ те же наборы: второй копии списка в системе нет,
разойтись читателю и сторожу негде по построению (страж —
`test_nodes.py::TestCensusRegistry::test_reader_and_scanner_share_one_registry`).

⚠ Границы обхода намеренно повторяют ОГРАНИЧЕНИЯ читателя, а не разметку OOXML:
в ячейку таблицы python-docx заглядывает только за прямыми `w:p`, поэтому и
сканер там не спускается ни в `w:sdt`, ни во вложенную `w:tbl`. Сделай иначе —
сторож объявит прочитанным то, что читатель не берёт.

⚠ Части колонтитулов берутся из СВЯЗЕЙ пакета, не из `document.sections`: раздел
с `is_linked_to_previous` вернул бы ту же часть дважды, а раздел без явной
ссылки — ни разу. `table_index` СКВОЗНОЙ по частям — совпади номер, таблица тела
и таблица колонтитула склеились бы в одну.

## 3b. Два запрета, которые держались ничем (закрыты этапом FIX-2)

| Запрет | Чем держался | Чем держится теперь | Живая проба |
|---|---|---|---|
| **2** — корпус v1 заморожен | `MANIFEST.sha256` = БЕЛЫЙ СПИСОК: имена читал из себя, видел «изменился» и «исчез», **не видел «ПОЯВИЛСЯ»** | `gate.check_manifest` сверяет состав охраняемых каталогов в ОБЕ стороны; каталоги выведены из самого манифеста (`docs/`, `_model/`) | лишний файл в `tests/corpus/docs/` → `ok=False`, «ФАЙЛ ПОЯВИЛСЯ»; после уборки → `ok=True` |
| **7** — реальные документы не коммитить | список игнорирования ПО ИМЕНАМ; **уже не сработал** (`DOG-REPO-BLOB`) | `.claude/hooks/guard.py`: `git add/commit/stash` осматривают содержимое по трём признакам — офисный формат вне корпуса, размер вне 64 КБ, `word/document.xml` внутри ZIP с любым именем | коммит `.docx` вне корпуса → `exit 2`; после уборки → `exit 0`; `selftest.py` 31/31 |

Замок 7 стоит **только на попадании в историю** — локальная работа с реальным
документом не ограничена ничем. Это ДЕШЁВЫЙ РУБЕЖ, а не поиск ПДн: тяжёлая
проверка на каждом коммите была бы снята первой же сессией.

## 4. Якорь через границу ячейки (`DOG-ANCHOR-SPLIT`, вариант ВЁРСТКИ) — закрыт FIX-2

У класса было два носителя, и закрывались они разными этапами: якорь, оторванный
ГРАНИЦЕЙ ЯЧЕЙКИ (ниже, FIX-2), и якорь, спрятанный УЗЛОМ-ОБЁРТКОЙ (`w:ins` и
прочие — NODES, §2). Долг в `FINDINGS.md` закрыт целиком.


Класс зеркальный A6: там разрыв рвал ЗНАЧЕНИЕ, здесь отрывает ЯКОРЬ от
значения («БИК» в одной ячейке строки, число в соседней). Нового механизма нет:
`detect_regex(anchor_prefix=…)` — текст слева, видимый ТОЛЬКО якорю;
`tokenizer._detect_boundary_entities` шаг 3c приклеивает хвост соседней ячейки.
**Граница различается по разметке, не по символу** (пустой `_boundary_sep`, то
есть `table_index`/`row_index`); мягкий перенос и граница абзаца не берутся.
**Направление ошибки — «лучше не найти»:** спан и координаты прежние, анти-якорь
и ограничитель соседних цифр смотрят на ту же склейку и только ужесточают.

**БИК реального договора: 0 → 1. 690 ложных БИК НЕ вернулись** (страж
`test_the_same_number_without_the_anchor_is_not_masked`).

⚠ Попутная находка: замер реального документа шёл по КОРОТКОМУ пути
(`pipeline.run_detection` — только посегментная детекция), где граничных окон
нет вовсе. Переведён на продуктовый `resolve_for_masking`. Отсюда `ORG 69→70`
в §6 — смена пути замера, не движение детекции.

## 5. Окно анти-якоря — своё у каждого типа

Ключ `anti_anchor_window:` в `entity_types.yaml`, умолчание 20 **не тронуто**.
Расширены ровно три типа: `PHONE`, `PERCENT`, `TERM` → 60. **`PASSPORT` остался
на 20** — проверяется стражем, а не обещанием.

Из семи видов случая, не закрытых этапом NEG: **4 закрыты полностью**
(`phone/doc-number`, `phone/registry`, `term/history`, `term/report-period` —
все 138→0), **2 частично** (`percent/impurity` 138→40, `percent/norm-ref`
138→52), **1 НЕ закрыт**: `term/norm-ref` 138/138 — дисквалификатор стоит
ПОСЛЕ значения («…как установлено статьёй 725 ГК РФ»), а окно левостороннее по
построению. Расширением окна недостижимо ни при каком N; нужен правый край —
**это новый механизм, и он не начат** (`FIX2-ANTIANCHOR-RIGHT`).

## 6. Гейт после NODES — КРАСНЫЙ, разбор поимённо

| Линия | Значение | Дельта |
|---|---|---|
| **а** precision | все 15 типов без движения; FP по негативам 2 | **0** |
| **б** leak_v2 | PER 989→993, PHONE 101→102, EMAIL 112→117, TOTAL 1826→1836 | **+10 / КРАСНОЕ** |
| **в** masking A | 100,00 % — инвариант держится | **0** |
| **г** masking B | 90,74 → 90,78 % | улучшение |
| **д** порча прозы | 1717 → 1717 | **0** |
| **е** границы | недобор PER 287→288 (символов 2059→2062), ВСЕГО 761→762 | **+1 / КРАСНОЕ** |
| **ж** зеркало подавления | 0 | 0 |
| Масок всего | 12600 → 12733 (на эталоне 10875 → **10931**) | + |
| Исходы | processed 324, крешей 0, отказов 0; `MANIFEST.sha256` 659 OK до и после | — |

**Разбор красного (это условие приёмки, а не оправдание).** Изменившихся
сущностей 56, **все до одной — в документах с признаком `ftr_pii`, ни одной из
тела**. 55 из них в сторону улучшения (`found` False→True, граница
`not_found`→`exact`); одна, `labor_0004__m2718_case` PER, найдена с недобором 3
символов — это и есть красная строка линии «е». Статус `leak_v2` сменили 13
сущностей, все на МУТИРОВАННЫХ вариантах (омоглиф / невидимые / перенос /
combo): `cession_0002__m1337_linebreak` EMAIL, `labor_0002__m1337_combo` EMAIL ×3,
`labor_0010__m2718_invisible` EMAIL ×2, `lease_0002__m2718_combo` PER,
`lease_0008__m2718_linebreak` PHONE + EMAIL, `lease_0010__m1337_linebreak` PER,
`loan_0004__m2718_combo` PER, `services_0014__m2718_linebreak` EMAIL,
`works_0008__m1337_combo` PER.

**Механизм честный и неприятный.** Раньше значение колонтитула не попадало в
анонимный текст ВОВСЕ — документ либо отклонялся, либо шёл `--allow-lossy` с
сайдкаром. Метрика видела `leak=none` не потому, что система защитила ПДн, а
потому, что выбросила кусок документа (ровно та ловушка, о которой предупреждает
`tests/test_future_contracts.py:510-529`). Теперь текст читается, и там, где
детекция известным образом не справляется — омоглифы и переносы внутри значения,
открытые долги, — значение уходит частично открытым.

**Итог по экспозиции всё равно в плюс:** recall 85,2 → 85,7 %, масок на эталоне
10875 → 10931, «открыто символов» 23325 → **22564**, «без маски своего типа»
1754 → 1698.

**Реальный документ** (`C:\shifrator_real\dog.docx`, продуктовый путь, набор
«Максимум»): **непрочитанных зон 6 → 0, encrypt больше не отказывает** — это и
есть выигрыш этапа. Числа по типам НЕ сдвинулись ни на одну единицу: BIK 1,
OGRN 33, ORG 70, PERSON 67, ADDRESS 28, EMAIL 10, PERCENT 84, TERM 77, SUM 8 —
цифра в цифру с MERGE-1. В восьми колонтитулах документа 58 символов, ПДн среди
них нет: номера страниц и имя исходного файла (**детектора имён файлов нет, и
решения о нём владелец не принимал**). Ожидание «что нашлось в колонтитулах»
исполнилось нулём — это факт документа, а не отказ мерить.

**Детерминизм:** 3 прогона на четырёх фикстурах, отпечаток сегментов и зон
совпал побитно (различных результатов 1 из 3).

**Восстановление:** `decrypt-file` вернул значение в СВОЮ часть архива
(`header1.xml`, `footer1.xml`); текст всех трёх переписанных частей совпал
побайтно. Разница в байтах самих частей ровно +1 и одинакова для
`document.xml`, которого правка не касалась, — это пред-существующая
сериализация (`ooxml_core.serialize_xml`), а не след этапа.

**Корпус v2, ложные маски на двойниках: 1891** (этапом NODES не мерился —
не его зона).

## 7. Собранная программа

`pyinstaller packaging/shifrator.spec` проходит; строгий страж спеки:
«hiddenimports покрывает все модули `src/` и `app/` (30 имён)». Живая проверка
`dist/SHIFRATOR/SHIFRATOR.exe` (порт из `launcher.lock`, **НЕ угадывать**) —
**все семь проверок пройдены**: запуск и интерфейс, обезличивание `agency_0003`
(18 масок), экран «Что маскировать», переключение набора, отчёт программы,
восстановление ИЗ СОХРАНЁННОЙ СЕССИИ по её id (нерешённых токенов 0, масок 0,
16 значений из 18 побайтно — два в канонической форме, этап E), отказ на
документе с непрочитанными зонами. Метка сборки — `build-20260729`.

## 8. Хронометр

`.claude/hooks/chrono.py` пишет `.chrono/<session>.tsv` сам.
Окно NODES: 16:20-17:35, ~75 минут, 66 вызовов, собственное время ~810 с
(гейт и два полных набора шли в фоне — в `sec` прибора не входят).

## 9. Типы

**21 объявленный тип**, 18 маскируемых, 3 отрицательных класса-барьера.
Детектор есть у всех маскируемых, кроме `DATE` (выключен конфигом). Эталон v1 —
15 типов; `PERCENT`/`TERM` — только v2; `INN_PERSON` — только v1. Негативы —
у 17 из 18 (без `EMAIL`, осознанно); в v2 наборы `negatives_a3`, `negatives_a7`
и `negatives_neg` (40 двойников, 8 типов × 5 видов случая, поле `kind`).

## 10. Рабочий контур документации

| Файл | Правило жизни | Потолок |
|---|---|---|
| `CLAUDE.md` (корень) | правится по месту | **150** строк |
| `docs/JOURNAL.md` | только дописывается снизу | нет |
| `docs/STATE.md` | перезаписывается целиком каждый этап | нет |
| `docs/ARCHITECTURE.md` | правится по месту | **400** строк |
| `docs/FINDINGS.md` | только открытые долги | нет |

Рядом неприкасаемые: `PRODUCT_LEGAL.md`, `NN_HISTORY.md`;
`known_leaks_stage_c.json` — реестр долга ADDRESS, читается гейтом. Остальное —
`docs/archive/`, опись переездов — `docs/archive/DOC_REGISTRY.md`; опись живых
инструментов разведки — `experiments/INDEX.md`. Замки в `.claude/hooks/`
(само-тест `selftest.py`, 31 проверка, гоняется из `pytest`).

## 11. Что красное или висит

1. **Гейт красный по 11 строкам** — см. §6 и §0 пункт 2. Решение о точке
   отсчёта за владельцем.
2. **`DOG-REPO-BLOB`** — см. §0. Решение владельца (сервер).
3. **`NODES-ATTR-TEXT`** (новое) — перепись закрывает текст в ЭЛЕМЕНТАХ, но не в
   АТРИБУТАХ: описание рисунка (`wp:docPr/@descr`), свойства пакета
   (`docProps/*`). Частота НЕ измерена. Расширять сверку вслепую нельзя —
   `docProps` есть в КАЖДОМ `.docx`, и отказ стал бы отказом на всём.
4. **`NODES-FOOTER-PAGENUM`** (новое) — «Стр. 1 из 3» маскируется как ADDRESS.
   Дефект ПРЕД-существующий (воспроизведён на голом `.txt`), но этап резко
   поднял частоту: нумерация живёт именно в нижнем колонтитуле. Зона детекции.
5. **Сноски, концевые сноски, примечания, надписи, вложенные таблицы** —
   по-прежнему ЗОНА + ОТКАЗ, и это честно объявлено (`ARCHITECTURE.md` §10).
   Три из пяти будущих контрактов `TestStage6UnreadZones` остаются xfail.
6. **`A7-ORG-NOANCHOR-UNSOLVABLE`** — организация без структурного якоря
   правилами НЕ РЕШАЕТСЯ; нужно решение владельца о цене.
7. **`FIX2-ANTIANCHOR-RIGHT`** — окно анти-якоря левостороннее, дисквалификатор
   ПОСЛЕ значения недостижим (`term/norm-ref`, 138/138). Не начат.
8. **Остатки переусердствования, 1891 ложных на двойниках v2:** `ACCOUNT` 354,
   `INN` 114, `INN_PER` 51 — упираются в инвариант этапа 4 и в ширину якорного
   окна, а не в отсутствие защиты. Решение владельца.
9. **`STORE-ACCUM-CONFIRM`**, **`V2-WALL-NOGUARD`**, **`V2-TRANCHE-NOGUARD`**,
   **yargy 73 % времени гейта** — без изменений.
10. **Часть 5 STORE**: тексты экрана «Что хранится» ждут утверждения владельцем.

## 12. Где мы по этапам

Пройдено: **0a-0d**, **1/1b**, **2/2b**, **3**, **4**, **A/A′/B/C/C′**, **D**,
**E/E′/E″**, **O2**, **U1-U5b**, **S1-S3**, **T0/T0V/GENV**, **CORPUS-V2/V2-B**,
**T-GOLD-A**, **T1**, **T1-UI**, **U4-FIX**, **T2-INN**, **ADDR-B**, **BUILD**,
**GATE-2**, **T-ARB**, **T4**, **T2**, **S0**, **DOC**, **C+**, **SPEED**,
**TESTOPT**, **DOG**, **A1**-**A6**, **BASE-A6**, **EXP**, **INSTR**, **A7**,
**STORE**, **MERGE-1**, **NEG**, **AUDIT**, **FIX-2**, **MERGE-2**,
**NODES** (этот).

Впереди: решение владельца по точке отсчёта гейта; чтение оставшихся зон
(сноски, примечания, надписи, вложенные таблицы); системная проверка
измерительного слоя; решение владельца по безъякорному ORG и по остаткам
переусердствования ACCOUNT/INN/INN_PER; решение владельца по `DOG-REPO-BLOB`
на сервере.

## 13. Ветки и рабочие деревья

Этап NODES сделан прямо в `main` одним коммитом, **не пушено**. Ветки блоков 1 и
2 (`exp`, `instr`, `a7`, `store`, `audit`, `neg`) **не удалены и лежат** — они
уже в `main`, но остаются точками отката.

**Рабочих деревьев `C:\Jesus\ARP_*` больше нет** — сняты после слияния
(`git worktree remove`). Единственное дерево — `C:\Jesus\ARP`.

⚠ Заводя новое дерево (`git worktree add`), **сразу junction на `venv` из
основного дерева** (`mklink /J venv <root>\venv`) — правило в `CLAUDE.md`.
Снимая дерево, **junction убрать ПЕРВЫМ** (`rmdir <дерево>\venv` — снимает
только ссылку), иначе рекурсивное удаление рискует уйти по ней в настоящий
`venv` основного дерева.
