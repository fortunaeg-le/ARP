# docs/reports/ — сводка отчётов (первоисточники, исторические)

> **Все файлы в этом каталоге — ИСТОРИЧЕСКИЕ первоисточники.** Они фиксируют состояние на
> момент своего написания, **НЕ обновляются и МОГУТ ВРАТЬ** относительно текущего кода.
> Источник истины — код в `src/` и [`../STATE.md`](../STATE.md). Этот INDEX — сводка: что в
> каком отчёте искать и что из этого до сих пор правда. Отчёты оставлены целиком как
> доказательная база (цифры, репро, методики), а не как руководство.

Хронология (сверху вниз — от ранних к поздним):

| Отчёт | Этап | О чём, одной строкой | Что из этого ещё правда |
|---|---|---|---|
| `test_report_stage1.md` | раунд 1 | Первый прогон тестов блоков 1–7 | исторично |
| `FINDINGS.md` | раунд 2 | Находки #1–#10 в «грязных» сценариях | частью закрыто |
| `BREAKING_REPORT.md` | раунд 3 | Попытки заставить систему соврать/утечь: B1–B6 (B3 — граница сегментов) | B3-механизм актуален (см. ARCHITECTURE) |
| `COMPONENT2_TEST_REPORT.md` | компонент 2 | Тесты детокенизации файлов, группы A–G | ⚠️ описанный «намеренно красный» пин «ZIP без целевых частей» УЖЕ ЗАКРЫТ — код бросает `OoxmlError`, тест зелёный |
| `WAVE1_VERIFICATION.md` | волна 1 | P1 (Вариант А — раздельные токены B3-пар), тихий успех decrypt-file, регистровая утечка | актуально (инвариант в ARCHITECTURE) |
| `RECON_REPORT.md` | перед волной 2 | Разведка 3 гипотез (yargy-стоимость, синтаксис, батчинг) | основания решений — в DECISIONS |
| `DETECTION_REBUILD.md` | волна 2 (исполнитель) | Пересборка детекции адреса: yargy сканер→парсер (A), синтаксис «ORG+ФИО» (B), батчинг B3 (C); recall 89/89 | recall-числа актуальны |
| `WAVE2_VERIFICATION.md` | волна 2 (проверка) | Подтверждает recall 89/89 + 15/15, находит 3 дефекта over-capture W2-D1/D2/D3 | ⚠️ **W2-D1/D2/D3 ИСПРАВЛЕНЫ и закоммичены** (`04452df`) — статус «блокер/открыто» в этом отчёте УСТАРЕЛ; методики и репро остаются доказательной базой |
| `COVERAGE_MEASUREMENT.md` | этап 2 (замер) | Полнота/утечки по замороженному корпусу (324 док.): residual leak 100 % обработанных, recall 55.9 %, **креш encrypt на 31 док.**, SNILS/BIRTHDATE без детектора | цифры актуальны для `04452df`; харнесс `tests/corpus/run_measurement.py` переиспользуем для контроля регресса |
| `COVERAGE_AUDIT.md` | этап 3 (враждебный аудит) | Независимая проверка замера: все цифры воспроизводятся байт-в-байт (recall/precision/креши/misses), детерминизм 0/10315; НО residual leak **занижен** — +68 недосчитанных числовых частичных утечек и −1070 сущностей крешей из знаменателя §2 → истинный leak **55.3 %**, а не 50.0 % | вердикт: **верить с оговорками**; отчёт не льстит системе, грешит в сторону недооценки утечек |
| `HANDOFF_STAGE_0A.md` | этап 0a (починка) | Фикс креша `encrypt` на 31/324 документах (`src/syntax_compound.py:137`, `o.end()`→`o.end`); все 324 документа теперь проходят без исключений; `pytest.ini` — `slow` исключён по умолчанию | актуально; следующий шаг — этап 0b (метрика утечки v2) |
| `HANDOFF_STAGE_0B.md` | этап 0b (метрика) | Добавлена `leak_v2` — честная частичная утечка в харнессе (v1 занижал: требовал выживания целого ядра); считается параллельно с v1; независимый нормализатор в `measure_lib.py` | актуально; метрика v2 — основа baseline 0c |
| `BASELINE.md` | этап 0c (замер) | **Актуальный baseline на `8330dd2`:** 324/324 обработаны, 0 крешей; recall_loose 56.3 %, leak_v1 49.7 %, leak_v2 45.9 %/40.9 % (BIK-excl); все разрезы (тип/категория/source/trick/FP); разведение эффектов 0a↔0b; диагностика ADDRESS | **актуально** — точка отсчёта регресс-гейта 0d (`results_baseline.json`) |
| `HANDOFF_STAGE_0C.md` | этап 0c (сдача) | Главные цифры baseline, что удивило (PHONE-порог, ADDRESS v2 −447, PASSPORT exact 0 %), куда смотреть на 0d (детекторы SNILS/BIRTHDATE, precision NER) | актуально |

**Пришпиливающие тесты (были красные «по замыслу», теперь ЗЕЛЁНЫЕ = дефекты закрыты):**
- `tests/test_wave2_overcapture.py` — пинил W2-D1/D2/D3. Зелёный. Не «зеленить» вручную —
  это защита от регресса; поломка чинится в коде.
- `tests/component2/test_e_malformed_input.py` — «ZIP без целевых частей» теперь ожидает
  `OoxmlError` и проходит.

Актуальный, приоритизированный список открытого — [`../STATE.md`](../STATE.md).
Замерные скрипты — `bench/` (см. `bench/README.md`).
