# ЧАСТЬ 0. СВЕРКА ЕДИНИЦ (один дамп, HEAD, 324 док.)
единица LAYOUT  — утекающих всего 1488; из них с символом вёрстки в эталоне: 786
единица R-RDR   — ошибочных с меткой вёрстки: 1367
пересечение двух определений: 414
только LAYOUT (утечка+символ, но метки вёрстки нет или ошибка не в поиске): 372
только R-RDR (метка вёрстки и ошибка, но НЕ утекает или символа нет): 953
  из них не утекает вовсе: 908
  из них утекает, но символа вёрстки в эталоне нет: 45
классы ошибок внутри единицы LAYOUT: [('не найдено', 400), ('недобор', 200), ('выжило в другом месте', 101), ('ok', 80), ('перебор', 5)]

# ЧАСТЬ 2. МЕХАНИЗМЫ (единица: ошибочная сущность с меткой; в скобках — из них утекающих)
  перенос строки внутри значения                                 всего  1151  ошибок   864  (утекает  333)
      mut:linebreak 849, linebreak_inside_entity 15
  разрыв между ячейками таблицы                                  всего    43  ошибок    43  (утекает   26)
      cell_boundary_split 29, mut:cell_split 14
  разрыв внутри абзаца (куски текста/runs)                       всего    16  ошибок     0  (утекает    0)
      
  невидимые символы внутри значения                              всего  1260  ошибок   287  (утекает   81)
      mut:invisible 279, zwj_and_soft_hyphen 5, invisible_chars 3
  значение вне тела (колонтитул/сноска/надпись/вложенная таблица) всего   170  ошибок   142  (утекает    2)
      in_footnote 41, in_header 35, nested_table 32, in_textbox 32, in_footer 2
  плотная строка / голая ячейка без предложения                  всего   115  ошибок    31  (утекает   17)
      dense_line 16, bare_cell_no_sentence 15
  прочие метки вёрстки: нет

  МЕХАНИЗМЫ БЕЗ ДАННЫХ на корпусе v1 (метки такого приёма в разметке нет):
   поля и элементы управления содержимым — НЕ ИЗМЕРЕНО
   правки в режиме рецензирования        — НЕ ИЗМЕРЕНО
   гиперссылки                           — НЕ ИЗМЕРЕНО

# ЧАСТЬ 3. ЧТО ДОХОДИТ ДО ДЕТЕКЦИИ (зоны вне тела)
  in_footer        всего  30  ошибок   2  из них в док. с объявленной непрочитанной зоной   0
  in_footnote      всего  41  ошибок  41  из них в док. с объявленной непрочитанной зоной  41
  in_header        всего  35  ошибок  35  из них в док. с объявленной непрочитанной зоной  18
  in_textbox       всего  32  ошибок  32  из них в док. с объявленной непрочитанной зоной  32
  nested_table     всего  32  ошибок  32  из них в док. с объявленной непрочитанной зоной  32
  виды объявленных зон: [('nested_table', 30), ('footnote', 30), ('textbox', 30)]
