# ДАМПЫ ПРОГОНА в историю не идут: 8 МБ (v2) и 1.5 МБ (наборы) на срез, а
# восстанавливаются они одной командой `measure_all.py` на неизменном src/ —
# та же линия, что у SEAM-JOIN и CHAR-NORM (docs/archive/DOC_REGISTRY.md §4).
# ИДУТ В ИСТОРИЮ: METRICS_FULL.md (отчёт), metrics_full.json (машинная копия
# для автоматического сличения со следующим срезом), runs.json (хронометраж)
# и логи прогонов — они и есть доказательная база чисел отчёта.
results_v2.json
results_profiles.json
