# -*- coding: utf-8 -*-
"""PER-SPREAD — запись в docs/JOURNAL.md (только дописывается снизу)."""
import io

ENTRY = """

---

## Этап PER-SPREAD — распространение найденной фамилии по документу (2026-08-18)

**Зона:** детекция (`src/anchor_registry.py`). Ночной прогон, владельца рядом нет.

### 0. Что было заявлено и что оказалось на самом деле

Заявка этапа: «фамилия, однажды найденная в документе, не распространяется на
остальные свои упоминания». Разведка ДО правки показала, что **механизм
распространения давно есть и работает**: `AnchorEngine` (`anchor_registry.py:1368`)
— якорь → реестр документа по лемме фамилии → проход 2 по ВСЕМ сегментам с
сопоставлением по всем нормальным формам pymorphy (падежи бесплатно) →
`expand_match` до связного ФИО → `guard`. Второй такой механизм строить было
не нужно и не строился; чинились ТРИ РАЗРЫВА существующего.

Адреса разведки — `experiments/per_spread/RECON.md`.

### 1. Три разрыва, найденные прогоном (не рассуждением)

**F1. Фамилию не метит морфология — якоря НЕТ ВООБЩЕ.** Ключ реестра людей —
лемма фамилии, а фамилию даёт помета `Surn` pymorphy. Фамилиям-омонимам
нарицательного (Синица, Пчела, Белка, Кисель, Коваль, Мельник) и иностранным
(Ким, де Врис) помету не дают. `_surname_key` возвращает `None`, якоря нет, и
человек открыт во ВСЕХ упоминаниях сразу — вместе с паспортом и СНИЛС рядом.
`agency_0007`: 9 из 10 эталонных PER открыты, ни одного спана.

Лечение — БЕЗ словаря фамилий: слот фамилии определяется ПОЗИЦИОННО. Пара
«имя + отчество», опознанная морфологией, однозначно называет человека; слово
вплотную СЛЕВА от такой пары занимает слот фамилии русского порядка
«Фамилия Имя Отчество».

Почему только слева — замерено ДО кода (`surn_slot_survey.py`, 108 чистых док.):
левый слот дал 9 уникальных кандидатов, ВСЕ настоящие фамилии; правый
(«Имя Отчество Фамилия») дал 91 мусор («Адрес» 50, «Дата» 36, «Индивидуальный» 5
— соседние поля карточки реквизитов) против ОДНОЙ настоящей фамилии. Правый слот
не берётся.

**F2. Голая фамилия одна в ячейке.** `guard` не метил однословный спан в начале
предложения (защита от омонима «Мороз ударил»). Ячейка вложенной таблицы,
состоящая ЦЕЛИКОМ из фамилии, попадала под тот же признак: `labor_0004`, сегмент
`t1_r1_c0` = `'Беляев'` — фамилия в реестре, все прочие вхождения закрыты, это
уходило открытым. Послабление УЗКОЕ: слева и справа в сегменте нет ни одного
значащего символа, значит предложения, внутрь которого мог бы попасть омоним,
здесь нет по построению.

**F3. Перебег прогона ФИО через перенос строки.** `per_search_view` меняет `\\n`
на пробел, `_FIO_MAX_WORDS = 4` разрешает четвёртое слово, и в ячейке
`Кузнецова Татьяна Сергеевна\\nКузнецова Татьяна Сергеевна` спан ложился как
`Кузнецова Татьяна Сергеевна Кузнецова`, оставляя ВТОРОЕ «Татьяна Сергеевна»
открытым. Маска короче значения, а полнота рапортует успех — ровно тот случай,
ради которого утечка меряется отдельно от полноты. Лечится структурно: ФИО не
содержит двух ЧИСТЫХ фамилий подряд.

### 2. Две правки, отменённые собственным замером

**«Роман» (уточнение F3).** Первая редакция обрывала прогон по любой помете
`Surn`. Токен-затравка сам может нести и Name, и Surn: «Макаров Роман
Дмитриевич» распался на «Роман Дмитриевич» с ключом реестра `('роман',)` в шести
документах. Поймано **сличением реестров якорей до/после** (`keys_diff.py`) —
инструмент завёлся ровно для этого и оправдался. Признак усилен до ЧИСТОЙ
фамилии (`_pure_surname`).

**Счётчик ролей в `_fio_run` (правка 4, ОТМЕНЕНА).** Лечила «две записи подряд в
одной ячейке» в проходе 1, где болезни нет. Полный гейт: недобор границ PER
+12 сущностей / +142 символа, и **снята одиночная защита с одного значения**
(`supply_0016__m1337_linebreak`) — линия «и», допуск 0. Откачена целиком, код
сохранён в `experiments/per_spread/patch4.py` как измеренный тупик.

**Правка 4' (принята).** Болезнь оказалась в ПРОХОДЕ 2: `expand_match`
расширялся влево на ЧУЖУЮ уже закрытую запись, спан пересекался с её маской, и
движок выбрасывал вхождение ЦЕЛИКОМ — вторая запись оставалась открытой
полностью. Пересечение — не приговор вхождению: повторяем расширение БЕЗ левого
крыла. Проход 1 не меняется вовсе.

### 3. Замер ДО/ПОСЛЕ — полный корпус v1, 324 документа

Точка «до» — `results_gate_current.json` коммита `b39aa57` (`src/` между ним и
началом этапа не менялся, проверено `git diff`). Точка «после» — прогон гейта
этого этапа. Сводка — `experiments/per_spread/compare.txt`.

**Утечка (`leak_v2`), продуктовый критерий — СНАЧАЛА ОНА:**

| счёт | до | после | дельта |
|---|---|---|---|
| PER, вхождений с утечкой | 894 из 3420 (26.14 %) | **615 (17.98 %)** | **−279** |
| PER, сущностей с утечкой | 567 из 2272 | **428** | **−139** |
| ВСЕ типы, вхождений с утечкой | 1533 из 12033 | **1254** | **−279** |
| PER, вхождений не найдено | 790 | 670 | −120 |

**Люди, защищённые ЦЕЛИКОМ** (все формы PER документа сведены по ключу фамилии,
`by_entity.person_key`; защищён = ни одна форма не дала утечки):

| | до | после |
|---|---|---|
| весь корпус (1824 человека) | 1398 — **76.64 %** | 1502 — **82.35 %** |
| чистые документы (581 человек) | 522 — **89.85 %** | 566 — **97.42 %** |

Старая оценка «72.7 % → 84.6 %» этим замером НЕ подтверждается и не
опровергается: она снята другим прибором (по маске, не по утечке) и на другую
дату. Здесь — своё измерение своим прибором, обе точки сняты сегодня.

**Ни один другой тип не сдвинулся ни на единицу** — ни по утечке, ни по
ненайденному: ADDRESS 210→210, PASSPORT 140→140, ORG 88→88, PHONE 46→46,
INN_PER 33→33, INN 28→28, BIK 20→20, KPP 19→19, SUM 17→17, OGRN 14→14,
ACCOUNT 10→10, BIRTHDATE 10→10, SNILS 4→4, EMAIL 0→0.

### 4. ЦЕНА В ЛОЖНЫХ СРАБАТЫВАНИЯХ — отдельной строкой

| класс ложных | до | после | дельта |
|---|---|---|---|
| **on-nothing** (порча документа: ни эталон, ни объявленный негатив) | 2544 | 2548 | **+4** |
| **on-negative** (маска на том, что корпус ПРЯМО объявил не-ПДн) | 318 | 318 | **0** |
| cross-type (эталон есть, тип маски другой — текст не испорчен) | 423 | 442 | +19 |
| precision[PER] | 99.96 % | **99.97 %** | лучше |

Все четыре новые on-nothing-маски названы поимённо и все — ORG, не PER:
`'ИП Мельник Д. Ю.'` ×3, `'ИП Гус\\nева Е. Ю.'` ×1. Механизм не в новом правиле, а
в существующем `syntax_compound`: он склеивает «ИП + ФИО» в одну сущность и
называет её `ORG`. Раньше этих людей не находили вовсе, склеивать было нечего.

**Ноль масок на объявленных негативах** — в частности, ни одной на
«Директор Отдела Продаж» и родне. Слот фамилии не занимают роль, статус физлица
и org-форма; проверяется теми же выражениями, которые детектор уже держит как
маркеры-доказательства якоря. Новых списков запрещённых слов не заведено.

### 5. Гейт: 16 красных строк против привычных 17

`experiments/per_spread/gate_per_spread.log`. 324 документа, крешей 0, отказов 0,
`MANIFEST.sha256` OK до и после. Планка не двигалась, `promote_baseline.py` не
запускался, корпус v1 не тронут.

**Ушли четыре красные строки** (все — прямое следствие этапа):
`TOTAL(BIK-excl)[leak_v2>=6]` 1470→1513 стало 1470→**1234**; то же для `>=8`;
`границы[ВСЕГО / недобор]` 471→472 стало 471→**470**, символов 3347→3359 стало
3347→**3329**.

**Пришли три красные строки, и все три — линия «г» (masking B):**

* `B границы / PER`: 84.45 % → 83.52 %. **Доля упала при РОСТЕ числителя.**
  В абсолюте (`experiments/per_spread/mc_b.txt`): масок, чья эталонная сущность
  покрыта объединением масок ЦЕЛИКОМ, стало **на 122 больше** (2667→2789), но
  масок, вообще легших на эталон, — на 173 больше (3119→3292). 51 новая маска
  покрывает своего человека не целиком. Это не ухудшение защиты: раньше у этих
  людей маски не было НИКАКОЙ, и в знаменатель они не входили — нечего было
  оценивать. Частичная защита пришла на место нулевой.
* `B границы` (агрегат): 88.14 % → 87.92 % — следует за PER, других слагаемых
  у падения нет.
* `B границы / ADDRESS`: 85.55 % → 85.51 %. **Про адреса здесь нет ничего.**
  Числитель и знаменатель упали на одно и то же число — 3 (1101/1287 →
  1098/1284). Это три маски `labor_0010` и двух его мутаций: текст
  `'Д.Ю.Мельник ИНН'` прежде закрывался маской `ADDRESS` (тип неверный, но по B
  зачтено), а теперь — маской `ORG` того самого ИП-склеивания. Три маски ушли из
  знаменателя ADDRESS в знаменатель ORG, где `B` вырос (92.23 % → 92.43 %).

**Тринадцать оставшихся красных строк — ровно те же, что на `main`** (KPP, BIK,
PASSPORT по линии «б»; `B границы / PASSPORT`; PHONE и EMAIL по линии «е»), с
теми же числами: этап их не касался.

**Строка «д» ухудшилась на 4 маски**: `over-mask прозы[ORG]` на `main` 322→347,
здесь 322→351. Те самые четыре ИП-склейки из §4.

**Реестр линии «и» пересобран** (`build_single_guard_registry.py`, 394 случая).
Причина законна и направление безопасно: **в класс вошли 19 значений, из класса
не вышло НИ ОДНО** (только в дампе 19, только в реестре 0). Это те же
новонайденные люди, которых `syntax_compound` подписывает `ORG`. Долг
`DEFGATE-CROSSTYPE-LATENT` вырос с 409/375 до **431/394** — записано в
`FINDINGS.md` как прямая цена этапа. Лечение (типизация «ИП + ФИО» как PER) —
смена победителя арбитража, решение владельца, ночью не принималось.

### 6. Приёмка

* **Полный набор тестов:** `1829 passed, 3 skipped, 6 xfailed in 568.97s` —
  **0 failed**. Двенадцать новых — `tests/test_per_spread.py`, и семь из них
  держат ЦЕНУ, а не выигрыш: роль/статус/org-форма не занимают слот фамилии,
  правый слот не берётся, улица-однофамилец не человек, омоним в начале
  предложения по-прежнему не метится, двусмысленный токен не режет ФИО.
* **Круг на собранной программе:** `КРУГ ПРОЙДЕН`, 17 шагов
  (`experiments/per_spread/accept_circle.log`). Этап тронул `src/` — круг
  обязателен по запрету 9.
* **Гейт:** прогнан, лог сохранён, 16 красных строк, каждая объяснена выше.
* **Планка НЕ двигалась.** Пересборка планки — отдельное решение владельца.

### 7. Что осталось открытым

* `PER-SPREAD-CASE-KILLED` (новый долг в `FINDINGS.md`): единственный документ,
  где правка ухудшила ТЯЖЕСТЬ утечки — `lease_0006__m2718_case` (весь текст
  строчный): 2 полных + 8 частичных → 7 полных + 3 частичных, число утёкших
  вхождений то же (10). Прежде часть значения случайно накрывал перебег прогона
  ФИО; перебег убран как дефект, а подобрать вхождение проходом 2 нельзя —
  `guard` требует заглавной, а её в документе нет ни одной. Послабление guard'а
  на документах с уничтоженным регистром — размен точности на полноту, решение
  владельца.
* Остаток утечки PER по чистым документам — 28 вхождений из 1140 (было 152).
  Что там осталось: `Ли Вэй` и `де Врис` (нет ни отчества, ни склонения — слот
  фамилии на них не работает по построению), «Тер-» от «Тер-Петросян»,
  одиночные фрагменты имён.

**ЗАБЛОКИРОВАНО: ничего.** Данных хватило на всё: и на разведку, и на обе
отменённые правки, и на объяснение каждой красной строки.

**Что удивило.** (1) Самая крупная дыра оказалась НЕ там, где заявлено: не
«найденная фамилия не распространяется», а «фамилия не находится вовсе, потому
что её не знает морфология» — и человек открыт целиком, а не частично.
(2) Правка, вылеченная в правильном месте (проход 2, 8 строк), дала −279
утечки; она же, вылеченная на один слой раньше (проход 1, счётчик ролей),
давала −245 и снимала защиту с чужого значения. Место лечения стоило 34 вхождений.
(3) Прибор «сличение реестров якорей до/после» поймал два разных дефекта, ни
один из которых не показал бы ни один тест: оба были не «сломалось», а
«появился лишний ключ».

**Слияние.** `per-spread -> main`: `src/anchor_registry.py`,
`tests/test_per_spread.py` (новый), `tests/corpus/known_single_guard.json`
(пересобран), `tests/corpus/results_gate_current.json` (свежий дамп),
`docs/FINDINGS.md`, `docs/STATE.md`, эта запись, разведка и логи прогонов в
`experiments/per_spread/`.
"""

P = "docs/JOURNAL.md"
s = io.open(P, encoding="utf-8").read()
assert "Этап PER-SPREAD" not in s
io.open(P, "w", encoding="utf-8", newline="\r\n").write(s.rstrip("\n") + "\n" + ENTRY)
print("JOURNAL.md дополнен")
