**Снимок на: 2026-08-19**, этап **CHAR-NORM** (символьная порча: свод омоглифов
расширен, пробел внутри цифровой группы схлопывается по форме записи; ветка
`char-norm`), **в `main` НЕ слито и НЕ запушено** — приёмка не зелёная по двум
строкам, разбор в §3д. Последний этап в `main` — **DATE-ON** (дата во всех
наборах), запушен. До него — CIRCLE-UI, UI-REBUILD-2, UI-REBUILD, MARKUP-SPREAD,
PAREN-ZERO, PER-SPREAD, SINGLE-GUARD, MERGE-W1, BUILD-NOW, DEFAULT-GATE.
Параллельно в рабочем дереве идёт незавершённый этап **DESKTOP-FIT** (ветка
`desktop-fit`, зона `app/`+`packaging/`) — его правки этой веткой не
коммитились.

**Что сделано этим этапом.** Тронут ОДИН файл продукта — `src/normalizer.py`
(плюс его тесты). Слой нормализации перед детекцией существовал с этапа 2 и все
шесть невидимых символов задания (`U+200B/00AD/00A0/200C/200D/FEFF`) снимал с
первого дня; работа была не «завести слой», а найти по дампу гейта, ГДЕ
существующий свод не срабатывает. Найдено и закрыто пять разрывов, каждый
различается по САМОМУ ТЕКСТУ, а не по типу сущности:

1. **'б' на краю цифрового токена** не сводилась никогда (правило A5 требовало
   строгой внутренности цепи) — «б597769253» (ИНН), «4Ч170335б» (КПП),
   «149ЗІ1О09843б» (ОГРН), «408028І057753526867б» (счёт) не находились ВООБЩЕ.
   Разрешено по двум признакам: начало цепи (литеру дома пишут суффиксом, не
   префиксом) и собственный токен от 6 знаков (шестизначного номера дома не
   бывает). «630099 12б» не тронут.
2. **Порог цепи** считал только настоящие цифры — код подразделения паспорта
   «б22-513» (6 знаков, 5 цифр) не набирал его НИКОГДА. Добавлен порог по длине
   цепи в знаках при минимуме двух настоящих цифр.
3. **Запятая и точка** между цифровыми токенами продолжают цепь — копейки суммы
   «73Ч 525,0О» перестали быть отдельным двузнаковым числом.
4. **Типографский пробел у знака пунктуации** внутри значения выбрасывается
   («19.11<NBSP>.1962», «+7<NBSP> (<NNBSP>495)»); соседство с БУКВОЙ по-прежнему
   запрещает выброс — «Иван<NBSP>Иванов» и «2023<NBSP>г.» не склеиваются.
5. **Обычный пробел внутри цифровой группы** схлопывается, но только там, где
   запись НЕ РАЗРЯДНАЯ: разрядная группировка имеет ровно одну форму (первая
   группа 1-3 знака, все последующие ровно по 3). Плюс два порога — от 4 групп
   и от 8 знаков. «12 000 000» не тронуто, «90 14 6170 85» (паспорт) склеено.

**Числа полного гейта (324 док., дамп ДО против дампа ПОСЛЕ, оценка обоих одним
гейтом).** Утечка ≥6 по агрегату BIK-excl **10.66 % → 9.38 %** (1234 → 1086
сущностей), ≥8 1160 → 1043. masking A **ровно 100.00 %**. masking B агрегат
87.90 → 88.27 %. Недобор границ (это утечка) 470 сущн./3329 симв. → 434/3026.
Красных строк гейта **20 → 10**. По классам порчи: `mut:homoglyph` 97 → 41,
`mut:digit_spaces` 56 → 27, `mut:invisible` 47 → 39, попутно `mut:combo`
338 → 293. **Ни один тип и ни один класс не вырос.**

**Ожидание задания (~8.2 % по агрегату) НЕ достигнуто:** снято 148 утечек из
названных 182. Остаток разобран поимённо (`CHARNORM-RESIDUE` в `FINDINGS.md`) и
в большинстве оказался НЕ символьным: после свода текст чистый, а `PER`
(иностранные имена, инициалы перед фамилией) и `ORG` без якоря всё равно не
находятся.

**Цена этапа названа числом, а не спрятана:** линия «д» 2897 → 2902 (+5, из них
`DATE` +4 — открытый долг `DATE-NORMREF-OVERMASK`), FP на негативах 645 → 654
(+9, из них `DATE` +13; без `DATE` линия бы упала), masking B по типу `INN`
87.32 → 87.26 % при РОСТЕ верных масок на 12 — дефект прибора
`CHARNORM-MASKB-B3-SEPARATOR`. Планка не двигалась, `promote_baseline.py` не
запускался, ни один тест не ослаблялся.

**Поймано набором, а не глазом, дважды.** (1) Первая редакция схлопывания
считала группой любой прогон знаков цифрового вида — и «число 7707083893 без»
разбиралось как три группы ('о' в «числО», 'б' в «Без»), пробелы вокруг
реквизита выбрасывались; поймал `test_regex_detector.py`. (2) Порог схлопывания
3 группы отнимал у мультиспана телефон, собираемый из группировки; поймал
`test_stage_e_spans.py`, порог поднят до 4 — тест не ослаблялся.

**Новые долги:** `CHARNORM-DOUBLES-ONLY-GROUP`, `CHARNORM-PHONE-NOPREFIX`,
`CHARNORM-DIGIT-AS-LETTER`, `CHARNORM-SPLIT-VS-MULTISPAN`,
`CHARNORM-ADDR-EATS-DIVCODE`, дефект прибора `CHARNORM-MASKB-B3-SEPARATOR`.

**Не сделано намеренно:** круг на собранной программе (запрет 9) — долг ветки
до слияния; `layout_repair` не трогался; в `scope_exclusions.py` ничего не
вносилось.

Источник истины — код и прогоны. Разошёлся с ними — прав код, а файл чинится.
