## 3д. Что сделал с краснотой этап CHAR-NORM — снял десять строк, добавил ноль

Оценка ОДНИМ действующим гейтом трёх пар (`experiments/char_norm_gate_delta2.log`):
точка отсчёта → дамп ДО этапа даёт **20 красных строк**; точка отсчёта → дамп
ПОСЛЕ даёт **10**. Снялись: линия «б» по `KPP` (×2 порога), `PASSPORT` (×2),
`ADDRESS` (×2); линия «г» по агрегату, `ADDRESS`, `PASSPORT`; линия «е» по
`PHONE` (×2). Ни одна строка не добавилась.

**Чистая дельта этапа (дамп ДО → дамп ПОСЛЕ, тот же гейт) — четыре строки, и
все четыре разобраны до конкретных значений:**

1. **(3) FP на негативах 645 → 654 (+9), и ВЕСЬ рост — `DATE`** (643 → 652).
   Прочие типы на объявленных негативах не сдвинулись ни на единицу
   (`CONTRACT_NO` 316, `PER` 1, `ADDRESS` 1 — до и после). Механизм ни разу не
   новый: свод символов ЧИНИТ испорченную дату («О3.О3.2024»,
   «03.03<NBSP>.2024», «0<SHY>3.о3.2024»), и она попадает под тип, у которого
   столкновение с разметкой v1 уже открыто (§3г, §0.10). Записи в
   `scope_exclusions.py` НЕ вносилось.
2. **(д) over-mask прозы ВСЕГО 2897 → 2902 (+5)**: `DATE` 348 → 352 (тот же
   открытый долг `DATE-NORMREF-OVERMASK`) и `PASSPORT_ISSUER` 451 → 452.
3. **(г) masking B по типу `INN` 87.32 → 87.26 %** — и это ЕДИНСТВЕННОЕ место,
   где число ухудшилось при улучшении факта. ВЕРНЫХ масок стало БОЛЬШЕ на 12
   (303/347 → 315/361). Падение доли разобрано поимённо: ИНН, рваный
   ТАБУЛЯЦИЕЙ на границе ячеек (`lease_0002__m2718_combo`, «б7<NBSP>50<TAB>72<WJ>О233»),
   был ПОЛНОЙ УТЕЧКОЙ, а стал закрыт двумя масками B3-пары; разделитель между
   половинами не принадлежит ни одной маске, и `_covered_by_union` считает
   эталон незакрытым, хотя запись сущности в том же дампе говорит `under=0,
   direction=exact`. Дефект ПРИБОРА, заведён как `CHARNORM-MASKB-B3-SEPARATOR`.
4. **(з) состав реестра `known_default_leaks.json`** — артефакт сравнения без
   загруженного реестра; на живом прогоне гейта линия «з» красной НЕ была.

**Линия «и» (маска чужого типа) — 395 → 395, один вошёл, один вышел вверх.**
Вышел `loan_0005__m1337_digit_spaces` / `PASSPORT` (было закрыто маской `PHONE`,
стало своей); вошёл `loan_0006__m1337_combo` / `PASSPORT` «З 71-970» (код
подразделения целиком накрыла маска `ADDRESS` — долг
`CHARNORM-ADDR-EATS-DIVCODE`). Опасное направление («был прикрыт чужой маской —
перестал быть прикрыт ничем») — НОЛЬ. Реестр пересобран с записью причины.

**Ни планка не двигалась, ни один тест не ослаблялся, `promote_baseline.py` не
запускался.** Полный набор — **1944 passed, 3 skipped, 6 xfailed, 0 failed**.
