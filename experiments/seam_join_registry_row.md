| (корень, SEAM-JOIN) | 8 | `seam_join_probe.py` (разложение утечки по приёму порчи и типу + поимённый список утекающих значений класса вёрстки), `seam_join_shape.py` (ФОРМА разрыва: внутри сегмента / через границу сегментов / соседние ячейки — этим замером и определился весь план этапа), `seam_join_ceiling.py` (ПОТОЛОК сшивки: найдётся ли значение вообще, если разрыв убрать), `seam_join_why.py` (журнал внутрисегментного ремонта с причиной каждого отказа), `seam_join_seamlog.py` (журнал стража сборки на ремонте стыка — он назвал 57 отказов PASSPORT), `seam_join_run.py` + `seam_join_diff.py` (прогон харнесса по выбранным документам и ПОИМЁННОЕ сличение двух дампов: утечка / найдено / точный спан / маски / over-mask / FP), `seam_join_maskb_probe.py` (цена немаскируемого шва в линии «г» — тот же харнесс с объединением, терпящим зазор в один символ; прибор гейта НЕ трогается) | `seam_join_probe.log`, `seam_join_shape.log`, `seam_join_ceiling.log`, `seam_join_seamlog.log`/`seam_join_seamlog2.log` (до и после правки якорей), `seam_join_gate_before_eval.log` (оценка дампа ДО этапа действующим гейтом), `seam_join_gate_after.log` (живой прогон, вскрывший линию «ж»), `seam_join_gate_after2.log` (живой прогон после правки), `seam_join_maskb.log`, `seam_join_v2_final.log` (корпус v2), `seam_join_pytest_full.log`, `seam_join_journal_entry.md`/`seam_join_findings.md` (текст записей, вынесен файлом — в командной строке он не проходил). Дампы прогонов (`sj_*.json`, 1.5-9.8 МБ) в репозиторий НЕ кладутся: дамп ДО этапа — побайтно `git show d37d53b:tests/corpus/results_gate_current.json` |
