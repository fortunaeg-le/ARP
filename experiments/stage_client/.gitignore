# Порождаемое и невоспроизводимое-в-репозитории. Всё, что здесь перечислено,
# восстанавливается запуском соседних скриптов.
#
# probe_out/        — .docx проб и большой документ ×10 (1.3 МБ): make_big_docx.py,
#                     probe_altcontent.py
# zones_*.json      — слепки вердиктов сканера зон: zones_snapshot.py
#                     (доказательство — РАВЕНСТВО ХЕШЕЙ, оно записано в журнале)
# store_report.log  — читает РЕАЛЬНОЕ хранилище владельца. Значений ПДн не
#                     печатает, но идентификаторы его сессий в публичный
#                     репозиторий не кладём (запрет 7).
probe_out/
zones_old.json
zones_new.json
store_report.log
pytest.log
